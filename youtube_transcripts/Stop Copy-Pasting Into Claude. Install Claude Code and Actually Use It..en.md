# Stop Copy-Pasting Into Claude. Install Claude Code and Actually Use It.

**Channel:** Jake Van Clief  
**Uploaded:** 2026-03-14  
**Duration:** 24:33  
**URL:** [https://www.youtube.com/watch?v=rHDA0WMXzy4](https://www.youtube.com/watch?v=rHDA0WMXzy4)  

---

## Transcript

`00:02` Welcome, welcome to a five session
`00:02` Welcome, welcome to a five session series. Now, this initial first session
`00:05` series. Now, this initial first session
`00:05` series. Now, this initial first session is on how to use Claude desktop and
`00:06` is on how to use Claude desktop and
`00:07` is on how to use Claude desktop and Claude code. We're going to be diving
`00:08` Claude code. We're going to be diving
`00:08` Claude code. We're going to be diving into a lot in the second session, which
`00:11` into a lot in the second session, which
`00:11` into a lot in the second session, which is actually Claude code in practice. So,
`00:13` is actually Claude code in practice. So,
`00:13` is actually Claude code in practice. So, if you've already gotten it installed,
`00:14` if you've already gotten it installed,
`00:15` if you've already gotten it installed, maybe you can jump there. However, I'd
`00:16` maybe you can jump there. However, I'd
`00:16` maybe you can jump there. However, I'd be, you know, I give a caveat there
`00:18` be, you know, I give a caveat there
`00:18` be, you know, I give a caveat there because I dive into certain things in
`00:20` because I dive into certain things in
`00:20` because I dive into certain things in this session, this first one, that are,
`00:22` this session, this first one, that are,
`00:22` this session, this first one, that are, well, maybe something you might not have
`00:25` well, maybe something you might not have
`00:25` well, maybe something you might not have installed or done for your Claude code.
`00:27` installed or done for your Claude code.
`00:27` installed or done for your Claude code. So, even if you're a veteran, I
`00:28` So, even if you're a veteran, I
`00:28` So, even if you're a veteran, I recommend you checking it out. Uh I also
`00:31` recommend you checking it out. Uh I also
`00:31` recommend you checking it out. Uh I also have a session three, it's going to be
`00:32` have a session three, it's going to be
`00:32` have a session three, it's going to be on Claude desktop as a thinking partner,
`00:34` on Claude desktop as a thinking partner,
`00:34` on Claude desktop as a thinking partner, right? How do you actually use this in
`00:36` right? How do you actually use this in
`00:36` right? How do you actually use this in more than just generating stuff for you?
`00:38` more than just generating stuff for you?
`00:38` more than just generating stuff for you? From there, session four is going to
`00:40` From there, session four is going to
`00:40` From there, session four is going to dive into making Claude code understand
`00:42` dive into making Claude code understand
`00:42` dive into making Claude code understand your product or your project, right?
`00:44` your product or your project, right?
`00:44` your product or your project, right? This is more than just asking it to run
`00:46` This is more than just asking it to run
`00:46` This is more than just asking it to run and hit something. How do you make it
`00:48` and hit something. How do you make it
`00:48` and hit something. How do you make it work with you in your project and get
`00:50` work with you in your project and get
`00:50` work with you in your project and get really good results consistently out of
`00:52` really good results consistently out of
`00:52` really good results consistently out of it. Finally, session five is going to
`00:54` it. Finally, session five is going to
`00:54` it. Finally, session five is going to dive into where all of this goes and how
`00:57` dive into where all of this goes and how
`00:57` dive into where all of this goes and how you can actually do a deeper dive into a
`00:59` you can actually do a deeper dive into a
`00:59` you can actually do a deeper dive into a file architecture system I call model
`01:01` file architecture system I call model
`01:01` file architecture system I call model workspace protocol. And that's really
`01:04` workspace protocol. And that's really
`01:04` workspace protocol. And that's really high-level good stuff. It's a separate
`01:05` high-level good stuff. It's a separate
`01:05` high-level good stuff. It's a separate course, but I touch on it a lot here in
`01:07` course, but I touch on it a lot here in
`01:07` course, but I touch on it a lot here in session five. So, let's go ahead and um
`01:10` session five. So, let's go ahead and um
`01:10` session five. So, let's go ahead and um dive in. Now, the goal for this session
`01:13` dive in. Now, the goal for this session
`01:13` dive in. Now, the goal for this session is to actually have you walk away with
`01:14` is to actually have you walk away with
`01:14` is to actually have you walk away with real deliverables, not just like
`01:16` real deliverables, not just like
`01:16` real deliverables, not just like awareness or whoa, I understand AI
`01:18` awareness or whoa, I understand AI
`01:18` awareness or whoa, I understand AI better, but actual artifacts sitting on
`01:20` better, but actual artifacts sitting on
`01:20` better, but actual artifacts sitting on your computer after each session. All
`01:22` your computer after each session. All
`01:22` your computer after each session. All three interfaces installed and working,
`01:25` three interfaces installed and working,
`01:25` three interfaces installed and working, real tasks that you can use and run on
`01:27` real tasks that you can use and run on
`01:27` real tasks that you can use and run on your files, a clear sense of when to use
`01:29` your files, a clear sense of when to use
`01:29` your files, a clear sense of when to use which tool, when not to, so you stop
`01:31` which tool, when not to, so you stop
`01:31` which tool, when not to, so you stop guessing. I'm also going to show you
`01:33` guessing. I'm also going to show you
`01:33` guessing. I'm also going to show you what a good Claude and D file is, a
`01:35` what a good Claude and D file is, a
`01:35` what a good Claude and D file is, a markdown file for Claude that completely
`01:37` markdown file for Claude that completely
`01:38` markdown file for Claude that completely changes how Claude works on your
`01:40` changes how Claude works on your
`01:40` changes how Claude works on your projects, and a preview of something
`01:42` projects, and a preview of something
`01:42` projects, and a preview of something bigger in session five that I just
`01:43` bigger in session five that I just
`01:43` bigger in session five that I just mentioned that might be the most
`01:45` mentioned that might be the most
`01:45` mentioned that might be the most valuable part of all of these sessions,
`01:46` valuable part of all of these sessions,
`01:46` valuable part of all of these sessions, but, you know, I think it's all pretty
`01:48` but, you know, I think it's all pretty
`01:48` but, you know, I think it's all pretty valuable.
`01:50` valuable.
`01:50` valuable. Now, most people use Claude like this.
`01:52` Now, most people use Claude like this.
`01:52` Now, most people use Claude like this. It's more like a search engine that
`01:54` It's more like a search engine that
`01:54` It's more like a search engine that writes better sentences or code or
`01:55` writes better sentences or code or
`01:55` writes better sentences or code or something like that. You type a
`01:56` something like that. You type a
`01:56` something like that. You type a question, you get an answer, and then
`01:58` question, you get an answer, and then
`01:58` question, you get an answer, and then you copy and paste it or move it
`02:00` you copy and paste it or move it
`02:00` you copy and paste it or move it somewhere. And that works, but it's like
`02:02` somewhere. And that works, but it's like
`02:02` somewhere. And that works, but it's like using a car to check the mailbox. The
`02:04` using a car to check the mailbox. The
`02:04` using a car to check the mailbox. The car can take you across the country, you
`02:06` car can take you across the country, you
`02:06` car can take you across the country, you just haven't left the driveway yet. And
`02:09` just haven't left the driveway yet. And
`02:09` just haven't left the driveway yet. And we're going to try to leave the driveway
`02:10` we're going to try to leave the driveway
`02:10` we're going to try to leave the driveway today.
`02:11` today.
`02:11` today. Now, Claude desktop, just the normal
`02:13` Now, Claude desktop, just the normal
`02:13` Now, Claude desktop, just the normal downloaded version, is very similar to
`02:15` downloaded version, is very similar to
`02:15` downloaded version, is very similar to the one that you'd log in on the
`02:16` the one that you'd log in on the
`02:16` the one that you'd log in on the website, and it's more of a
`02:18` website, and it's more of a
`02:18` website, and it's more of a conversational tool with all the good
`02:19` conversational tool with all the good
`02:19` conversational tool with all the good stuff that Claude has, and you can get a
`02:21` stuff that Claude has, and you can get a
`02:21` stuff that Claude has, and you can get a lot done with it, and it's great for
`02:23` lot done with it, and it's great for
`02:23` lot done with it, and it's great for planning, thinking out ideas, building
`02:25` planning, thinking out ideas, building
`02:25` planning, thinking out ideas, building stuff, all that jazz, but there is a
`02:26` stuff, all that jazz, but there is a
`02:26` stuff, all that jazz, but there is a limit to what it can do at scale. It
`02:29` limit to what it can do at scale. It
`02:29` limit to what it can do at scale. It can't see your files necessarily, and
`02:31` can't see your files necessarily, and
`02:31` can't see your files necessarily, and I'm talking purely just about the chat
`02:32` I'm talking purely just about the chat
`02:32` I'm talking purely just about the chat version. It can't run code, it can't
`02:35` version. It can't run code, it can't
`02:35` version. It can't run code, it can't edit anything in your projects, and
`02:37` edit anything in your projects, and
`02:37` edit anything in your projects, and everything has to go through copy,
`02:39` everything has to go through copy,
`02:39` everything has to go through copy, paste, or uploads. And that's not
`02:41` paste, or uploads. And that's not
`02:41` paste, or uploads. And that's not because the model is limited, it's
`02:42` because the model is limited, it's
`02:43` because the model is limited, it's extremely powerful, just the desktop
`02:44` extremely powerful, just the desktop
`02:44` extremely powerful, just the desktop version, but the model is the same one
`02:46` version, but the model is the same one
`02:46` version, but the model is the same one powering everything else. The problem is
`02:48` powering everything else. The problem is
`02:49` powering everything else. The problem is the interface. It just can't reach your
`02:51` the interface. It just can't reach your
`02:51` the interface. It just can't reach your stuff. So, if you've ever felt like
`02:52` stuff. So, if you've ever felt like
`02:52` stuff. So, if you've ever felt like Claude is like kind of gets it, but not
`02:54` Claude is like kind of gets it, but not
`02:54` Claude is like kind of gets it, but not quite, it's probably because you're
`02:56` quite, it's probably because you're
`02:56` quite, it's probably because you're working with a more pasted expert
`02:59` working with a more pasted expert
`02:59` working with a more pasted expert excerpt excerpt instead of your actual
`03:02` excerpt excerpt instead of your actual
`03:02` excerpt excerpt instead of your actual project.
`03:03` project.
`03:03` project. Now, this is what I actually use for
`03:06` Now, this is what I actually use for
`03:06` Now, this is what I actually use for almost everything. Claude code living
`03:08` almost everything. Claude code living
`03:08` almost everything. Claude code living inside VS code. Your files are on the
`03:11` inside VS code. Your files are on the
`03:11` inside VS code. Your files are on the left, you can easily see them. If you're
`03:13` left, you can easily see them. If you're
`03:13` left, you can easily see them. If you're on Mac, it's a little bit different as
`03:15` on Mac, it's a little bit different as
`03:15` on Mac, it's a little bit different as well, and Claude is on the right. It
`03:17` well, and Claude is on the right. It
`03:17` well, and Claude is on the right. It reads your projects, edits your files in
`03:19` reads your projects, edits your files in
`03:19` reads your projects, edits your files in line, runs commands. You don't copy and
`03:22` line, runs commands. You don't copy and
`03:22` line, runs commands. You don't copy and paste anything, you just describe what
`03:23` paste anything, you just describe what
`03:23` paste anything, you just describe what you need, and it works directly within
`03:25` you need, and it works directly within
`03:25` you need, and it works directly within the files, right? If you already use VS
`03:28` the files, right? If you already use VS
`03:28` the files, right? If you already use VS code, Visual Studio Code, and you're
`03:30` code, Visual Studio Code, and you're
`03:30` code, Visual Studio Code, and you're open to trying it, this is actually
`03:31` open to trying it, this is actually
`03:31` open to trying it, this is actually where I'd start. For those of you who've
`03:33` where I'd start. For those of you who've
`03:33` where I'd start. For those of you who've never opened VS code before, we'll get
`03:35` never opened VS code before, we'll get
`03:35` never opened VS code before, we'll get you set up in the hands-on section, and
`03:37` you set up in the hands-on section, and
`03:37` you set up in the hands-on section, and it's simpler than it looks, but just
`03:39` it's simpler than it looks, but just
`03:39` it's simpler than it looks, but just keep that note. I'm going to walk you
`03:41` keep that note. I'm going to walk you
`03:41` keep that note. I'm going to walk you through install instructions and
`03:42` through install instructions and
`03:42` through install instructions and everything for that. There's also just
`03:44` everything for that. There's also just
`03:44` everything for that. There's also just the most generic way to use it, which is
`03:46` the most generic way to use it, which is
`03:46` the most generic way to use it, which is Claude through the terminal. Uh it's the
`03:48` Claude through the terminal. Uh it's the
`03:48` Claude through the terminal. Uh it's the same engine under the hood, but a kind
`03:50` same engine under the hood, but a kind
`03:50` same engine under the hood, but a kind of different wrapper. The terminal
`03:51` of different wrapper. The terminal
`03:51` of different wrapper. The terminal version is better for when your work
`03:53` version is better for when your work
`03:53` version is better for when your work doesn't live in a code project,
`03:55` doesn't live in a code project,
`03:55` doesn't live in a code project, processing a folder of documents,
`03:57` processing a folder of documents,
`03:57` processing a folder of documents, managing files, cleaning up your
`03:59` managing files, cleaning up your
`03:59` managing files, cleaning up your downloads folder, anything where you
`04:00` downloads folder, anything where you
`04:00` downloads folder, anything where you just need Claude to read a bunch of
`04:02` just need Claude to read a bunch of
`04:02` just need Claude to read a bunch of files and do something with them. You
`04:04` files and do something with them. You
`04:04` files and do something with them. You don't need this one to get value from
`04:05` don't need this one to get value from
`04:06` don't need this one to get value from this course, but if you're already
`04:07` this course, but if you're already
`04:07` this course, but if you're already comfortable in the terminal, it's
`04:08` comfortable in the terminal, it's
`04:08` comfortable in the terminal, it's actually faster for some jobs and I do
`04:10` actually faster for some jobs and I do
`04:10` actually faster for some jobs and I do it once in a while.
`04:12` it once in a while.
`04:12` it once in a while. And this is the thing that most people
`04:13` And this is the thing that most people
`04:13` And this is the thing that most people miss. It's not three different AIs, it's
`04:17` miss. It's not three different AIs, it's
`04:17` miss. It's not three different AIs, it's one model, three different ways to
`04:20` one model, three different ways to
`04:20` one model, three different ways to access it. Technically, there's a little
`04:22` access it. Technically, there's a little
`04:22` access it. Technically, there's a little bit of fine-tuning difference with
`04:24` bit of fine-tuning difference with
`04:24` bit of fine-tuning difference with Claude Code and whatnot, but at the end
`04:25` Claude Code and whatnot, but at the end
`04:26` Claude Code and whatnot, but at the end of the day, the desktop is a
`04:27` of the day, the desktop is a
`04:27` of the day, the desktop is a conversation layer, VS Code is an editor
`04:30` conversation layer, VS Code is an editor
`04:30` conversation layer, VS Code is an editor layer, and the terminal is the command
`04:33` layer, and the terminal is the command
`04:33` layer, and the terminal is the command line layer. Same brain behind all three.
`04:36` line layer. Same brain behind all three.
`04:36` line layer. Same brain behind all three. The difference is what each interface
`04:38` The difference is what each interface
`04:38` The difference is what each interface lets that brain see and do. Although,
`04:41` lets that brain see and do. Although,
`04:41` lets that brain see and do. Although, quick note, there's things out there
`04:43` quick note, there's things out there
`04:43` quick note, there's things out there called Cursor or Wind Surf or Co-pilot
`04:45` called Cursor or Wind Surf or Co-pilot
`04:46` called Cursor or Wind Surf or Co-pilot or Google's Anti-gravity, and those are
`04:48` or Google's Anti-gravity, and those are
`04:48` or Google's Anti-gravity, and those are completely different products that are
`04:50` completely different products that are
`04:50` completely different products that are built on top of VS Code. So, VS Code is
`04:53` built on top of VS Code. So, VS Code is
`04:53` built on top of VS Code. So, VS Code is the source IDE, right? An interface for
`04:56` the source IDE, right? An interface for
`04:56` the source IDE, right? An interface for developer environments, but the uh
`04:59` developer environments, but the uh
`04:59` developer environments, but the uh actual buildings or the interface of the
`05:01` actual buildings or the interface of the
`05:01` actual buildings or the interface of the layers are just supporting different
`05:03` layers are just supporting different
`05:03` layers are just supporting different models or different processes. Some of
`05:05` models or different processes. Some of
`05:05` models or different processes. Some of them actually use Claude and some don't.
`05:08` them actually use Claude and some don't.
`05:08` them actually use Claude and some don't. Some mix multiple models, and I do have
`05:10` Some mix multiple models, and I do have
`05:10` Some mix multiple models, and I do have an entirely separate course diving into
`05:12` an entirely separate course diving into
`05:12` an entirely separate course diving into the pros and cons of each of them, which
`05:14` the pros and cons of each of them, which
`05:14` the pros and cons of each of them, which one you actually want and breaks it
`05:16` one you actually want and breaks it
`05:16` one you actually want and breaks it down, but this course is specifically
`05:18` down, but this course is specifically
`05:18` down, but this course is specifically for Claude's tools and the way inside of
`05:21` for Claude's tools and the way inside of
`05:21` for Claude's tools and the way inside of the Anthropic environment, and that's
`05:23` the Anthropic environment, and that's
`05:23` the Anthropic environment, and that's all we're covering here. Now, let me
`05:24` all we're covering here. Now, let me
`05:24` all we're covering here. Now, let me show you what the difference between all
`05:26` show you what the difference between all
`05:26` show you what the difference between all of these kind of looks like, uh just
`05:28` of these kind of looks like, uh just
`05:28` of these kind of looks like, uh just really quick off the bat if you've never
`05:29` really quick off the bat if you've never
`05:29` really quick off the bat if you've never used any of them. So, first of all, we
`05:31` used any of them. So, first of all, we
`05:31` used any of them. So, first of all, we have Claude Desktop or the Claude normal
`05:35` have Claude Desktop or the Claude normal
`05:35` have Claude Desktop or the Claude normal one that you're actually going in
`05:36` one that you're actually going in
`05:36` one that you're actually going in through there. And let's say I wanted to
`05:37` through there. And let's say I wanted to
`05:37` through there. And let's say I wanted to just be able to summarize a document,
`05:39` just be able to summarize a document,
`05:39` just be able to summarize a document, right? We come in, we have to go find
`05:41` right? We come in, we have to go find
`05:41` right? We come in, we have to go find the document wherever we had put it, we
`05:43` the document wherever we had put it, we
`05:43` the document wherever we had put it, we come in, we open it, we upload it, and
`05:46` come in, we open it, we upload it, and
`05:46` come in, we open it, we upload it, and then from there I can say summarize this
`05:48` then from there I can say summarize this
`05:48` then from there I can say summarize this Python breakdown PDF and suggest three
`05:51` Python breakdown PDF and suggest three
`05:51` Python breakdown PDF and suggest three improvements. And it's going to go ahead
`05:53` improvements. And it's going to go ahead
`05:53` improvements. And it's going to go ahead and it's going to go through and do
`05:54` and it's going to go through and do
`05:54` and it's going to go through and do that. Right now I'm using Opus 4.6,
`05:56` that. Right now I'm using Opus 4.6,
`05:56` that. Right now I'm using Opus 4.6, which is the more powerful one,
`05:58` which is the more powerful one,
`05:58` which is the more powerful one, um and it's doing a pretty good job,
`05:59` um and it's doing a pretty good job,
`06:00` um and it's doing a pretty good job, right? It's good. It's good. It's nice,
`06:02` right? It's good. It's good. It's nice,
`06:02` right? It's good. It's good. It's nice, and I'm sure it's going to be a great
`06:03` and I'm sure it's going to be a great
`06:03` and I'm sure it's going to be a great one when we're done with this, right?
`06:05` one when we're done with this, right?
`06:05` one when we're done with this, right? But then from there, I actually want to
`06:09` But then from there, I actually want to
`06:09` But then from there, I actually want to be able to possibly use VS Code, right?
`06:11` be able to possibly use VS Code, right?
`06:11` be able to possibly use VS Code, right? And so this is what it looks like,
`06:13` And so this is what it looks like,
`06:13` And so this is what it looks like, right? This is your VS Code. You have
`06:14` right? This is your VS Code. You have
`06:15` right? This is your VS Code. You have your file explorer here where you're
`06:16` your file explorer here where you're
`06:16` your file explorer here where you're able to see all your files and you'll be
`06:18` able to see all your files and you'll be
`06:18` able to see all your files and you'll be able to break them down. You'll I have
`06:20` able to break them down. You'll I have
`06:20` able to break them down. You'll I have an extension that allows me to open a
`06:21` an extension that allows me to open a
`06:21` an extension that allows me to open a whole bunch of Claude Code tabs. That's
`06:23` whole bunch of Claude Code tabs. That's
`06:23` whole bunch of Claude Code tabs. That's another immediate usefulness out of this
`06:25` another immediate usefulness out of this
`06:25` another immediate usefulness out of this is the fact that I can have a whole
`06:27` is the fact that I can have a whole
`06:27` is the fact that I can have a whole bunch of conversations all going at once
`06:29` bunch of conversations all going at once
`06:29` bunch of conversations all going at once and I can swap between them. But it's
`06:31` and I can swap between them. But it's
`06:31` and I can swap between them. But it's the same aspect, except this time I can
`06:33` the same aspect, except this time I can
`06:33` the same aspect, except this time I can just say summarize Python breakdown PDF.
`06:35` just say summarize Python breakdown PDF.
`06:35` just say summarize Python breakdown PDF. It's going to immediately know which
`06:37` It's going to immediately know which
`06:37` It's going to immediately know which file we're in. It's going to go find it,
`06:39` file we're in. It's going to go find it,
`06:39` file we're in. It's going to go find it, and it's going to go read and search
`06:40` and it's going to go read and search
`06:40` and it's going to go read and search through it immediately. So that's
`06:42` through it immediately. So that's
`06:42` through it immediately. So that's already one less layer of work that you
`06:44` already one less layer of work that you
`06:44` already one less layer of work that you have to do, and it still does all the
`06:46` have to do, and it still does all the
`06:46` have to do, and it still does all the same good stuff that the normal 4.6
`06:48` same good stuff that the normal 4.6
`06:48` same good stuff that the normal 4.6 would bear. You can also do the same
`06:50` would bear. You can also do the same
`06:50` would bear. You can also do the same thing inside of
`06:51` thing inside of
`06:51` thing inside of uh the terminal. I have this terminal
`06:53` uh the terminal. I have this terminal
`06:53` uh the terminal. I have this terminal open inside of this file here, right? As
`06:56` open inside of this file here, right? As
`06:56` open inside of this file here, right? As simple as this. I come in, I open, I hit
`06:59` simple as this. I come in, I open, I hit
`06:59` simple as this. I come in, I open, I hit open terminal, and it immediately is
`07:01` open terminal, and it immediately is
`07:01` open terminal, and it immediately is inside this folder, right? I just type
`07:03` inside this folder, right? I just type
`07:03` inside this folder, right? I just type Claude and it opens it up. And I can say
`07:05` Claude and it opens it up. And I can say
`07:06` Claude and it opens it up. And I can say the same thing here. Same power as the
`07:08` the same thing here. Same power as the
`07:08` the same thing here. Same power as the VS Code one, except the difference is I
`07:10` VS Code one, except the difference is I
`07:10` VS Code one, except the difference is I have to read everything inside the
`07:11` have to read everything inside the
`07:11` have to read everything inside the terminal. Again, something nice, but
`07:14` terminal. Again, something nice, but
`07:14` terminal. Again, something nice, but it just depends on what you kind of want
`07:16` it just depends on what you kind of want
`07:16` it just depends on what you kind of want there. Now, the pattern here is pretty
`07:18` there. Now, the pattern here is pretty
`07:18` there. Now, the pattern here is pretty straightforward if you haven't noticed,
`07:19` straightforward if you haven't noticed,
`07:19` straightforward if you haven't noticed, right? The normal Claude chat is just
`07:21` right? The normal Claude chat is just
`07:21` right? The normal Claude chat is just for good conversation and some work that
`07:23` for good conversation and some work that
`07:23` for good conversation and some work that you just want to bust out quickly. It's
`07:25` you just want to bust out quickly. It's
`07:25` you just want to bust out quickly. It's also good for finding memory. Code is
`07:27` also good for finding memory. Code is
`07:27` also good for finding memory. Code is more for action, right? Being able to
`07:29` more for action, right? Being able to
`07:29` more for action, right? Being able to quickly scale things, right? It's not as
`07:31` quickly scale things, right? It's not as
`07:31` quickly scale things, right? It's not as useful the difference in one file, but
`07:32` useful the difference in one file, but
`07:33` useful the difference in one file, but as soon as I have a hundred files that I
`07:34` as soon as I have a hundred files that I
`07:34` as soon as I have a hundred files that I need to roll through, it's much easier
`07:36` need to roll through, it's much easier
`07:36` need to roll through, it's much easier to actually use Claude Code. The
`07:38` to actually use Claude Code. The
`07:38` to actually use Claude Code. The difference is simply what they can touch
`07:41` difference is simply what they can touch
`07:41` difference is simply what they can touch and how they access it. The one worth
`07:43` and how they access it. The one worth
`07:43` and how they access it. The one worth paying attention to on this chart is the
`07:45` paying attention to on this chart is the
`07:45` paying attention to on this chart is the iterate on output. With the normal chat
`07:47` iterate on output. With the normal chat
`07:47` iterate on output. With the normal chat one, you have to read the response,
`07:49` one, you have to read the response,
`07:49` one, you have to read the response, decide what's wrong, copy it, start
`07:51` decide what's wrong, copy it, start
`07:51` decide what's wrong, copy it, start again, but with Claude code, you can say
`07:54` again, but with Claude code, you can say
`07:54` again, but with Claude code, you can say that's not right, fix this, and move it
`07:56` that's not right, fix this, and move it
`07:56` that's not right, fix this, and move it into a separate folder. You can break
`07:58` into a separate folder. You can break
`07:58` into a separate folder. You can break down your processes as you're going
`08:00` down your processes as you're going
`08:00` down your processes as you're going along and you're delegating instead of
`08:03` along and you're delegating instead of
`08:03` along and you're delegating instead of rounds of copy and paste in a single
`08:05` rounds of copy and paste in a single
`08:05` rounds of copy and paste in a single chat environment. For those of you
`08:07` chat environment. For those of you
`08:07` chat environment. For those of you who've been using the chat or the
`08:08` who've been using the chat or the
`08:08` who've been using the chat or the desktop and feel like you're getting
`08:10` desktop and feel like you're getting
`08:10` desktop and feel like you're getting these issues or there's a lot of manual
`08:12` these issues or there's a lot of manual
`08:12` these issues or there's a lot of manual work to kind of continuously get good
`08:14` work to kind of continuously get good
`08:14` work to kind of continuously get good results even if you copy and paste
`08:16` results even if you copy and paste
`08:16` results even if you copy and paste prompts, this is exactly why. Your
`08:19` prompts, this is exactly why. Your
`08:19` prompts, this is exactly why. Your interface is playing the middleman
`08:22` interface is playing the middleman
`08:22` interface is playing the middleman stopping you from working as well. The
`08:24` stopping you from working as well. The
`08:24` stopping you from working as well. The folder structure, being able to access
`08:26` folder structure, being able to access
`08:26` folder structure, being able to access files, all of this is extremely
`08:28` files, all of this is extremely
`08:28` files, all of this is extremely important. Now again, I use VS Code
`08:31` important. Now again, I use VS Code
`08:31` important. Now again, I use VS Code extension for almost everything. I'm in
`08:33` extension for almost everything. I'm in
`08:33` extension for almost everything. I'm in VS Code anyway for most of my projects
`08:35` VS Code anyway for most of my projects
`08:35` VS Code anyway for most of my projects cuz I do a lot of coding. So having
`08:37` cuz I do a lot of coding. So having
`08:37` cuz I do a lot of coding. So having Claude right there in the sidebar and
`08:39` Claude right there in the sidebar and
`08:39` Claude right there in the sidebar and being able to open multiple work trees
`08:41` being able to open multiple work trees
`08:41` being able to open multiple work trees is the fastest path. The terminal is
`08:43` is the fastest path. The terminal is
`08:43` is the fastest path. The terminal is nice if I want to go quickly, but most
`08:44` nice if I want to go quickly, but most
`08:44` nice if I want to go quickly, but most of the time I just open up VS Code
`08:46` of the time I just open up VS Code
`08:46` of the time I just open up VS Code anyway. The desktop is something that I
`08:48` anyway. The desktop is something that I
`08:48` anyway. The desktop is something that I can use from anywhere even though you
`08:50` can use from anywhere even though you
`08:50` can use from anywhere even though you can do remote access with the VS Code
`08:52` can do remote access with the VS Code
`08:53` can do remote access with the VS Code terminal now, so I can use it on my
`08:54` terminal now, so I can use it on my
`08:54` terminal now, so I can use it on my phone. It's just sometimes a cleaner
`08:56` phone. It's just sometimes a cleaner
`08:56` phone. It's just sometimes a cleaner interface for when I'm not doing these
`08:58` interface for when I'm not doing these
`08:58` interface for when I'm not doing these deep stuff. Now your setup might look a
`09:00` deep stuff. Now your setup might look a
`09:00` deep stuff. Now your setup might look a little bit different from mine if you're
`09:02` little bit different from mine if you're
`09:02` little bit different from mine if you're on Mac or you're you know different
`09:03` on Mac or you're you know different
`09:04` on Mac or you're you know different colors and stuff like that and that's
`09:05` colors and stuff like that and that's
`09:05` colors and stuff like that and that's fine. Some people end up using desktop
`09:07` fine. Some people end up using desktop
`09:08` fine. Some people end up using desktop 80% of the time because their work is
`09:09` 80% of the time because their work is
`09:09` 80% of the time because their work is mostly thinking and planning anyway. So
`09:12` mostly thinking and planning anyway. So
`09:12` mostly thinking and planning anyway. So some of you might live in the terminal
`09:14` some of you might live in the terminal
`09:14` some of you might live in the terminal for whatever reasons, right? Just being
`09:16` for whatever reasons, right? Just being
`09:16` for whatever reasons, right? Just being able to use those commands. But the
`09:17` able to use those commands. But the
`09:17` able to use those commands. But the point is knowing that each one is good
`09:20` point is knowing that each one is good
`09:20` point is knowing that each one is good for you in specific areas, so just pick
`09:23` for you in specific areas, so just pick
`09:23` for you in specific areas, so just pick the right one and what you like working
`09:25` the right one and what you like working
`09:25` the right one and what you like working with and feels right for you rather than
`09:27` with and feels right for you rather than
`09:27` with and feels right for you rather than just forcing everything through one
`09:29` just forcing everything through one
`09:29` just forcing everything through one interface. Just play around with it, see
`09:31` interface. Just play around with it, see
`09:31` interface. Just play around with it, see what you like.
`09:33` what you like.
`09:33` what you like. The desktop is kind of the easiest to
`09:35` The desktop is kind of the easiest to
`09:35` The desktop is kind of the easiest to use. It's the least control over what
`09:38` use. It's the least control over what
`09:38` use. It's the least control over what Claude can do. The terminal is flexible,
`09:42` Claude can do. The terminal is flexible,
`09:42` Claude can do. The terminal is flexible, arguably the most flexible because you
`09:45` arguably the most flexible because you
`09:45` arguably the most flexible because you can kind of open it up anywhere and you
`09:46` can kind of open it up anywhere and you
`09:46` can kind of open it up anywhere and you can open multiple terminals in different
`09:48` can open multiple terminals in different
`09:48` can open multiple terminals in different ways and bounce between it, but it's
`09:50` ways and bounce between it, but it's
`09:50` ways and bounce between it, but it's kind of a weird learning curve is not as
`09:52` kind of a weird learning curve is not as
`09:52` kind of a weird learning curve is not as visual.
`09:53` visual.
`09:53` visual. But at the end of the day, the VS Code
`09:55` But at the end of the day, the VS Code
`09:55` But at the end of the day, the VS Code one is this middle ground, I think. It
`09:57` one is this middle ground, I think. It
`09:57` one is this middle ground, I think. It gives you this familiar editor if you're
`09:59` gives you this familiar editor if you're
`09:59` gives you this familiar editor if you're used to it or at least an area to see
`10:02` used to it or at least an area to see
`10:02` used to it or at least an area to see and it has the full power of the normal
`10:04` and it has the full power of the normal
`10:04` and it has the full power of the normal Claude code.
`10:05` Claude code.
`10:05` Claude code. Okay, you might have noticed that the
`10:07` Okay, you might have noticed that the
`10:07` Okay, you might have noticed that the lighting has changed and I have changed
`10:08` lighting has changed and I have changed
`10:08` lighting has changed and I have changed my shirt as well as my hair. That is
`10:10` my shirt as well as my hair. That is
`10:10` my shirt as well as my hair. That is because it is the next day. I am
`10:11` because it is the next day. I am
`10:11` because it is the next day. I am recording all of this, building
`10:13` recording all of this, building
`10:13` recording all of this, building everything that you're seeing. So, just
`10:14` everything that you're seeing. So, just
`10:14` everything that you're seeing. So, just friendly reminder, I'm doing this all on
`10:15` friendly reminder, I'm doing this all on
`10:15` friendly reminder, I'm doing this all on my own and this is free. So,
`10:18` my own and this is free. So,
`10:18` my own and this is free. So, just bear with me here. Trying a little
`10:19` just bear with me here. Trying a little
`10:19` just bear with me here. Trying a little hard. But, let's go ahead and go into
`10:21` hard. But, let's go ahead and go into
`10:21` hard. But, let's go ahead and go into installations of all of these, right?
`10:23` installations of all of these, right?
`10:24` installations of all of these, right? So, we're not just going to look at one,
`10:25` So, we're not just going to look at one,
`10:25` So, we're not just going to look at one, we're going to look at all of them. So,
`10:26` we're going to look at all of them. So,
`10:26` we're going to look at all of them. So, doing Claude
`10:28` doing Claude
`10:28` doing Claude to install Claude desktop, it's actually
`10:30` to install Claude desktop, it's actually
`10:30` to install Claude desktop, it's actually quite simple.
`10:32` quite simple.
`10:32` quite simple. You just go Claude desktop.
`10:35` You just go Claude desktop.
`10:35` You just go Claude desktop. You can look straight on, you know,
`10:37` You can look straight on, you know,
`10:37` You can look straight on, you know, anywhere. Download Claude. You're going
`10:39` anywhere. Download Claude. You're going
`10:39` anywhere. Download Claude. You're going to make sure it's actually from
`10:40` to make sure it's actually from
`10:40` to make sure it's actually from Anthropic, from Claude. You're going to
`10:42` Anthropic, from Claude. You're going to
`10:42` Anthropic, from Claude. You're going to look, are you on Mac? Are you on
`10:44` look, are you on Mac? Are you on
`10:44` look, are you on Mac? Are you on Windows? Or you ARM 65? For those of you
`10:47` Windows? Or you ARM 65? For those of you
`10:47` Windows? Or you ARM 65? For those of you who are curious what that even means,
`10:49` who are curious what that even means,
`10:49` who are curious what that even means, there's certain ways in which your
`10:51` there's certain ways in which your
`10:51` there's certain ways in which your operating system works and the way it's
`10:53` operating system works and the way it's
`10:53` operating system works and the way it's coded. Most of you are just going to be
`10:55` coded. Most of you are just going to be
`10:55` coded. Most of you are just going to be normal Windows. If you ever need to
`10:58` normal Windows. If you ever need to
`10:58` normal Windows. If you ever need to check if you are ARM 65, it has to do
`11:00` check if you are ARM 65, it has to do
`11:00` check if you are ARM 65, it has to do with the type of processor that you
`11:02` with the type of processor that you
`11:02` with the type of processor that you have. If you're on Windows, you simply
`11:05` have. If you're on Windows, you simply
`11:05` have. If you're on Windows, you simply just type in settings, you go to
`11:08` just type in settings, you go to
`11:08` just type in settings, you go to settings, you go to
`11:11` settings, you go to
`11:11` settings, you go to system, right here under system, then
`11:14` system, right here under system, then
`11:14` system, right here under system, then you're going to scroll all the way down
`11:15` you're going to scroll all the way down
`11:15` you're going to scroll all the way down and it should have something called
`11:17` and it should have something called
`11:17` and it should have something called about. This is on Windows 11, obviously.
`11:19` about. This is on Windows 11, obviously.
`11:19` about. This is on Windows 11, obviously. You could find this roughly the same on
`11:21` You could find this roughly the same on
`11:21` You could find this roughly the same on any other one.
`11:22` any other one.
`11:22` any other one. And if you look at this, it should say
`11:24` And if you look at this, it should say
`11:24` And if you look at this, it should say Windows specifications and device
`11:26` Windows specifications and device
`11:26` Windows specifications and device specifications. Under system type, it
`11:29` specifications. Under system type, it
`11:29` specifications. Under system type, it should say 64-bit
`11:31` should say 64-bit
`11:31` should say 64-bit operating system on x64-based
`11:34` operating system on x64-based
`11:34` operating system on x64-based processor. That's the standard Windows.
`11:37` processor. That's the standard Windows.
`11:37` processor. That's the standard Windows. That's the normal Sometimes they'll say
`11:39` That's the normal Sometimes they'll say
`11:39` That's the normal Sometimes they'll say x86, which is technically similar. It
`11:42` x86, which is technically similar. It
`11:42` x86, which is technically similar. It would actually say 64-based arm-based
`11:45` would actually say 64-based arm-based
`11:45` would actually say 64-based arm-based processor if you had an arm system.
`11:48` processor if you had an arm system.
`11:48` processor if you had an arm system. Some AMD processors, right? I have an
`11:50` Some AMD processors, right? I have an
`11:50` Some AMD processors, right? I have an AMD Ryzen 9
`11:52` AMD Ryzen 9
`11:52` AMD Ryzen 9 are similar in that way. Just a kind of
`11:54` are similar in that way. Just a kind of
`11:54` are similar in that way. Just a kind of certain note there for those who are
`11:55` certain note there for those who are
`11:55` certain note there for those who are curious. If you want to dive into
`11:58` curious. If you want to dive into
`11:58` curious. If you want to dive into why that's the case, what of that means,
`12:00` why that's the case, what of that means,
`12:00` why that's the case, what of that means, it has to do with
`12:02` it has to do with
`12:02` it has to do with actual instructions for the processor
`12:04` actual instructions for the processor
`12:04` actual instructions for the processor and lower languages. I'll probably make
`12:06` and lower languages. I'll probably make
`12:06` and lower languages. I'll probably make a course on that later on.
`12:08` a course on that later on.
`12:08` a course on that later on. But let me know if that's something you
`12:09` But let me know if that's something you
`12:09` But let me know if that's something you need. So you download it and then all
`12:12` need. So you download it and then all
`12:12` need. So you download it and then all you're going to do is actually just
`12:13` you're going to do is actually just
`12:13` you're going to do is actually just click install.
`12:15` click install.
`12:15` click install. I think I had one downloaded. Yep, and
`12:16` I think I had one downloaded. Yep, and
`12:16` I think I had one downloaded. Yep, and you'll just click Claude setup. It'll
`12:19` you'll just click Claude setup. It'll
`12:19` you'll just click Claude setup. It'll install it. You give it permissions and
`12:21` install it. You give it permissions and
`12:21` install it. You give it permissions and then
`12:21` then
`12:21` then it's just like if you were using Claude,
`12:23` it's just like if you were using Claude,
`12:23` it's just like if you were using Claude, the initial one,
`12:26` the initial one,
`12:26` the initial one, anywhere else you log in. Now I have the
`12:29` anywhere else you log in. Now I have the
`12:29` anywhere else you log in. Now I have the maximum account, so I also get access to
`12:31` maximum account, so I also get access to
`12:31` maximum account, so I also get access to something called Claude Co-work, which
`12:33` something called Claude Co-work, which
`12:33` something called Claude Co-work, which is a version of Claude code that's able
`12:35` is a version of Claude code that's able
`12:35` is a version of Claude code that's able to work through all your folders inside
`12:38` to work through all your folders inside
`12:38` to work through all your folders inside of the app. So this is kind of like the
`12:40` of the app. So this is kind of like the
`12:40` of the app. So this is kind of like the chat plus the ability to actually use
`12:42` chat plus the ability to actually use
`12:43` chat plus the ability to actually use it. And then there's actual Claude code,
`12:45` it. And then there's actual Claude code,
`12:45` it. And then there's actual Claude code, which is built around you selecting
`12:47` which is built around you selecting
`12:47` which is built around you selecting folders and being able to edit it, which
`12:49` folders and being able to edit it, which
`12:49` folders and being able to edit it, which we'll dive into a bit later. But that's
`12:51` we'll dive into a bit later. But that's
`12:51` we'll dive into a bit later. But that's it. It handles most of it for you and it
`12:53` it. It handles most of it for you and it
`12:53` it. It handles most of it for you and it also helps you install Claude code the
`12:55` also helps you install Claude code the
`12:55` also helps you install Claude code the non-manual way. So you should be able to
`12:57` non-manual way. So you should be able to
`12:58` non-manual way. So you should be able to do this in that way. However, I'm going
`12:59` do this in that way. However, I'm going
`13:00` do this in that way. However, I'm going to show you some ways to handle it so
`13:02` to show you some ways to handle it so
`13:02` to show you some ways to handle it so that you can have it directly on your
`13:03` that you can have it directly on your
`13:03` that you can have it directly on your system as well outside of Claude code.
`13:06` system as well outside of Claude code.
`13:06` system as well outside of Claude code. Now installing Claude code, you do
`13:08` Now installing Claude code, you do
`13:08` Now installing Claude code, you do require to have certain things. You need
`13:10` require to have certain things. You need
`13:10` require to have certain things. You need something called Node.js. Now there's a
`13:13` something called Node.js. Now there's a
`13:13` something called Node.js. Now there's a couple ways to install this. You can
`13:14` couple ways to install this. You can
`13:14` couple ways to install this. You can even if you just downloaded the app, you
`13:17` even if you just downloaded the app, you
`13:17` even if you just downloaded the app, you can actually go to code and have Claude
`13:19` can actually go to code and have Claude
`13:19` can actually go to code and have Claude code install it for you. However, for
`13:22` code install it for you. However, for
`13:22` code install it for you. However, for those of you who want to do it manually
`13:24` those of you who want to do it manually
`13:24` those of you who want to do it manually yourself, there's a couple ways. You can
`13:26` yourself, there's a couple ways. You can
`13:26` yourself, there's a couple ways. You can either go to Node.js, right? And you can
`13:28` either go to Node.js, right? And you can
`13:28` either go to Node.js, right? And you can just download their installer. It says
`13:30` just download their installer. It says
`13:30` just download their installer. It says get Node.js. This is just essentially a
`13:33` get Node.js. This is just essentially a
`13:33` get Node.js. This is just essentially a JavaScript library that allows it to run
`13:37` JavaScript library that allows it to run
`13:37` JavaScript library that allows it to run on your computer. JavaScript is required
`13:40` on your computer. JavaScript is required
`13:40` on your computer. JavaScript is required for Claude code to work a certain way.
`13:42` for Claude code to work a certain way.
`13:42` for Claude code to work a certain way. Sometimes this actually is installed
`13:44` Sometimes this actually is installed
`13:44` Sometimes this actually is installed with this into a certain extent, but
`13:47` with this into a certain extent, but
`13:47` with this into a certain extent, but generally isn't. So, you need to make
`13:48` generally isn't. So, you need to make
`13:48` generally isn't. So, you need to make sure that it's installed there. Once you
`13:50` sure that it's installed there. Once you
`13:50` sure that it's installed there. Once you have Node.js installed,
`13:53` have Node.js installed,
`13:53` have Node.js installed, uh you can then use something called an
`13:55` uh you can then use something called an
`13:55` uh you can then use something called an NPM command, right? This is super
`13:57` NPM command, right? This is super
`13:57` NPM command, right? This is super useful. I think you all should, if
`13:58` useful. I think you all should, if
`13:58` useful. I think you all should, if you're diving into startups or anything
`13:59` you're diving into startups or anything
`14:00` you're diving into startups or anything like this, you should have this
`14:01` like this, you should have this
`14:01` like this, you should have this installed anyway because it makes life
`14:02` installed anyway because it makes life
`14:02` installed anyway because it makes life 10 times easier down the line. Whenever
`14:04` 10 times easier down the line. Whenever
`14:04` 10 times easier down the line. Whenever I'm doing animations or stuff like that,
`14:06` I'm doing animations or stuff like that,
`14:06` I'm doing animations or stuff like that, being able to do what we're about to do
`14:08` being able to do what we're about to do
`14:08` being able to do what we're about to do here is really nice. So, you want to
`14:09` here is really nice. So, you want to
`14:10` here is really nice. So, you want to open the command prompt. There's a
`14:11` open the command prompt. There's a
`14:11` open the command prompt. There's a couple ways to do that. You can either
`14:12` couple ways to do that. You can either
`14:12` couple ways to do that. You can either just type in CMD. If you're on um
`14:16` just type in CMD. If you're on um
`14:16` just type in CMD. If you're on um Mac, you want to type in your integrated
`14:18` Mac, you want to type in your integrated
`14:18` Mac, you want to type in your integrated terminal. It's probably what it would
`14:20` terminal. It's probably what it would
`14:20` terminal. It's probably what it would look like. It should pop up something
`14:22` look like. It should pop up something
`14:22` look like. It should pop up something similar to this. And all you're going to
`14:24` similar to this. And all you're going to
`14:24` similar to this. And all you're going to do, if NPM was stalled
`14:26` do, if NPM was stalled
`14:26` do, if NPM was stalled properly into your path, uh which means
`14:29` properly into your path, uh which means
`14:29` properly into your path, uh which means essentially that your computer can
`14:31` essentially that your computer can
`14:31` essentially that your computer can access it from anywhere, you should be
`14:33` access it from anywhere, you should be
`14:33` access it from anywhere, you should be able to put this command in and it will
`14:35` able to put this command in and it will
`14:35` able to put this command in and it will actually install Claude code directly
`14:39` actually install Claude code directly
`14:39` actually install Claude code directly from Claude and Anthropic's status
`14:42` from Claude and Anthropic's status
`14:42` from Claude and Anthropic's status stores. It'll install it onto your
`14:43` stores. It'll install it onto your
`14:43` stores. It'll install it onto your computer. Again, if you're having
`14:46` computer. Again, if you're having
`14:46` computer. Again, if you're having trouble with this, you can actually
`14:47` trouble with this, you can actually
`14:47` trouble with this, you can actually install Claude through the desktop and
`14:50` install Claude through the desktop and
`14:50` install Claude through the desktop and have it help you walk through this or
`14:52` have it help you walk through this or
`14:52` have it help you walk through this or just use Claude in general to kind of
`14:54` just use Claude in general to kind of
`14:54` just use Claude in general to kind of help you walk through it. I highly
`14:55` help you walk through it. I highly
`14:55` help you walk through it. I highly recommend that as I may miss something
`14:57` recommend that as I may miss something
`14:57` recommend that as I may miss something that your computer doesn't have or
`14:59` that your computer doesn't have or
`14:59` that your computer doesn't have or there's certain little things about
`15:01` there's certain little things about
`15:01` there's certain little things about packages and permissions and where it's
`15:03` packages and permissions and where it's
`15:03` packages and permissions and where it's installed on your computer. Uh I highly
`15:06` installed on your computer. Uh I highly
`15:06` installed on your computer. Uh I highly recommend Claude, ChatGPT. They can all
`15:08` recommend Claude, ChatGPT. They can all
`15:08` recommend Claude, ChatGPT. They can all help you through that process.
`15:10` help you through that process.
`15:10` help you through that process. Um but, if you were to do it that way,
`15:11` Um but, if you were to do it that way,
`15:11` Um but, if you were to do it that way, that's something you can do. Once it's
`15:13` that's something you can do. Once it's
`15:13` that's something you can do. Once it's installed and properly there, all it
`15:16` installed and properly there, all it
`15:16` installed and properly there, all it should be is literally from anywhere on
`15:18` should be is literally from anywhere on
`15:18` should be is literally from anywhere on your computer, if it was installed
`15:19` your computer, if it was installed
`15:19` your computer, if it was installed right, you can type in Claude and it'll
`15:22` right, you can type in Claude and it'll
`15:22` right, you can type in Claude and it'll actually that command will go and say,
`15:24` actually that command will go and say,
`15:24` actually that command will go and say, "Hey, do you trust this workspace?" Yes,
`15:25` "Hey, do you trust this workspace?" Yes,
`15:25` "Hey, do you trust this workspace?" Yes, I do. It'll have you log in and you can
`15:28` I do. It'll have you log in and you can
`15:28` I do. It'll have you log in and you can either log in with an API key. You
`15:31` either log in with an API key. You
`15:31` either log in with an API key. You actually need to go to Claude API
`15:33` actually need to go to Claude API
`15:33` actually need to go to Claude API developer platform, right? Which is
`15:35` developer platform, right? Which is
`15:35` developer platform, right? Which is still on claude.com. It's still your
`15:37` still on claude.com. It's still your
`15:37` still on claude.com. It's still your same account if you want to log in with
`15:39` same account if you want to log in with
`15:39` same account if you want to log in with it. Now, if you log in and it takes you
`15:41` it. Now, if you log in and it takes you
`15:41` it. Now, if you log in and it takes you back to your normal chat account, you
`15:42` back to your normal chat account, you
`15:43` back to your normal chat account, you accidentally clicked back on normal.
`15:45` accidentally clicked back on normal.
`15:45` accidentally clicked back on normal. What sometimes helps people is actually
`15:47` What sometimes helps people is actually
`15:47` What sometimes helps people is actually going to typing in Anthropic console and
`15:50` going to typing in Anthropic console and
`15:50` going to typing in Anthropic console and specifically finding platform.claude
`15:53` specifically finding platform.claude
`15:53` specifically finding platform.claude and signing in on that part and that
`15:55` and signing in on that part and that
`15:55` and signing in on that part and that sometimes forces it to go through. It
`15:57` sometimes forces it to go through. It
`15:57` sometimes forces it to go through. It should look something similar to this,
`15:59` should look something similar to this,
`16:00` should look something similar to this, right? It's a little bit different than
`16:01` right? It's a little bit different than
`16:01` right? It's a little bit different than the normal Claude, but you should be
`16:03` the normal Claude, but you should be
`16:03` the normal Claude, but you should be able to go down where it says API keys
`16:05` able to go down where it says API keys
`16:05` able to go down where it says API keys on the bottom left. You'll have to go
`16:08` on the bottom left. You'll have to go
`16:08` on the bottom left. You'll have to go through. Now, you should have created an
`16:09` through. Now, you should have created an
`16:09` through. Now, you should have created an organization or maybe you have to add in
`16:12` organization or maybe you have to add in
`16:12` organization or maybe you have to add in billing cuz this is separate from your
`16:14` billing cuz this is separate from your
`16:14` billing cuz this is separate from your normal Claude account
`16:15` normal Claude account
`16:16` normal Claude account and certain billing that you actually
`16:17` and certain billing that you actually
`16:17` and certain billing that you actually have to do.
`16:18` have to do.
`16:18` have to do. Don't worry, you can't see all of my key
`16:20` Don't worry, you can't see all of my key
`16:20` Don't worry, you can't see all of my key there, which is nice, but you can
`16:22` there, which is nice, but you can
`16:22` there, which is nice, but you can actually just create a new key and you
`16:25` actually just create a new key and you
`16:25` actually just create a new key and you can copy that and give it to the
`16:27` can copy that and give it to the
`16:27` can copy that and give it to the terminal. Now, I don't recommend this at
`16:30` terminal. Now, I don't recommend this at
`16:30` terminal. Now, I don't recommend this at all because at the end of the day, if
`16:33` all because at the end of the day, if
`16:33` all because at the end of the day, if you are paying for a subscription
`16:34` you are paying for a subscription
`16:34` you are paying for a subscription account, you can just log in to Claude
`16:37` account, you can just log in to Claude
`16:37` account, you can just log in to Claude code through this through your
`16:38` code through this through your
`16:38` code through this through your subscription account. It'll say, "Hey,
`16:40` subscription account. It'll say, "Hey,
`16:40` subscription account. It'll say, "Hey, log in with subscription." You'll click
`16:41` log in with subscription." You'll click
`16:41` log in with subscription." You'll click it. It'll open up in your tab. You hit
`16:44` it. It'll open up in your tab. You hit
`16:44` it. It'll open up in your tab. You hit authorize. Boom, you're done. But for
`16:46` authorize. Boom, you're done. But for
`16:46` authorize. Boom, you're done. But for those of you who have maybe a reason to
`16:49` those of you who have maybe a reason to
`16:49` those of you who have maybe a reason to do this and there are certain software
`16:51` do this and there are certain software
`16:51` do this and there are certain software reasons which I described later on why.
`16:53` reasons which I described later on why.
`16:53` reasons which I described later on why. Just wanted to show what that looks like
`16:56` Just wanted to show what that looks like
`16:56` Just wanted to show what that looks like and I'm probably going to show it in
`16:57` and I'm probably going to show it in
`16:57` and I'm probably going to show it in future, you know, ones as well, but
`16:59` future, you know, ones as well, but
`16:59` future, you know, ones as well, but important to understand here, right?
`17:01` important to understand here, right?
`17:01` important to understand here, right? Then from there,
`17:02` Then from there,
`17:02` Then from there, you have Claude anywhere on your
`17:06` you have Claude anywhere on your
`17:06` you have Claude anywhere on your computer, right? You can have it access
`17:08` computer, right? You can have it access
`17:08` computer, right? You can have it access any sort of folders, things like that
`17:11` any sort of folders, things like that
`17:11` any sort of folders, things like that and it's ready to go. And there's a lot
`17:13` and it's ready to go. And there's a lot
`17:13` and it's ready to go. And there's a lot of different commands that you can use,
`17:15` of different commands that you can use,
`17:15` of different commands that you can use, right? Web app testing, you can download
`17:17` right? Web app testing, you can download
`17:17` right? Web app testing, you can download skills. There's all sorts of things of
`17:19` skills. There's all sorts of things of
`17:19` skills. There's all sorts of things of sub agents having Claude use agents. Um
`17:22` sub agents having Claude use agents. Um
`17:22` sub agents having Claude use agents. Um I'll go into that in future lectures for
`17:24` I'll go into that in future lectures for
`17:24` I'll go into that in future lectures for sure as well, but I just want everyone
`17:26` sure as well, but I just want everyone
`17:26` sure as well, but I just want everyone to understand and understand how to be
`17:28` to understand and understand how to be
`17:28` to understand and understand how to be installed, so to speak. This, in my
`17:30` installed, so to speak. This, in my
`17:30` installed, so to speak. This, in my opinion, is the best combination, being
`17:32` opinion, is the best combination, being
`17:32` opinion, is the best combination, being inside of VS Code. For those of you who
`17:34` inside of VS Code. For those of you who
`17:34` inside of VS Code. For those of you who don't know what VS Code is, it's called
`17:36` don't know what VS Code is, it's called
`17:36` don't know what VS Code is, it's called Visual Studio Code. It's the generic
`17:38` Visual Studio Code. It's the generic
`17:38` Visual Studio Code. It's the generic coding app that Microsoft has made. It's
`17:40` coding app that Microsoft has made. It's
`17:40` coding app that Microsoft has made. It's very nice. If you're familiar with
`17:42` very nice. If you're familiar with
`17:42` very nice. If you're familiar with Cursor or Anti-Gravity or any of these
`17:45` Cursor or Anti-Gravity or any of these
`17:45` Cursor or Anti-Gravity or any of these agentic coding tools, they're actually
`17:48` agentic coding tools, they're actually
`17:48` agentic coding tools, they're actually all just built off of VS Code, so I
`17:50` all just built off of VS Code, so I
`17:50` all just built off of VS Code, so I prefer to just use VS Code because
`17:51` prefer to just use VS Code because
`17:51` prefer to just use VS Code because there's no bloat, there's no extraness.
`17:53` there's no bloat, there's no extraness.
`17:53` there's no bloat, there's no extraness. They have any access that you want in
`17:55` They have any access that you want in
`17:55` They have any access that you want in there. It's already built for people to
`17:57` there. It's already built for people to
`17:57` there. It's already built for people to be coding. You don't need to know how to
`17:59` be coding. You don't need to know how to
`17:59` be coding. You don't need to know how to code to use it, right? It lets you open
`18:01` code to use it, right? It lets you open
`18:01` code to use it, right? It lets you open any sort of folders and look at it in a
`18:04` any sort of folders and look at it in a
`18:04` any sort of folders and look at it in a completely different way than you've
`18:05` completely different way than you've
`18:05` completely different way than you've never seen it if you've ever used it.
`18:07` never seen it if you've ever used it.
`18:07` never seen it if you've ever used it. It's made for developers, but I actually
`18:09` It's made for developers, but I actually
`18:09` It's made for developers, but I actually like it for a lot of other reasons. So,
`18:11` like it for a lot of other reasons. So,
`18:11` like it for a lot of other reasons. So, let's open up my content development
`18:13` let's open up my content development
`18:13` let's open up my content development folder, which I already have one open.
`18:16` folder, which I already have one open.
`18:16` folder, which I already have one open. And essentially, it lets you see your
`18:19` And essentially, it lets you see your
`18:19` And essentially, it lets you see your entire folder. Again, let me show you
`18:21` entire folder. Again, let me show you
`18:21` entire folder. Again, let me show you what this looks like just normally. So,
`18:23` what this looks like just normally. So,
`18:23` what this looks like just normally. So, if you were to go onto your normal
`18:25` if you were to go onto your normal
`18:25` if you were to go onto your normal Windows, this is what this looks like.
`18:26` Windows, this is what this looks like.
`18:26` Windows, this is what this looks like. It's the same thing. But now, instead of
`18:28` It's the same thing. But now, instead of
`18:28` It's the same thing. But now, instead of having to click into it to see
`18:29` having to click into it to see
`18:29` having to click into it to see everything, I can open it up like trees
`18:32` everything, I can open it up like trees
`18:32` everything, I can open it up like trees and see all of my folders and processes.
`18:35` and see all of my folders and processes.
`18:35` and see all of my folders and processes. From there, I can actually open up
`18:38` From there, I can actually open up
`18:38` From there, I can actually open up these
`18:39` these
`18:40` these uh oops, these documents as well to be
`18:43` uh oops, these documents as well to be
`18:43` uh oops, these documents as well to be able to see what's going on inside of
`18:44` able to see what's going on inside of
`18:44` able to see what's going on inside of them without having to close the folder
`18:46` them without having to close the folder
`18:46` them without having to close the folder or have a separate tab. But even better,
`18:48` or have a separate tab. But even better,
`18:48` or have a separate tab. But even better, I can open up Claude right in the
`18:50` I can open up Claude right in the
`18:50` I can open up Claude right in the middle, and all I have to do is hit this
`18:52` middle, and all I have to do is hit this
`18:52` middle, and all I have to do is hit this little button here, right? Um this
`18:54` little button here, right? Um this
`18:54` little button here, right? Um this button I can also I have Codex
`18:56` button I can also I have Codex
`18:56` button I can also I have Codex installed, OpenAI's Codex, to be able to
`18:58` installed, OpenAI's Codex, to be able to
`18:58` installed, OpenAI's Codex, to be able to pop over here and use it when I need to.
`19:00` pop over here and use it when I need to.
`19:00` pop over here and use it when I need to. But right now, we're focusing on Claude.
`19:02` But right now, we're focusing on Claude.
`19:02` But right now, we're focusing on Claude. This is super great because what if I'm
`19:04` This is super great because what if I'm
`19:04` This is super great because what if I'm having multiple chats and want to switch
`19:06` having multiple chats and want to switch
`19:06` having multiple chats and want to switch between them and work on multiple things
`19:08` between them and work on multiple things
`19:08` between them and work on multiple things at the same time? This becomes super
`19:10` at the same time? This becomes super
`19:10` at the same time? This becomes super useful. I can read what it's editing
`19:12` useful. I can read what it's editing
`19:12` useful. I can read what it's editing while bouncing between chats, and it
`19:14` while bouncing between chats, and it
`19:14` while bouncing between chats, and it just becomes so nice. But, how do I get
`19:17` just becomes so nice. But, how do I get
`19:17` just becomes so nice. But, how do I get this here? Well, first, you need to make
`19:19` this here? Well, first, you need to make
`19:19` this here? Well, first, you need to make sure you've done the installation
`19:20` sure you've done the installation
`19:20` sure you've done the installation process before with the normal Claude
`19:23` process before with the normal Claude
`19:23` process before with the normal Claude code. But, once it's on and once you
`19:25` code. But, once it's on and once you
`19:25` code. But, once it's on and once you install
`19:26` install
`19:26` install um VS Code, Visual Studio Code, again,
`19:28` um VS Code, Visual Studio Code, again,
`19:28` um VS Code, Visual Studio Code, again, if you're on Windows, you can literally
`19:30` if you're on Windows, you can literally
`19:30` if you're on Windows, you can literally just download it from the App Store.
`19:32` just download it from the App Store.
`19:32` just download it from the App Store. Um it should pop up pretty easily.
`19:34` Um it should pop up pretty easily.
`19:35` Um it should pop up pretty easily. Just install that. Make sure it's good
`19:36` Just install that. Make sure it's good
`19:36` Just install that. Make sure it's good to go. And from there, you actually will
`19:39` to go. And from there, you actually will
`19:39` to go. And from there, you actually will open up VS Code, and you want to
`19:41` open up VS Code, and you want to
`19:41` open up VS Code, and you want to actually look for something called
`19:43` actually look for something called
`19:43` actually look for something called extensions. So, on the left here, you're
`19:45` extensions. So, on the left here, you're
`19:45` extensions. So, on the left here, you're going to have a whole bunch of options,
`19:47` going to have a whole bunch of options,
`19:47` going to have a whole bunch of options, but this one right here with the squares
`19:49` but this one right here with the squares
`19:49` but this one right here with the squares and the one slightly tilted called
`19:51` and the one slightly tilted called
`19:51` and the one slightly tilted called extensions. You can also hit control
`19:53` extensions. You can also hit control
`19:53` extensions. You can also hit control shift X. Will open it up, and you can
`19:56` shift X. Will open it up, and you can
`19:56` shift X. Will open it up, and you can just type in Claude code, and it says
`19:59` just type in Claude code, and it says
`19:59` just type in Claude code, and it says Claude code for VS Code, and you're
`20:01` Claude code for VS Code, and you're
`20:01` Claude code for VS Code, and you're going to have a little button blue
`20:03` going to have a little button blue
`20:03` going to have a little button blue button that says install. You'll click
`20:05` button that says install. You'll click
`20:05` button that says install. You'll click install, and that's it. You might have
`20:07` install, and that's it. You might have
`20:07` install, and that's it. You might have to restart it, but you should now see
`20:09` to restart it, but you should now see
`20:09` to restart it, but you should now see this there. One important note some
`20:11` this there. One important note some
`20:11` this there. One important note some people get confused by is that when you
`20:13` people get confused by is that when you
`20:13` people get confused by is that when you open up VS Code, and you open up like
`20:16` open up VS Code, and you open up like
`20:16` open up VS Code, and you open up like maybe a brand new folder that you've
`20:17` maybe a brand new folder that you've
`20:17` maybe a brand new folder that you've never touched before. So, let's do uh
`20:20` never touched before. So, let's do uh
`20:20` never touched before. So, let's do uh let's do this one.
`20:22` let's do this one.
`20:22` let's do this one. Right? Yes, I trust the authors. Uh if
`20:25` Right? Yes, I trust the authors. Uh if
`20:25` Right? Yes, I trust the authors. Uh if sometimes you have this, right? Where
`20:26` sometimes you have this, right? Where
`20:26` sometimes you have this, right? Where there's nothing there,
`20:28` there's nothing there,
`20:28` there's nothing there, and you see nowhere to add anything. You
`20:30` and you see nowhere to add anything. You
`20:30` and you see nowhere to add anything. You actually need to click on something and
`20:32` actually need to click on something and
`20:32` actually need to click on something and have a page open to get that little
`20:35` have a page open to get that little
`20:35` have a page open to get that little extension open. Also, VS Code has its
`20:37` extension open. Also, VS Code has its
`20:37` extension open. Also, VS Code has its own chat built in as well that you can
`20:39` own chat built in as well that you can
`20:39` own chat built in as well that you can use just in case something's wrong with
`20:41` use just in case something's wrong with
`20:41` use just in case something's wrong with Claude, or you're not paying for the
`20:43` Claude, or you're not paying for the
`20:43` Claude, or you're not paying for the account. And what's really cool is you
`20:45` account. And what's really cool is you
`20:46` account. And what's really cool is you can add your own language models in here
`20:48` can add your own language models in here
`20:48` can add your own language models in here if you log in, and you can add Claude to
`20:50` if you log in, and you can add Claude to
`20:50` if you log in, and you can add Claude to this as well. I just like to use
`20:52` this as well. I just like to use
`20:52` this as well. I just like to use directly through Claude cuz there's it's
`20:54` directly through Claude cuz there's it's
`20:54` directly through Claude cuz there's it's cheaper, it works better, it's cleaner,
`20:56` cheaper, it works better, it's cleaner,
`20:56` cheaper, it works better, it's cleaner, there's less bloat, and I really think
`20:57` there's less bloat, and I really think
`20:57` there's less bloat, and I really think it's the way to go. I'll have a whole
`20:59` it's the way to go. I'll have a whole
`20:59` it's the way to go. I'll have a whole video on which models are good at what,
`21:01` video on which models are good at what,
`21:01` video on which models are good at what, which one's better. At the end of the
`21:03` which one's better. At the end of the
`21:03` which one's better. At the end of the day, pick one and build the systems and
`21:06` day, pick one and build the systems and
`21:06` day, pick one and build the systems and the knowledge of how to use that one
`21:08` the knowledge of how to use that one
`21:08` the knowledge of how to use that one well, it will easily transfer all the
`21:10` well, it will easily transfer all the
`21:10` well, it will easily transfer all the other ones and you can make your choice
`21:12` other ones and you can make your choice
`21:12` other ones and you can make your choice after you get decent at using one, okay?
`21:16` after you get decent at using one, okay?
`21:16` after you get decent at using one, okay? Simple as that.
`21:17` Simple as that.
`21:17` Simple as that. And if you have any questions about this
`21:19` And if you have any questions about this
`21:19` And if you have any questions about this whole install process, please throw them
`21:21` whole install process, please throw them
`21:21` whole install process, please throw them in the community. Happy to help out.
`21:23` in the community. Happy to help out.
`21:23` in the community. Happy to help out. Happy to try to figure it out for you.
`21:25` Happy to try to figure it out for you.
`21:25` Happy to try to figure it out for you. That's the main goal I had for all of
`21:26` That's the main goal I had for all of
`21:26` That's the main goal I had for all of this.
`21:28` this.
`21:28` this. Now, one thing that I want you all to
`21:30` Now, one thing that I want you all to
`21:30` Now, one thing that I want you all to do, as I said, this isn't just a
`21:31` do, as I said, this isn't just a
`21:31` do, as I said, this isn't just a lecture. I'm going to give you some
`21:33` lecture. I'm going to give you some
`21:33` lecture. I'm going to give you some homework or at least something that you
`21:34` homework or at least something that you
`21:34` homework or at least something that you can do and I'll have links for files and
`21:36` can do and I'll have links for files and
`21:36` can do and I'll have links for files and documents that are a little extra
`21:38` documents that are a little extra
`21:38` documents that are a little extra curricular if you need to practice, but
`21:40` curricular if you need to practice, but
`21:40` curricular if you need to practice, but pick one thing from your own work and
`21:43` pick one thing from your own work and
`21:43` pick one thing from your own work and actually try to get it summarized. Put
`21:45` actually try to get it summarized. Put
`21:45` actually try to get it summarized. Put it in a folder, have Claude code
`21:47` it in a folder, have Claude code
`21:47` it in a folder, have Claude code summarize it, try to use it with your
`21:49` summarize it, try to use it with your
`21:49` summarize it, try to use it with your normal Claude desktop, whatever you
`21:51` normal Claude desktop, whatever you
`21:51` normal Claude desktop, whatever you choose, and try to get it to answer
`21:53` choose, and try to get it to answer
`21:53` choose, and try to get it to answer things or build things from that single
`21:55` things or build things from that single
`21:55` things or build things from that single file. Run it in desktop, then try the VS
`21:58` file. Run it in desktop, then try the VS
`21:58` file. Run it in desktop, then try the VS Code extension if you want. Maybe look
`22:01` Code extension if you want. Maybe look
`22:01` Code extension if you want. Maybe look at the outputs, but think about the
`22:02` at the outputs, but think about the
`22:02` at the outputs, but think about the workflow, right? This is really
`22:04` workflow, right? This is really
`22:04` workflow, right? This is really important. You want to be able to build
`22:08` important. You want to be able to build
`22:08` important. You want to be able to build with these. You want to create scale.
`22:10` with these. You want to create scale.
`22:10` with these. You want to create scale. It's not just about installing something
`22:12` It's not just about installing something
`22:12` It's not just about installing something that's good. It's about thinking about,
`22:14` that's good. It's about thinking about,
`22:14` that's good. It's about thinking about, all right, if I'm going to be building a
`22:15` all right, if I'm going to be building a
`22:15` all right, if I'm going to be building a lot of stuff, if I'm running a company,
`22:17` lot of stuff, if I'm running a company,
`22:17` lot of stuff, if I'm running a company, running a startup, running a content
`22:18` running a startup, running a content
`22:18` running a startup, running a content things, what works best for me to create
`22:21` things, what works best for me to create
`22:21` things, what works best for me to create these builds? In my opinion, I love just
`22:24` these builds? In my opinion, I love just
`22:24` these builds? In my opinion, I love just having a folder that I enable to
`22:25` having a folder that I enable to
`22:25` having a folder that I enable to navigate easily inside of VS Code
`22:29` navigate easily inside of VS Code
`22:29` navigate easily inside of VS Code because that means I have all the access
`22:32` because that means I have all the access
`22:32` because that means I have all the access to stuff. I can organize it, right? All
`22:33` to stuff. I can organize it, right? All
`22:34` to stuff. I can organize it, right? All of your courses I'm building right here.
`22:36` of your courses I'm building right here.
`22:36` of your courses I'm building right here. I'm working with Claude Code because
`22:37` I'm working with Claude Code because
`22:37` I'm working with Claude Code because again, I don't want it to automate the
`22:39` again, I don't want it to automate the
`22:39` again, I don't want it to automate the whole process. I want it to help me make
`22:41` whole process. I want it to help me make
`22:41` whole process. I want it to help me make this process better because I'm doing
`22:43` this process better because I'm doing
`22:43` this process better because I'm doing this all on my own. I would usually have
`22:45` this all on my own. I would usually have
`22:45` this all on my own. I would usually have to hire a full team to do all of this,
`22:47` to hire a full team to do all of this,
`22:47` to hire a full team to do all of this, but because I've organized it in how I
`22:49` but because I've organized it in how I
`22:49` but because I've organized it in how I work, how I think, like, oh, I've got my
`22:51` work, how I think, like, oh, I've got my
`22:51` work, how I think, like, oh, I've got my cleat notes for all of you here. I have
`22:53` cleat notes for all of you here. I have
`22:54` cleat notes for all of you here. I have my scripts for my long form and stuff
`22:56` my scripts for my long form and stuff
`22:56` my scripts for my long form and stuff that I can edit easily in here. Um, if I
`22:58` that I can edit easily in here. Um, if I
`22:58` that I can edit easily in here. Um, if I don't like a script, I can change it and
`23:00` don't like a script, I can change it and
`23:00` don't like a script, I can change it and then Claude can easily reference it,
`23:02` then Claude can easily reference it,
`23:02` then Claude can easily reference it, right? That's why I like using VS Code
`23:04` right? That's why I like using VS Code
`23:04` right? That's why I like using VS Code so much. It's just It's a really great
`23:07` so much. It's just It's a really great
`23:07` so much. It's just It's a really great platform to look at, but you can use the
`23:09` platform to look at, but you can use the
`23:09` platform to look at, but you can use the normal Claude, you know, code or code
`23:11` normal Claude, you know, code or code
`23:11` normal Claude, you know, code or code work to do this as well. It's just I
`23:14` work to do this as well. It's just I
`23:14` work to do this as well. It's just I find it harder because it's I don't get
`23:16` find it harder because it's I don't get
`23:17` find it harder because it's I don't get to see everything. I want to be able to
`23:18` to see everything. I want to be able to
`23:18` to see everything. I want to be able to click back and forth quickly and it just
`23:20` click back and forth quickly and it just
`23:20` click back and forth quickly and it just kind of gets in the way. So, practice
`23:22` kind of gets in the way. So, practice
`23:22` kind of gets in the way. So, practice with it, try to see what you like
`23:24` with it, try to see what you like
`23:24` with it, try to see what you like better, what works for you, then we can
`23:27` better, what works for you, then we can
`23:27` better, what works for you, then we can think about sitting down on one specific
`23:29` think about sitting down on one specific
`23:29` think about sitting down on one specific tool and using that only.
`23:32` tool and using that only.
`23:32` tool and using that only. If you do have any comparisons or
`23:33` If you do have any comparisons or
`23:33` If you do have any comparisons or anything where you're like, "Hey, this
`23:35` anything where you're like, "Hey, this
`23:35` anything where you're like, "Hey, this happened with one." Please post them. Is
`23:36` happened with one." Please post them. Is
`23:36` happened with one." Please post them. Is there anything that surprised you where
`23:38` there anything that surprised you where
`23:38` there anything that surprised you where you're like, "Whoa, I didn't know Claude
`23:39` you're like, "Whoa, I didn't know Claude
`23:39` you're like, "Whoa, I didn't know Claude could see that on my computer or execute
`23:41` could see that on my computer or execute
`23:41` could see that on my computer or execute stuff." This is kind of what we want to
`23:43` stuff." This is kind of what we want to
`23:43` stuff." This is kind of what we want to do.
`23:44` do.
`23:44` do. Now,
`23:45` Now,
`23:45` Now, this is the end of the first session.
`23:47` this is the end of the first session.
`23:47` this is the end of the first session. I'm going to be posting this right now
`23:49` I'm going to be posting this right now
`23:49` I'm going to be posting this right now after I'm done editing and taking out
`23:51` after I'm done editing and taking out
`23:51` after I'm done editing and taking out all the nasty parts where I maybe
`23:53` all the nasty parts where I maybe
`23:53` all the nasty parts where I maybe skipped something or said something
`23:54` skipped something or said something
`23:54` skipped something or said something wrong, but session two is actually going
`23:56` wrong, but session two is actually going
`23:56` wrong, but session two is actually going to be showing you Claude code in
`23:58` to be showing you Claude code in
`23:58` to be showing you Claude code in practice. I'm going to be filming that
`23:59` practice. I'm going to be filming that
`23:59` practice. I'm going to be filming that now. Hopefully, by the time you're
`24:01` now. Hopefully, by the time you're
`24:01` now. Hopefully, by the time you're watching this video, I'll have already
`24:03` watching this video, I'll have already
`24:03` watching this video, I'll have already posted it. If you're early comer, then
`24:05` posted it. If you're early comer, then
`24:05` posted it. If you're early comer, then you probably aren't going to see it, but
`24:07` you probably aren't going to see it, but
`24:07` you probably aren't going to see it, but I promise you I'm working hard to get
`24:08` I promise you I'm working hard to get
`24:08` I promise you I'm working hard to get this all out to you all. So, yeah, stay
`24:10` this all out to you all. So, yeah, stay
`24:10` this all out to you all. So, yeah, stay tuned. We're going to be diving into a
`24:12` tuned. We're going to be diving into a
`24:12` tuned. We're going to be diving into a lot in that session about how to use
`24:14` lot in that session about how to use
`24:14` lot in that session about how to use Claude code, its real power, how you can
`24:17` Claude code, its real power, how you can
`24:17` Claude code, its real power, how you can see and get that kind of workflow
`24:19` see and get that kind of workflow
`24:19` see and get that kind of workflow process. Cuz you see, this isn't just
`24:20` process. Cuz you see, this isn't just
`24:20` process. Cuz you see, this isn't just about using an AI model, this is about
`24:22` about using an AI model, this is about
`24:22` about using an AI model, this is about designing your workflow to really get a
`24:24` designing your workflow to really get a
`24:24` designing your workflow to really get a lot out of it. But,
`24:26` lot out of it. But,
`24:26` lot out of it. But, till next time, friends. Much love and
`24:29` till next time, friends. Much love and
`24:29` till next time, friends. Much love and please stick with me as I build this
`24:31` please stick with me as I build this
`24:31` please stick with me as I build this channel and this community.