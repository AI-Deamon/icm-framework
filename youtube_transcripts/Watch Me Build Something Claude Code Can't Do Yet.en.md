# Watch Me Build Something Claude Code Can't Do Yet

**Channel:** Jake Van Clief  
**Uploaded:** 2026-03-03  
**Duration:** 14:50  
**URL:** [https://www.youtube.com/watch?v=KC0VEZuo4OI](https://www.youtube.com/watch?v=KC0VEZuo4OI)  

---

## Transcript

`00:01` I figured I would record my work
`00:01` I figured I would record my work process. This is less of a lecture video
`00:03` process. This is less of a lecture video
`00:03` process. This is less of a lecture video and more of just kind of almost like a
`00:04` and more of just kind of almost like a
`00:04` and more of just kind of almost like a live stream.
`00:06` live stream.
`00:06` live stream. So, basically what I'm thinking is I
`00:09` So, basically what I'm thinking is I
`00:09` So, basically what I'm thinking is I should make a front end for this, but I
`00:10` should make a front end for this, but I
`00:10` should make a front end for this, but I don't want to make a front end that
`00:12` don't want to make a front end that
`00:12` don't want to make a front end that calls Claude because then we have to use
`00:14` calls Claude because then we have to use
`00:14` calls Claude because then we have to use API fees. I want to make a front end
`00:16` API fees. I want to make a front end
`00:16` API fees. I want to make a front end that lets me navigate all of my works
`00:19` that lets me navigate all of my works
`00:19` that lets me navigate all of my works here, right? Because I can take scripts
`00:21` here, right? Because I can take scripts
`00:21` here, right? Because I can take scripts and I can take them and turn them into,
`00:23` and I can take them and turn them into,
`00:23` and I can take them and turn them into, you know, full animations.
`00:25` you know, full animations.
`00:25` you know, full animations. Um and I want to be able to, you know,
`00:28` Um and I want to be able to, you know,
`00:28` Um and I want to be able to, you know, have my workflow to create these
`00:29` have my workflow to create these
`00:29` have my workflow to create these animations, but have it where it's much
`00:31` animations, but have it where it's much
`00:31` animations, but have it where it's much more organized and clear. Kind of like
`00:32` more organized and clear. Kind of like
`00:32` more organized and clear. Kind of like how there's a front end for when my
`00:34` how there's a front end for when my
`00:34` how there's a front end for when my animations are done and I can look at it
`00:36` animations are done and I can look at it
`00:36` animations are done and I can look at it them and look through them. But, you
`00:38` them and look through them. But, you
`00:38` them and look through them. But, you know, something a little bit cleaner
`00:39` know, something a little bit cleaner
`00:39` know, something a little bit cleaner there. I found a couple GitHub repos
`00:41` there. I found a couple GitHub repos
`00:41` there. I found a couple GitHub repos that already do that. Claude CLI, Claude
`00:43` that already do that. Claude CLI, Claude
`00:43` that already do that. Claude CLI, Claude Code Web, and Opcode do similar things,
`00:46` Code Web, and Opcode do similar things,
`00:46` Code Web, and Opcode do similar things, but they're not really doing everything
`00:48` but they're not really doing everything
`00:48` but they're not really doing everything that I wanted. So, I went ahead and
`00:50` that I wanted. So, I went ahead and
`00:50` that I wanted. So, I went ahead and created a large PRD, right? Product
`00:52` created a large PRD, right? Product
`00:52` created a large PRD, right? Product Requirements Document, um that describes
`00:55` Requirements Document, um that describes
`00:55` Requirements Document, um that describes it to Claude Code so it always knows
`00:56` it to Claude Code so it always knows
`00:56` it to Claude Code so it always knows where we are, right? Hey, here's the
`00:58` where we are, right? Hey, here's the
`00:58` where we are, right? Hey, here's the repos that I was talking about. Here's
`01:00` repos that I was talking about. Here's
`01:00` repos that I was talking about. Here's the stacks that those repos have. You
`01:02` the stacks that those repos have. You
`01:02` the stacks that those repos have. You can have Claude create these PRDs for
`01:04` can have Claude create these PRDs for
`01:04` can have Claude create these PRDs for you as well.
`01:05` you as well.
`01:05` you as well. Um and kind of like what I liked about
`01:07` Um and kind of like what I liked about
`01:07` Um and kind of like what I liked about them, but what I wanted to cut, right?
`01:09` them, but what I wanted to cut, right?
`01:09` them, but what I wanted to cut, right? We want to get rid of most of the front
`01:10` We want to get rid of most of the front
`01:10` We want to get rid of most of the front ends on these other repos because the
`01:12` ends on these other repos because the
`01:12` ends on these other repos because the end of the day I just I don't know. I
`01:14` end of the day I just I don't know. I
`01:14` end of the day I just I don't know. I didn't I didn't I wasn't vibing with
`01:16` didn't I didn't I wasn't vibing with
`01:16` didn't I didn't I wasn't vibing with them, I guess is the question. I don't
`01:17` them, I guess is the question. I don't
`01:17` them, I guess is the question. I don't know what what is
`01:19` know what what is
`01:19` know what what is I'm sure I have it Here we go. Yeah, I
`01:20` I'm sure I have it Here we go. Yeah, I
`01:20` I'm sure I have it Here we go. Yeah, I just they weren't fitting what I wanted.
`01:23` just they weren't fitting what I wanted.
`01:23` just they weren't fitting what I wanted. They didn't look I don't know. It just
`01:24` They didn't look I don't know. It just
`01:24` They didn't look I don't know. It just didn't This isn't my workflow. I'd
`01:26` didn't This isn't my workflow. I'd
`01:27` didn't This isn't my workflow. I'd rather just sit and work inside of VS
`01:29` rather just sit and work inside of VS
`01:29` rather just sit and work inside of VS Code in the folders instead of use these
`01:31` Code in the folders instead of use these
`01:31` Code in the folders instead of use these types of fronts. This one was okay, but
`01:33` types of fronts. This one was okay, but
`01:33` types of fronts. This one was okay, but I didn't need all this crazy choice. I
`01:35` I didn't need all this crazy choice. I
`01:35` I didn't need all this crazy choice. I didn't need and drop it and all these
`01:37` didn't need and drop it and all these
`01:37` didn't need and drop it and all these things. I wanted something for me in my
`01:39` things. I wanted something for me in my
`01:39` things. I wanted something for me in my area and I'll make it modular later.
`01:42` area and I'll make it modular later.
`01:42` area and I'll make it modular later. But, they had a lot of really good ideas
`01:44` But, they had a lot of really good ideas
`01:44` But, they had a lot of really good ideas inside of this as well as like the way
`01:46` inside of this as well as like the way
`01:46` inside of this as well as like the way they were doing things. So, that's kind
`01:48` they were doing things. So, that's kind
`01:48` they were doing things. So, that's kind of what I was going to kind of build and
`01:50` of what I was going to kind of build and
`01:50` of what I was going to kind of build and look at here. So, the first thing I did
`01:53` look at here. So, the first thing I did
`01:53` look at here. So, the first thing I did is
`01:54` is
`01:54` is um
`01:54` um
`01:54` um I'm inside this workspace because this
`01:56` I'm inside this workspace because this
`01:56` I'm inside this workspace because this has a lot of the information and
`01:58` has a lot of the information and
`01:58` has a lot of the information and processes, so I built a new folder that
`02:01` processes, so I built a new folder that
`02:01` processes, so I built a new folder that says front end, right? And so that way,
`02:03` says front end, right? And so that way,
`02:03` says front end, right? And so that way, you know, I have my my stuff is already
`02:06` you know, I have my my stuff is already
`02:06` you know, I have my my stuff is already all here that I can reference later or I
`02:08` all here that I can reference later or I
`02:08` all here that I can reference later or I can have Claude go running Brad as
`02:10` can have Claude go running Brad as
`02:10` can have Claude go running Brad as context easier without having to upload
`02:12` context easier without having to upload
`02:13` context easier without having to upload it.
`02:13` it.
`02:13` it. Um but I just dropped my PRD in here and
`02:16` Um but I just dropped my PRD in here and
`02:16` Um but I just dropped my PRD in here and it went through and described it, right?
`02:18` it went through and described it, right?
`02:18` it went through and described it, right? Here's what the workspace looks like and
`02:20` Here's what the workspace looks like and
`02:20` Here's what the workspace looks like and thinking this is a pretty good
`02:21` thinking this is a pretty good
`02:21` thinking this is a pretty good architecture. I went through and edited
`02:23` architecture. I went through and edited
`02:23` architecture. I went through and edited a little bit cuz I Claude generate a lot
`02:25` a little bit cuz I Claude generate a lot
`02:25` a little bit cuz I Claude generate a lot of this, but I was thinking about, okay,
`02:27` of this, but I was thinking about, okay,
`02:27` of this, but I was thinking about, okay, I want a workflow map as well. So we
`02:30` I want a workflow map as well. So we
`02:30` I want a workflow map as well. So we create like nodes and stuff. You know,
`02:31` create like nodes and stuff. You know,
`02:31` create like nodes and stuff. You know, have you ever seen like mind maps and
`02:33` have you ever seen like mind maps and
`02:33` have you ever seen like mind maps and web maps where you can look through?
`02:35` web maps where you can look through?
`02:35` web maps where you can look through? Well, I want a similar thing, but in
`02:37` Well, I want a similar thing, but in
`02:37` Well, I want a similar thing, but in that thing be able to edit these
`02:39` that thing be able to edit these
`02:39` that thing be able to edit these folders. So like I start at script lab.
`02:42` folders. So like I start at script lab.
`02:42` folders. So like I start at script lab. I create a short form script, right? And
`02:44` I create a short form script, right? And
`02:44` I create a short form script, right? And I get all these different scripts. Then
`02:46` I get all these different scripts. Then
`02:46` I get all these different scripts. Then that moves into my animation studio and
`02:49` that moves into my animation studio and
`02:49` that moves into my animation studio and my animation studio creates all sorts of
`02:51` my animation studio creates all sorts of
`02:51` my animation studio creates all sorts of things and and processes, right? Uh and
`02:53` things and and processes, right? Uh and
`02:53` things and and processes, right? Uh and goes through a workflow where I go into
`02:55` goes through a workflow where I go into
`02:55` goes through a workflow where I go into a spec and I build that and then that
`02:57` a spec and I build that and then that
`02:57` a spec and I build that and then that turns into a render which is then, you
`02:59` turns into a render which is then, you
`03:00` turns into a render which is then, you know, something that I can actually uh
`03:03` know, something that I can actually uh
`03:03` know, something that I can actually uh build, so to speak. Um or look at the
`03:06` build, so to speak. Um or look at the
`03:06` build, so to speak. Um or look at the animations from. And so that's something
`03:09` animations from. And so that's something
`03:09` animations from. And so that's something I want to map visually rather than just
`03:11` I want to map visually rather than just
`03:11` I want to map visually rather than just stare at, you know, a folder structure.
`03:13` stare at, you know, a folder structure.
`03:13` stare at, you know, a folder structure. And this is great. It organizes it well,
`03:15` And this is great. It organizes it well,
`03:15` And this is great. It organizes it well, but I think it could be even better.
`03:17` but I think it could be even better.
`03:17` but I think it could be even better. Um and so that's kind of what I want to
`03:20` Um and so that's kind of what I want to
`03:20` Um and so that's kind of what I want to do with all of this. Maybe even have a
`03:22` do with all of this. Maybe even have a
`03:22` do with all of this. Maybe even have a chat, right? Just a panel right there
`03:24` chat, right? Just a panel right there
`03:24` chat, right? Just a panel right there that you see much similar to this. And
`03:26` that you see much similar to this. And
`03:26` that you see much similar to this. And again, we're relying on Claude's code's
`03:28` again, we're relying on Claude's code's
`03:28` again, we're relying on Claude's code's infrastructure. We're not creating a
`03:30` infrastructure. We're not creating a
`03:30` infrastructure. We're not creating a separate app. We're not using anything
`03:32` separate app. We're not using anything
`03:32` separate app. We're not using anything doing anything craziness. We're just
`03:34` doing anything craziness. We're just
`03:34` doing anything craziness. We're just making something better, a better
`03:35` making something better, a better
`03:35` making something better, a better interface for how I want to work with
`03:38` interface for how I want to work with
`03:38` interface for how I want to work with Claude code.
`03:39` Claude code.
`03:39` Claude code. Um now we've got the idea of a panel,
`03:42` Um now we've got the idea of a panel,
`03:42` Um now we've got the idea of a panel, right? So it's like, okay, I want to
`03:43` right? So it's like, okay, I want to
`03:43` right? So it's like, okay, I want to have maybe my workflow map on the left,
`03:46` have maybe my workflow map on the left,
`03:46` have maybe my workflow map on the left, file editor in the middle, and Claude
`03:48` file editor in the middle, and Claude
`03:48` file editor in the middle, and Claude code on the right, and being able to
`03:49` code on the right, and being able to
`03:49` code on the right, and being able to open multiple chats is going to be super
`03:51` open multiple chats is going to be super
`03:51` open multiple chats is going to be super important there Um and being able to
`03:53` important there Um and being able to
`03:53` important there Um and being able to collapse them. And again, this is why
`03:55` collapse them. And again, this is why
`03:55` collapse them. And again, this is why it's important to do a PRD because you
`03:57` it's important to do a PRD because you
`03:57` it's important to do a PRD because you can lay all this out and edit it. If you
`03:59` can lay all this out and edit it. If you
`03:59` can lay all this out and edit it. If you didn't like this, you don't want Claude
`04:00` didn't like this, you don't want Claude
`04:01` didn't like this, you don't want Claude to just run out and start building
`04:02` to just run out and start building
`04:02` to just run out and start building stuff. You want it to be right from the
`04:04` stuff. You want it to be right from the
`04:04` stuff. You want it to be right from the get-go and then it expands on what you
`04:06` get-go and then it expands on what you
`04:06` get-go and then it expands on what you like. Um
`04:08` like. Um
`04:08` like. Um So I'm going to look at pipeline
`04:09` So I'm going to look at pipeline
`04:09` So I'm going to look at pipeline definitions. I'm going to look at
`04:12` definitions. I'm going to look at
`04:12` definitions. I'm going to look at kind of okay, here is probably what has
`04:15` kind of okay, here is probably what has
`04:15` kind of okay, here is probably what has worked well in the past. Here's the root
`04:17` worked well in the past. Here's the root
`04:18` worked well in the past. Here's the root path. So this is built off of my actual
`04:21` path. So this is built off of my actual
`04:21` path. So this is built off of my actual workflow here. And this is a JavaScript.
`04:23` workflow here. And this is a JavaScript.
`04:23` workflow here. And this is a JavaScript. You don't necessarily need to write it
`04:24` You don't necessarily need to write it
`04:24` You don't necessarily need to write it like this. You could write it where it
`04:26` like this. You could write it where it
`04:26` like this. You could write it where it looks something more like flat code,
`04:28` looks something more like flat code,
`04:28` looks something more like flat code, right? Like hey, to write a script, I'm
`04:30` right? Like hey, to write a script, I'm
`04:30` right? Like hey, to write a script, I'm in this folder. To learn script
`04:33` in this folder. To learn script
`04:33` in this folder. To learn script structure, go to this folder. That's
`04:35` structure, go to this folder. That's
`04:35` structure, go to this folder. That's another way you could do that. However,
`04:37` another way you could do that. However,
`04:37` another way you could do that. However, if we're going to put it in a front end,
`04:39` if we're going to put it in a front end,
`04:39` if we're going to put it in a front end, we're actually going to need some sort
`04:40` we're actually going to need some sort
`04:40` we're actually going to need some sort of front end style image or or or code,
`04:44` of front end style image or or or code,
`04:45` of front end style image or or or code, you know, React or anything like that,
`04:46` you know, React or anything like that,
`04:46` you know, React or anything like that, but JavaScript is a really common one
`04:48` but JavaScript is a really common one
`04:48` but JavaScript is a really common one that everyone uses. And again, you can
`04:50` that everyone uses. And again, you can
`04:50` that everyone uses. And again, you can ask Claude to build something like this.
`04:52` ask Claude to build something like this.
`04:52` ask Claude to build something like this. And I'm going to be making more courses
`04:54` And I'm going to be making more courses
`04:55` And I'm going to be making more courses that dive deeper into what JavaScript
`04:57` that dive deeper into what JavaScript
`04:57` that dive deeper into what JavaScript is, what Python is, like how do you read
`04:59` is, what Python is, like how do you read
`04:59` is, what Python is, like how do you read this? But at the end of the day, you'll
`05:01` this? But at the end of the day, you'll
`05:01` this? But at the end of the day, you'll start to learn that all programming
`05:02` start to learn that all programming
`05:03` start to learn that all programming languages are roughly the same. They
`05:04` languages are roughly the same. They
`05:04` languages are roughly the same. They have some sort of definition and then
`05:06` have some sort of definition and then
`05:06` have some sort of definition and then they have a description for that
`05:07` they have a description for that
`05:07` they have a description for that definition and then that description and
`05:09` definition and then that description and
`05:09` definition and then that description and definition can be paired with others
`05:12` definition can be paired with others
`05:12` definition can be paired with others which are then sent to other
`05:13` which are then sent to other
`05:13` which are then sent to other descriptions and definitions so that you
`05:15` descriptions and definitions so that you
`05:15` descriptions and definitions so that you can then look at stuff. It's the same
`05:16` can then look at stuff. It's the same
`05:16` can then look at stuff. It's the same thing here, right? You have a folder and
`05:19` thing here, right? You have a folder and
`05:19` thing here, right? You have a folder and in that folder you have definitions,
`05:21` in that folder you have definitions,
`05:21` in that folder you have definitions, right? So you have a description of what
`05:22` right? So you have a description of what
`05:22` right? So you have a description of what is should be in there and then you have
`05:24` is should be in there and then you have
`05:24` is should be in there and then you have things that work inside of that folder.
`05:26` things that work inside of that folder.
`05:26` things that work inside of that folder. This is the same thing here but written
`05:28` This is the same thing here but written
`05:28` This is the same thing here but written out programmatically. I'm very
`05:30` out programmatically. I'm very
`05:30` out programmatically. I'm very simplifying a lot of this, but that's
`05:32` simplifying a lot of this, but that's
`05:32` simplifying a lot of this, but that's kind of the idea here.
`05:34` kind of the idea here.
`05:34` kind of the idea here. Um I'm also giving the idea of like what
`05:36` Um I'm also giving the idea of like what
`05:36` Um I'm also giving the idea of like what I want. I want to be able to click
`05:37` I want. I want to be able to click
`05:37` I want. I want to be able to click nodes. I want to be able to click sub
`05:40` nodes. I want to be able to click sub
`05:40` nodes. I want to be able to click sub stuff. So instead of me just clicking
`05:42` stuff. So instead of me just clicking
`05:42` stuff. So instead of me just clicking like this, the same exact thing's
`05:44` like this, the same exact thing's
`05:44` like this, the same exact thing's happening, but it's like a really
`05:45` happening, but it's like a really
`05:45` happening, but it's like a really pretty, you know, visual designer or
`05:47` pretty, you know, visual designer or
`05:47` pretty, you know, visual designer or something like that.
`05:49` something like that.
`05:49` something like that. Um and I'm going to keep it clean and
`05:50` Um and I'm going to keep it clean and
`05:50` Um and I'm going to keep it clean and minimal.
`05:51` minimal.
`05:51` minimal. Um
`05:52` Um
`05:52` Um matrix maybe? No, I don't know if I want
`05:55` matrix maybe? No, I don't know if I want
`05:55` matrix maybe? No, I don't know if I want to add matrix. So, anything I add here
`05:57` to add matrix. So, anything I add here
`05:57` to add matrix. So, anything I add here is what it's going to be for. I want to
`05:58` is what it's going to be for. I want to
`05:58` is what it's going to be for. I want to think more about my visual style,
`06:00` think more about my visual style,
`06:00` think more about my visual style, honestly, but I think it's good where it
`06:02` honestly, but I think it's good where it
`06:02` honestly, but I think it's good where it is. Um and after that, it's just simple
`06:05` is. Um and after that, it's just simple
`06:05` is. Um and after that, it's just simple things like that. Here's the steps that
`06:07` things like that. Here's the steps that
`06:07` things like that. Here's the steps that I want you to take, right? Make sure
`06:09` I want you to take, right? Make sure
`06:09` I want you to take, right? Make sure you're removing all these features. The
`06:10` you're removing all these features. The
`06:10` you're removing all these features. The first thing we're going to do is
`06:11` first thing we're going to do is
`06:11` first thing we're going to do is actually look at and clone these repos.
`06:14` actually look at and clone these repos.
`06:14` actually look at and clone these repos. So, when I say clone a repo, that means
`06:16` So, when I say clone a repo, that means
`06:16` So, when I say clone a repo, that means literally downloading someone else's
`06:19` literally downloading someone else's
`06:19` literally downloading someone else's code or GitHub. And again, I'm making
`06:22` code or GitHub. And again, I'm making
`06:22` code or GitHub. And again, I'm making videos on what GitHub is, what Git is,
`06:24` videos on what GitHub is, what Git is,
`06:24` videos on what GitHub is, what Git is, as well. Uh so, keep an eye out for
`06:26` as well. Uh so, keep an eye out for
`06:26` as well. Uh so, keep an eye out for those. But, someone already did a lot of
`06:28` those. But, someone already did a lot of
`06:28` those. But, someone already did a lot of work and then they put it up on GitHub
`06:31` work and then they put it up on GitHub
`06:31` work and then they put it up on GitHub and then I can come in and actually look
`06:32` and then I can come in and actually look
`06:32` and then I can come in and actually look at it, see what their code is. So, my
`06:35` at it, see what their code is. So, my
`06:35` at it, see what their code is. So, my goal is to actually download this, strip
`06:37` goal is to actually download this, strip
`06:37` goal is to actually download this, strip away everything that I don't like uh of
`06:40` away everything that I don't like uh of
`06:40` away everything that I don't like uh of both of these, and kind of create
`06:42` both of these, and kind of create
`06:42` both of these, and kind of create something from it, right? Don't reinvent
`06:44` something from it, right? Don't reinvent
`06:44` something from it, right? Don't reinvent the wheel, make a better wheel. Uh clean
`06:46` the wheel, make a better wheel. Uh clean
`06:46` the wheel, make a better wheel. Uh clean up the wheel, make the wheel yours. And
`06:48` up the wheel, make the wheel yours. And
`06:48` up the wheel, make the wheel yours. And I'm doing that through steps, and that's
`06:50` I'm doing that through steps, and that's
`06:50` I'm doing that through steps, and that's why I have this here. The PRD is saying,
`06:51` why I have this here. The PRD is saying,
`06:51` why I have this here. The PRD is saying, "This is what we need to do first before
`06:53` "This is what we need to do first before
`06:53` "This is what we need to do first before we start building anything." Um
`06:56` we start building anything." Um
`06:56` we start building anything." Um we're going to add in off tokens and
`06:58` we're going to add in off tokens and
`06:58` we're going to add in off tokens and things like that, which basically what
`07:00` things like that, which basically what
`07:00` things like that, which basically what this is doing is it's just allowing it
`07:01` this is doing is it's just allowing it
`07:02` this is doing is it's just allowing it to connect to the Claude CL2. Basically,
`07:04` to connect to the Claude CL2. Basically,
`07:04` to connect to the Claude CL2. Basically, someone had come up with a really good
`07:06` someone had come up with a really good
`07:06` someone had come up with a really good session persistence. So, if I sit here
`07:08` session persistence. So, if I sit here
`07:08` session persistence. So, if I sit here and see all of this, I have persistence
`07:11` and see all of this, I have persistence
`07:11` and see all of this, I have persistence in here. But, this is an extension for
`07:12` in here. But, this is an extension for
`07:12` in here. But, this is an extension for VS Code. Someone had to build every
`07:15` VS Code. Someone had to build every
`07:15` VS Code. Someone had to build every single one of these buttons. And so, we
`07:17` single one of these buttons. And so, we
`07:17` single one of these buttons. And so, we want to rebuild some of these or just in
`07:19` want to rebuild some of these or just in
`07:19` want to rebuild some of these or just in this case, take them from someone else
`07:21` this case, take them from someone else
`07:21` this case, take them from someone else who's already built them, right? Um and
`07:24` who's already built them, right? Um and
`07:24` who's already built them, right? Um and the idea by the end of that is have
`07:26` the idea by the end of that is have
`07:26` the idea by the end of that is have something like that's clean there. Then,
`07:28` something like that's clean there. Then,
`07:28` something like that's clean there. Then, we're going to move into actually
`07:29` we're going to move into actually
`07:29` we're going to move into actually implementing the front end, then at the
`07:32` implementing the front end, then at the
`07:32` implementing the front end, then at the end on actually implementing the
`07:33` end on actually implementing the
`07:33` end on actually implementing the workflow because I think that's going to
`07:35` workflow because I think that's going to
`07:35` workflow because I think that's going to be the hardest. And at a certain point,
`07:37` be the hardest. And at a certain point,
`07:37` be the hardest. And at a certain point, Claude's going to run out of tokens. I
`07:39` Claude's going to run out of tokens. I
`07:39` Claude's going to run out of tokens. I might even want to start a whole other
`07:41` might even want to start a whole other
`07:41` might even want to start a whole other Claude process. So, um yeah, I'm going
`07:44` Claude process. So, um yeah, I'm going
`07:44` Claude process. So, um yeah, I'm going to go ahead and nail that down and kind
`07:46` to go ahead and nail that down and kind
`07:46` to go ahead and nail that down and kind of work on it. I already did partially
`07:48` of work on it. I already did partially
`07:48` of work on it. I already did partially do that, right? So, I asked Claude,
`07:50` do that, right? So, I asked Claude,
`07:50` do that, right? So, I asked Claude, "Hey, read this PRD and make sure you're
`07:53` "Hey, read this PRD and make sure you're
`07:53` "Hey, read this PRD and make sure you're up to speed on everything um and you
`07:55` up to speed on everything um and you
`07:55` up to speed on everything um and you understand it, right?" And it's like,
`07:56` understand it, right?" And it's like,
`07:56` understand it, right?" And it's like, "Cool, got it. Here's the idea." I kind
`07:59` "Cool, got it. Here's the idea." I kind
`07:59` "Cool, got it. Here's the idea." I kind of liked it, but I wanted to make sure
`08:00` of liked it, but I wanted to make sure
`08:00` of liked it, but I wanted to make sure that it understood what I really wanted,
`08:02` that it understood what I really wanted,
`08:02` that it understood what I really wanted, right? I wanted to clean and edit stuff.
`08:05` right? I wanted to clean and edit stuff.
`08:05` right? I wanted to clean and edit stuff. I want to have control over files and
`08:06` I want to have control over files and
`08:06` I want to have control over files and routing. Uh other ones are annoying. I
`08:09` routing. Uh other ones are annoying. I
`08:09` routing. Uh other ones are annoying. I literally say that. Um I want to be able
`08:11` literally say that. Um I want to be able
`08:11` literally say that. Um I want to be able to automate things, swap files around. I
`08:13` to automate things, swap files around. I
`08:13` to automate things, swap files around. I just want to make sure you understand
`08:15` just want to make sure you understand
`08:15` just want to make sure you understand this. The abstract goal isn't a prettier
`08:17` this. The abstract goal isn't a prettier
`08:17` this. The abstract goal isn't a prettier Claude front end. It's a control surface
`08:20` Claude front end. It's a control surface
`08:20` Claude front end. It's a control surface that I can fluidly switch, right?
`08:22` that I can fluidly switch, right?
`08:22` that I can fluidly switch, right? Between I'm driving or Claude's driving.
`08:24` Between I'm driving or Claude's driving.
`08:24` Between I'm driving or Claude's driving. That's such a good way to put it. I love
`08:25` That's such a good way to put it. I love
`08:25` That's such a good way to put it. I love that. Um and that's the idea, right? Is
`08:28` that. Um and that's the idea, right? Is
`08:28` that. Um and that's the idea, right? Is sometimes I want to edit every
`08:29` sometimes I want to edit every
`08:29` sometimes I want to edit every individual file, but sometimes I just
`08:31` individual file, but sometimes I just
`08:31` individual file, but sometimes I just want to automate the whole thing, but
`08:32` want to automate the whole thing, but
`08:33` want to automate the whole thing, but trust that it's automating it the way I
`08:35` trust that it's automating it the way I
`08:35` trust that it's automating it the way I want. Um so, I'm going to say that's
`08:37` want. Um so, I'm going to say that's
`08:37` want. Um so, I'm going to say that's perfect.
`08:39` perfect.
`08:39` perfect. Let's go ahead and start.
`08:42` Let's go ahead and start.
`08:43` Let's go ahead and start. And um in this case, again, this version
`08:46` And um in this case, again, this version
`08:47` And um in this case, again, this version of Claude code is really nice because
`08:49` of Claude code is really nice because
`08:49` of Claude code is really nice because it's using my folders, my files, my text
`08:53` it's using my folders, my files, my text
`08:53` it's using my folders, my files, my text as these prompts, right? These are
`08:55` as these prompts, right? These are
`08:55` as these prompts, right? These are essentially just bunch of prompts that
`08:57` essentially just bunch of prompts that
`08:57` essentially just bunch of prompts that are being fed into it. But in this case,
`08:59` are being fed into it. But in this case,
`08:59` are being fed into it. But in this case, I don't have to write this out. It's
`09:02` I don't have to write this out. It's
`09:02` I don't have to write this out. It's actually referencing that file and it's
`09:04` actually referencing that file and it's
`09:04` actually referencing that file and it's a
`09:05` a
`09:05` a uh uh
`09:06` uh uh
`09:06` uh uh almost it's not a stateless prompt. Um
`09:09` almost it's not a stateless prompt. Um
`09:09` almost it's not a stateless prompt. Um when when I say stateless, right?
`09:10` when when I say stateless, right?
`09:10` when when I say stateless, right? Imagine your memory. You have something
`09:13` Imagine your memory. You have something
`09:13` Imagine your memory. You have something that's in memory, right? You downloaded
`09:15` that's in memory, right? You downloaded
`09:15` that's in memory, right? You downloaded it. It has a state on your computer.
`09:17` it. It has a state on your computer.
`09:17` it. It has a state on your computer. It's sitting there and it's held well.
`09:19` It's sitting there and it's held well.
`09:19` It's sitting there and it's held well. In this case, when you're sending
`09:21` In this case, when you're sending
`09:21` In this case, when you're sending prompts to Claude, as soon as you leave
`09:23` prompts to Claude, as soon as you leave
`09:23` prompts to Claude, as soon as you leave that conversation or you start to the
`09:25` that conversation or you start to the
`09:25` that conversation or you start to the next one, it's gone. It's stateless. It
`09:27` next one, it's gone. It's stateless. It
`09:27` next one, it's gone. It's stateless. It doesn't have its original structure in
`09:29` doesn't have its original structure in
`09:29` doesn't have its original structure in the same way. This makes a prompt exist
`09:32` the same way. This makes a prompt exist
`09:32` the same way. This makes a prompt exist permanently. So, I can always reference
`09:33` permanently. So, I can always reference
`09:33` permanently. So, I can always reference it. I can redo it. I can tell Claude to
`09:35` it. I can redo it. I can tell Claude to
`09:35` it. I can redo it. I can tell Claude to check it out again. We can restart very
`09:38` check it out again. We can restart very
`09:38` check it out again. We can restart very quickly. That's why it's nice having
`09:39` quickly. That's why it's nice having
`09:39` quickly. That's why it's nice having markdown files because they don't take a
`09:42` markdown files because they don't take a
`09:42` markdown files because they don't take a lot of memory, which is a really great
`09:45` lot of memory, which is a really great
`09:45` lot of memory, which is a really great one. They take less tokens than a PDF or
`09:47` one. They take less tokens than a PDF or
`09:47` one. They take less tokens than a PDF or a doc file, and they're really easy for
`09:50` a doc file, and they're really easy for
`09:50` a doc file, and they're really easy for AI to read. They are very well trained
`09:54` AI to read. They are very well trained
`09:54` AI to read. They are very well trained to understand markdown styles. And if I
`09:56` to understand markdown styles. And if I
`09:56` to understand markdown styles. And if I do want to do something with this
`09:58` do want to do something with this
`09:58` do want to do something with this markdown file, I can actually upload it
`10:01` markdown file, I can actually upload it
`10:01` markdown file, I can actually upload it to Google Docs or something like that,
`10:03` to Google Docs or something like that,
`10:03` to Google Docs or something like that, and it will turn all of these into
`10:05` and it will turn all of these into
`10:05` and it will turn all of these into formatting. Like it'll bold these, it'll
`10:07` formatting. Like it'll bold these, it'll
`10:07` formatting. Like it'll bold these, it'll make these bigger because that's how
`10:08` make these bigger because that's how
`10:08` make these bigger because that's how markdown's designed to be, and that's
`10:10` markdown's designed to be, and that's
`10:10` markdown's designed to be, and that's what I'm going to make my front end work
`10:12` what I'm going to make my front end work
`10:12` what I'm going to make my front end work on. It's going to be allowing me to use
`10:15` on. It's going to be allowing me to use
`10:15` on. It's going to be allowing me to use markdown in that way. So,
`10:17` markdown in that way. So,
`10:17` markdown in that way. So, I think that's pretty cool. I'm going to
`10:19` I think that's pretty cool. I'm going to
`10:19` I think that's pretty cool. I'm going to let this kind of rip through. As you can
`10:20` let this kind of rip through. As you can
`10:20` let this kind of rip through. As you can see, it's currently Let me actually
`10:22` see, it's currently Let me actually
`10:22` see, it's currently Let me actually explain this for for some of you who
`10:23` explain this for for some of you who
`10:23` explain this for for some of you who aren't familiar with it. What's
`10:25` aren't familiar with it. What's
`10:25` aren't familiar with it. What's happening is it's going to enter plan
`10:26` happening is it's going to enter plan
`10:26` happening is it's going to enter plan mode, and there's plan mode, and then
`10:28` mode, and there's plan mode, and then
`10:28` mode, and there's plan mode, and then there's execution mode. Um usually it
`10:30` there's execution mode. Um usually it
`10:30` there's execution mode. Um usually it chooses on its own, but you can affect
`10:32` chooses on its own, but you can affect
`10:32` chooses on its own, but you can affect it and change it right here. Uh it's
`10:34` it and change it right here. Uh it's
`10:34` it and change it right here. Uh it's going to go through, and it's going to
`10:35` going to go through, and it's going to
`10:35` going to go through, and it's going to plan what our plan is, which is really
`10:37` plan what our plan is, which is really
`10:37` plan what our plan is, which is really good. This is what we want it to do
`10:39` good. This is what we want it to do
`10:39` good. This is what we want it to do because now it's going to create
`10:40` because now it's going to create
`10:40` because now it's going to create individual plans off of this. And if
`10:42` individual plans off of this. And if
`10:42` individual plans off of this. And if you're using um 4.6, right? So, if I
`10:44` you're using um 4.6, right? So, if I
`10:45` you're using um 4.6, right? So, if I type \{{}slash} model, I can switch model.
`10:48` type \{{}slash} model, I can switch model.
`10:48` type \{{}slash} model, I can switch model. Um if you're using 4.6 or the default,
`10:51` Um if you're using 4.6 or the default,
`10:51` Um if you're using 4.6 or the default, which is usually 4.6, it actually also
`10:54` which is usually 4.6, it actually also
`10:54` which is usually 4.6, it actually also creates something called sub agents
`10:56` creates something called sub agents
`10:56` creates something called sub agents where it saves kind of time and says,
`10:57` where it saves kind of time and says,
`10:57` where it saves kind of time and says, "Hey, let me send an agent to go study
`11:00` "Hey, let me send an agent to go study
`11:00` "Hey, let me send an agent to go study the workspace. Let me send something
`11:02` the workspace. Let me send something
`11:02` the workspace. Let me send something else other than this main one to save on
`11:04` else other than this main one to save on
`11:04` else other than this main one to save on tokens to go study and fetch these
`11:06` tokens to go study and fetch these
`11:06` tokens to go study and fetch these things." As you can see, the tool is
`11:08` things." As you can see, the tool is
`11:08` things." As you can see, the tool is going, "Oh, I need to do this. Let me
`11:10` going, "Oh, I need to do this. Let me
`11:10` going, "Oh, I need to do this. Let me look at this. Let me understand how all
`11:12` look at this. Let me understand how all
`11:12` look at this. Let me understand how all of this is structured." Right? Claude is
`11:14` of this is structured." Right? Claude is
`11:14` of this is structured." Right? Claude is acting as the
`11:16` acting as the
`11:16` acting as the uh kind of What do you call it? Manager
`11:18` uh kind of What do you call it? Manager
`11:18` uh kind of What do you call it? Manager of these agents automatically. Um and
`11:21` of these agents automatically. Um and
`11:21` of these agents automatically. Um and then it's doing some bash commands. So,
`11:23` then it's doing some bash commands. So,
`11:23` then it's doing some bash commands. So, a bash is literally just, right? If I
`11:25` a bash is literally just, right? If I
`11:25` a bash is literally just, right? If I was to sit here and open up a terminal
`11:27` was to sit here and open up a terminal
`11:27` was to sit here and open up a terminal and type something in, say like, "Oh,
`11:30` and type something in, say like, "Oh,
`11:30` and type something in, say like, "Oh, find." Or if I could even run Claude
`11:32` find." Or if I could even run Claude
`11:32` find." Or if I could even run Claude again right here. It's just doing a
`11:34` again right here. It's just doing a
`11:34` again right here. It's just doing a command instead of a source. So in this
`11:35` command instead of a source. So in this
`11:36` command instead of a source. So in this case, it's doing a bash command, which
`11:37` case, it's doing a bash command, which
`11:38` case, it's doing a bash command, which is just searching for a look for
`11:40` is just searching for a look for
`11:40` is just searching for a look for structure this, right? This is where
`11:42` structure this, right? This is where
`11:42` structure this, right? This is where it's trying to find some sort of file.
`11:44` it's trying to find some sort of file.
`11:44` it's trying to find some sort of file. Hey, we're going to script lab. So it's
`11:46` Hey, we're going to script lab. So it's
`11:46` Hey, we're going to script lab. So it's actually going through and this is why
`11:48` actually going through and this is why
`11:48` actually going through and this is why this is really important. I did this
`11:50` this is really important. I did this
`11:50` this is really important. I did this conversation inside of my actual
`11:52` conversation inside of my actual
`11:53` conversation inside of my actual workspace instead of a separate folder.
`11:55` workspace instead of a separate folder.
`11:55` workspace instead of a separate folder. Because if I did it in a separate
`11:56` Because if I did it in a separate
`11:56` Because if I did it in a separate folder, it wouldn't be able to pull
`11:58` folder, it wouldn't be able to pull
`11:58` folder, it wouldn't be able to pull context from my actual workflow, the
`12:00` context from my actual workflow, the
`12:01` context from my actual workflow, the things that's working. So as I'm
`12:03` things that's working. So as I'm
`12:03` things that's working. So as I'm building this front end, it's able to
`12:05` building this front end, it's able to
`12:05` building this front end, it's able to reference the thing that we're building
`12:07` reference the thing that we're building
`12:07` reference the thing that we're building the front end on, but only referencing
`12:09` the front end on, but only referencing
`12:09` the front end on, but only referencing it when I tell it to. I'm not saying,
`12:11` it when I tell it to. I'm not saying,
`12:11` it when I tell it to. I'm not saying, "Hey, go read everything here." I'm
`12:13` "Hey, go read everything here." I'm
`12:13` "Hey, go read everything here." I'm saying, "Hey, there's certain processes
`12:15` saying, "Hey, there's certain processes
`12:15` saying, "Hey, there's certain processes at certain steps, go reference parts of
`12:18` at certain steps, go reference parts of
`12:18` at certain steps, go reference parts of it, right?" It's all about breaking it
`12:20` it, right?" It's all about breaking it
`12:20` it, right?" It's all about breaking it down and putting it into control. And
`12:22` down and putting it into control. And
`12:22` down and putting it into control. And it's doing that a couple different times
`12:23` it's doing that a couple different times
`12:23` it's doing that a couple different times here or there. It's doing a web fetch
`12:25` here or there. It's doing a web fetch
`12:25` here or there. It's doing a web fetch and immediately went and it found um
`12:27` and immediately went and it found um
`12:27` and immediately went and it found um this JSON package for the Claude code
`12:29` this JSON package for the Claude code
`12:30` this JSON package for the Claude code UI, right? So what this is going to do
`12:32` UI, right? So what this is going to do
`12:32` UI, right? So what this is going to do is basically how is it structured, how
`12:34` is basically how is it structured, how
`12:34` is basically how is it structured, how is it delivering it? Let's pull that
`12:36` is it delivering it? Let's pull that
`12:36` is it delivering it? Let's pull that from these people who've already done
`12:37` from these people who've already done
`12:37` from these people who've already done the hard work and actually get that
`12:39` the hard work and actually get that
`12:39` the hard work and actually get that together,
`12:40` together,
`12:40` together, um
`12:41` um
`12:41` um which I think is nice.
`12:43` which I think is nice.
`12:43` which I think is nice. Uh oops, wrong one. From there,
`12:46` Uh oops, wrong one. From there,
`12:46` Uh oops, wrong one. From there, let's see. It's just doing a whole bunch
`12:48` let's see. It's just doing a whole bunch
`12:48` let's see. It's just doing a whole bunch of web fetches. So it's pulling
`12:49` of web fetches. So it's pulling
`12:49` of web fetches. So it's pulling individual files rather than the entire
`12:51` individual files rather than the entire
`12:51` individual files rather than the entire repo itself, which actually is better
`12:53` repo itself, which actually is better
`12:53` repo itself, which actually is better than what I told it to do originally. Uh
`12:55` than what I told it to do originally. Uh
`12:55` than what I told it to do originally. Uh so that's pretty cool. Uh originally I
`12:58` so that's pretty cool. Uh originally I
`12:58` so that's pretty cool. Uh originally I was just going to say clone the repo,
`12:59` was just going to say clone the repo,
`12:59` was just going to say clone the repo, but it makes a lot more sense for it
`13:01` but it makes a lot more sense for it
`13:01` but it makes a lot more sense for it just to search through the repo and then
`13:03` just to search through the repo and then
`13:03` just to search through the repo and then only download what we need or just
`13:04` only download what we need or just
`13:04` only download what we need or just straight up copy and paste it. Now it's
`13:06` straight up copy and paste it. Now it's
`13:06` straight up copy and paste it. Now it's sending another agent to go implement
`13:08` sending another agent to go implement
`13:08` sending another agent to go implement another part of it. Um it looks like
`13:10` another part of it. Um it looks like
`13:10` another part of it. Um it looks like it's doing a find command to look for
`13:13` it's doing a find command to look for
`13:13` it's doing a find command to look for some sort of parts inside of it. Uh
`13:16` some sort of parts inside of it. Uh
`13:16` some sort of parts inside of it. Uh let's see. Max, yep, it's trying to find
`13:18` let's see. Max, yep, it's trying to find
`13:18` let's see. Max, yep, it's trying to find certain parts of the Claude code you web
`13:20` certain parts of the Claude code you web
`13:20` certain parts of the Claude code you web app, the actual aspect. And then it's
`13:22` app, the actual aspect. And then it's
`13:22` app, the actual aspect. And then it's going back and looking. So this is now
`13:23` going back and looking. So this is now
`13:23` going back and looking. So this is now on to the next GitHub repo, the other
`13:26` on to the next GitHub repo, the other
`13:26` on to the next GitHub repo, the other one that I wanted to pull from. and it's
`13:28` one that I wanted to pull from. and it's
`13:28` one that I wanted to pull from. and it's pulling for this. And as you can see, if
`13:30` pulling for this. And as you can see, if
`13:30` pulling for this. And as you can see, if we had been using Claude before and I
`13:31` we had been using Claude before and I
`13:31` we had been using Claude before and I just gave basic things, it kind of
`13:33` just gave basic things, it kind of
`13:33` just gave basic things, it kind of responds quickly. But because we have an
`13:35` responds quickly. But because we have an
`13:35` responds quickly. But because we have an entire PRD that it's following, right?
`13:38` entire PRD that it's following, right?
`13:38` entire PRD that it's following, right? And I said, "Hey, you can go and do your
`13:40` And I said, "Hey, you can go and do your
`13:40` And I said, "Hey, you can go and do your thing." It's going to go through and do
`13:42` thing." It's going to go through and do
`13:42` thing." It's going to go through and do all of this. And it might stop at
`13:44` all of this. And it might stop at
`13:44` all of this. And it might stop at certain points to ask for, you know,
`13:45` certain points to ask for, you know,
`13:45` certain points to ask for, you know, help. But this is how you get Claude to
`13:47` help. But this is how you get Claude to
`13:47` help. But this is how you get Claude to like work for a long time on its own and
`13:50` like work for a long time on its own and
`13:50` like work for a long time on its own and kind of trust its outputs. And of
`13:52` kind of trust its outputs. And of
`13:52` kind of trust its outputs. And of course, we can watch what it's doing, uh
`13:54` course, we can watch what it's doing, uh
`13:54` course, we can watch what it's doing, uh which is really nice. So,
`13:56` which is really nice. So,
`13:56` which is really nice. So, um I'll go ahead and post another video
`13:58` um I'll go ahead and post another video
`13:58` um I'll go ahead and post another video here soon on kind of
`14:00` here soon on kind of
`14:01` here soon on kind of what we are doing or what is actually
`14:03` what we are doing or what is actually
`14:03` what we are doing or what is actually being done. Um this is probably going to
`14:06` being done. Um this is probably going to
`14:06` being done. Um this is probably going to be thrown into session two of one of
`14:09` be thrown into session two of one of
`14:09` be thrown into session two of one of these courses. I'm kind of making these
`14:11` these courses. I'm kind of making these
`14:11` these courses. I'm kind of making these as I go and see where they go. Uh you
`14:13` as I go and see where they go. Uh you
`14:13` as I go and see where they go. Uh you shouldn't have paid to see this. This
`14:15` shouldn't have paid to see this. This
`14:15` shouldn't have paid to see this. This should be free. Uh that's
`14:17` should be free. Uh that's
`14:17` should be free. Uh that's The paid ones I want to be more
`14:19` The paid ones I want to be more
`14:19` The paid ones I want to be more structured, have workbooks, be like an
`14:21` structured, have workbooks, be like an
`14:21` structured, have workbooks, be like an actual college course, which I do teach
`14:23` actual college course, which I do teach
`14:23` actual college course, which I do teach college courses. That's a real thing uh
`14:25` college courses. That's a real thing uh
`14:25` college courses. That's a real thing uh as well. So, that's the other side of
`14:27` as well. So, that's the other side of
`14:27` as well. So, that's the other side of this. If you're feeling rushed or
`14:28` this. If you're feeling rushed or
`14:28` this. If you're feeling rushed or confused by anything I'm doing, don't
`14:30` confused by anything I'm doing, don't
`14:30` confused by anything I'm doing, don't worry. I'm going to break every little
`14:32` worry. I'm going to break every little
`14:32` worry. I'm going to break every little concepts of these down into individual
`14:34` concepts of these down into individual
`14:34` concepts of these down into individual courses for you to be able to really
`14:36` courses for you to be able to really
`14:36` courses for you to be able to really catch up and understand. But for those
`14:38` catch up and understand. But for those
`14:38` catch up and understand. But for those of you who already kind of understand
`14:39` of you who already kind of understand
`14:39` of you who already kind of understand all of this, I wanted to show what I do
`14:41` all of this, I wanted to show what I do
`14:41` all of this, I wanted to show what I do on a daily basis. This is something I I
`14:43` on a daily basis. This is something I I
`14:43` on a daily basis. This is something I I work with every day, so yeah. Have a
`14:46` work with every day, so yeah. Have a
`14:46` work with every day, so yeah. Have a wonderful time. See everyone. All right,
`14:48` wonderful time. See everyone. All right,
`14:48` wonderful time. See everyone. All right, I guess I'll update you on the next
`14:49` I guess I'll update you on the next
`14:49` I guess I'll update you on the next session.