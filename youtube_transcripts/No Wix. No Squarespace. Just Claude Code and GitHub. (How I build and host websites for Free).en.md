# No Wix. No Squarespace. Just Claude Code and GitHub. (How I build and host websites for Free)

**Channel:** Jake Van Clief  
**Uploaded:** 2026-04-07  
**Duration:** 29:22  
**URL:** [https://www.youtube.com/watch?v=ozkx_eUfjY0](https://www.youtube.com/watch?v=ozkx_eUfjY0)  

---

## Transcript

`00:02` So, today is going to be a fun one. I'm
`00:02` So, today is going to be a fun one. I'm going to try to recreate this person's
`00:03` going to try to recreate this person's
`00:04` going to try to recreate this person's website with just Claude code, some
`00:06` website with just Claude code, some
`00:06` website with just Claude code, some folders, and GitHub for free in under
`00:09` folders, and GitHub for free in under
`00:09` folders, and GitHub for free in under maybe 10, 20 minutes, if that. Might
`00:11` maybe 10, 20 minutes, if that. Might
`00:11` maybe 10, 20 minutes, if that. Might take a little longer. We'll find out
`00:12` take a little longer. We'll find out
`00:12` take a little longer. We'll find out here. But, today I'm going to show you
`00:14` here. But, today I'm going to show you
`00:14` here. But, today I'm going to show you essentially not only how to completely
`00:17` essentially not only how to completely
`00:17` essentially not only how to completely rebuild a website for someone else other
`00:19` rebuild a website for someone else other
`00:19` rebuild a website for someone else other than yourself, a client, or just your
`00:21` than yourself, a client, or just your
`00:21` than yourself, a client, or just your own website, but also show how to host
`00:23` own website, but also show how to host
`00:23` own website, but also show how to host it completely for free on GitHub and not
`00:25` it completely for free on GitHub and not
`00:25` it completely for free on GitHub and not have to worry about any of these crazy
`00:27` have to worry about any of these crazy
`00:27` have to worry about any of these crazy services like Wix or anything like that.
`00:29` services like Wix or anything like that.
`00:30` services like Wix or anything like that. So, let's go ahead and dive in and I'll
`00:31` So, let's go ahead and dive in and I'll
`00:31` So, let's go ahead and dive in and I'll show you the process here. Now, first
`00:33` show you the process here. Now, first
`00:33` show you the process here. Now, first things first, I usually like to look
`00:34` things first, I usually like to look
`00:34` things first, I usually like to look through the website. He has a couple of
`00:36` through the website. He has a couple of
`00:36` through the website. He has a couple of different things here or there where
`00:37` different things here or there where
`00:37` different things here or there where essentially he has bike week interviews
`00:39` essentially he has bike week interviews
`00:39` essentially he has bike week interviews or different previous issues, things
`00:41` or different previous issues, things
`00:41` or different previous issues, things like that, right? Uh decent little
`00:43` like that, right? Uh decent little
`00:43` like that, right? Uh decent little website to kind of in, get people's
`00:45` website to kind of in, get people's
`00:45` website to kind of in, get people's emails, get them interested. He's
`00:46` emails, get them interested. He's
`00:46` emails, get them interested. He's obviously interested in motorcycles and
`00:48` obviously interested in motorcycles and
`00:48` obviously interested in motorcycles and the stories and the processes behind
`00:50` the stories and the processes behind
`00:50` the stories and the processes behind that. So, the first thing I'm going to
`00:52` that. So, the first thing I'm going to
`00:52` that. So, the first thing I'm going to do is actually just copy this and run
`00:54` do is actually just copy this and run
`00:54` do is actually just copy this and run over to Claude in the web. And I'm
`00:56` over to Claude in the web. And I'm
`00:56` over to Claude in the web. And I'm simply going to say paste it in here and
`00:58` simply going to say paste it in here and
`00:58` simply going to say paste it in here and say, "Hey, brother man,
`01:00` say, "Hey, brother man,
`01:01` say, "Hey, brother man, let's go ahead and look at this website,
`01:05` let's go ahead and look at this website,
`01:05` let's go ahead and look at this website, click through the sub pages if you can
`01:10` click through the sub pages if you can
`01:10` click through the sub pages if you can to understand it." Now, Claude usually
`01:12` to understand it." Now, Claude usually
`01:12` to understand it." Now, Claude usually can be able to bust this out, no
`01:14` can be able to bust this out, no
`01:14` can be able to bust this out, no problem. If it does struggle, sometimes
`01:16` problem. If it does struggle, sometimes
`01:16` problem. If it does struggle, sometimes I'll just come in and I'll actually take
`01:18` I'll just come in and I'll actually take
`01:18` I'll just come in and I'll actually take screenshots of each of the pages and
`01:20` screenshots of each of the pages and
`01:20` screenshots of each of the pages and I'll go through. And it does help for
`01:21` I'll go through. And it does help for
`01:21` I'll go through. And it does help for you to understand how it looks. But,
`01:23` you to understand how it looks. But,
`01:23` you to understand how it looks. But, generally speaking, it can do a pretty
`01:25` generally speaking, it can do a pretty
`01:25` generally speaking, it can do a pretty decent job. It immediately is pulling
`01:27` decent job. It immediately is pulling
`01:27` decent job. It immediately is pulling up. It's got different areas that it's
`01:29` up. It's got different areas that it's
`01:29` up. It's got different areas that it's looking through. It looks like it Yep,
`01:31` looking through. It looks like it Yep,
`01:31` looking through. It looks like it Yep, it was able to find under posts one of
`01:33` it was able to find under posts one of
`01:33` it was able to find under posts one of the actual posts here or there. So,
`01:35` the actual posts here or there. So,
`01:35` the actual posts here or there. So, we're going to go ahead and let it
`01:36` we're going to go ahead and let it
`01:36` we're going to go ahead and let it explore and navigate this for a moment.
`01:38` explore and navigate this for a moment.
`01:38` explore and navigate this for a moment. Now, it went through and it saw
`01:40` Now, it went through and it saw
`01:40` Now, it went through and it saw everything. It did a really good job. It
`01:41` everything. It did a really good job. It
`01:41` everything. It did a really good job. It understood where it was going, who was
`01:43` understood where it was going, who was
`01:43` understood where it was going, who was there, picked out some interviews, all
`01:44` there, picked out some interviews, all
`01:44` there, picked out some interviews, all that good jazz. But, what I actually
`01:46` that good jazz. But, what I actually
`01:47` that good jazz. But, what I actually want it to do, the reason I'm doing this
`01:48` want it to do, the reason I'm doing this
`01:48` want it to do, the reason I'm doing this is I need it to understand the website
`01:50` is I need it to understand the website
`01:50` is I need it to understand the website so that it can create the next prompt,
`01:52` so that it can create the next prompt,
`01:52` so that it can create the next prompt, which is based on that, give me a
`01:53` which is based on that, give me a
`01:53` which is based on that, give me a markdown file that describes the
`01:55` markdown file that describes the
`01:55` markdown file that describes the structure and build of the website and
`01:56` structure and build of the website and
`01:57` structure and build of the website and parts of it. My goal is to give this to
`01:59` parts of it. My goal is to give this to
`01:59` parts of it. My goal is to give this to Claude code. And this is important
`02:00` Claude code. And this is important
`02:00` Claude code. And this is important because then it's going to create a
`02:02` because then it's going to create a
`02:02` because then it's going to create a document for Claude to read, not for me,
`02:05` document for Claude to read, not for me,
`02:05` document for Claude to read, not for me, and allow it to understand the website
`02:07` and allow it to understand the website
`02:07` and allow it to understand the website before we create a design plan uh and
`02:10` before we create a design plan uh and
`02:10` before we create a design plan uh and then build a new one. So, I'm letting it
`02:12` then build a new one. So, I'm letting it
`02:12` then build a new one. So, I'm letting it understand
`02:13` understand
`02:13` understand I'm doing, why I'm doing it, and who's
`02:15` I'm doing, why I'm doing it, and who's
`02:16` I'm doing, why I'm doing it, and who's going to be reading it. But, I'm also
`02:17` going to be reading it. But, I'm also
`02:17` going to be reading it. But, I'm also doing it in a way so that it's aligned
`02:19` doing it in a way so that it's aligned
`02:19` doing it in a way so that it's aligned with my thinking cuz again, this is how
`02:21` with my thinking cuz again, this is how
`02:21` with my thinking cuz again, this is how I would want to build the website
`02:22` I would want to build the website
`02:22` I would want to build the website naturally. So, I went ahead and I pushed
`02:24` naturally. So, I went ahead and I pushed
`02:24` naturally. So, I went ahead and I pushed that through, and the goal here is to
`02:26` that through, and the goal here is to
`02:26` that through, and the goal here is to get some sort of markdown file that
`02:28` get some sort of markdown file that
`02:28` get some sort of markdown file that essentially describes the skeleton of
`02:30` essentially describes the skeleton of
`02:30` essentially describes the skeleton of the website, where it is. And maybe I
`02:31` the website, where it is. And maybe I
`02:31` the website, where it is. And maybe I can add some screenshots in. But, we're
`02:33` can add some screenshots in. But, we're
`02:33` can add some screenshots in. But, we're eventually going to give this to Claude
`02:35` eventually going to give this to Claude
`02:35` eventually going to give this to Claude code on my desktop. So, Claude went
`02:37` code on my desktop. So, Claude went
`02:37` code on my desktop. So, Claude went ahead and started building it out. And
`02:39` ahead and started building it out. And
`02:39` ahead and started building it out. And so, I'm just going to do a quick review.
`02:40` so, I'm just going to do a quick review.
`02:40` so, I'm just going to do a quick review. We could probably trust this, but it got
`02:42` We could probably trust this, but it got
`02:42` We could probably trust this, but it got the URL right. We can always
`02:43` the URL right. We can always
`02:43` the URL right. We can always double-check. Yep, it went to the right
`02:45` double-check. Yep, it went to the right
`02:45` double-check. Yep, it went to the right URL. It got the taglines, processes
`02:48` URL. It got the taglines, processes
`02:48` URL. It got the taglines, processes here. Of course, it's always good to
`02:49` here. Of course, it's always good to
`02:49` here. Of course, it's always good to re-edit these based on your process.
`02:51` re-edit these based on your process.
`02:51` re-edit these based on your process. This is why it's better to maybe sit
`02:53` This is why it's better to maybe sit
`02:53` This is why it's better to maybe sit down with a client or whoever you're
`02:54` down with a client or whoever you're
`02:54` down with a client or whoever you're working with and really understand what
`02:56` working with and really understand what
`02:56` working with and really understand what their goal is for the website because
`02:58` their goal is for the website because
`02:59` their goal is for the website because whatever you start with is what's going
`03:00` whatever you start with is what's going
`03:00` whatever you start with is what's going to be expanded. But, in this case, I
`03:02` to be expanded. But, in this case, I
`03:02` to be expanded. But, in this case, I really like what they did. It found all
`03:04` really like what they did. It found all
`03:04` really like what they did. It found all the slugs. It found all the form IDs. It
`03:06` the slugs. It found all the form IDs. It
`03:06` the slugs. It found all the form IDs. It looked at the content types and
`03:07` looked at the content types and
`03:08` looked at the content types and processes. It even pulled out the brand
`03:09` processes. It even pulled out the brand
`03:09` processes. It even pulled out the brand voice, which again, I would always come
`03:11` voice, which again, I would always come
`03:12` voice, which again, I would always come back in and re-edit, but that's pretty
`03:13` back in and re-edit, but that's pretty
`03:13` back in and re-edit, but that's pretty good. Uh visual identity is good. Tech
`03:16` good. Uh visual identity is good. Tech
`03:16` good. Uh visual identity is good. Tech stack, it got an idea of what the tech
`03:17` stack, it got an idea of what the tech
`03:17` stack, it got an idea of what the tech stack is being on Beehiive in this
`03:19` stack is being on Beehiive in this
`03:19` stack is being on Beehiive in this aspect. Uh but, we'll probably change
`03:21` aspect. Uh but, we'll probably change
`03:21` aspect. Uh but, we'll probably change that ourselves here. Um it even gave
`03:23` that ourselves here. Um it even gave
`03:23` that ourselves here. Um it even gave some limitations, which I didn't ask uh
`03:25` some limitations, which I didn't ask uh
`03:25` some limitations, which I didn't ask uh ask for it, but that's kind of the idea.
`03:28` ask for it, but that's kind of the idea.
`03:28` ask for it, but that's kind of the idea. Some notes for rebuilding. Interesting.
`03:29` Some notes for rebuilding. Interesting.
`03:29` Some notes for rebuilding. Interesting. Let's see. Content is the product. Any
`03:31` Let's see. Content is the product. Any
`03:31` Let's see. Content is the product. Any new site needs to make long-form stories
`03:32` new site needs to make long-form stories
`03:32` new site needs to make long-form stories and photographs center piece. Subscribe
`03:34` and photographs center piece. Subscribe
`03:34` and photographs center piece. Subscribe CTAs primary conversion action. No
`03:36` CTAs primary conversion action. No
`03:36` CTAs primary conversion action. No matter what you ride, positioning should
`03:38` matter what you ride, positioning should
`03:38` matter what you ride, positioning should inform every design choice. Ooh, that's
`03:39` inform every design choice. Ooh, that's
`03:39` inform every design choice. Ooh, that's interesting.
`03:40` interesting.
`03:40` interesting. I like that point. Story submission
`03:42` I like that point. Story submission
`03:42` I like that point. Story submission indicators. This is where it would be
`03:43` indicators. This is where it would be
`03:43` indicators. This is where it would be nice to uh maybe have some sort of
`03:45` nice to uh maybe have some sort of
`03:45` nice to uh maybe have some sort of branding advice or branding software. I
`03:47` branding advice or branding software. I
`03:47` branding advice or branding software. I know I work with Joe, who some of you
`03:49` know I work with Joe, who some of you
`03:49` know I work with Joe, who some of you might have seen in my afternoon tea,
`03:50` might have seen in my afternoon tea,
`03:50` might have seen in my afternoon tea, who's created a form website for you to
`03:52` who's created a form website for you to
`03:52` who's created a form website for you to be able to send out forms to the people
`03:54` be able to send out forms to the people
`03:54` be able to send out forms to the people who are looking at your website or your
`03:56` who are looking at your website or your
`03:56` who are looking at your website or your customers, and they'll actually click on
`03:58` customers, and they'll actually click on
`03:58` customers, and they'll actually click on images that they like more and creates a
`04:00` images that they like more and creates a
`04:00` images that they like more and creates a whole heat map. I'll make a video on
`04:01` whole heat map. I'll make a video on
`04:01` whole heat map. I'll make a video on that later. But, this is where you can
`04:03` that later. But, this is where you can
`04:03` that later. But, this is where you can then edit and take that information and
`04:05` then edit and take that information and
`04:05` then edit and take that information and plop it in. You always want to start
`04:06` plop it in. You always want to start
`04:06` plop it in. You always want to start with the best data. But, in this case,
`04:08` with the best data. But, in this case,
`04:08` with the best data. But, in this case, we're just going to go ahead and take
`04:09` we're just going to go ahead and take
`04:09` we're just going to go ahead and take it. I'm going to go ahead and download
`04:10` it. I'm going to go ahead and download
`04:10` it. I'm going to go ahead and download it as a markdown file and have it ready
`04:12` it as a markdown file and have it ready
`04:12` it as a markdown file and have it ready to go. Now, I just went ahead and
`04:15` to go. Now, I just went ahead and
`04:15` to go. Now, I just went ahead and created a little folder on my desktop
`04:17` created a little folder on my desktop
`04:17` created a little folder on my desktop here, and this is going to be our
`04:18` here, and this is going to be our
`04:18` here, and this is going to be our workspace. This is where we're going to
`04:19` workspace. This is where we're going to
`04:19` workspace. This is where we're going to build the website. This is where the
`04:20` build the website. This is where the
`04:20` build the website. This is where the website's going to live, and we're
`04:21` website's going to live, and we're
`04:21` website's going to live, and we're actually eventually going to push to
`04:23` actually eventually going to push to
`04:23` actually eventually going to push to GitHub from here as well. So, first
`04:25` GitHub from here as well. So, first
`04:25` GitHub from here as well. So, first things first, I'm just going to click
`04:26` things first, I'm just going to click
`04:26` things first, I'm just going to click and drag that markdown file over here.
`04:27` and drag that markdown file over here.
`04:27` and drag that markdown file over here. Now, I could just go ahead and open my
`04:30` Now, I could just go ahead and open my
`04:30` Now, I could just go ahead and open my terminal here, right? And I could run
`04:32` terminal here, right? And I could run
`04:32` terminal here, right? And I could run Claude code directly inside my terminal.
`04:35` Claude code directly inside my terminal.
`04:35` Claude code directly inside my terminal. Oops, if I spell Claude right. And then,
`04:37` Oops, if I spell Claude right. And then,
`04:37` Oops, if I spell Claude right. And then, I'll be sitting inside this workspace,
`04:39` I'll be sitting inside this workspace,
`04:39` I'll be sitting inside this workspace, and now I have Claude, and I can have it
`04:41` and now I have Claude, and I can have it
`04:41` and now I have Claude, and I can have it start building and reading things. But,
`04:42` start building and reading things. But,
`04:42` start building and reading things. But, me personally, I like being able to work
`04:45` me personally, I like being able to work
`04:45` me personally, I like being able to work inside of an IDE with Claude. So, if you
`04:47` inside of an IDE with Claude. So, if you
`04:47` inside of an IDE with Claude. So, if you haven't seen how to install it that way,
`04:50` haven't seen how to install it that way,
`04:50` haven't seen how to install it that way, we can go ahead and watch one of my
`04:52` we can go ahead and watch one of my
`04:52` we can go ahead and watch one of my other videos where I dive into that.
`04:54` other videos where I dive into that.
`04:54` other videos where I dive into that. But, in this case, we're actually going
`04:55` But, in this case, we're actually going
`04:55` But, in this case, we're actually going to pull up Claude here. And again,
`04:57` to pull up Claude here. And again,
`04:57` to pull up Claude here. And again, usually I work on multiple monitors. And
`05:00` usually I work on multiple monitors. And
`05:00` usually I work on multiple monitors. And we're going to actually pull up that
`05:01` we're going to actually pull up that
`05:01` we're going to actually pull up that folder. So, we're going to come in, go
`05:03` folder. So, we're going to come in, go
`05:03` folder. So, we're going to come in, go to desktop, scroll down to should be Z,
`05:05` to desktop, scroll down to should be Z,
`05:06` to desktop, scroll down to should be Z, Zach's website. Boom. All right. Now, I
`05:08` Zach's website. Boom. All right. Now, I
`05:09` Zach's website. Boom. All right. Now, I trust the authors. I don't need this
`05:10` trust the authors. I don't need this
`05:10` trust the authors. I don't need this anymore. Well, I do, but not in that
`05:12` anymore. Well, I do, but not in that
`05:12` anymore. Well, I do, but not in that way. So, now I can pull it up, and we
`05:14` way. So, now I can pull it up, and we
`05:14` way. So, now I can pull it up, and we can see the exact same markdown file
`05:16` can see the exact same markdown file
`05:16` can see the exact same markdown file that Claude just made is right in here.
`05:18` that Claude just made is right in here.
`05:18` that Claude just made is right in here. Now, I went ahead and typed out a
`05:20` Now, I went ahead and typed out a
`05:20` Now, I went ahead and typed out a prompt, and essentially all I said is
`05:22` prompt, and essentially all I said is
`05:22` prompt, and essentially all I said is what I wanted to do, right? I want to be
`05:24` what I wanted to do, right? I want to be
`05:24` what I wanted to do, right? I want to be able to build a website. But, we're also
`05:26` able to build a website. But, we're also
`05:26` able to build a website. But, we're also This is again, the first prompts matter
`05:28` This is again, the first prompts matter
`05:28` This is again, the first prompts matter a lot. This is how you can stop wasting
`05:30` a lot. This is how you can stop wasting
`05:30` a lot. This is how you can stop wasting a lot of tokens. You want to provide as
`05:32` a lot of tokens. You want to provide as
`05:32` a lot of tokens. You want to provide as much context in the right way. Think
`05:34` much context in the right way. Think
`05:34` much context in the right way. Think about your thinking in the right way so
`05:36` about your thinking in the right way so
`05:36` about your thinking in the right way so that you don't have to do it later and
`05:38` that you don't have to do it later and
`05:38` that you don't have to do it later and just burn through tokens. So, in this
`05:40` just burn through tokens. So, in this
`05:40` just burn through tokens. So, in this case, hey, we want to create a folder
`05:42` case, hey, we want to create a folder
`05:42` case, hey, we want to create a folder structure that allows me to edit and
`05:43` structure that allows me to edit and
`05:43` structure that allows me to edit and keep track of changes, but also is
`05:47` keep track of changes, but also is
`05:47` keep track of changes, but also is simple for you to navigate. Talking
`05:49` simple for you to navigate. Talking
`05:49` simple for you to navigate. Talking directly at We need one Claude MD that
`05:52` directly at We need one Claude MD that
`05:52` directly at We need one Claude MD that says where files are and what they are
`05:53` says where files are and what they are
`05:54` says where files are and what they are doing for easier routing. Then maybe
`05:55` doing for easier routing. Then maybe
`05:55` doing for easier routing. Then maybe smaller markdown files in each folder
`05:58` smaller markdown files in each folder
`05:58` smaller markdown files in each folder describing why or what's there, brand
`06:00` describing why or what's there, brand
`06:00` describing why or what's there, brand voice, things like that. What I mean by
`06:02` voice, things like that. What I mean by
`06:02` voice, things like that. What I mean by that, and again, this is my folder
`06:04` that, and again, this is my folder
`06:04` that, and again, this is my folder architecture process, my giant 10-page
`06:07` architecture process, my giant 10-page
`06:07` architecture process, my giant 10-page research paper condensed into a
`06:08` research paper condensed into a
`06:08` research paper condensed into a sentence. What I mean by that is we are
`06:10` sentence. What I mean by that is we are
`06:10` sentence. What I mean by that is we are going to create a new website that looks
`06:12` going to create a new website that looks
`06:12` going to create a new website that looks entirely looks great, but can be
`06:15` entirely looks great, but can be
`06:15` entirely looks great, but can be deployed on GitHub. Now, I'm
`06:17` deployed on GitHub. Now, I'm
`06:17` deployed on GitHub. Now, I'm specifically saying this now because
`06:19` specifically saying this now because
`06:19` specifically saying this now because this is going to Sometimes Claude or
`06:20` this is going to Sometimes Claude or
`06:20` this is going to Sometimes Claude or these other things go way crazy and
`06:22` these other things go way crazy and
`06:22` these other things go way crazy and think about deployments and all this
`06:23` think about deployments and all this
`06:23` think about deployments and all this stuff. Hey, we're just going to deploy
`06:25` stuff. Hey, we're just going to deploy
`06:25` stuff. Hey, we're just going to deploy it on GitHub. Now, GitHub has a feature
`06:27` it on GitHub. Now, GitHub has a feature
`06:27` it on GitHub. Now, GitHub has a feature called GitHub Pages where you can deploy
`06:29` called GitHub Pages where you can deploy
`06:29` called GitHub Pages where you can deploy websites and things like that for free
`06:31` websites and things like that for free
`06:31` websites and things like that for free really easily. And I'm going to describe
`06:33` really easily. And I'm going to describe
`06:33` really easily. And I'm going to describe that here shortly, but I just want
`06:35` that here shortly, but I just want
`06:35` that here shortly, but I just want everyone to understand that that's an
`06:36` everyone to understand that that's an
`06:36` everyone to understand that that's an important concept, and I'm saying it now
`06:39` important concept, and I'm saying it now
`06:39` important concept, and I'm saying it now rather than wait later so it doesn't
`06:41` rather than wait later so it doesn't
`06:41` rather than wait later so it doesn't burn tokens wasting on things. It's how
`06:43` burn tokens wasting on things. It's how
`06:43` burn tokens wasting on things. It's how I'm able to keep my token usage so down
`06:45` I'm able to keep my token usage so down
`06:45` I'm able to keep my token usage so down is I'm ten spending so much time for my
`06:47` is I'm ten spending so much time for my
`06:47` is I'm ten spending so much time for my initial prompts or processes. Not that
`06:49` initial prompts or processes. Not that
`06:49` initial prompts or processes. Not that much time, but you know what I mean.
`06:51` much time, but you know what I mean.
`06:51` much time, but you know what I mean. Now, I'm saying let's decide if we're
`06:52` Now, I'm saying let's decide if we're
`06:53` Now, I'm saying let's decide if we're using React or something else. But, the
`06:54` using React or something else. But, the
`06:54` using React or something else. But, the goal is to create something good-looking
`06:56` goal is to create something good-looking
`06:56` goal is to create something good-looking and easy now. The full build doesn't
`06:58` and easy now. The full build doesn't
`06:58` and easy now. The full build doesn't have to happen today.
`07:00` have to happen today.
`07:00` have to happen today. As in, we don't need user accounts for
`07:02` As in, we don't need user accounts for
`07:02` As in, we don't need user accounts for anything like that for posts. It can be
`07:03` anything like that for posts. It can be
`07:03` anything like that for posts. It can be direct manual submissions or code
`07:04` direct manual submissions or code
`07:04` direct manual submissions or code changes.
`07:05` changes.
`07:06` changes. But, we want to start and template for
`07:07` But, we want to start and template for
`07:07` But, we want to start and template for future building. So, the idea is even if
`07:09` future building. So, the idea is even if
`07:09` future building. So, the idea is even if you There's going to be a lot more
`07:10` you There's going to be a lot more
`07:10` you There's going to be a lot more complex tasks. Maybe he wants to user
`07:12` complex tasks. Maybe he wants to user
`07:12` complex tasks. Maybe he wants to user logins. Maybe he wants all these other
`07:14` logins. Maybe he wants all these other
`07:14` logins. Maybe he wants all these other things. We don't have to build that now.
`07:15` things. We don't have to build that now.
`07:15` things. We don't have to build that now. We just have to set up the framework to
`07:17` We just have to set up the framework to
`07:17` We just have to set up the framework to add it later, and we want to create the
`07:18` add it later, and we want to create the
`07:18` add it later, and we want to create the base level now. But, the last part is
`07:21` base level now. But, the last part is
`07:21` base level now. But, the last part is super important. I said ask me three
`07:23` super important. I said ask me three
`07:23` super important. I said ask me three questions to understand more. Now,
`07:26` questions to understand more. Now,
`07:26` questions to understand more. Now, immediately it's going to go through,
`07:27` immediately it's going to go through,
`07:27` immediately it's going to go through, it's going to read all of this, and it's
`07:28` it's going to read all of this, and it's
`07:28` it's going to read all of this, and it's going to get a lot of the context that I
`07:30` going to get a lot of the context that I
`07:30` going to get a lot of the context that I didn't have to write in here about the
`07:32` didn't have to write in here about the
`07:32` didn't have to write in here about the website. So, it goes through and listens
`07:34` website. So, it goes through and listens
`07:34` website. So, it goes through and listens to that. Creates that context. Then it's
`07:36` to that. Creates that context. Then it's
`07:36` to that. Creates that context. Then it's going to read what I said, and then it's
`07:37` going to read what I said, and then it's
`07:37` going to read what I said, and then it's going to ask me three questions. The
`07:39` going to ask me three questions. The
`07:39` going to ask me three questions. The reason you want to do This allows you to
`07:41` reason you want to do This allows you to
`07:41` reason you want to do This allows you to really prompt it and align it. Before it
`07:43` really prompt it and align it. Before it
`07:43` really prompt it and align it. Before it starts creating any files, editing
`07:44` starts creating any files, editing
`07:44` starts creating any files, editing anything, creating anything, you want to
`07:46` anything, creating anything, you want to
`07:46` anything, creating anything, you want to make sure it is exactly aligned with
`07:49` make sure it is exactly aligned with
`07:49` make sure it is exactly aligned with your goals and your context. If you
`07:51` your goals and your context. If you
`07:51` your goals and your context. If you aren't, you have to spend so much time
`07:53` aren't, you have to spend so much time
`07:53` aren't, you have to spend so much time fixing it later, deleting things,
`07:54` fixing it later, deleting things,
`07:54` fixing it later, deleting things, rebuilding, and that burns so many
`07:56` rebuilding, and that burns so many
`07:56` rebuilding, and that burns so many tokens that you just don't need to burn.
`07:59` tokens that you just don't need to burn.
`07:59` tokens that you just don't need to burn. So, I'm going to go ahead and read
`08:00` So, I'm going to go ahead and read
`08:00` So, I'm going to go ahead and read through this and answer them very
`08:01` through this and answer them very
`08:01` through this and answer them very quickly. Now that I'm done with all of
`08:03` quickly. Now that I'm done with all of
`08:03` quickly. Now that I'm done with all of that, I'm going to say one last thing.
`08:04` that, I'm going to say one last thing.
`08:04` that, I'm going to say one last thing. Based on all of that, I want you to use
`08:06` Based on all of that, I want you to use
`08:06` Based on all of that, I want you to use the UX UI design skill pro and create a
`08:09` the UX UI design skill pro and create a
`08:09` the UX UI design skill pro and create a PRD file for this. And for those of you
`08:11` PRD file for this. And for those of you
`08:11` PRD file for this. And for those of you who don't know what that skill is,
`08:13` who don't know what that skill is,
`08:13` who don't know what that skill is, essentially it's a really solid skill
`08:15` essentially it's a really solid skill
`08:15` essentially it's a really solid skill for Claude. And again, for those of you
`08:17` for Claude. And again, for those of you
`08:17` for Claude. And again, for those of you who don't know what skills are, I make
`08:18` who don't know what skills are, I make
`08:18` who don't know what skills are, I make more videos on that, but essentially
`08:20` more videos on that, but essentially
`08:20` more videos on that, but essentially just packages prompts to do a certain
`08:22` just packages prompts to do a certain
`08:22` just packages prompts to do a certain thing. This skill is designed to really
`08:24` thing. This skill is designed to really
`08:24` thing. This skill is designed to really make Claude or other AI be able to
`08:26` make Claude or other AI be able to
`08:26` make Claude or other AI be able to generate designs and processes for
`08:28` generate designs and processes for
`08:28` generate designs and processes for really good-looking websites. I already
`08:30` really good-looking websites. I already
`08:30` really good-looking websites. I already have it installed locally on my
`08:31` have it installed locally on my
`08:31` have it installed locally on my computer, and I'll probably make another
`08:33` computer, and I'll probably make another
`08:33` computer, and I'll probably make another video on how to install skills, what
`08:35` video on how to install skills, what
`08:35` video on how to install skills, what those skills should be, so on and so
`08:37` those skills should be, so on and so
`08:37` those skills should be, so on and so forth here soon. But, that's essentially
`08:39` forth here soon. But, that's essentially
`08:39` forth here soon. But, that's essentially I'm going to ask it. And then, I'm
`08:40` I'm going to ask it. And then, I'm
`08:40` I'm going to ask it. And then, I'm asking it to create a PRD, a product
`08:42` asking it to create a PRD, a product
`08:42` asking it to create a PRD, a product requirements document. Basically, a
`08:44` requirements document. Basically, a
`08:44` requirements document. Basically, a large markdown that's going to say
`08:45` large markdown that's going to say
`08:45` large markdown that's going to say where, what needs to be, how to do it.
`08:47` where, what needs to be, how to do it.
`08:47` where, what needs to be, how to do it. And I'm saying use the design skill to
`08:49` And I'm saying use the design skill to
`08:49` And I'm saying use the design skill to think about the PRD. And again, we're
`08:51` think about the PRD. And again, we're
`08:51` think about the PRD. And again, we're still not implementing any of the
`08:53` still not implementing any of the
`08:53` still not implementing any of the software yet. We're getting all of the
`08:54` software yet. We're getting all of the
`08:54` software yet. We're getting all of the skeletons right, making sure it's good,
`08:57` skeletons right, making sure it's good,
`08:57` skeletons right, making sure it's good, and then I'm going to say, "Hey, Claude,
`08:58` and then I'm going to say, "Hey, Claude,
`08:59` and then I'm going to say, "Hey, Claude, rip it." But, let's go ahead and move
`09:01` rip it." But, let's go ahead and move
`09:01` rip it." But, let's go ahead and move from there. But, always saying, "Hey,
`09:03` from there. But, always saying, "Hey,
`09:03` from there. But, always saying, "Hey, always feel free to ask me questions
`09:04` always feel free to ask me questions
`09:04` always feel free to ask me questions about alignment, making sure it's
`09:06` about alignment, making sure it's
`09:06` about alignment, making sure it's aligned with what I want." So, I'm going
`09:07` aligned with what I want." So, I'm going
`09:07` aligned with what I want." So, I'm going to go ahead and push that. Mind you, we
`09:09` to go ahead and push that. Mind you, we
`09:09` to go ahead and push that. Mind you, we are one, two, three, four prompts in.
`09:12` are one, two, three, four prompts in.
`09:12` are one, two, three, four prompts in. So, I've only typed four prompts during
`09:15` So, I've only typed four prompts during
`09:15` So, I've only typed four prompts during this whole process, right? But, look at
`09:17` this whole process, right? But, look at
`09:18` this whole process, right? But, look at all the thinking and processing I'm
`09:19` all the thinking and processing I'm
`09:19` all the thinking and processing I'm doing for each prompt. And that's going
`09:22` doing for each prompt. And that's going
`09:22` doing for each prompt. And that's going to allow us to use way less tokens, way
`09:24` to allow us to use way less tokens, way
`09:24` to allow us to use way less tokens, way less prompts, but getting a way better
`09:26` less prompts, but getting a way better
`09:26` less prompts, but getting a way better output. And again, I always recommend
`09:28` output. And again, I always recommend
`09:28` output. And again, I always recommend that you look through what Claude is
`09:30` that you look through what Claude is
`09:30` that you look through what Claude is doing, right? Look through its thoughts,
`09:31` doing, right? Look through its thoughts,
`09:31` doing, right? Look through its thoughts, right? Did it understand what you're
`09:33` right? Did it understand what you're
`09:33` right? Did it understand what you're trying to do? What is it doing first?
`09:34` trying to do? What is it doing first?
`09:34` trying to do? What is it doing first? Well, it's going and loading that skill,
`09:36` Well, it's going and loading that skill,
`09:36` Well, it's going and loading that skill, like I said it. And then, that skill is
`09:38` like I said it. And then, that skill is
`09:38` like I said it. And then, that skill is going to tell it to generate things. And
`09:39` going to tell it to generate things. And
`09:39` going to tell it to generate things. And actually, inside of that skill, it has
`09:42` actually, inside of that skill, it has
`09:42` actually, inside of that skill, it has uh Python programs that are actually
`09:44` uh Python programs that are actually
`09:45` uh Python programs that are actually running so that it can use less tokens
`09:47` running so that it can use less tokens
`09:47` running so that it can use less tokens to talk and do things. So, these Python
`09:49` to talk and do things. So, these Python
`09:49` to talk and do things. So, these Python scripts are actually running processes
`09:52` scripts are actually running processes
`09:52` scripts are actually running processes to actually create designs or things
`09:54` to actually create designs or things
`09:54` to actually create designs or things like that to offload the need to use so
`09:56` like that to offload the need to use so
`09:56` like that to offload the need to use so many tokens. Then from there, it's
`09:58` many tokens. Then from there, it's
`09:58` many tokens. Then from there, it's actually going through and doing some
`10:00` actually going through and doing some
`10:00` actually going through and doing some web fetching here. It's going to the
`10:01` web fetching here. It's going to the
`10:01` web fetching here. It's going to the website. It's reading the website,
`10:03` website. It's reading the website,
`10:03` website. It's reading the website, looking at it, understanding what's
`10:04` looking at it, understanding what's
`10:05` looking at it, understanding what's inside of it. This is really important
`10:07` inside of it. This is really important
`10:07` inside of it. This is really important to look through cuz if at any point you
`10:08` to look through cuz if at any point you
`10:08` to look through cuz if at any point you notice that it's doing something weird
`10:10` notice that it's doing something weird
`10:10` notice that it's doing something weird where let's see it's synthesizing all of
`10:12` where let's see it's synthesizing all of
`10:12` where let's see it's synthesizing all of this. It's looking at the typography.
`10:13` this. It's looking at the typography.
`10:13` this. It's looking at the typography. Ooh, I don't want that typography. I
`10:15` Ooh, I don't want that typography. I
`10:15` Ooh, I don't want that typography. I want different typography. This is where
`10:17` want different typography. This is where
`10:17` want different typography. This is where being an expert in your field, you're
`10:18` being an expert in your field, you're
`10:18` being an expert in your field, you're not being replaced by AI. You can now
`10:21` not being replaced by AI. You can now
`10:21` not being replaced by AI. You can now amplify we our self with the AI because
`10:24` amplify we our self with the AI because
`10:24` amplify we our self with the AI because you'll know whether or not that's a bad
`10:26` you'll know whether or not that's a bad
`10:26` you'll know whether or not that's a bad typography. Most people who've studied
`10:28` typography. Most people who've studied
`10:28` typography. Most people who've studied typography or haven't studied typography
`10:30` typography or haven't studied typography
`10:30` typography or haven't studied typography would never recognize that in the AI
`10:32` would never recognize that in the AI
`10:32` would never recognize that in the AI system. You can give the nuanced opinion
`10:34` system. You can give the nuanced opinion
`10:34` system. You can give the nuanced opinion to direct this system to do crazy good
`10:37` to direct this system to do crazy good
`10:37` to direct this system to do crazy good things in those small areas. Obviously,
`10:39` things in those small areas. Obviously,
`10:39` things in those small areas. Obviously, I'm not going to do that now for this
`10:40` I'm not going to do that now for this
`10:40` I'm not going to do that now for this one cuz I think it's going to choose
`10:42` one cuz I think it's going to choose
`10:42` one cuz I think it's going to choose pretty decent, but this is where the
`10:43` pretty decent, but this is where the
`10:43` pretty decent, but this is where the creative side, the infinite side of
`10:46` creative side, the infinite side of
`10:46` creative side, the infinite side of human creativity comes into working with
`10:48` human creativity comes into working with
`10:48` human creativity comes into working with these systems. So, I'm going to go ahead
`10:49` these systems. So, I'm going to go ahead
`10:49` these systems. So, I'm going to go ahead and let this kind of work itself out and
`10:52` and let this kind of work itself out and
`10:52` and let this kind of work itself out and come back to you here in a moment. It
`10:53` come back to you here in a moment. It
`10:53` come back to you here in a moment. It finished and I'm going to go through
`10:55` finished and I'm going to go through
`10:55` finished and I'm going to go through everything here but I wanted to hit
`10:56` everything here but I wanted to hit
`10:56` everything here but I wanted to hit something important. Now, it did a lot
`10:58` something important. Now, it did a lot
`10:58` something important. Now, it did a lot of work. It went through and searched.
`10:59` of work. It went through and searched.
`10:59` of work. It went through and searched. It created some extra files that were
`11:01` It created some extra files that were
`11:01` It created some extra files that were about to go wander through and I'm going
`11:02` about to go wander through and I'm going
`11:02` about to go wander through and I'm going to explain why. But one important
`11:04` to explain why. But one important
`11:04` to explain why. But one important concept that I've seen people talking
`11:05` concept that I've seen people talking
`11:05` concept that I've seen people talking about today is about system memory. This
`11:07` about today is about system memory. This
`11:07` about today is about system memory. This idea of oh, we got to create a mind map
`11:09` idea of oh, we got to create a mind map
`11:09` idea of oh, we got to create a mind map for AI. Dude, they're already doing
`11:11` for AI. Dude, they're already doing
`11:11` for AI. Dude, they're already doing that. I see so many people building
`11:12` that. I see so many people building
`11:12` that. I see so many people building these huge complex systems of YAML files
`11:15` these huge complex systems of YAML files
`11:15` these huge complex systems of YAML files and Obsidian and at the end of the day
`11:17` and Obsidian and at the end of the day
`11:17` and Obsidian and at the end of the day Anthropic and these other companies are
`11:18` Anthropic and these other companies are
`11:18` Anthropic and these other companies are going to build harnesses way better than
`11:20` going to build harnesses way better than
`11:20` going to build harnesses way better than anyone here will. A couple of you might
`11:22` anyone here will. A couple of you might
`11:22` anyone here will. A couple of you might be doing a good job out there and not to
`11:23` be doing a good job out there and not to
`11:23` be doing a good job out there and not to hate on it, but look, right here this is
`11:25` hate on it, but look, right here this is
`11:25` hate on it, but look, right here this is a memory index. They're actually writing
`11:26` a memory index. They're actually writing
`11:27` a memory index. They're actually writing to memory to a project file on your
`11:29` to memory to a project file on your
`11:29` to memory to a project file on your actual installed Claude. Look, right
`11:31` actual installed Claude. Look, right
`11:31` actual installed Claude. Look, right here this is on my projects inside of my
`11:34` here this is on my projects inside of my
`11:34` here this is on my projects inside of my total Claude workspace. So, everywhere I
`11:36` total Claude workspace. So, everywhere I
`11:36` total Claude workspace. So, everywhere I go on my computer, I'm going to have
`11:37` go on my computer, I'm going to have
`11:37` go on my computer, I'm going to have this memory index here and it's all just
`11:39` this memory index here and it's all just
`11:39` this memory index here and it's all just markdown files that are referencing
`11:41` markdown files that are referencing
`11:41` markdown files that are referencing other MDs, right? So, it's like oh, you
`11:43` other MDs, right? So, it's like oh, you
`11:43` other MDs, right? So, it's like oh, you have this project that you're working on
`11:45` have this project that you're working on
`11:45` have this project that you're working on here. Go ahead and take you to the rest
`11:47` here. Go ahead and take you to the rest
`11:47` here. Go ahead and take you to the rest of that project, right? So, it created
`11:49` of that project, right? So, it created
`11:49` of that project, right? So, it created that AMC website build. Here's what it
`11:51` that AMC website build. Here's what it
`11:51` that AMC website build. Here's what it is. Here's the description. Here's the
`11:53` is. Here's the description. Here's the
`11:53` is. Here's the description. Here's the type. Here's what's going on inside of
`11:55` type. Here's what's going on inside of
`11:55` type. Here's what's going on inside of it. Here's the reasons for it. Hey, the
`11:57` it. Here's the reasons for it. Hey, the
`11:57` it. Here's the reasons for it. Hey, the user is doing this inside of it as in
`11:59` user is doing this inside of it as in
`11:59` user is doing this inside of it as in me. I'm doing this because of Subscribe
`12:01` me. I'm doing this because of Subscribe
`12:01` me. I'm doing this because of Subscribe Gift. I can just come in here and add
`12:02` Gift. I can just come in here and add
`12:02` Gift. I can just come in here and add stuff to it now and it will always be
`12:04` stuff to it now and it will always be
`12:04` stuff to it now and it will always be able to reference it every time I'm
`12:06` able to reference it every time I'm
`12:06` able to reference it every time I'm inside of this project. So, the whole
`12:08` inside of this project. So, the whole
`12:08` inside of this project. So, the whole idea of having to build insane project
`12:11` idea of having to build insane project
`12:11` idea of having to build insane project memory systems and harnesses for it, I
`12:13` memory systems and harnesses for it, I
`12:13` memory systems and harnesses for it, I don't think you really need to cuz it's
`12:14` don't think you really need to cuz it's
`12:14` don't think you really need to cuz it's already happening automatically and even
`12:16` already happening automatically and even
`12:16` already happening automatically and even if you did want to do this yourself with
`12:17` if you did want to do this yourself with
`12:18` if you did want to do this yourself with a local model, all you need is another
`12:19` a local model, all you need is another
`12:19` a local model, all you need is another set of folders and another set of
`12:21` set of folders and another set of
`12:21` set of folders and another set of markdown files. I'm always saying, but
`12:23` markdown files. I'm always saying, but
`12:23` markdown files. I'm always saying, but I'll have a whole other videos here. so
`12:25` I'll have a whole other videos here. so
`12:25` I'll have a whole other videos here. so it went ahead and created my Claude MD.
`12:27` it went ahead and created my Claude MD.
`12:27` it went ahead and created my Claude MD. It created my PRD file. It also
`12:31` It created my PRD file. It also
`12:31` It created my PRD file. It also created my brand voice, design tokens,
`12:33` created my brand voice, design tokens,
`12:33` created my brand voice, design tokens, my layouts, and a series of read means
`12:35` my layouts, and a series of read means
`12:35` my layouts, and a series of read means around components. And this is all built
`12:38` around components. And this is all built
`12:38` around components. And this is all built from just a couple prompts immediately.
`12:40` from just a couple prompts immediately.
`12:40` from just a couple prompts immediately. So, we have the main master for design,
`12:43` So, we have the main master for design,
`12:43` So, we have the main master for design, which is basically let's actually go
`12:44` which is basically let's actually go
`12:44` which is basically let's actually go ahead and close it and look at it like
`12:46` ahead and close it and look at it like
`12:46` ahead and close it and look at it like this. This is just it's saying, "Hey,
`12:48` this. This is just it's saying, "Hey,
`12:48` this. This is just it's saying, "Hey, here's the way in which we want to
`12:49` here's the way in which we want to
`12:49` here's the way in which we want to design. Here's the colors that we're
`12:51` design. Here's the colors that we're
`12:51` design. Here's the colors that we're going to choose. Here's the different
`12:52` going to choose. Here's the different
`12:53` going to choose. Here's the different values to shadow depths and processes.
`12:55` values to shadow depths and processes.
`12:55` values to shadow depths and processes. Here's some actual examples of templates
`12:57` Here's some actual examples of templates
`12:57` Here's some actual examples of templates and actual files we want to do. Here's
`12:59` and actual files we want to do. Here's
`12:59` and actual files we want to do. Here's some guidelines as well as specific
`13:01` some guidelines as well as specific
`13:01` some guidelines as well as specific patterns. Don't use these, which this
`13:03` patterns. Don't use these, which this
`13:03` patterns. Don't use these, which this comes from the UI UX Pro. X it often
`13:05` comes from the UI UX Pro. X it often
`13:05` comes from the UI UX Pro. X it often builds these aspects. And then even has
`13:07` builds these aspects. And then even has
`13:07` builds these aspects. And then even has a delivery checklist for it to go
`13:09` a delivery checklist for it to go
`13:09` a delivery checklist for it to go through there. Same with everything
`13:10` through there. Same with everything
`13:10` through there. Same with everything else. It's going through and said, "All
`13:12` else. It's going through and said, "All
`13:12` else. It's going through and said, "All right, well, we're going to need docs
`13:13` right, well, we're going to need docs
`13:13` right, well, we're going to need docs for your brand voice for us to reference
`13:15` for your brand voice for us to reference
`13:15` for your brand voice for us to reference through. We're going to need to make
`13:16` through. We're going to need to make
`13:16` through. We're going to need to make sure that there's a source files for all
`13:18` sure that there's a source files for all
`13:18` sure that there's a source files for all of the components that we're going to
`13:19` of the components that we're going to
`13:19` of the components that we're going to build out." So, it went through and it
`13:20` build out." So, it went through and it
`13:21` build out." So, it went through and it did it pretty well. I'm happy with it.
`13:22` did it pretty well. I'm happy with it.
`13:22` did it pretty well. I'm happy with it. The Claude MD says how to start it,
`13:25` The Claude MD says how to start it,
`13:25` The Claude MD says how to start it, where the reference site is. It shows
`13:27` where the reference site is. It shows
`13:27` where the reference site is. It shows what our folder layout is going to look
`13:28` what our folder layout is going to look
`13:28` what our folder layout is going to look like. It hasn't even built most of these
`13:31` like. It hasn't even built most of these
`13:31` like. It hasn't even built most of these folders yet, right? These folders don't
`13:33` folders yet, right? These folders don't
`13:33` folders yet, right? These folders don't exist yet. But Claude is knows because
`13:36` exist yet. But Claude is knows because
`13:36` exist yet. But Claude is knows because of how how we're talking what that
`13:38` of how how we're talking what that
`13:38` of how how we're talking what that should look like. And I could even come
`13:39` should look like. And I could even come
`13:39` should look like. And I could even come in and edit these and say, "Hey, I don't
`13:40` in and edit these and say, "Hey, I don't
`13:41` in and edit these and say, "Hey, I don't really like that we're organizing it
`13:42` really like that we're organizing it
`13:42` really like that we're organizing it this way." But I'm pretty happy with
`13:44` this way." But I'm pretty happy with
`13:44` this way." But I'm pretty happy with this and this is a pretty short
`13:45` this and this is a pretty short
`13:45` this and this is a pretty short Claude.md, so it's not going to waste
`13:47` Claude.md, so it's not going to waste
`13:47` Claude.md, so it's not going to waste too many tokens every time we we load
`13:49` too many tokens every time we we load
`13:49` too many tokens every time we we load it. But other than that, pretty killer.
`13:52` it. But other than that, pretty killer.
`13:52` it. But other than that, pretty killer. So, I'm just going to go ahead and look
`13:53` So, I'm just going to go ahead and look
`13:53` So, I'm just going to go ahead and look in. Let's see. Do you have Zach's logo?
`13:55` in. Let's see. Do you have Zach's logo?
`13:55` in. Let's see. Do you have Zach's logo? Should we subscribe form, hit API, or
`13:59` Should we subscribe form, hit API, or
`13:59` Should we subscribe form, hit API, or just link to the Subscribe Beehiiv page?
`14:01` just link to the Subscribe Beehiiv page?
`14:01` just link to the Subscribe Beehiiv page? Do you have URLs for social channels?
`14:03` Do you have URLs for social channels?
`14:03` Do you have URLs for social channels? Any of these five questions resolved
`14:04` Any of these five questions resolved
`14:04` Any of these five questions resolved before building? I think we're Again, I
`14:06` before building? I think we're Again, I
`14:06` before building? I think we're Again, I want this to be really simple. So, these
`14:08` want this to be really simple. So, these
`14:08` want this to be really simple. So, these usually I would answer very
`14:09` usually I would answer very
`14:10` usually I would answer very specifically. However, at this point, I
`14:12` specifically. However, at this point, I
`14:12` specifically. However, at this point, I think the only thing I really want to do
`14:14` think the only thing I really want to do
`14:14` think the only thing I really want to do is maybe include something from Zach's
`14:16` is maybe include something from Zach's
`14:16` is maybe include something from Zach's actual site. So, I'm going to go ahead
`14:17` actual site. So, I'm going to go ahead
`14:18` actual site. So, I'm going to go ahead and pull that, throw it in here. Other
`14:19` and pull that, throw it in here. Other
`14:19` and pull that, throw it in here. Other than that, I'm going to say, "Hey, let's
`14:20` than that, I'm going to say, "Hey, let's
`14:20` than that, I'm going to say, "Hey, let's keep it simple. Let's go ahead and just
`14:22` keep it simple. Let's go ahead and just
`14:22` keep it simple. Let's go ahead and just build something." I went ahead and just
`14:24` build something." I went ahead and just
`14:24` build something." I went ahead and just took a screenshot his logo and throw it
`14:26` took a screenshot his logo and throw it
`14:26` took a screenshot his logo and throw it in. Sometimes it struggles using
`14:27` in. Sometimes it struggles using
`14:27` in. Sometimes it struggles using screenshots directly from chat, so it's
`14:29` screenshots directly from chat, so it's
`14:29` screenshots directly from chat, so it's better to toss it in. So, I just said,
`14:30` better to toss it in. So, I just said,
`14:30` better to toss it in. So, I just said, "Hey, rebuild it." And I'm being very
`14:32` "Hey, rebuild it." And I'm being very
`14:32` "Hey, rebuild it." And I'm being very lazy with this right now, but I just
`14:34` lazy with this right now, but I just
`14:34` lazy with this right now, but I just want to get something out there. If I
`14:35` want to get something out there. If I
`14:35` want to get something out there. If I was to make this really clean, I would
`14:37` was to make this really clean, I would
`14:37` was to make this really clean, I would spend a lot more time editing each of
`14:39` spend a lot more time editing each of
`14:39` spend a lot more time editing each of these, looking at the documents, what
`14:40` these, looking at the documents, what
`14:40` these, looking at the documents, what does it sound like? But for this case,
`14:42` does it sound like? But for this case,
`14:42` does it sound like? But for this case, this is still really good. And this is
`14:43` this is still really good. And this is
`14:43` this is still really good. And this is what you should do for your website if
`14:45` what you should do for your website if
`14:45` what you should do for your website if you were building it. And so,
`14:48` you were building it. And so,
`14:48` you were building it. And so, immediately it goes in, gives some
`14:49` immediately it goes in, gives some
`14:49` immediately it goes in, gives some examples of recommendations for the
`14:51` examples of recommendations for the
`14:51` examples of recommendations for the logo. I think I'm going to go ahead and
`14:53` logo. I think I'm going to go ahead and
`14:53` logo. I think I'm going to go ahead and let it just grab it. And then I forgot
`14:55` let it just grab it. And then I forgot
`14:55` let it just grab it. And then I forgot to respond to its other questions, so
`14:56` to respond to its other questions, so
`14:56` to respond to its other questions, so I'm going to respond to those as well.
`14:58` I'm going to respond to those as well.
`14:58` I'm going to respond to those as well. Cool. So, I just went, "Hey, go build
`15:00` Cool. So, I just went, "Hey, go build
`15:00` Cool. So, I just went, "Hey, go build for something cool. Build whatever SVG."
`15:02` for something cool. Build whatever SVG."
`15:02` for something cool. Build whatever SVG." Again, the more creative control you
`15:04` Again, the more creative control you
`15:04` Again, the more creative control you take in this situation, the better the
`15:05` take in this situation, the better the
`15:06` take in this situation, the better the output will be later than instead of
`15:07` output will be later than instead of
`15:07` output will be later than instead of having to redo it and waste all these
`15:09` having to redo it and waste all these
`15:09` having to redo it and waste all these tokens. And the cool part is now too,
`15:11` tokens. And the cool part is now too,
`15:11` tokens. And the cool part is now too, what I can start doing as this is all
`15:13` what I can start doing as this is all
`15:13` what I can start doing as this is all getting built, I can actually add other
`15:15` getting built, I can actually add other
`15:15` getting built, I can actually add other instances of Claude and say, "Hey, right
`15:17` instances of Claude and say, "Hey, right
`15:17` instances of Claude and say, "Hey, right now we're building out the website. I
`15:18` now we're building out the website. I
`15:18` now we're building out the website. I need you to go research SVGs based on
`15:20` need you to go research SVGs based on
`15:20` need you to go research SVGs based on this data." And it's just going to pull
`15:21` this data." And it's just going to pull
`15:21` this data." And it's just going to pull directly from brand voice, all these
`15:23` directly from brand voice, all these
`15:23` directly from brand voice, all these things. I could have it start writing
`15:24` things. I could have it start writing
`15:25` things. I could have it start writing articles. I could create multiple
`15:26` articles. I could create multiple
`15:26` articles. I could create multiple instances that are doing different
`15:27` instances that are doing different
`15:27` instances that are doing different things, which is this whole idea. I can
`15:29` things, which is this whole idea. I can
`15:29` things, which is this whole idea. I can now create infinite agents that are
`15:31` now create infinite agents that are
`15:31` now create infinite agents that are handling different parts of the workflow
`15:34` handling different parts of the workflow
`15:34` handling different parts of the workflow on the fly by using one single type of
`15:36` on the fly by using one single type of
`15:36` on the fly by using one single type of coding agent, which is what I talk about
`15:38` coding agent, which is what I talk about
`15:38` coding agent, which is what I talk about in most of my aspects. So, it went ahead
`15:40` in most of my aspects. So, it went ahead
`15:40` in most of my aspects. So, it went ahead and created a nice to-do list to go
`15:42` and created a nice to-do list to go
`15:42` and created a nice to-do list to go build. I'm going to go ahead and let it
`15:43` build. I'm going to go ahead and let it
`15:43` build. I'm going to go ahead and let it do that and we'll be right back again.
`15:45` do that and we'll be right back again.
`15:45` do that and we'll be right back again. So, it went ahead and busted through a
`15:47` So, it went ahead and busted through a
`15:47` So, it went ahead and busted through a whole bunch of creation.
`15:48` whole bunch of creation.
`15:48` whole bunch of creation. Looks like it created quite a lot of
`15:50` Looks like it created quite a lot of
`15:50` Looks like it created quite a lot of components and aspects here. It's using
`15:52` components and aspects here. It's using
`15:52` components and aspects here. It's using Astro, which is another area that you
`15:54` Astro, which is another area that you
`15:54` Astro, which is another area that you can actually go and do development in
`15:56` can actually go and do development in
`15:56` can actually go and do development in and that uses mainly TypeScript and
`15:58` and that uses mainly TypeScript and
`15:58` and that uses mainly TypeScript and things like that. So, we'll see how that
`16:00` things like that. So, we'll see how that
`16:00` things like that. So, we'll see how that kind of looks out, but it did really
`16:02` kind of looks out, but it did really
`16:02` kind of looks out, but it did really good work. But the best part is is when
`16:04` good work. But the best part is is when
`16:04` good work. But the best part is is when it finished, it went ahead and ran the
`16:06` it finished, it went ahead and ran the
`16:06` it finished, it went ahead and ran the Astro server for me. And so, I was able
`16:08` Astro server for me. And so, I was able
`16:08` Astro server for me. And so, I was able to just click this link really quick and
`16:10` to just click this link really quick and
`16:10` to just click this link really quick and pull it up. And this is what it built.
`16:12` pull it up. And this is what it built.
`16:12` pull it up. And this is what it built. Now, immediately right off the bat, I
`16:13` Now, immediately right off the bat, I
`16:13` Now, immediately right off the bat, I hate the logo. I would absolutely
`16:15` hate the logo. I would absolutely
`16:15` hate the logo. I would absolutely completely change that, but that's okay.
`16:16` completely change that, but that's okay.
`16:17` completely change that, but that's okay. That's an easy fix. We can import a
`16:18` That's an easy fix. We can import a
`16:18` That's an easy fix. We can import a logo. We can build a better one later
`16:20` logo. We can build a better one later
`16:20` logo. We can build a better one later on. Or I'll show you what I'm going to
`16:21` on. Or I'll show you what I'm going to
`16:21` on. Or I'll show you what I'm going to do here now. You can just chat with
`16:23` do here now. You can just chat with
`16:23` do here now. You can just chat with Claude and say what you don't like. Now,
`16:25` Claude and say what you don't like. Now,
`16:25` Claude and say what you don't like. Now, because I didn't like a lot, I went
`16:27` because I didn't like a lot, I went
`16:27` because I didn't like a lot, I went ahead and grabbed some screenshots of
`16:29` ahead and grabbed some screenshots of
`16:29` ahead and grabbed some screenshots of what I didn't like really quick. And I
`16:31` what I didn't like really quick. And I
`16:31` what I didn't like really quick. And I also went ahead and added a screenshot
`16:33` also went ahead and added a screenshot
`16:33` also went ahead and added a screenshot of the logo. Now, it'd be much better if
`16:34` of the logo. Now, it'd be much better if
`16:34` of the logo. Now, it'd be much better if I had the actual SVG. I can get that
`16:36` I had the actual SVG. I can get that
`16:36` I had the actual SVG. I can get that later. It doesn't matter. It's just a
`16:37` later. It doesn't matter. It's just a
`16:37` later. It doesn't matter. It's just a placeholder. I went ahead and linked it
`16:39` placeholder. I went ahead and linked it
`16:39` placeholder. I went ahead and linked it to that. And I also basically said,
`16:40` to that. And I also basically said,
`16:40` to that. And I also basically said, "Hey, I think we could make this more
`16:41` "Hey, I think we could make this more
`16:41` "Hey, I think we could make this more visually dense. Also, let's switch all
`16:43` visually dense. Also, let's switch all
`16:43` visually dense. Also, let's switch all of the colors to white in nature." Now,
`16:45` of the colors to white in nature." Now,
`16:45` of the colors to white in nature." Now, again, this is why it's important to
`16:46` again, this is why it's important to
`16:46` again, this is why it's important to know what you want beforehand. I should
`16:48` know what you want beforehand. I should
`16:48` know what you want beforehand. I should have described exactly what colors I
`16:50` have described exactly what colors I
`16:50` have described exactly what colors I wanted in the previous prompts. And so,
`16:52` wanted in the previous prompts. And so,
`16:52` wanted in the previous prompts. And so, now I'm wasting tokens changing it. All
`16:55` now I'm wasting tokens changing it. All
`16:55` now I'm wasting tokens changing it. All of this could be set up exactly how I
`16:57` of this could be set up exactly how I
`16:57` of this could be set up exactly how I wanted it well before burning any of
`16:59` wanted it well before burning any of
`16:59` wanted it well before burning any of these tokens. However, I didn't take the
`17:01` these tokens. However, I didn't take the
`17:01` these tokens. However, I didn't take the time to do that. So, in this case, it's
`17:03` time to do that. So, in this case, it's
`17:03` time to do that. So, in this case, it's going through and I explained what that
`17:05` going through and I explained what that
`17:05` going through and I explained what that was. And I also said, "Hey,
`17:06` was. And I also said, "Hey,
`17:06` was. And I also said, "Hey, specifically, could you download some
`17:08` specifically, could you download some
`17:08` specifically, could you download some stock photos or pictures of riding
`17:10` stock photos or pictures of riding
`17:10` stock photos or pictures of riding through the forest and throw them in the
`17:11` through the forest and throw them in the
`17:11` through the forest and throw them in the background? I want to do it in a
`17:12` background? I want to do it in a
`17:12` background? I want to do it in a creative way." And I also told it to
`17:14` creative way." And I also told it to
`17:14` creative way." And I also told it to make a plan first, right? So, it should
`17:17` make a plan first, right? So, it should
`17:17` make a plan first, right? So, it should be able to come through and do that. So,
`17:19` be able to come through and do that. So,
`17:19` be able to come through and do that. So, here's the right out. It's too much dark
`17:20` here's the right out. It's too much dark
`17:20` here's the right out. It's too much dark empty space. Cool. Monotone dark pillow
`17:22` empty space. Cool. Monotone dark pillow
`17:22` empty space. Cool. Monotone dark pillow palette. Absolutely did not like that.
`17:24` palette. Absolutely did not like that.
`17:24` palette. Absolutely did not like that. No real photography. Did not like that.
`17:26` No real photography. Did not like that.
`17:26` No real photography. Did not like that. Here's the color shift. So, we're going
`17:27` Here's the color shift. So, we're going
`17:27` Here's the color shift. So, we're going to do dark cream, sage, forest, gold,
`17:29` to do dark cream, sage, forest, gold,
`17:29` to do dark cream, sage, forest, gold, mauve. Love that. Again, would have done
`17:31` mauve. Love that. Again, would have done
`17:31` mauve. Love that. Again, would have done this earlier, saved me a lot of time.
`17:33` this earlier, saved me a lot of time.
`17:33` this earlier, saved me a lot of time. And here's some images to source to be
`17:35` And here's some images to source to be
`17:35` And here's some images to source to be able to roll through. Those sound good
`17:37` able to roll through. Those sound good
`17:37` able to roll through. Those sound good to me. Let's go ahead and do it. Again,
`17:39` to me. Let's go ahead and do it. Again,
`17:39` to me. Let's go ahead and do it. Again, I would spend way more time working on
`17:41` I would spend way more time working on
`17:41` I would spend way more time working on this. We're just trying to get something
`17:42` this. We're just trying to get something
`17:42` this. We're just trying to get something out there. So, I'm just going to say,
`17:44` out there. So, I'm just going to say,
`17:44` out there. So, I'm just going to say, "Yes, that all works." Again, this is
`17:47` "Yes, that all works." Again, this is
`17:47` "Yes, that all works." Again, this is also a point. You don't need Wix or all
`17:49` also a point. You don't need Wix or all
`17:50` also a point. You don't need Wix or all of these things to design your website.
`17:51` of these things to design your website.
`17:51` of these things to design your website. You can use natural language and folders
`17:54` You can use natural language and folders
`17:54` You can use natural language and folders to do it now. It's actually easier. You
`17:56` to do it now. It's actually easier. You
`17:56` to do it now. It's actually easier. You can create your own interface. Yes,
`17:58` can create your own interface. Yes,
`17:58` can create your own interface. Yes, there might be times where it's nicer to
`18:00` there might be times where it's nicer to
`18:00` there might be times where it's nicer to have a direct interface like Canva or
`18:02` have a direct interface like Canva or
`18:02` have a direct interface like Canva or something like that. But sometimes maybe
`18:04` something like that. But sometimes maybe
`18:04` something like that. But sometimes maybe not. It just depends on the kind of
`18:05` not. It just depends on the kind of
`18:05` not. It just depends on the kind of person you are and what you're doing.
`18:07` person you are and what you're doing.
`18:07` person you are and what you're doing. So, I'm going to go ahead and let it do
`18:08` So, I'm going to go ahead and let it do
`18:08` So, I'm going to go ahead and let it do those changes and show you what changed.
`18:10` those changes and show you what changed.
`18:10` those changes and show you what changed. All right, it went ahead and finished.
`18:11` All right, it went ahead and finished.
`18:11` All right, it went ahead and finished. There was a couple little errors like
`18:13` There was a couple little errors like
`18:13` There was a couple little errors like syntax errors I noticed. And if you ever
`18:15` syntax errors I noticed. And if you ever
`18:15` syntax errors I noticed. And if you ever get that or there's an error that pops
`18:16` get that or there's an error that pops
`18:16` get that or there's an error that pops up, you can literally just paste it into
`18:17` up, you can literally just paste it into
`18:17` up, you can literally just paste it into Claude and it should help. Or if you
`18:19` Claude and it should help. Or if you
`18:19` Claude and it should help. Or if you know enough about computer programming,
`18:21` know enough about computer programming,
`18:21` know enough about computer programming, CSS, HTML, whatever you're working in,
`18:23` CSS, HTML, whatever you're working in,
`18:23` CSS, HTML, whatever you're working in, you can go fix it. But it looks amazing.
`18:26` you can go fix it. But it looks amazing.
`18:26` you can go fix it. But it looks amazing. It did a really good job this second
`18:27` It did a really good job this second
`18:27` It did a really good job this second time around. It brought in all of the
`18:29` time around. It brought in all of the
`18:29` time around. It brought in all of the kind of
`18:30` kind of
`18:30` kind of images I wanted. Obviously, it'd be much
`18:32` images I wanted. Obviously, it'd be much
`18:32` images I wanted. Obviously, it'd be much better if I curated the images and chose
`18:34` better if I curated the images and chose
`18:34` better if I curated the images and chose them there. But it still links perfectly
`18:36` them there. But it still links perfectly
`18:36` them there. But it still links perfectly to each of those rides and processes in
`18:39` to each of those rides and processes in
`18:39` to each of those rides and processes in terms of the about page, in terms of
`18:41` terms of the about page, in terms of
`18:41` terms of the about page, in terms of submitting what they publish, why they
`18:43` submitting what they publish, why they
`18:43` submitting what they publish, why they publish it, where it's there, how it
`18:45` publish it, where it's there, how it
`18:45` publish it, where it's there, how it works. Boom, go to submit your story.
`18:47` works. Boom, go to submit your story.
`18:48` works. Boom, go to submit your story. We've got an entire submission page
`18:49` We've got an entire submission page
`18:49` We've got an entire submission page there as well. That should work
`18:51` there as well. That should work
`18:51` there as well. That should work relatively well once we hook it up. This
`18:53` relatively well once we hook it up. This
`18:53` relatively well once we hook it up. This is what I'm talking about. It it looks
`18:55` is what I'm talking about. It it looks
`18:55` is what I'm talking about. It it looks good. It's super simple and it didn't
`18:57` good. It's super simple and it didn't
`18:57` good. It's super simple and it didn't take us too much time. And Claude did
`18:59` take us too much time. And Claude did
`18:59` take us too much time. And Claude did most of the work in all of it. But
`19:00` most of the work in all of it. But
`19:00` most of the work in all of it. But there's one problem. If you look right
`19:02` there's one problem. If you look right
`19:02` there's one problem. If you look right here, it says local host, which means
`19:04` here, it says local host, which means
`19:04` here, it says local host, which means I'm hosting this locally on my computer.
`19:07` I'm hosting this locally on my computer.
`19:07` I'm hosting this locally on my computer. We need to push this to GitHub. Now, for
`19:09` We need to push this to GitHub. Now, for
`19:09` We need to push this to GitHub. Now, for those of you unfamiliar with GitHub, you
`19:11` those of you unfamiliar with GitHub, you
`19:11` those of you unfamiliar with GitHub, you saw that there's a lot of skills or you
`19:13` saw that there's a lot of skills or you
`19:13` saw that there's a lot of skills or you might be downloading things from there
`19:14` might be downloading things from there
`19:14` might be downloading things from there that are big files or zip files. But
`19:16` that are big files or zip files. But
`19:16` that are big files or zip files. But it's a place for people to organize
`19:18` it's a place for people to organize
`19:18` it's a place for people to organize text, code, and folders in a way where
`19:21` text, code, and folders in a way where
`19:21` text, code, and folders in a way where they can submit it and make changes to
`19:23` they can submit it and make changes to
`19:23` they can submit it and make changes to it or contribute with other people. Now,
`19:26` it or contribute with other people. Now,
`19:26` it or contribute with other people. Now, me personally, I host a lot of different
`19:28` me personally, I host a lot of different
`19:28` me personally, I host a lot of different projects on GitHub as well both from the
`19:31` projects on GitHub as well both from the
`19:31` projects on GitHub as well both from the coding side where we have all sorts of
`19:33` coding side where we have all sorts of
`19:33` coding side where we have all sorts of different code like my ethics engines
`19:35` different code like my ethics engines
`19:35` different code like my ethics engines and research that I did for it. And you
`19:37` and research that I did for it. And you
`19:37` and research that I did for it. And you can put all sorts of stuff in there like
`19:39` can put all sorts of stuff in there like
`19:39` can put all sorts of stuff in there like Python and JavaScript or whatever you'd
`19:42` Python and JavaScript or whatever you'd
`19:42` Python and JavaScript or whatever you'd like to be able to really just double
`19:44` like to be able to really just double
`19:44` like to be able to really just double down on this. But it also lets you do
`19:46` down on this. But it also lets you do
`19:46` down on this. But it also lets you do something really interesting. Let's say
`19:48` something really interesting. Let's say
`19:48` something really interesting. Let's say you went the simple route and you didn't
`19:50` you went the simple route and you didn't
`19:50` you went the simple route and you didn't do any of the work that I just showed
`19:52` do any of the work that I just showed
`19:52` do any of the work that I just showed you all over the last 10 15 minutes, but
`19:54` you all over the last 10 15 minutes, but
`19:54` you all over the last 10 15 minutes, but instead you just created a little HTML
`19:56` instead you just created a little HTML
`19:56` instead you just created a little HTML file and you could do this inside of you
`19:58` file and you could do this inside of you
`19:58` file and you could do this inside of you know Claude where Claude makes the
`20:00` know Claude where Claude makes the
`20:00` know Claude where Claude makes the little artifact for your website. You
`20:02` little artifact for your website. You
`20:02` little artifact for your website. You downloaded it and then you have an HTML
`20:04` downloaded it and then you have an HTML
`20:04` downloaded it and then you have an HTML webpage that you were able to put on.
`20:06` webpage that you were able to put on.
`20:06` webpage that you were able to put on. Well, you can actually create a
`20:07` Well, you can actually create a
`20:07` Well, you can actually create a repository. So once you sign up for an
`20:10` repository. So once you sign up for an
`20:10` repository. So once you sign up for an account and get into GitHub, you'll have
`20:12` account and get into GitHub, you'll have
`20:12` account and get into GitHub, you'll have an option to essentially create a new
`20:14` an option to essentially create a new
`20:14` an option to essentially create a new repository, right? So if we're at our
`20:16` repository, right? So if we're at our
`20:16` repository, right? So if we're at our main home there, you just hit new and
`20:18` main home there, you just hit new and
`20:18` main home there, you just hit new and you name it whatever you want. New
`20:21` you name it whatever you want. New
`20:21` you name it whatever you want. New website and you can hit create
`20:23` website and you can hit create
`20:23` website and you can hit create repository. And when you do, again,
`20:25` repository. And when you do, again,
`20:25` repository. And when you do, again, hopefully it was there, you can upload
`20:27` hopefully it was there, you can upload
`20:27` hopefully it was there, you can upload that file. Literally, I can come in, I
`20:30` that file. Literally, I can come in, I
`20:30` that file. Literally, I can come in, I can find whatever that file was, right?
`20:32` can find whatever that file was, right?
`20:33` can find whatever that file was, right? Here's just an HTML file that just
`20:34` Here's just an HTML file that just
`20:34` Here's just an HTML file that just showed that website and I can hit upload
`20:36` showed that website and I can hit upload
`20:36` showed that website and I can hit upload and it'll come right here. And that's
`20:38` and it'll come right here. And that's
`20:38` and it'll come right here. And that's what I already did. Now, I named it
`20:40` what I already did. Now, I named it
`20:40` what I already did. Now, I named it index.html
`20:42` index.html
`20:42` index.html because that's something that GitHub is
`20:44` because that's something that GitHub is
`20:44` because that's something that GitHub is looking for. It looks for a file named
`20:46` looking for. It looks for a file named
`20:47` looking for. It looks for a file named index.html
`20:48` index.html
`20:48` index.html when you use the feature that I'm about
`20:50` when you use the feature that I'm about
`20:50` when you use the feature that I'm about to show you. But if you come over here
`20:52` to show you. But if you come over here
`20:52` to show you. But if you come over here to settings. So you've got the file
`20:53` to settings. So you've got the file
`20:53` to settings. So you've got the file uploaded, you have whatever the
`20:55` uploaded, you have whatever the
`20:56` uploaded, you have whatever the repository's called, you come over to
`20:58` repository's called, you come over to
`20:58` repository's called, you come over to settings
`20:59` settings
`20:59` settings and then you come down to something
`21:00` and then you come down to something
`21:00` and then you come down to something called pages.
`21:02` called pages.
`21:02` called pages. And in pages, you won't see this part,
`21:05` And in pages, you won't see this part,
`21:05` And in pages, you won't see this part, but you will see Actually, we can do it
`21:07` but you will see Actually, we can do it
`21:07` but you will see Actually, we can do it over here. It'll look something like
`21:09` over here. It'll look something like
`21:09` over here. It'll look something like this. It'll say deploy from branch and
`21:11` this. It'll say deploy from branch and
`21:11` this. It'll say deploy from branch and you want to keep it right there and
`21:13` you want to keep it right there and
`21:13` you want to keep it right there and you'll select you'll have an option for
`21:15` you'll select you'll have an option for
`21:15` you'll select you'll have an option for main because you'll actually have files
`21:17` main because you'll actually have files
`21:17` main because you'll actually have files in there. Now, I didn't upload that file
`21:19` in there. Now, I didn't upload that file
`21:19` in there. Now, I didn't upload that file there at this point, but I did over here
`21:21` there at this point, but I did over here
`21:21` there at this point, but I did over here and I was able to select main.
`21:23` and I was able to select main.
`21:23` and I was able to select main. That's all you have to do. You collect
`21:26` That's all you have to do. You collect
`21:26` That's all you have to do. You collect you select pages, you hit main and you
`21:29` you select pages, you hit main and you
`21:29` you select pages, you hit main and you have something called an HTML file
`21:31` have something called an HTML file
`21:31` have something called an HTML file called index. GitHub will automatically
`21:33` called index. GitHub will automatically
`21:33` called index. GitHub will automatically recognize it, look for it and then
`21:35` recognize it, look for it and then
`21:35` recognize it, look for it and then create a deployment and then it'll give
`21:37` create a deployment and then it'll give
`21:37` create a deployment and then it'll give you a link. After about a minute or two,
`21:39` you a link. After about a minute or two,
`21:39` you a link. After about a minute or two, you refresh this page and it'll give you
`21:40` you refresh this page and it'll give you
`21:40` you refresh this page and it'll give you a link of that GitHub repo where you can
`21:43` a link of that GitHub repo where you can
`21:43` a link of that GitHub repo where you can then come in and now I can send this
`21:45` then come in and now I can send this
`21:45` then come in and now I can send this HTML link to anyone in the world and
`21:47` HTML link to anyone in the world and
`21:47` HTML link to anyone in the world and they'll be able to access it from
`21:48` they'll be able to access it from
`21:48` they'll be able to access it from anywhere else. That is a pushed website.
`21:51` anywhere else. That is a pushed website.
`21:51` anywhere else. That is a pushed website. You also have the option to be able to
`21:52` You also have the option to be able to
`21:52` You also have the option to be able to change the domain. So you have actually
`21:54` change the domain. So you have actually
`21:54` change the domain. So you have actually want to own the domain, you can just put
`21:56` want to own the domain, you can just put
`21:56` want to own the domain, you can just put it in there and go through your DNS
`21:58` it in there and go through your DNS
`21:58` it in there and go through your DNS settings. I'm not going to talk about it
`21:59` settings. I'm not going to talk about it
`21:59` settings. I'm not going to talk about it in this video, but it's just about
`22:01` in this video, but it's just about
`22:01` in this video, but it's just about adding a custom domain to GitHub.
`22:03` adding a custom domain to GitHub.
`22:04` adding a custom domain to GitHub. There's a lot of videos out there on how
`22:05` There's a lot of videos out there on how
`22:05` There's a lot of videos out there on how to do that. However, I did that same
`22:07` to do that. However, I did that same
`22:08` to do that. However, I did that same thing or I'm going to do that same thing
`22:10` thing or I'm going to do that same thing
`22:10` thing or I'm going to do that same thing for our website here. But in this case,
`22:13` for our website here. But in this case,
`22:13` for our website here. But in this case, I did it from the Claude command line
`22:15` I did it from the Claude command line
`22:15` I did it from the Claude command line and I actually uninstalled all of the
`22:17` and I actually uninstalled all of the
`22:17` and I actually uninstalled all of the GitHub stuff on my computer so that you
`22:19` GitHub stuff on my computer so that you
`22:19` GitHub stuff on my computer so that you all could see what it looks like if you
`22:20` all could see what it looks like if you
`22:20` all could see what it looks like if you were doing it fresh. All I said is love
`22:22` were doing it fresh. All I said is love
`22:22` were doing it fresh. All I said is love it, looks great, clicked through it.
`22:24` it, looks great, clicked through it.
`22:24` it, looks great, clicked through it. Let's create a GitHub repo and push it
`22:26` Let's create a GitHub repo and push it
`22:26` Let's create a GitHub repo and push it to publish on pages. Now, I already have
`22:28` to publish on pages. Now, I already have
`22:28` to publish on pages. Now, I already have an account logged in and already have
`22:31` an account logged in and already have
`22:31` an account logged in and already have something there, but it noticed that oh
`22:32` something there, but it noticed that oh
`22:32` something there, but it noticed that oh wait, you don't have a GitHub access.
`22:34` wait, you don't have a GitHub access.
`22:34` wait, you don't have a GitHub access. Now, usually it uses something called
`22:36` Now, usually it uses something called
`22:36` Now, usually it uses something called GitHub command line interface and this
`22:38` GitHub command line interface and this
`22:38` GitHub command line interface and this is how AI or you yourself just using the
`22:41` is how AI or you yourself just using the
`22:41` is how AI or you yourself just using the command line can push these repos to
`22:45` command line can push these repos to
`22:45` command line can push these repos to that website instead of you having to go
`22:47` that website instead of you having to go
`22:47` that website instead of you having to go in, click and drag and uploading it. But
`22:49` in, click and drag and uploading it. But
`22:49` in, click and drag and uploading it. But you could do it the other way, the
`22:50` you could do it the other way, the
`22:50` you could do it the other way, the manual way as well by just literally
`22:53` manual way as well by just literally
`22:53` manual way as well by just literally copying this entire page and throwing it
`22:55` copying this entire page and throwing it
`22:55` copying this entire page and throwing it in. But for this more complex stuff,
`22:57` in. But for this more complex stuff,
`22:57` in. But for this more complex stuff, it's better to push it and create a repo
`23:00` it's better to push it and create a repo
`23:00` it's better to push it and create a repo for three different reasons.
`23:01` for three different reasons.
`23:02` for three different reasons. The first being it's not just an HTML
`23:04` The first being it's not just an HTML
`23:04` The first being it's not just an HTML file. So it's not as simple as throwing
`23:06` file. So it's not as simple as throwing
`23:06` file. So it's not as simple as throwing an HTML. I could do that. I could put
`23:08` an HTML. I could do that. I could put
`23:08` an HTML. I could do that. I could put that in there and get a website up very
`23:10` that in there and get a website up very
`23:10` that in there and get a website up very quickly, but I want a little bit more
`23:11` quickly, but I want a little bit more
`23:11` quickly, but I want a little bit more complexity and I want to be able to work
`23:13` complexity and I want to be able to work
`23:13` complexity and I want to be able to work on it over time and make it a better
`23:15` on it over time and make it a better
`23:15` on it over time and make it a better website. So I went ahead and told it to
`23:18` website. So I went ahead and told it to
`23:18` website. So I went ahead and told it to push that. It goes ahead. It actually
`23:20` push that. It goes ahead. It actually
`23:20` push that. It goes ahead. It actually downloaded the CLI for me. I was
`23:21` downloaded the CLI for me. I was
`23:21` downloaded the CLI for me. I was surprised it was able to do that. It
`23:23` surprised it was able to do that. It
`23:23` surprised it was able to do that. It even had me special log in my device. So
`23:26` even had me special log in my device. So
`23:26` even had me special log in my device. So it opened up the browser. I
`23:27` it opened up the browser. I
`23:27` it opened up the browser. I authenticated my GitHub account, which
`23:29` authenticated my GitHub account, which
`23:29` authenticated my GitHub account, which is what you would usually do if you were
`23:30` is what you would usually do if you were
`23:30` is what you would usually do if you were installing it manually. But it was just
`23:32` installing it manually. But it was just
`23:32` installing it manually. But it was just really cool to see Claude get away with
`23:34` really cool to see Claude get away with
`23:34` really cool to see Claude get away with it. I authenticated as my account and
`23:36` it. I authenticated as my account and
`23:36` it. I authenticated as my account and then it went through and created a
`23:37` then it went through and created a
`23:37` then it went through and created a series of files. Now, there's a lot of
`23:39` series of files. Now, there's a lot of
`23:39` series of files. Now, there's a lot of things it does to your code base to kind
`23:42` things it does to your code base to kind
`23:42` things it does to your code base to kind of set it up to be pushed to get, but
`23:44` of set it up to be pushed to get, but
`23:44` of set it up to be pushed to get, but one of the most important that I want
`23:46` one of the most important that I want
`23:46` one of the most important that I want everyone to understand, I'm sorry if
`23:48` everyone to understand, I'm sorry if
`23:48` everyone to understand, I'm sorry if you're a programmer watching this and
`23:49` you're a programmer watching this and
`23:49` you're a programmer watching this and you already know it, the gitignore file.
`23:52` you already know it, the gitignore file.
`23:52` you already know it, the gitignore file. This is so important because so many
`23:54` This is so important because so many
`23:54` This is so important because so many people don't understand what this is. If
`23:57` people don't understand what this is. If
`23:57` people don't understand what this is. If you're creating an app or a software or
`23:59` you're creating an app or a software or
`23:59` you're creating an app or a software or a website and there's something in your
`24:01` a website and there's something in your
`24:02` a website and there's something in your local folders that you don't want being
`24:04` local folders that you don't want being
`24:04` local folders that you don't want being pushed to the public, you have to say
`24:07` pushed to the public, you have to say
`24:07` pushed to the public, you have to say that in this folder. So specifically,
`24:10` that in this folder. So specifically,
`24:10` that in this folder. So specifically, one thing that I see a lot of people do,
`24:11` one thing that I see a lot of people do,
`24:11` one thing that I see a lot of people do, there's a thing called a .env file. This
`24:14` there's a thing called a .env file. This
`24:14` there's a thing called a .env file. This is where people put API keys and things
`24:16` is where people put API keys and things
`24:17` is where people put API keys and things like that. If that's not in your
`24:18` like that. If that's not in your
`24:18` like that. If that's not in your gitignore, when you say, "Hey Claude,
`24:21` gitignore, when you say, "Hey Claude,
`24:21` gitignore, when you say, "Hey Claude, push this to my git repo." it's going to
`24:23` push this to my git repo." it's going to
`24:23` push this to my git repo." it's going to push the whole folder up there. So if
`24:26` push the whole folder up there. So if
`24:26` push the whole folder up there. So if you don't want your brand voice or
`24:28` you don't want your brand voice or
`24:28` you don't want your brand voice or things like that going out there or any
`24:30` things like that going out there or any
`24:30` things like that going out there or any of the MD files going out there, which
`24:32` of the MD files going out there, which
`24:32` of the MD files going out there, which is what you could do, you could add .md
`24:35` is what you could do, you could add .md
`24:35` is what you could do, you could add .md to this folder or you could tell Claude
`24:37` to this folder or you could tell Claude
`24:37` to this folder or you could tell Claude to do it, right? At this point, I'm
`24:39` to do it, right? At this point, I'm
`24:39` to do it, right? At this point, I'm happy pushing this just so it's out
`24:40` happy pushing this just so it's out
`24:40` happy pushing this just so it's out there and it's something cool to look
`24:42` there and it's something cool to look
`24:42` there and it's something cool to look at. But this is that aspect, right?
`24:44` at. But this is that aspect, right?
`24:44` at. But this is that aspect, right? Also, there's certain files you don't
`24:46` Also, there's certain files you don't
`24:46` Also, there's certain files you don't want being out there because then people
`24:47` want being out there because then people
`24:47` want being out there because then people can see how your website's being built
`24:50` can see how your website's being built
`24:50` can see how your website's being built and what's there. And this is where
`24:51` and what's there. And this is where
`24:51` and what's there. And this is where having a public git repo becomes a
`24:53` having a public git repo becomes a
`24:53` having a public git repo becomes a security issue because then it's easier
`24:55` security issue because then it's easier
`24:55` security issue because then it's easier to know what the files are and to
`24:57` to know what the files are and to
`24:57` to know what the files are and to possibly hack it. But in most websites
`24:59` possibly hack it. But in most websites
`24:59` possibly hack it. But in most websites like this, there's not too much stress
`25:01` like this, there's not too much stress
`25:01` like this, there's not too much stress that you need to worry about for that
`25:03` that you need to worry about for that
`25:03` that you need to worry about for that and there's ways to hide that, which
`25:04` and there's ways to hide that, which
`25:04` and there's ways to hide that, which we'll dive into other videos. In this
`25:06` we'll dive into other videos. In this
`25:06` we'll dive into other videos. In this case, I'm just trying to get a website
`25:07` case, I'm just trying to get a website
`25:07` case, I'm just trying to get a website out there that works, looks clean and is
`25:09` out there that works, looks clean and is
`25:09` out there that works, looks clean and is there. So that's what we're doing. Now,
`25:11` there. So that's what we're doing. Now,
`25:11` there. So that's what we're doing. Now, it went ahead and actually pushed it to
`25:13` it went ahead and actually pushed it to
`25:13` it went ahead and actually pushed it to the repo and did something called GitHub
`25:15` the repo and did something called GitHub
`25:15` the repo and did something called GitHub actions, which is a little separate from
`25:17` actions, which is a little separate from
`25:17` actions, which is a little separate from the normal GitHub pages deploy, but I'll
`25:19` the normal GitHub pages deploy, but I'll
`25:19` the normal GitHub pages deploy, but I'll describe that in a moment. However, even
`25:22` describe that in a moment. However, even
`25:22` describe that in a moment. However, even though it pushed the repo and it got
`25:24` though it pushed the repo and it got
`25:24` though it pushed the repo and it got everything in there and again, I didn't
`25:25` everything in there and again, I didn't
`25:25` everything in there and again, I didn't add any of my MDs or anything to the
`25:28` add any of my MDs or anything to the
`25:28` add any of my MDs or anything to the gitignore, so it's all out there now and
`25:29` gitignore, so it's all out there now and
`25:29` gitignore, so it's all out there now and I'll make sure everyone gets the link
`25:31` I'll make sure everyone gets the link
`25:31` I'll make sure everyone gets the link for this to look at, too, if they want
`25:33` for this to look at, too, if they want
`25:33` for this to look at, too, if they want to mess around with it. But it did
`25:35` to mess around with it. But it did
`25:35` to mess around with it. But it did something wrong. What it did is it
`25:36` something wrong. What it did is it
`25:36` something wrong. What it did is it didn't create the right paths for the
`25:38` didn't create the right paths for the
`25:38` didn't create the right paths for the CSS files. So what happens is when I
`25:41` CSS files. So what happens is when I
`25:41` CSS files. So what happens is when I actually click on that live site, we
`25:43` actually click on that live site, we
`25:43` actually click on that live site, we don't get any of the CSS content. We
`25:45` don't get any of the CSS content. We
`25:45` don't get any of the CSS content. We just get the pure text, the pure Astro
`25:48` just get the pure text, the pure Astro
`25:48` just get the pure text, the pure Astro text for this, which is a serious
`25:50` text for this, which is a serious
`25:51` text for this, which is a serious problem cuz this makes the site look
`25:52` problem cuz this makes the site look
`25:52` problem cuz this makes the site look good or look bad, look really bad. And
`25:55` good or look bad, look really bad. And
`25:55` good or look bad, look really bad. And now, usually be like, "Oh god, it all
`25:57` now, usually be like, "Oh god, it all
`25:57` now, usually be like, "Oh god, it all broke." No, it didn't. Just say, "Hey,
`25:59` broke." No, it didn't. Just say, "Hey,
`25:59` broke." No, it didn't. Just say, "Hey, you messed the paths up." Or even just
`26:01` you messed the paths up." Or even just
`26:01` you messed the paths up." Or even just take a screenshot of it if you don't
`26:02` take a screenshot of it if you don't
`26:02` take a screenshot of it if you don't know and just say, "Hey, problems with
`26:04` know and just say, "Hey, problems with
`26:04` know and just say, "Hey, problems with file talking." I say file talking,
`26:06` file talking." I say file talking,
`26:06` file talking." I say file talking, that's paths. I'm very weird with my
`26:08` that's paths. I'm very weird with my
`26:08` that's paths. I'm very weird with my Claude, but essentially it's just the
`26:10` Claude, but essentially it's just the
`26:10` Claude, but essentially it's just the pathing was and it recognized it, saw
`26:11` pathing was and it recognized it, saw
`26:11` pathing was and it recognized it, saw that there was the wrong links, there
`26:13` that there was the wrong links, there
`26:13` that there was the wrong links, there was no prefixes in the base URL. So it's
`26:16` was no prefixes in the base URL. So it's
`26:16` was no prefixes in the base URL. So it's going through. And this is the power of
`26:18` going through. And this is the power of
`26:18` going through. And this is the power of Git.
`26:19` Git.
`26:19` Git. It is published. It is pushed. But now
`26:21` It is published. It is pushed. But now
`26:21` It is published. It is pushed. But now because I have a git repo, I can edit
`26:23` because I have a git repo, I can edit
`26:23` because I have a git repo, I can edit something on my computer. I can test it
`26:25` something on my computer. I can test it
`26:25` something on my computer. I can test it here and then I can push again and I
`26:28` here and then I can push again and I
`26:28` here and then I can push again and I don't have to go through all that
`26:30` don't have to go through all that
`26:30` don't have to go through all that process. It's already linked. This file
`26:32` process. It's already linked. This file
`26:32` process. It's already linked. This file setup is now linked to that GitHub, so
`26:35` setup is now linked to that GitHub, so
`26:35` setup is now linked to that GitHub, so anyone else can go see my changes later
`26:37` anyone else can go see my changes later
`26:37` anyone else can go see my changes later after I actually push them, which is the
`26:40` after I actually push them, which is the
`26:40` after I actually push them, which is the real powerful part behind Git, which
`26:42` real powerful part behind Git, which
`26:42` real powerful part behind Git, which again deserves a whole video in itself.
`26:44` again deserves a whole video in itself.
`26:44` again deserves a whole video in itself. And now, realizing that I'm making this
`26:46` And now, realizing that I'm making this
`26:46` And now, realizing that I'm making this video, that's what I'm going to do. But
`26:48` video, that's what I'm going to do. But
`26:48` video, that's what I'm going to do. But that's the concept here is now I get to
`26:51` that's the concept here is now I get to
`26:51` that's the concept here is now I get to be able to edit on this repeatedly
`26:53` be able to edit on this repeatedly
`26:53` be able to edit on this repeatedly rather having one index.html file that I
`26:56` rather having one index.html file that I
`26:56` rather having one index.html file that I have to constantly re-upload or push to
`26:58` have to constantly re-upload or push to
`26:58` have to constantly re-upload or push to Wix or use someone else's service. I can
`27:00` Wix or use someone else's service. I can
`27:00` Wix or use someone else's service. I can just log on to my local computer, talk
`27:02` just log on to my local computer, talk
`27:02` just log on to my local computer, talk to Claude, say, "Hey,
`27:04` to Claude, say, "Hey,
`27:04` to Claude, say, "Hey, I want to change this about the
`27:05` I want to change this about the
`27:05` I want to change this about the website." And it changes it and pushes
`27:07` website." And it changes it and pushes
`27:07` website." And it changes it and pushes it. We've spent the last 20 minutes, 15
`27:10` it. We've spent the last 20 minutes, 15
`27:10` it. We've spent the last 20 minutes, 15 minutes building the structure to allow
`27:13` minutes building the structure to allow
`27:13` minutes building the structure to allow us to change the structure every week or
`27:16` us to change the structure every week or
`27:16` us to change the structure every week or so without having to rebuild the whole
`27:18` so without having to rebuild the whole
`27:18` so without having to rebuild the whole thing. It's all about the organization.
`27:21` thing. It's all about the organization.
`27:21` thing. It's all about the organization. Went ahead and went through and looked
`27:23` Went ahead and went through and looked
`27:23` Went ahead and went through and looked at all the pathing and solved all those
`27:24` at all the pathing and solved all those
`27:24` at all the pathing and solved all those problems. It pushed it to GitHub. It
`27:26` problems. It pushed it to GitHub. It
`27:26` problems. It pushed it to GitHub. It waited for it to deploy and then it gave
`27:28` waited for it to deploy and then it gave
`27:28` waited for it to deploy and then it gave me the link to our new freshly deployed
`27:31` me the link to our new freshly deployed
`27:31` me the link to our new freshly deployed website, which this link I can now give
`27:33` website, which this link I can now give
`27:33` website, which this link I can now give to anyone. They can go find and you can
`27:36` to anyone. They can go find and you can
`27:36` to anyone. They can go find and you can now have a fully live published website
`27:39` now have a fully live published website
`27:39` now have a fully live published website that you can now edit any way you want.
`27:42` that you can now edit any way you want.
`27:42` that you can now edit any way you want. Maybe you're using it and you want to
`27:43` Maybe you're using it and you want to
`27:43` Maybe you're using it and you want to change the logo. You just come into here
`27:46` change the logo. You just come into here
`27:46` change the logo. You just come into here in your normal file, say, "Hey, I want
`27:47` in your normal file, say, "Hey, I want
`27:47` in your normal file, say, "Hey, I want to change the logo." Pushes it. You can
`27:49` to change the logo." Pushes it. You can
`27:49` to change the logo." Pushes it. You can turn off your computer, you can go away.
`27:51` turn off your computer, you can go away.
`27:51` turn off your computer, you can go away. You don't need anyone telling you where
`27:53` You don't need anyone telling you where
`27:53` You don't need anyone telling you where or what to build. You don't have Wix or
`27:55` or what to build. You don't have Wix or
`27:55` or what to build. You don't have Wix or anyone telling you how to build it. Now,
`27:57` anyone telling you how to build it. Now,
`27:57` anyone telling you how to build it. Now, granted, sometimes the AI gives you a
`27:59` granted, sometimes the AI gives you a
`27:59` granted, sometimes the AI gives you a little less control. That nuanced detail
`28:01` little less control. That nuanced detail
`28:01` little less control. That nuanced detail is where that perfect control, but I
`28:03` is where that perfect control, but I
`28:03` is where that perfect control, but I think you can get to that nuanced detail
`28:05` think you can get to that nuanced detail
`28:05` think you can get to that nuanced detail with language most of the time. So
`28:07` with language most of the time. So
`28:07` with language most of the time. So that's it. That's This video went a
`28:09` that's it. That's This video went a
`28:09` that's it. That's This video went a little longer than I expected. There is
`28:11` little longer than I expected. There is
`28:11` little longer than I expected. There is a lot more information that I ended up
`28:12` a lot more information that I ended up
`28:12` a lot more information that I ended up lecturing on. I feel like the actual
`28:14` lecturing on. I feel like the actual
`28:14` lecturing on. I feel like the actual method. I could have done the simple
`28:16` method. I could have done the simple
`28:16` method. I could have done the simple route and just shown you how to take an
`28:18` route and just shown you how to take an
`28:18` route and just shown you how to take an index.html and have you submit that cuz
`28:20` index.html and have you submit that cuz
`28:20` index.html and have you submit that cuz you can do that, too, right? This could
`28:22` you can do that, too, right? This could
`28:22` you can do that, too, right? This could all be put into a single HTML file and
`28:24` all be put into a single HTML file and
`28:24` all be put into a single HTML file and we could have pushed it and it would
`28:25` we could have pushed it and it would
`28:25` we could have pushed it and it would have been ready to go immediately. But
`28:27` have been ready to go immediately. But
`28:27` have been ready to go immediately. But again, we are thinking about long-term.
`28:29` again, we are thinking about long-term.
`28:29` again, we are thinking about long-term. We're not just here to build something
`28:30` We're not just here to build something
`28:30` We're not just here to build something quick. What does it mean if I was to
`28:32` quick. What does it mean if I was to
`28:32` quick. What does it mean if I was to keep updating this website to add more,
`28:34` keep updating this website to add more,
`28:35` keep updating this website to add more, to structure it? Well, it comes back to
`28:37` to structure it? Well, it comes back to
`28:37` to structure it? Well, it comes back to how you're structuring your process
`28:39` how you're structuring your process
`28:39` how you're structuring your process here, what you're doing. And now I have
`28:41` here, what you're doing. And now I have
`28:41` here, what you're doing. And now I have design I can wander through and look at.
`28:43` design I can wander through and look at.
`28:43` design I can wander through and look at. I have aspects to actually edit and add
`28:46` I have aspects to actually edit and add
`28:46` I have aspects to actually edit and add more packages in and clean the website
`28:48` more packages in and clean the website
`28:48` more packages in and clean the website up and change things to a whole other
`28:50` up and change things to a whole other
`28:50` up and change things to a whole other level. So I hope that helps everyone. I
`28:52` level. So I hope that helps everyone. I
`28:53` level. So I hope that helps everyone. I hope you enjoy it and check out American
`28:55` hope you enjoy it and check out American
`28:55` hope you enjoy it and check out American Moto
`28:56` Moto
`28:56` Moto .com .co. I forget what the original
`28:58` .com .co. I forget what the original
`28:58` .com .co. I forget what the original website was originally. But that's the
`29:00` website was originally. But that's the
`29:00` website was originally. But that's the video. That's how you can make a website
`29:02` video. That's how you can make a website
`29:02` video. That's how you can make a website in less than 30 minutes, I think, 20
`29:04` in less than 30 minutes, I think, 20
`29:04` in less than 30 minutes, I think, 20 minutes. I don't know, it depends how
`29:06` minutes. I don't know, it depends how
`29:06` minutes. I don't know, it depends how long you get errors and stuff like that
`29:07` long you get errors and stuff like that
`29:08` long you get errors and stuff like that for, I suppose. So happy learning
`29:09` for, I suppose. So happy learning
`29:09` for, I suppose. So happy learning everyone and we'll see you around. If
`29:11` everyone and we'll see you around. If
`29:11` everyone and we'll see you around. If you want to learn more, again, join the
`29:13` you want to learn more, again, join the
`29:13` you want to learn more, again, join the community, all that good jazz. I have
`29:15` community, all that good jazz. I have
`29:15` community, all that good jazz. I have plenty more videos coming out every day
`29:18` plenty more videos coming out every day
`29:18` plenty more videos coming out every day that I can push them. So have a
`29:19` that I can push them. So have a
`29:19` that I can push them. So have a wonderful time. See you everyone.