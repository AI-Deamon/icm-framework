# Why I Stopped Building AI Agents and Started Using Claude Cowork

**Channel:** Jake Van Clief  
**Uploaded:** 2026-03-21  
**Duration:** 51:22  
**URL:** [https://www.youtube.com/watch?v=0fCQ-4J_jzk](https://www.youtube.com/watch?v=0fCQ-4J_jzk)  

---

## Transcript

`00:01` I'm sorry, this is just more efficient.
`00:01` I'm sorry, this is just more efficient. This is what I'm going to do. I don't
`00:03` This is what I'm going to do. I don't
`00:03` This is what I'm going to do. I don't want to spend all this money on these
`00:04` want to spend all this money on these
`00:04` want to spend all this money on these other API calls constantly going out.
`00:06` other API calls constantly going out.
`00:06` other API calls constantly going out. And if I have multiple clients, I'm
`00:08` And if I have multiple clients, I'm
`00:08` And if I have multiple clients, I'm going to convince them to get their own
`00:10` going to convince them to get their own
`00:10` going to convince them to get their own subscription and just do it because two
`00:12` subscription and just do it because two
`00:12` subscription and just do it because two things. If I have 2,000 clients, they're
`00:15` things. If I have 2,000 clients, they're
`00:15` things. If I have 2,000 clients, they're all paying me money and then I'm
`00:16` all paying me money and then I'm
`00:16` all paying me money and then I'm managing all these API things. Sure, I'm
`00:18` managing all these API things. Sure, I'm
`00:18` managing all these API things. Sure, I'm making money. Sure, it's handling
`00:19` making money. Sure, it's handling
`00:19` making money. Sure, it's handling certain problems. But what's the job to
`00:21` certain problems. But what's the job to
`00:21` certain problems. But what's the job to be done? The thing is I'm trying to
`00:22` be done? The thing is I'm trying to
`00:22` be done? The thing is I'm trying to create value. This is going to get
`00:24` create value. This is going to get
`00:24` create value. This is going to get updated naturally. I don't have to spend
`00:25` updated naturally. I don't have to spend
`00:26` updated naturally. I don't have to spend any money, any effort, any work other
`00:28` any money, any effort, any work other
`00:28` any money, any effort, any work other than my monthly subscription to get an
`00:30` than my monthly subscription to get an
`00:30` than my monthly subscription to get an entire team of people building a better
`00:33` entire team of people building a better
`00:33` entire team of people building a better agent for me. Okay, so I wanted to try
`00:35` agent for me. Okay, so I wanted to try
`00:35` agent for me. Okay, so I wanted to try something a little different today. I
`00:37` something a little different today. I
`00:37` something a little different today. I wanted to use Claude Co-work instead of
`00:39` wanted to use Claude Co-work instead of
`00:39` wanted to use Claude Co-work instead of my usual VS Code setup to essentially
`00:42` my usual VS Code setup to essentially
`00:42` my usual VS Code setup to essentially create a writing and scripting area. But
`00:44` create a writing and scripting area. But
`00:44` create a writing and scripting area. But not just like, oh hey, let's write some
`00:46` not just like, oh hey, let's write some
`00:46` not just like, oh hey, let's write some scripts. I want it used on things that
`00:48` scripts. I want it used on things that
`00:48` scripts. I want it used on things that have been working well, my actual social
`00:50` have been working well, my actual social
`00:50` have been working well, my actual social media, things like that. And what I'm
`00:52` media, things like that. And what I'm
`00:52` media, things like that. And what I'm going to do is I created a folder on my
`00:54` going to do is I created a folder on my
`00:54` going to do is I created a folder on my desktop called writing and scripting.
`00:56` desktop called writing and scripting.
`00:56` desktop called writing and scripting. Now, I already have a kind of
`00:58` Now, I already have a kind of
`00:58` Now, I already have a kind of architectured folder that does this kind
`01:00` architectured folder that does this kind
`01:01` architectured folder that does this kind of stuff, but I wanted to start fresh to
`01:03` of stuff, but I wanted to start fresh to
`01:03` of stuff, but I wanted to start fresh to kind of figure out what I wanted and
`01:04` kind of figure out what I wanted and
`01:04` kind of figure out what I wanted and then apply it to my main folder later.
`01:07` then apply it to my main folder later.
`01:07` then apply it to my main folder later. So I logged into my good old Claude
`01:09` So I logged into my good old Claude
`01:09` So I logged into my good old Claude desktop app and just clicked on Co-work
`01:12` desktop app and just clicked on Co-work
`01:12` desktop app and just clicked on Co-work and then went ahead and selected my
`01:13` and then went ahead and selected my
`01:13` and then went ahead and selected my folder that I had there.
`01:15` folder that I had there.
`01:15` folder that I had there. Um this is a brand new one. Now, the way
`01:17` Um this is a brand new one. Now, the way
`01:17` Um this is a brand new one. Now, the way I want to kind of frame this is you want
`01:20` I want to kind of frame this is you want
`01:20` I want to kind of frame this is you want to break down your processes in your
`01:22` to break down your processes in your
`01:22` to break down your processes in your head first and then figure out how to
`01:24` head first and then figure out how to
`01:24` head first and then figure out how to tell that to Claude. So it's not just,
`01:26` tell that to Claude. So it's not just,
`01:26` tell that to Claude. So it's not just, oh let me have this copy and paste
`01:28` oh let me have this copy and paste
`01:28` oh let me have this copy and paste prompt. Anyone that's selling you copy
`01:30` prompt. Anyone that's selling you copy
`01:30` prompt. Anyone that's selling you copy and paste prompts minus like templates
`01:32` and paste prompts minus like templates
`01:32` and paste prompts minus like templates and markdown files, it's not really
`01:33` and markdown files, it's not really
`01:33` and markdown files, it's not really going to help you. So the main thing
`01:35` going to help you. So the main thing
`01:35` going to help you. So the main thing that I'm going to do here is actually
`01:37` that I'm going to do here is actually
`01:37` that I'm going to do here is actually kind of describe um what I'm doing. And
`01:39` kind of describe um what I'm doing. And
`01:39` kind of describe um what I'm doing. And usually I do voice-to-text, but I want
`01:41` usually I do voice-to-text, but I want
`01:41` usually I do voice-to-text, but I want to type it out in front of you.
`02:00` [music]
`02:00` [music] >> Right? We don't want to tell it just
`02:02` >> Right? We don't want to tell it just
`02:02` >> Right? We don't want to tell it just simply tasks to do. We want to
`02:04` simply tasks to do. We want to
`02:04` simply tasks to do. We want to conceptualize the working space for it
`02:07` conceptualize the working space for it
`02:07` conceptualize the working space for it before we start working in that space.
`02:09` before we start working in that space.
`02:09` before we start working in that space. And so I'm just going to go ahead and
`02:11` And so I'm just going to go ahead and
`02:11` And so I'm just going to go ahead and send that to that. Um now again, usually
`02:14` send that to that. Um now again, usually
`02:14` send that to that. Um now again, usually I work inside of VS Code when I'm doing
`02:16` I work inside of VS Code when I'm doing
`02:16` I work inside of VS Code when I'm doing this. I I just find it a lot easier to
`02:18` this. I I just find it a lot easier to
`02:18` this. I I just find it a lot easier to navigate the files um and create like
`02:21` navigate the files um and create like
`02:21` navigate the files um and create like Claude MDs like this. Like, hey, here's
`02:24` Claude MDs like this. Like, hey, here's
`02:24` Claude MDs like this. Like, hey, here's how it actually works and this is what
`02:25` how it actually works and this is what
`02:25` how it actually works and this is what my actual um file system looks like.
`02:28` my actual um file system looks like.
`02:28` my actual um file system looks like. I've used a much longer process over
`02:30` I've used a much longer process over
`02:30` I've used a much longer process over months creating this uh because it's
`02:32` months creating this uh because it's
`02:32` months creating this uh because it's just based around my workflow, what I
`02:34` just based around my workflow, what I
`02:34` just based around my workflow, what I like to do, my fuzzy tasks.
`02:36` like to do, my fuzzy tasks.
`02:36` like to do, my fuzzy tasks. Um but I wanted to start fresh and I
`02:38` Um but I wanted to start fresh and I
`02:38` Um but I wanted to start fresh and I wanted to use Claude Co-work to show
`02:40` wanted to use Claude Co-work to show
`02:40` wanted to use Claude Co-work to show that you don't necessarily need to use
`02:42` that you don't necessarily need to use
`02:42` that you don't necessarily need to use VS Code. It's just a different way of
`02:44` VS Code. It's just a different way of
`02:44` VS Code. It's just a different way of working. So obviously it's going to ask
`02:45` working. So obviously it's going to ask
`02:45` working. So obviously it's going to ask me some question. So what's the main
`02:47` me some question. So what's the main
`02:47` me some question. So what's the main subject matter of your channel? Um I am
`02:49` subject matter of your channel? Um I am
`02:49` subject matter of your channel? Um I am going to paste transcripts
`02:51` going to paste transcripts
`02:51` going to paste transcripts and everything. I want you to ask me
`02:53` and everything. I want you to ask me
`02:53` and everything. I want you to ask me questions like this after I send you the
`02:55` questions like this after I send you the
`02:55` questions like this after I send you the transcripts. So the reason I'm doing
`02:56` transcripts. So the reason I'm doing
`02:56` transcripts. So the reason I'm doing this is I know that if I told it here,
`02:58` this is I know that if I told it here,
`02:58` this is I know that if I told it here, it might be missing some context. So
`03:00` it might be missing some context. So
`03:00` it might be missing some context. So it's the wrong order in which to give
`03:02` it's the wrong order in which to give
`03:02` it's the wrong order in which to give this information cuz you want to think
`03:03` this information cuz you want to think
`03:03` this information cuz you want to think about the order you're giving
`03:05` about the order you're giving
`03:05` about the order you're giving information to Claude. It needs to it's
`03:07` information to Claude. It needs to it's
`03:07` information to Claude. It needs to it's slowly creating a map, right? Every word
`03:10` slowly creating a map, right? Every word
`03:10` slowly creating a map, right? Every word you give Claude is a coordinate in a
`03:13` you give Claude is a coordinate in a
`03:13` you give Claude is a coordinate in a massive multi-dimensional vector space.
`03:16` massive multi-dimensional vector space.
`03:16` massive multi-dimensional vector space. So you are giving it a map to find data.
`03:19` So you are giving it a map to find data.
`03:19` So you are giving it a map to find data. The data that you want it to create
`03:22` The data that you want it to create
`03:22` The data that you want it to create technically exists inside of it already.
`03:24` technically exists inside of it already.
`03:24` technically exists inside of it already. You're just creating the map and the
`03:27` You're just creating the map and the
`03:27` You're just creating the map and the instructions to go find it and generate
`03:29` instructions to go find it and generate
`03:29` instructions to go find it and generate it. Right? And so you want to make sure
`03:31` it. Right? And so you want to make sure
`03:31` it. Right? And so you want to make sure the order in which you're giving it is
`03:33` the order in which you're giving it is
`03:33` the order in which you're giving it is going to change, right? If you take a
`03:34` going to change, right? If you take a
`03:34` going to change, right? If you take a left and then a right on a map versus a
`03:37` left and then a right on a map versus a
`03:37` left and then a right on a map versus a right and a left, you're going to end up
`03:38` right and a left, you're going to end up
`03:38` right and a left, you're going to end up somewhere else. And so you want to make
`03:40` somewhere else. And so you want to make
`03:40` somewhere else. And so you want to make sure you know how you want to describe
`03:43` sure you know how you want to describe
`03:43` sure you know how you want to describe this. So in my head, I know that I want
`03:45` this. So in my head, I know that I want
`03:45` this. So in my head, I know that I want to tell it after. I just want to focus
`03:47` to tell it after. I just want to focus
`03:47` to tell it after. I just want to focus on this kind of process. Um
`03:51` on this kind of process. Um
`03:51` on this kind of process. Um perhaps we wait to build the skeleton
`03:55` perhaps we wait to build the skeleton
`03:55` perhaps we wait to build the skeleton till you get those. Um right? And this
`03:59` till you get those. Um right? And this
`03:59` till you get those. Um right? And this is me messing up in the original prompt.
`04:01` is me messing up in the original prompt.
`04:01` is me messing up in the original prompt. I should have known that it needed more
`04:03` I should have known that it needed more
`04:03` I should have known that it needed more information on my channel and perhaps
`04:05` information on my channel and perhaps
`04:05` information on my channel and perhaps described something there or added it
`04:07` described something there or added it
`04:07` described something there or added it in. But because it's asking me this
`04:09` in. But because it's asking me this
`04:09` in. But because it's asking me this question, I can immediately see, oh,
`04:11` question, I can immediately see, oh,
`04:11` question, I can immediately see, oh, it's misaligned in exactly what I want
`04:13` it's misaligned in exactly what I want
`04:13` it's misaligned in exactly what I want to say. So let me go ahead and switch it
`04:16` to say. So let me go ahead and switch it
`04:16` to say. So let me go ahead and switch it up a little bit. Now [snorts] from
`04:17` up a little bit. Now [snorts] from
`04:17` up a little bit. Now [snorts] from there, how many pillars do you typically
`04:19` there, how many pillars do you typically
`04:19` there, how many pillars do you typically work with?
`04:20` work with?
`04:20` work with? Let's decide as we explore this space.
`04:24` Let's decide as we explore this space.
`04:24` Let's decide as we explore this space. So now I realize what I should have done
`04:26` So now I realize what I should have done
`04:26` So now I realize what I should have done as my first prompt is explore the
`04:28` as my first prompt is explore the
`04:28` as my first prompt is explore the transcripts and everything first and
`04:30` transcripts and everything first and
`04:30` transcripts and everything first and then create the kind of folder space.
`04:33` then create the kind of folder space.
`04:33` then create the kind of folder space. But now it knows that the end goal of
`04:35` But now it knows that the end goal of
`04:35` But now it knows that the end goal of exploring all this is a folder space. Um
`04:40` exploring all this is a folder space. Um
`04:40` exploring all this is a folder space. Um first by looking at the info and such.
`04:45` first by looking at the info and such.
`04:45` first by looking at the info and such. Um and then for scripts and writing as I
`04:47` Um and then for scripts and writing as I
`04:47` Um and then for scripts and writing as I do in my single idea folder or folder um
`04:50` do in my single idea folder or folder um
`04:50` do in my single idea folder or folder um yeah, single idea folder. That one's
`04:51` yeah, single idea folder. That one's
`04:51` yeah, single idea folder. That one's fair. So now it should go read that and
`04:54` fair. So now it should go read that and
`04:54` fair. So now it should go read that and it should, if we answered that
`04:56` it should, if we answered that
`04:56` it should, if we answered that correctly, say, hey, here's what I'll
`04:57` correctly, say, hey, here's what I'll
`04:57` correctly, say, hey, here's what I'll do, create a minimal receiving
`04:59` do, create a minimal receiving
`04:59` do, create a minimal receiving structure, dump transcripts and start a
`05:01` structure, dump transcripts and start a
`05:01` structure, dump transcripts and start a Claude. Cool. Happy with that. So what's
`05:03` Claude. Cool. Happy with that. So what's
`05:03` Claude. Cool. Happy with that. So what's going to end up happening is the folder
`05:04` going to end up happening is the folder
`05:04` going to end up happening is the folder that I created right here on my desktop,
`05:07` that I created right here on my desktop,
`05:07` that I created right here on my desktop, it should be immediately creating stuff
`05:09` it should be immediately creating stuff
`05:09` it should be immediately creating stuff in there. And as we can see, that's
`05:10` in there. And as we can see, that's
`05:10` in there. And as we can see, that's exactly what's going on. Right? Writing
`05:12` exactly what's going on. Right? Writing
`05:12` exactly what's going on. Right? Writing a file, creating the initial raw folder.
`05:15` a file, creating the initial raw folder.
`05:15` a file, creating the initial raw folder. So let's see, source material, we've got
`05:16` So let's see, source material, we've got
`05:16` So let's see, source material, we've got transcripts, voice brand, community
`05:18` transcripts, voice brand, community
`05:18` transcripts, voice brand, community concepts. Okay, cool. So it's source
`05:20` concepts. Okay, cool. So it's source
`05:20` concepts. Okay, cool. So it's source material there. We've got the Claude
`05:22` material there. We've got the Claude
`05:22` material there. We've got the Claude code, which again, this is why I like
`05:24` code, which again, this is why I like
`05:24` code, which again, this is why I like working in VS Code cuz I don't have to
`05:25` working in VS Code cuz I don't have to
`05:25` working in VS Code cuz I don't have to open it up in another folder or file,
`05:28` open it up in another folder or file,
`05:28` open it up in another folder or file, but hey, same same but different. Um
`05:31` but hey, same same but different. Um
`05:31` but hey, same same but different. Um cool, it's already mapped that out. And
`05:33` cool, it's already mapped that out. And
`05:33` cool, it's already mapped that out. And again, the reason we want to map out the
`05:35` again, the reason we want to map out the
`05:35` again, the reason we want to map out the folder structure in the Claude MD is
`05:37` folder structure in the Claude MD is
`05:37` folder structure in the Claude MD is because every time Claude's going to
`05:39` because every time Claude's going to
`05:39` because every time Claude's going to have to go and look through all my
`05:41` have to go and look through all my
`05:41` have to go and look through all my folders to see what's in there. But if
`05:42` folders to see what's in there. But if
`05:42` folders to see what's in there. But if we tell it what is in there simply and
`05:44` we tell it what is in there simply and
`05:44` we tell it what is in there simply and quickly, it'll be able to roll through
`05:47` quickly, it'll be able to roll through
`05:47` quickly, it'll be able to roll through it. Um we're going to edit this Claude
`05:49` it. Um we're going to edit this Claude
`05:49` it. Um we're going to edit this Claude MD as we go along. This is to save
`05:51` MD as we go along. This is to save
`05:51` MD as we go along. This is to save tokens or if I stop and move along. We
`05:53` tokens or if I stop and move along. We
`05:53` tokens or if I stop and move along. We don't want to just fill this Claude MD
`05:55` don't want to just fill this Claude MD
`05:55` don't want to just fill this Claude MD with a whole bunch of stuff. We're only
`05:56` with a whole bunch of stuff. We're only
`05:56` with a whole bunch of stuff. We're only constantly removing and putting back in,
`05:58` constantly removing and putting back in,
`05:59` constantly removing and putting back in, removing, putting back in. There's ways
`06:00` removing, putting back in. There's ways
`06:00` removing, putting back in. There's ways to do this with memory files and things
`06:02` to do this with memory files and things
`06:02` to do this with memory files and things like that. But I wanted to start really
`06:04` like that. But I wanted to start really
`06:04` like that. But I wanted to start really fresh to have control of everything and
`06:06` fresh to have control of everything and
`06:06` fresh to have control of everything and I want to make my own skills along the
`06:08` I want to make my own skills along the
`06:08` I want to make my own skills along the way. And the reason I want to make my
`06:10` way. And the reason I want to make my
`06:10` way. And the reason I want to make my own skills is right now Claude has great
`06:13` own skills is right now Claude has great
`06:13` own skills is right now Claude has great skills that I can use, right? So if I I
`06:15` skills that I can use, right? So if I I
`06:15` skills that I can use, right? So if I I come over and we go to customize and we
`06:18` come over and we go to customize and we
`06:18` come over and we go to customize and we go to per plugins or skills, there's
`06:20` go to per plugins or skills, there's
`06:20` go to per plugins or skills, there's skills that you can automatically use.
`06:23` skills that you can automatically use.
`06:23` skills that you can automatically use. But skills are again just markdown
`06:25` But skills are again just markdown
`06:25` But skills are again just markdown files. They're processes. And if we look
`06:27` files. They're processes. And if we look
`06:27` files. They're processes. And if we look at brand and marketing and we go to the
`06:29` at brand and marketing and we go to the
`06:29` at brand and marketing and we go to the skills, they have a brand voice skill. I
`06:31` skills, they have a brand voice skill. I
`06:31` skills, they have a brand voice skill. I don't like this personally. The way I do
`06:34` don't like this personally. The way I do
`06:34` don't like this personally. The way I do my brand personality or how I would ask
`06:35` my brand personality or how I would ask
`06:35` my brand personality or how I would ask my questions would be a little
`06:37` my questions would be a little
`06:37` my questions would be a little different. If I was making this skill, I
`06:39` different. If I was making this skill, I
`06:39` different. If I was making this skill, I would write it a little different. It's
`06:40` would write it a little different. It's
`06:40` would write it a little different. It's not a bad skill. In fact, it's a great
`06:42` not a bad skill. In fact, it's a great
`06:42` not a bad skill. In fact, it's a great one. But one of the big things is the
`06:44` one. But one of the big things is the
`06:44` one. But one of the big things is the kind of way in which we're breaking down
`06:47` kind of way in which we're breaking down
`06:47` kind of way in which we're breaking down the pillars. I'm going to take a
`06:48` the pillars. I'm going to take a
`06:48` the pillars. I'm going to take a different approach, which you'll see
`06:50` different approach, which you'll see
`06:50` different approach, which you'll see towards the end of this video. This
`06:51` towards the end of this video. This
`06:51` towards the end of this video. This might end up being a really long video.
`06:53` might end up being a really long video.
`06:53` might end up being a really long video. So I apologize for that, but
`06:55` So I apologize for that, but
`06:55` So I apologize for that, but you know, that's how the cookie crumbles
`06:57` you know, that's how the cookie crumbles
`06:57` you know, that's how the cookie crumbles sometimes. So yeah, skills are great.
`06:59` sometimes. So yeah, skills are great.
`06:59` sometimes. So yeah, skills are great. You should download skills, but it's
`07:00` You should download skills, but it's
`07:00` You should download skills, but it's even better to make your own skills
`07:02` even better to make your own skills
`07:02` even better to make your own skills uh if you can. So let me go ahead and go
`07:04` uh if you can. So let me go ahead and go
`07:04` uh if you can. So let me go ahead and go back to our thing. Content here.
`07:07` back to our thing. Content here.
`07:07` back to our thing. Content here. So it's made that. All right. So now
`07:09` So it's made that. All right. So now
`07:09` So it's made that. All right. So now what we're going to do is I'm going to
`07:10` what we're going to do is I'm going to
`07:10` what we're going to do is I'm going to go ahead and start pasting some
`07:12` go ahead and start pasting some
`07:12` go ahead and start pasting some transcripts. So um I already
`07:15` transcripts. So um I already
`07:15` transcripts. So um I already kind of took a transcript. These are
`07:17` kind of took a transcript. These are
`07:17` kind of took a transcript. These are actually from Instagram videos that I
`07:19` actually from Instagram videos that I
`07:19` actually from Instagram videos that I took transcripts from. So these first
`07:21` took transcripts from. So these first
`07:21` took transcripts from. So these first set are from Instagram videos, two of
`07:23` set are from Instagram videos, two of
`07:23` set are from Instagram videos, two of them. Sorry, I let the recording run
`07:25` them. Sorry, I let the recording run
`07:25` them. Sorry, I let the recording run long. I I recorded it quickly with um uh
`07:27` long. I I recorded it quickly with um uh
`07:27` long. I I recorded it quickly with um uh OBS cuz I was lazy and just transcribed
`07:29` OBS cuz I was lazy and just transcribed
`07:29` OBS cuz I was lazy and just transcribed it quickly. So it's like double
`07:29` it quickly. So it's like double
`07:29` it quickly. So it's like double recorded. Uh then the next set is from
`07:32` recorded. Uh then the next set is from
`07:32` recorded. Uh then the next set is from one single YouTube video that I'm good
`07:34` one single YouTube video that I'm good
`07:34` one single YouTube video that I'm good right now, Gemini. Um I don't need the
`07:36` right now, Gemini. Um I don't need the
`07:37` right now, Gemini. Um I don't need the timestamps on this one. So we're just
`07:38` timestamps on this one. So we're just
`07:38` timestamps on this one. So we're just going to go wee.
`07:43` Talking, talking, talking. And we'll hit
`07:43` Talking, talking, talking. And we'll hit control C. There we are.
`07:46` control C. There we are.
`07:46` control C. There we are. Pop that in there. Okay. Also, I have
`07:48` Pop that in there. Okay. Also, I have
`07:48` Pop that in there. Okay. Also, I have two rabbits in my lap right now. If you
`07:50` two rabbits in my lap right now. If you
`07:50` two rabbits in my lap right now. If you see me fidgeting around, um uh they they
`07:53` see me fidgeting around, um uh they they
`07:53` see me fidgeting around, um uh they they got into a fight and I basically had to
`07:56` got into a fight and I basically had to
`07:56` got into a fight and I basically had to wash them and whatnot. And so uh they're
`07:59` wash them and whatnot. And so uh they're
`07:59` wash them and whatnot. And so uh they're kind of warming up in my lap right now
`08:01` kind of warming up in my lap right now
`08:01` kind of warming up in my lap right now as I record this video. So if you ever
`08:03` as I record this video. So if you ever
`08:03` as I record this video. So if you ever see me fidgeting, that's that's exactly
`08:05` see me fidgeting, that's that's exactly
`08:05` see me fidgeting, that's that's exactly why. I've got two wet rabbits in my lap.
`08:08` why. I've got two wet rabbits in my lap.
`08:08` why. I've got two wet rabbits in my lap. Now this next transcript is a different
`08:13` Now this next transcript is a different
`08:13` Now this next transcript is a different type of video where instead of sharing
`08:17` type of video where instead of sharing
`08:17` type of video where instead of sharing my screen, I create animations. Now
`08:21` my screen, I create animations. Now
`08:21` my screen, I create animations. Now Claude has memory on this. This Claude,
`08:23` Claude has memory on this. This Claude,
`08:23` Claude has memory on this. This Claude, I've set the animations, but if you
`08:25` I've set the animations, but if you
`08:25` I've set the animations, but if you didn't have memory, this is what you
`08:26` didn't have memory, this is what you
`08:26` didn't have memory, this is what you would want to describe. And I'm just
`08:27` would want to describe. And I'm just
`08:27` would want to describe. And I'm just describing it to double-check. There's
`08:29` describing it to double-check. There's
`08:29` describing it to double-check. There's actually a research paper out there. Um
`08:31` actually a research paper out there. Um
`08:31` actually a research paper out there. Um I'll probably post it in post-editing
`08:33` I'll probably post it in post-editing
`08:33` I'll probably post it in post-editing right here where it says saying
`08:34` right here where it says saying
`08:34` right here where it says saying something twice actually increases the
`08:37` something twice actually increases the
`08:37` something twice actually increases the accuracy in context windows. Uh so it's
`08:39` accuracy in context windows. Uh so it's
`08:39` accuracy in context windows. Uh so it's not a bad thing to have it in memory and
`08:40` not a bad thing to have it in memory and
`08:40` not a bad thing to have it in memory and then also remind it again of that
`08:43` then also remind it again of that
`08:43` then also remind it again of that memory. Um so this transcript file is
`08:45` memory. Um so this transcript file is
`08:45` memory. Um so this transcript file is I'm sharing my screen and create
`08:46` I'm sharing my screen and create
`08:46` I'm sharing my screen and create animations. Other videos I do or uh
`08:51` animations. Other videos I do or uh
`08:51` animations. Other videos I do or uh sorry.
`08:52` sorry.
`08:52` sorry. Sharing my screen.
`08:54` Sharing my screen.
`08:54` Sharing my screen. Oh my gosh, I lost my train of Now this
`08:55` Oh my gosh, I lost my train of Now this
`08:55` Oh my gosh, I lost my train of Now this next transcript is a different type of
`08:56` next transcript is a different type of
`08:56` next transcript is a different type of video where instead of sharing my
`08:58` video where instead of sharing my
`08:58` video where instead of sharing my screen, I create animations and do
`09:00` screen, I create animations and do
`09:00` screen, I create animations and do voiceover.
`09:02` voiceover.
`09:02` voiceover. This
`09:03` This
`09:04` This and this one did well, too.
`09:07` and this one did well, too.
`09:07` and this one did well, too. >> [snorts]
`09:07` >> [snorts]
`09:07` >> [snorts] >> Um
`09:09` >> Um
`09:09` >> Um So let's go find that one. Go to my
`09:11` So let's go find that one. Go to my
`09:11` So let's go find that one. Go to my channel. Oh my gosh, I broke 10K. I
`09:13` channel. Oh my gosh, I broke 10K. I
`09:13` channel. Oh my gosh, I broke 10K. I didn't even notice. Holy crap. Thank
`09:14` didn't even notice. Holy crap. Thank
`09:14` didn't even notice. Holy crap. Thank you, everyone. Super kind. Uh that's
`09:17` you, everyone. Super kind. Uh that's
`09:17` you, everyone. Super kind. Uh that's I've been trying to get subscribers on
`09:19` I've been trying to get subscribers on
`09:19` I've been trying to get subscribers on YouTube for so long. I've been posting
`09:21` YouTube for so long. I've been posting
`09:21` YouTube for so long. I've been posting forever.
`09:22` forever.
`09:22` forever. Um and it's probably up like, I mean,
`09:24` Um and it's probably up like, I mean,
`09:24` Um and it's probably up like, I mean, years ago I started posting and finally,
`09:26` years ago I started posting and finally,
`09:26` years ago I started posting and finally, finally, I think right about here I
`09:30` finally, I think right about here I
`09:30` finally, I think right about here I started getting more views and then kind
`09:32` started getting more views and then kind
`09:32` started getting more views and then kind of none and then more views again and
`09:33` of none and then more views again and
`09:34` of none and then more views again and then this one, which is the one I'm
`09:35` then this one, which is the one I'm
`09:35` then this one, which is the one I'm going to copy, started getting more. So
`09:36` going to copy, started getting more. So
`09:36` going to copy, started getting more. So I'm happy that people are digging it. Um
`09:39` I'm happy that people are digging it. Um
`09:39` I'm happy that people are digging it. Um that's why I need to start get more
`09:40` that's why I need to start get more
`09:40` that's why I need to start get more focused on how I'm creating these videos
`09:42` focused on how I'm creating these videos
`09:42` focused on how I'm creating these videos cuz uh there's obviously something there
`09:44` cuz uh there's obviously something there
`09:44` cuz uh there's obviously something there that works. So let's see. This one was a
`09:46` that works. So let's see. This one was a
`09:46` that works. So let's see. This one was a good video. I really liked it. Lots of
`09:48` good video. I really liked it. Lots of
`09:48` good video. I really liked it. Lots of other people also liked it. So we're
`09:49` other people also liked it. So we're
`09:49` other people also liked it. So we're going to go ahead and throw that in
`09:51` going to go ahead and throw that in
`09:51` going to go ahead and throw that in here, too. So okay.
`09:54` here, too. So okay.
`09:54` here, too. So okay. So
`09:56` So
`09:56` So I have a bunch of comments and things as
`10:00` I have a bunch of comments and things as
`10:00` I have a bunch of comments and things as well. We will look at for the audience,
`10:05` well. We will look at for the audience,
`10:05` well. We will look at for the audience, but I just want you to understand my
`10:09` but I just want you to understand my
`10:09` but I just want you to understand my voice.
`10:10` voice.
`10:10` voice. The things that work. These are my best
`10:14` The things that work. These are my best
`10:14` The things that work. These are my best videos and highest views.
`10:18` videos and highest views.
`10:18` videos and highest views. Um
`10:20` Um
`10:20` Um I feel like that's fair. Pull the
`10:21` I feel like that's fair. Pull the
`10:21` I feel like that's fair. Pull the content from what works best. Now
`10:23` content from what works best. Now
`10:23` content from what works best. Now granted, both of the my best videos I
`10:26` granted, both of the my best videos I
`10:26` granted, both of the my best videos I didn't script beforehand. So, I'm going
`10:28` didn't script beforehand. So, I'm going
`10:28` didn't script beforehand. So, I'm going to be kind of hesitant on how much I
`10:31` to be kind of hesitant on how much I
`10:31` to be kind of hesitant on how much I lock in scripting and writing because
`10:34` lock in scripting and writing because
`10:34` lock in scripting and writing because obviously I got one that did 1.4 million
`10:37` obviously I got one that did 1.4 million
`10:37` obviously I got one that did 1.4 million and I just recorded it that day. I had
`10:39` and I just recorded it that day. I had
`10:39` and I just recorded it that day. I had another one, my best YouTube video I
`10:41` another one, my best YouTube video I
`10:41` another one, my best YouTube video I recorded like this. So, I'm going to be
`10:43` recorded like this. So, I'm going to be
`10:43` recorded like this. So, I'm going to be careful how much I'm actually worrying
`10:46` careful how much I'm actually worrying
`10:46` careful how much I'm actually worrying about Claude
`10:47` about Claude
`10:47` about Claude or doing this normal content process
`10:50` or doing this normal content process
`10:50` or doing this normal content process because I think this copy paste content
`10:53` because I think this copy paste content
`10:53` because I think this copy paste content production is actually why some people
`10:54` production is actually why some people
`10:54` production is actually why some people are struggling to get their views and
`10:57` are struggling to get their views and
`10:57` are struggling to get their views and stuff to get things out there
`11:00` stuff to get things out there
`11:00` stuff to get things out there >> [snorts]
`11:00` >> [snorts]
`11:00` >> [snorts] >> within reason of course. Now again, I
`11:03` >> within reason of course. Now again, I
`11:03` >> within reason of course. Now again, I think I'm going to end up let me
`11:04` think I'm going to end up let me
`11:04` think I'm going to end up let me actually open this file up in VS Code
`11:06` actually open this file up in VS Code
`11:06` actually open this file up in VS Code anyway.
`11:08` anyway.
`11:08` anyway. Just so for those of you who haven't
`11:09` Just so for those of you who haven't
`11:10` Just so for those of you who haven't used VS Code a bunch or are just getting
`11:11` used VS Code a bunch or are just getting
`11:12` used VS Code a bunch or are just getting into it can understand like
`11:14` into it can understand like
`11:14` into it can understand like why I use it over the Claude app.
`11:17` why I use it over the Claude app.
`11:17` why I use it over the Claude app. So, we'll go to my desktop. I think it
`11:19` So, we'll go to my desktop. I think it
`11:19` So, we'll go to my desktop. I think it What did I call this? It was writing and
`11:21` What did I call this? It was writing and
`11:21` What did I call this? It was writing and scripting. Yeah.
`11:23` scripting. Yeah.
`11:23` scripting. Yeah. So,
`11:24` So,
`11:24` So, for me I I use I have a second monitor.
`11:27` for me I I use I have a second monitor.
`11:27` for me I I use I have a second monitor. So, I usually put this on another
`11:28` So, I usually put this on another
`11:28` So, I usually put this on another monitor. But now I'm able to just
`11:30` monitor. But now I'm able to just
`11:30` monitor. But now I'm able to just quickly pull up and I can bring Claude
`11:32` quickly pull up and I can bring Claude
`11:32` quickly pull up and I can bring Claude right in here and chat about it and be
`11:34` right in here and chat about it and be
`11:34` right in here and chat about it and be like, "Hey, I want to work on this or
`11:37` like, "Hey, I want to work on this or
`11:37` like, "Hey, I want to work on this or similarly I want to add this." And then
`11:39` similarly I want to add this." And then
`11:39` similarly I want to add this." And then I can add multiple instances of it to be
`11:41` I can add multiple instances of it to be
`11:41` I can add multiple instances of it to be able to write and talk which I just I
`11:43` able to write and talk which I just I
`11:43` able to write and talk which I just I love that ability so much which you can
`11:46` love that ability so much which you can
`11:46` love that ability so much which you can kind of do in here with chats and things
`11:48` kind of do in here with chats and things
`11:48` kind of do in here with chats and things like that, but it's just it feels a bit
`11:50` like that, but it's just it feels a bit
`11:50` like that, but it's just it feels a bit more clunky to do that and I prefer to
`11:51` more clunky to do that and I prefer to
`11:51` more clunky to do that and I prefer to do it in VS Code, but teach their own.
`11:53` do it in VS Code, but teach their own.
`11:53` do it in VS Code, but teach their own. Some people want to focus on one task at
`11:55` Some people want to focus on one task at
`11:55` Some people want to focus on one task at a time. I'm insane and like to multitask
`11:58` a time. I'm insane and like to multitask
`11:58` a time. I'm insane and like to multitask and have like 37 different instances of
`12:00` and have like 37 different instances of
`12:00` and have like 37 different instances of Claude running
`12:02` Claude running
`12:02` Claude running at a time, but you know, it's the way it
`12:04` at a time, but you know, it's the way it
`12:04` at a time, but you know, it's the way it is. And sometimes I like to look and see
`12:05` is. And sometimes I like to look and see
`12:06` is. And sometimes I like to look and see what's going on, what it's kind of
`12:07` what's going on, what it's kind of
`12:07` what's going on, what it's kind of creating.
`12:08` creating.
`12:08` creating. I like that it's doing its to-do list in
`12:10` I like that it's doing its to-do list in
`12:10` I like that it's doing its to-do list in JSON format, nice structured to just be
`12:12` JSON format, nice structured to just be
`12:12` JSON format, nice structured to just be able to like, "Hey, this is what we want
`12:13` able to like, "Hey, this is what we want
`12:13` able to like, "Hey, this is what we want to do."
`12:15` to do."
`12:15` to do." Which is really good. Let's see what its
`12:17` Which is really good. Let's see what its
`12:17` Which is really good. Let's see what its thought process where is it at here.
`12:19` thought process where is it at here.
`12:19` thought process where is it at here. Um So, user give me three transcripts,
`12:22` Um So, user give me three transcripts,
`12:22` Um So, user give me three transcripts, two from Instagram. And for those of you
`12:23` two from Instagram. And for those of you
`12:23` two from Instagram. And for those of you who aren't reading its thought process,
`12:25` who aren't reading its thought process,
`12:25` who aren't reading its thought process, you should really, really start doing
`12:26` you should really, really start doing
`12:26` you should really, really start doing that more because it helps you inform
`12:29` that more because it helps you inform
`12:29` that more because it helps you inform yourself of what happens when you give
`12:31` yourself of what happens when you give
`12:31` yourself of what happens when you give it something and then you can
`12:32` it something and then you can
`12:32` it something and then you can re-evaluate and be like, "Oh, it thought
`12:33` re-evaluate and be like, "Oh, it thought
`12:33` re-evaluate and be like, "Oh, it thought of this. Maybe I can say this
`12:35` of this. Maybe I can say this
`12:35` of this. Maybe I can say this differently in in my next prompts or
`12:37` differently in in my next prompts or
`12:37` differently in in my next prompts or injections or whatever you're looking
`12:38` injections or whatever you're looking
`12:38` injections or whatever you're looking at."
`12:39` at."
`12:39` at." They want me to understand their voice
`12:41` They want me to understand their voice
`12:41` They want me to understand their voice and content before building. Let me do
`12:42` and content before building. Let me do
`12:42` and content before building. Let me do an absorb here. Key observation subject
`12:43` an absorb here. Key observation subject
`12:43` an absorb here. Key observation subject matter AI specifically folder
`12:45` matter AI specifically folder
`12:45` matter AI specifically folder architecture concept using file system
`12:47` architecture concept using file system
`12:47` architecture concept using file system markdown instead of coded agent
`12:48` markdown instead of coded agent
`12:49` markdown instead of coded agent frameworks. He's teaching people how to
`12:50` frameworks. He's teaching people how to
`12:50` frameworks. He's teaching people how to use Claude code AI tools with folder
`12:52` use Claude code AI tools with folder
`12:52` use Claude code AI tools with folder based routing. That's fair enough.
`12:53` based routing. That's fair enough.
`12:53` based routing. That's fair enough. Authority, yep. I did I was in the
`12:55` Authority, yep. I did I was in the
`12:55` Authority, yep. I did I was in the Marine Corps. I did teach professors.
`12:58` Marine Corps. I did teach professors.
`12:58` Marine Corps. I did teach professors. I have been in the AI space. I've been
`12:59` I have been in the AI space. I've been
`12:59` I have been in the AI space. I've been in it more than two years, so might want
`13:01` in it more than two years, so might want
`13:01` in it more than two years, so might want to double check that one.
`13:03` to double check that one.
`13:03` to double check that one. Um
`13:04` Um
`13:04` Um You don't need to build agents with
`13:05` You don't need to build agents with
`13:05` You don't need to build agents with Python frameworks. And I mean, yeah, you
`13:07` Python frameworks. And I mean, yeah, you
`13:08` Python frameworks. And I mean, yeah, you don't. That's or at least the concept of
`13:10` don't. That's or at least the concept of
`13:10` don't. That's or at least the concept of what people are trying to do with
`13:11` what people are trying to do with
`13:11` what people are trying to do with agents. I I do kind of hit on that from
`13:14` agents. I I do kind of hit on that from
`13:14` agents. I I do kind of hit on that from a marketing standpoint. You have got all
`13:16` a marketing standpoint. You have got all
`13:16` a marketing standpoint. You have got all these crazy frameworks out there like
`13:18` these crazy frameworks out there like
`13:18` these crazy frameworks out there like Crew AI.
`13:20` Crew AI.
`13:20` Crew AI. Not crazy. They're not that crazy. But
`13:22` Not crazy. They're not that crazy. But
`13:22` Not crazy. They're not that crazy. But like Crew AI or Semantic Kernel is a big
`13:26` like Crew AI or Semantic Kernel is a big
`13:26` like Crew AI or Semantic Kernel is a big one that Microsoft put out that you can
`13:28` one that Microsoft put out that you can
`13:28` one that Microsoft put out that you can do in Python.
`13:30` do in Python.
`13:30` do in Python. Um
`13:31` Um
`13:31` Um If it ever loads.
`13:33` If it ever loads.
`13:33` If it ever loads. It's just basically a way Let me see.
`13:35` It's just basically a way Let me see.
`13:35` It's just basically a way Let me see. Yeah, here. It's basically you have your
`13:36` Yeah, here. It's basically you have your
`13:36` Yeah, here. It's basically you have your code base which is separated from your
`13:38` code base which is separated from your
`13:38` code base which is separated from your models. You can swap models out without
`13:40` models. You can swap models out without
`13:40` models. You can swap models out without it ruining your code base. You can do
`13:41` it ruining your code base. You can do
`13:42` it ruining your code base. You can do hooks and plugins and things like that.
`13:43` hooks and plugins and things like that.
`13:43` hooks and plugins and things like that. Basically everything that you can do
`13:46` Basically everything that you can do
`13:46` Basically everything that you can do right here with a couple button clicks
`13:48` right here with a couple button clicks
`13:48` right here with a couple button clicks or with a folder.
`13:50` or with a folder.
`13:50` or with a folder. And you know, there's certain use cases
`13:52` And you know, there's certain use cases
`13:52` And you know, there's certain use cases for enterprise where you might want
`13:53` for enterprise where you might want
`13:53` for enterprise where you might want something fancy like like these or you
`13:56` something fancy like like these or you
`13:56` something fancy like like these or you know, you've got LangChain of course as
`13:58` know, you've got LangChain of course as
`13:58` know, you've got LangChain of course as well.
`14:00` well.
`14:00` well. LangGraph.
`14:02` LangGraph.
`14:03` LangGraph. Ooh, you know what's a fun one? I don't
`14:04` Ooh, you know what's a fun one? I don't
`14:04` Ooh, you know what's a fun one? I don't know if they're still doing it is DSPy.
`14:07` know if they're still doing it is DSPy.
`14:07` know if they're still doing it is DSPy. Yeah, these guys. These guys are
`14:08` Yeah, these guys. These guys are
`14:08` Yeah, these guys. These guys are interesting. They're It's a framework
`14:10` interesting. They're It's a framework
`14:10` interesting. They're It's a framework for modular software that actually
`14:11` for modular software that actually
`14:11` for modular software that actually iterates on the code itself. It's been a
`14:14` iterates on the code itself. It's been a
`14:14` iterates on the code itself. It's been a while since I've kind of messed around
`14:16` while since I've kind of messed around
`14:16` while since I've kind of messed around with it. So, we'll have to see how
`14:17` with it. So, we'll have to see how
`14:17` with it. So, we'll have to see how they've updated cuz it's all about
`14:19` they've updated cuz it's all about
`14:19` they've updated cuz it's all about optimizing prompts and weights and
`14:20` optimizing prompts and weights and
`14:20` optimizing prompts and weights and things like that.
`14:22` things like that.
`14:22` things like that. I like their ideas. I'm I'm curious to
`14:24` I like their ideas. I'm I'm curious to
`14:25` I like their ideas. I'm I'm curious to see the feasibility of it being widely
`14:26` see the feasibility of it being widely
`14:27` see the feasibility of it being widely adopted, but that's you know, I don't
`14:29` adopted, but that's you know, I don't
`14:29` adopted, but that's you know, I don't think that always matters. Something
`14:30` think that always matters. Something
`14:30` think that always matters. Something doesn't need to be widely adopted for it
`14:32` doesn't need to be widely adopted for it
`14:32` doesn't need to be widely adopted for it to be useful, but wide adoption makes it
`14:34` to be useful, but wide adoption makes it
`14:34` to be useful, but wide adoption makes it easier to have impact which is important
`14:37` easier to have impact which is important
`14:37` easier to have impact which is important there. Anyway,
`14:38` there. Anyway,
`14:38` there. Anyway, beside the point there.
`14:40` beside the point there.
`14:40` beside the point there. Let's go back to our boy here. So, it's
`14:42` Let's go back to our boy here. So, it's
`14:42` Let's go back to our boy here. So, it's going through and it's creating some
`14:43` going through and it's creating some
`14:43` going through and it's creating some series of files. So, um voice character
`14:47` series of files. So, um voice character
`14:47` series of files. So, um voice character direct and confident with slightly
`14:48` direct and confident with slightly
`14:48` direct and confident with slightly confrontational edge. I guess that's
`14:50` confrontational edge. I guess that's
`14:50` confrontational edge. I guess that's fair. I might switch this up. I have my
`14:52` fair. I might switch this up. I have my
`14:52` fair. I might switch this up. I have my own voice file, but breaks down
`14:54` own voice file, but breaks down
`14:54` own voice file, but breaks down technical content simply weaves in
`14:56` technical content simply weaves in
`14:56` technical content simply weaves in historical examples like Jacquard loom.
`14:58` historical examples like Jacquard loom.
`14:58` historical examples like Jacquard loom. One thing about having an AI talk about
`15:01` One thing about having an AI talk about
`15:01` One thing about having an AI talk about your voice without you writing about
`15:02` your voice without you writing about
`15:02` your voice without you writing about your voice is it's going to write about
`15:04` your voice is it's going to write about
`15:04` your voice is it's going to write about your voice in a way that an AI would
`15:06` your voice in a way that an AI would
`15:06` your voice in a way that an AI would write about your voice. Even the best
`15:07` write about your voice. Even the best
`15:07` write about your voice. Even the best models still struggle with this in my
`15:09` models still struggle with this in my
`15:09` models still struggle with this in my opinion. I mean, Opus 4.6 on it, right?
`15:12` opinion. I mean, Opus 4.6 on it, right?
`15:12` opinion. I mean, Opus 4.6 on it, right? Claude has been one of the top models
`15:13` Claude has been one of the top models
`15:13` Claude has been one of the top models for handling voice. So has Kling. I
`15:16` for handling voice. So has Kling. I
`15:16` for handling voice. So has Kling. I mean, I've seen Grok do a good job.
`15:18` mean, I've seen Grok do a good job.
`15:18` mean, I've seen Grok do a good job. ChatGPT once in a while can as well. At
`15:20` ChatGPT once in a while can as well. At
`15:20` ChatGPT once in a while can as well. At the end of the day, if you want your
`15:21` the end of the day, if you want your
`15:21` the end of the day, if you want your voice characteristics, you probably
`15:23` voice characteristics, you probably
`15:23` voice characteristics, you probably should write or at least use voice to
`15:25` should write or at least use voice to
`15:25` should write or at least use voice to text out a bit. So, we'll mess around
`15:27` text out a bit. So, we'll mess around
`15:27` text out a bit. So, we'll mess around with that here in a bit. Content types,
`15:30` with that here in a bit. Content types,
`15:30` with that here in a bit. Content types, yep, that's spot on. That's exactly what
`15:32` yep, that's spot on. That's exactly what
`15:32` yep, that's spot on. That's exactly what I want to do more. Screen sharing
`15:35` I want to do more. Screen sharing
`15:35` I want to do more. Screen sharing and then also animations.
`15:42` Oh, this is pulling it from I got to say
`15:42` Oh, this is pulling it from I got to say that the other ones performed better as
`15:44` that the other ones performed better as
`15:44` that the other ones performed better as well cuz this used to be my best
`15:46` well cuz this used to be my best
`15:46` well cuz this used to be my best performing video and in my memory I
`15:48` performing video and in my memory I
`15:48` performing video and in my memory I said, "Hey, this is my best performing
`15:50` said, "Hey, this is my best performing
`15:50` said, "Hey, this is my best performing video. Can we look at it?" So, I might
`15:51` video. Can we look at it?" So, I might
`15:51` video. Can we look at it?" So, I might need to double check that, but again,
`15:54` need to double check that, but again,
`15:54` need to double check that, but again, this is why it's important to read what
`15:55` this is why it's important to read what
`15:55` this is why it's important to read what it's thinking. You want to look Every
`15:57` it's thinking. You want to look Every
`15:57` it's thinking. You want to look Every single one of these words is a
`15:59` single one of these words is a
`15:59` single one of these words is a coordinate in its vector space. It's
`16:02` coordinate in its vector space. It's
`16:02` coordinate in its vector space. It's directing where it needs to go. So, you
`16:04` directing where it needs to go. So, you
`16:04` directing where it needs to go. So, you need to make sure
`16:05` need to make sure
`16:05` need to make sure it is nuanced aligned with everything
`16:07` it is nuanced aligned with everything
`16:08` it is nuanced aligned with everything you're thinking. If it's off a little
`16:09` you're thinking. If it's off a little
`16:09` you're thinking. If it's off a little bit, that little bit grows the more you
`16:12` bit, that little bit grows the more you
`16:12` bit, that little bit grows the more you use it. So, you need to nip that in the
`16:13` use it. So, you need to nip that in the
`16:13` use it. So, you need to nip that in the bud as soon as you see it saying
`16:15` bud as soon as you see it saying
`16:15` bud as soon as you see it saying something you might not like. Um let's
`16:17` something you might not like. Um let's
`16:17` something you might not like. Um let's see.
`16:18` see.
`16:18` see. All three transcripts are saved. Here's
`16:20` All three transcripts are saved. Here's
`16:20` All three transcripts are saved. Here's what I'm picking out. Your voice is a
`16:21` what I'm picking out. Your voice is a
`16:21` what I'm picking out. Your voice is a clear pattern. You teach through layers.
`16:23` clear pattern. You teach through layers.
`16:23` clear pattern. You teach through layers. You start with something People think
`16:24` You start with something People think
`16:24` You start with something People think they understand and peel it back and
`16:26` they understand and peel it back and
`16:26` they understand and peel it back and show them what's going on. I do like
`16:27` show them what's going on. I do like
`16:27` show them what's going on. I do like that. That's fair. Instagram clips do it
`16:28` that. That's fair. Instagram clips do it
`16:29` that. That's fair. Instagram clips do it fast and punchy obviously cuz short
`16:30` fast and punchy obviously cuz short
`16:30` fast and punchy obviously cuz short attention spans. Tutorial does it by
`16:32` attention spans. Tutorial does it by
`16:32` attention spans. Tutorial does it by walking through live folder structure.
`16:34` walking through live folder structure.
`16:34` walking through live folder structure. Sometimes I do, sometimes I don't.
`16:36` Sometimes I do, sometimes I don't.
`16:36` Sometimes I do, sometimes I don't. Animation crosses 200 years of computing
`16:38` Animation crosses 200 years of computing
`16:38` Animation crosses 200 years of computing history. Absolutely. Same instinct every
`16:40` history. Absolutely. Same instinct every
`16:40` history. Absolutely. Same instinct every time. Go deeper, connect it to something
`16:42` time. Go deeper, connect it to something
`16:42` time. Go deeper, connect it to something real, make the listener feel like they
`16:44` real, make the listener feel like they
`16:44` real, make the listener feel like they just gained x-ray vision. I like that
`16:46` just gained x-ray vision. I like that
`16:46` just gained x-ray vision. I like that last part. All right, that was pretty
`16:47` last part. All right, that was pretty
`16:47` last part. All right, that was pretty good. I'm not going to lie. That was
`16:48` good. I'm not going to lie. That was
`16:48` good. I'm not going to lie. That was good on the AI.
`16:50` good on the AI.
`16:50` good on the AI. You direct and lead with your authority,
`16:51` You direct and lead with your authority,
`16:51` You direct and lead with your authority, but you earn it. Marine Corps's crypto
`16:53` but you earn it. Marine Corps's crypto
`16:53` but you earn it. Marine Corps's crypto work. Yep, yep, yep. Two content modes
`16:56` work. Yep, yep, yep. Two content modes
`16:56` work. Yep, yep, yep. Two content modes that are working. Practical, tactical.
`16:58` that are working. Practical, tactical.
`16:58` that are working. Practical, tactical. Oh my god, I love that rhyme. Narrative
`17:00` Oh my god, I love that rhyme. Narrative
`17:00` Oh my god, I love that rhyme. Narrative and conceptual. One thread pulled
`17:01` and conceptual. One thread pulled
`17:01` and conceptual. One thread pulled through history. Yeah, okay. That's
`17:04` through history. Yeah, okay. That's
`17:04` through history. Yeah, okay. That's fair. I do think these are two really
`17:05` fair. I do think these are two really
`17:05` fair. I do think these are two really solid content modes. So, this is this is
`17:08` solid content modes. So, this is this is
`17:08` solid content modes. So, this is this is this is useful, right? Because this
`17:09` this is useful, right? Because this
`17:09` this is useful, right? Because this isn't locking me into a concrete way of
`17:12` isn't locking me into a concrete way of
`17:12` isn't locking me into a concrete way of producing a single video where every
`17:14` producing a single video where every
`17:14` producing a single video where every video has to be structured this way.
`17:16` video has to be structured this way.
`17:16` video has to be structured this way. It's more of like, "Hey, this is the
`17:18` It's more of like, "Hey, this is the
`17:18` It's more of like, "Hey, this is the rough concept of the videos we want to
`17:20` rough concept of the videos we want to
`17:20` rough concept of the videos we want to sit in. This is what people seem to
`17:22` sit in. This is what people seem to
`17:22` sit in. This is what people seem to like. Provide value in a tactical way or
`17:25` like. Provide value in a tactical way or
`17:25` like. Provide value in a tactical way or give a narrative of something." And
`17:26` give a narrative of something." And
`17:26` give a narrative of something." And honestly, I think both of these modes
`17:28` honestly, I think both of these modes
`17:28` honestly, I think both of these modes could probably be combined together as
`17:30` could probably be combined together as
`17:30` could probably be combined together as well.
`17:31` well.
`17:31` well. What I'm noticing about this. Animation
`17:32` What I'm noticing about this. Animation
`17:32` What I'm noticing about this. Animation videos work because it's familiar act.
`17:34` videos work because it's familiar act.
`17:34` videos work because it's familiar act. Yeah, that's fair. Take clear positions.
`17:37` Yeah, that's fair. Take clear positions.
`17:37` Yeah, that's fair. Take clear positions. Absolutely. I just did that 5 seconds
`17:40` Absolutely. I just did that 5 seconds
`17:40` Absolutely. I just did that 5 seconds ago.
`17:41` ago.
`17:41` ago. That's sure works cuz it's generally
`17:42` That's sure works cuz it's generally
`17:42` That's sure works cuz it's generally useful. I'm ready for your comments and
`17:44` useful. I'm ready for your comments and
`17:44` useful. I'm ready for your comments and brand voice. Okay. First though, we're
`17:46` brand voice. Okay. First though, we're
`17:46` brand voice. Okay. First though, we're going to address before we give it
`17:48` going to address before we give it
`17:48` going to address before we give it anything else, let's address these main
`17:49` anything else, let's address these main
`17:49` anything else, let's address these main things. So, I think I think you got
`17:52` things. So, I think I think you got
`17:52` things. So, I think I think you got pretty close close
`17:54` pretty close close
`17:54` pretty close close on the voice.
`17:55` on the voice.
`17:55` on the voice. Um but the main goal here main goal is
`17:57` Um but the main goal here main goal is
`17:57` Um but the main goal here main goal is to create a way of thinking around my
`17:59` to create a way of thinking around my
`17:59` to create a way of thinking around my voice. I know my voice and can handle it
`18:02` voice. I know my voice and can handle it
`18:02` voice. I know my voice and can handle it naturally most of the time. I want to
`18:04` naturally most of the time. I want to
`18:04` naturally most of the time. I want to cat to create files that catch you up to
`18:08` cat to create files that catch you up to
`18:08` cat to create files that catch you up to being aligned with my voice and more
`18:10` being aligned with my voice and more
`18:10` being aligned with my voice and more importantly thought process. But that
`18:12` importantly thought process. But that
`18:12` importantly thought process. But that being said, I think you are darn close.
`18:15` being said, I think you are darn close.
`18:15` being said, I think you are darn close. So, the goal here is to not lock
`18:17` So, the goal here is to not lock
`18:17` So, the goal here is to not lock ourselves in concrete with exact
`18:21` ourselves in concrete with exact
`18:21` ourselves in concrete with exact structures or format, but certainly have
`18:24` structures or format, but certainly have
`18:24` structures or format, but certainly have a good set of guiding themes,
`18:26` a good set of guiding themes,
`18:26` a good set of guiding themes, principles, or pillars, etc. Now, for
`18:29` principles, or pillars, etc. Now, for
`18:29` principles, or pillars, etc. Now, for the comments.
`18:31` the comments.
`18:31` the comments. These are going to be huge. You will see
`18:33` These are going to be huge. You will see
`18:33` These are going to be huge. You will see some of my comments
`18:35` some of my comments
`18:35` some of my comments as well, but it shows both why people
`18:38` as well, but it shows both why people
`18:38` as well, but it shows both why people like my videos and more importantly what
`18:41` like my videos and more importantly what
`18:41` like my videos and more importantly what what they need and want. And now this
`18:43` what they need and want. And now this
`18:43` what they need and want. And now this part is going to be harder for a lot of
`18:45` part is going to be harder for a lot of
`18:45` part is going to be harder for a lot of you to do and why it's so important to
`18:47` you to do and why it's so important to
`18:47` you to do and why it's so important to share your work, show your work.
`18:49` share your work, show your work.
`18:49` share your work, show your work. Speaking of that,
`18:51` Speaking of that,
`18:51` Speaking of that, great book, not sponsored at all. Uh if
`18:55` great book, not sponsored at all. Uh if
`18:55` great book, not sponsored at all. Uh if just buy this. Just buy this book and
`18:57` just buy this. Just buy this book and
`18:57` just buy this. Just buy this book and read it. It takes like you could read it
`18:58` read it. It takes like you could read it
`18:58` read it. It takes like you could read it in an afternoon. This is one of the most
`19:01` in an afternoon. This is one of the most
`19:01` in an afternoon. This is one of the most fundamental books that I have ever read,
`19:03` fundamental books that I have ever read,
`19:03` fundamental books that I have ever read, watched, whatever you want to call it.
`19:05` watched, whatever you want to call it.
`19:05` watched, whatever you want to call it. Um
`19:06` Um
`19:06` Um It's It's just all about showing your
`19:08` It's It's just all about showing your
`19:08` It's It's just all about showing your work. You should show the whole process.
`19:10` work. You should show the whole process.
`19:10` work. You should show the whole process. What I'm doing here is showing my work.
`19:12` What I'm doing here is showing my work.
`19:12` What I'm doing here is showing my work. Right? This is how you get out there.
`19:13` Right? This is how you get out there.
`19:13` Right? This is how you get out there. It's how you learn. It's how you can
`19:15` It's how you learn. It's how you can
`19:15` It's how you learn. It's how you can teach others. It's that whole deal. By
`19:17` teach others. It's that whole deal. By
`19:17` teach others. It's that whole deal. By showing the work, you then get things
`19:19` showing the work, you then get things
`19:19` showing the work, you then get things that you might even if it's one or two
`19:21` that you might even if it's one or two
`19:21` that you might even if it's one or two comments. Right? You get an idea of what
`19:23` comments. Right? You get an idea of what
`19:23` comments. Right? You get an idea of what people need and want. Now, this video I
`19:25` people need and want. Now, this video I
`19:25` people need and want. Now, this video I didn't get as much, so I'm actually not
`19:27` didn't get as much, so I'm actually not
`19:27` didn't get as much, so I'm actually not going to use the comments. I mean, I got
`19:29` going to use the comments. I mean, I got
`19:29` going to use the comments. I mean, I got a bunch of comments obviously. Like I
`19:31` a bunch of comments obviously. Like I
`19:31` a bunch of comments obviously. Like I think what we were we did Yeah, 46
`19:33` think what we were we did Yeah, 46
`19:33` think what we were we did Yeah, 46 comments. That's pretty good for some of
`19:34` comments. That's pretty good for some of
`19:34` comments. That's pretty good for some of my videos. But it wasn't as you know,
`19:37` my videos. But it wasn't as you know,
`19:37` my videos. But it wasn't as you know, the the feedback isn't exactly what I
`19:39` the the feedback isn't exactly what I
`19:39` the the feedback isn't exactly what I wanted. Here in this video we get a lot
`19:41` wanted. Here in this video we get a lot
`19:41` wanted. Here in this video we get a lot more
`19:42` more
`19:42` more specific example. Hey, I was looking for
`19:44` specific example. Hey, I was looking for
`19:44` specific example. Hey, I was looking for this or honestly, I'll share this with
`19:46` this or honestly, I'll share this with
`19:46` this or honestly, I'll share this with this. Right? These are really good. So,
`19:48` this. Right? These are really good. So,
`19:48` this. Right? These are really good. So, I'm just going to rip through all of
`19:49` I'm just going to rip through all of
`19:49` I'm just going to rip through all of this. And And there's also a lot more
`19:52` this. And And there's also a lot more
`19:52` this. And And there's also a lot more comments on this video. Now, I just take
`19:54` comments on this video. Now, I just take
`19:54` comments on this video. Now, I just take that and I'm going to paste it right in
`19:55` that and I'm going to paste it right in
`19:56` that and I'm going to paste it right in here, bunch of them.
`19:57` here, bunch of them.
`19:57` here, bunch of them. Uh these are comments from the folder
`19:59` Uh these are comments from the folder
`19:59` Uh these are comments from the folder architecture video. I think we should
`20:02` architecture video. I think we should
`20:02` architecture video. I think we should take our time with each one and go
`20:04` take our time with each one and go
`20:04` take our time with each one and go through them.
`20:05` through them.
`20:05` through them. I will also give you a bunch of intros
`20:08` I will also give you a bunch of intros
`20:08` I will also give you a bunch of intros from my school community. I usually
`20:10` from my school community. I usually
`20:10` from my school community. I usually wouldn't give it this much. Usually I'd
`20:11` wouldn't give it this much. Usually I'd
`20:12` wouldn't give it this much. Usually I'd scrape it, put it into an Excel sheet or
`20:14` scrape it, put it into an Excel sheet or
`20:14` scrape it, put it into an Excel sheet or something like that. But right now,
`20:15` something like that. But right now,
`20:15` something like that. But right now, we're just we're trying to move quickly.
`20:17` we're just we're trying to move quickly.
`20:17` we're just we're trying to move quickly. I think it'll handle the idea. I I'm not
`20:19` I think it'll handle the idea. I I'm not
`20:19` I think it'll handle the idea. I I'm not trying you know, I want to spend
`20:21` trying you know, I want to spend
`20:21` trying you know, I want to spend individual time with each person in
`20:22` individual time with each person in
`20:22` individual time with each person in comment. I'm just trying to get a an
`20:24` comment. I'm just trying to get a an
`20:24` comment. I'm just trying to get a an overall vibe of them. I mean, the more
`20:26` overall vibe of them. I mean, the more
`20:26` overall vibe of them. I mean, the more details and data I could create the
`20:28` details and data I could create the
`20:28` details and data I could create the better.
`20:29` better.
`20:29` better. Um but we're rolling through it here.
`20:31` Um but we're rolling through it here.
`20:31` Um but we're rolling through it here. Ooh, all right. I believe in you,
`20:33` Ooh, all right. I believe in you,
`20:33` Ooh, all right. I believe in you, Claude. Look at all of it. Go get them.
`20:36` Claude. Look at all of it. Go get them.
`20:36` Claude. Look at all of it. Go get them. Squeesh. Whatever I do, do not hit show
`20:39` Squeesh. Whatever I do, do not hit show
`20:39` Squeesh. Whatever I do, do not hit show more on that is what I should do.
`20:42` more on that is what I should do.
`20:42` more on that is what I should do. Okay. Claude thinks it's a gold mine.
`20:44` Okay. Claude thinks it's a gold mine.
`20:44` Okay. Claude thinks it's a gold mine. That's not surprising. Massive amount of
`20:46` That's not surprising. Massive amount of
`20:46` That's not surprising. Massive amount of data user wants to save these comments
`20:48` data user wants to save these comments
`20:48` data user wants to save these comments and analyze them. Oh, shoot. I didn't
`20:50` and analyze them. Oh, shoot. I didn't
`20:50` and analyze them. Oh, shoot. I didn't mean to save it. Oh, well. You know
`20:51` mean to save it. Oh, well. You know
`20:51` mean to save it. Oh, well. You know what? Let's see how it handles it. I'm
`20:53` what? Let's see how it handles it. I'm
`20:53` what? Let's see how it handles it. I'm very curious. I think it'll be able to
`20:55` very curious. I think it'll be able to
`20:55` very curious. I think it'll be able to go through it. That was a lot of
`20:56` go through it. That was a lot of
`20:56` go through it. That was a lot of comments without me organizing them,
`20:59` comments without me organizing them,
`20:59` comments without me organizing them, cleaning it up at all, throwing it in
`21:01` cleaning it up at all, throwing it in
`21:01` cleaning it up at all, throwing it in Excel. I usually like to do that for
`21:03` Excel. I usually like to do that for
`21:03` Excel. I usually like to do that for these models, but 4.6 has been pretty
`21:06` these models, but 4.6 has been pretty
`21:06` these models, but 4.6 has been pretty monstrous at handling most of these
`21:08` monstrous at handling most of these
`21:08` monstrous at handling most of these tasks. So, we'll go ahead and let them.
`21:10` tasks. So, we'll go ahead and let them.
`21:10` tasks. So, we'll go ahead and let them. But um appropriate folders and give the
`21:13` But um appropriate folders and give the
`21:13` But um appropriate folders and give the analysis. So, oh, it's yeah, okay. It's
`21:15` analysis. So, oh, it's yeah, okay. It's
`21:15` analysis. So, oh, it's yeah, okay. It's running their agents to go handle that.
`21:16` running their agents to go handle that.
`21:16` running their agents to go handle that. And this is another reason, right? Like
`21:18` And this is another reason, right? Like
`21:18` And this is another reason, right? Like >> [snorts]
`21:18` >> [snorts]
`21:18` >> [snorts] >> in order for me to do the work and
`21:20` >> in order for me to do the work and
`21:20` >> in order for me to do the work and create an agentic system that this is
`21:23` create an agentic system that this is
`21:23` create an agentic system that this is doing off of a single subscription uh
`21:25` doing off of a single subscription uh
`21:25` doing off of a single subscription uh with zero API call. Oh, not zero.
`21:27` with zero API call. Oh, not zero.
`21:27` with zero API call. Oh, not zero. Obviously, there's API calls going in
`21:29` Obviously, there's API calls going in
`21:29` Obviously, there's API calls going in the background. But like I don't have to
`21:31` the background. But like I don't have to
`21:31` the background. But like I don't have to pay for an API subscription. This is
`21:32` pay for an API subscription. This is
`21:32` pay for an API subscription. This is just a straight-up one once-a-month
`21:35` just a straight-up one once-a-month
`21:35` just a straight-up one once-a-month payment. It's the same amount every
`21:37` payment. It's the same amount every
`21:37` payment. It's the same amount every month. I'm sorry. This is just more
`21:38` month. I'm sorry. This is just more
`21:39` month. I'm sorry. This is just more efficient. This is what I'm going to do.
`21:40` efficient. This is what I'm going to do.
`21:40` efficient. This is what I'm going to do. I don't want to spend all this money on
`21:42` I don't want to spend all this money on
`21:42` I don't want to spend all this money on these other API calls constantly going
`21:44` these other API calls constantly going
`21:44` these other API calls constantly going out. And if I have multiple clients, I'm
`21:46` out. And if I have multiple clients, I'm
`21:46` out. And if I have multiple clients, I'm going to convince them to get their own
`21:48` going to convince them to get their own
`21:48` going to convince them to get their own subscription and just do it. Because two
`21:50` subscription and just do it. Because two
`21:50` subscription and just do it. Because two things. If I have 2,000 clients and
`21:52` things. If I have 2,000 clients and
`21:52` things. If I have 2,000 clients and they're all paying me money and then I'm
`21:53` they're all paying me money and then I'm
`21:53` they're all paying me money and then I'm managing all these API things. Sure, I'm
`21:55` managing all these API things. Sure, I'm
`21:55` managing all these API things. Sure, I'm making money. Sure, it's handling
`21:57` making money. Sure, it's handling
`21:57` making money. Sure, it's handling certain problems. But what's the job to
`21:59` certain problems. But what's the job to
`21:59` certain problems. But what's the job to be done? The thing is I'm trying to
`22:00` be done? The thing is I'm trying to
`22:00` be done? The thing is I'm trying to create value. The clients are going to
`22:02` create value. The clients are going to
`22:02` create value. The clients are going to pay me to either create a more robust
`22:05` pay me to either create a more robust
`22:05` pay me to either create a more robust system that works for Claude or B, we're
`22:08` system that works for Claude or B, we're
`22:08` system that works for Claude or B, we're going to look at something that we
`22:09` going to look at something that we
`22:09` going to look at something that we absolutely have to create that needs
`22:11` absolutely have to create that needs
`22:11` absolutely have to create that needs this kind of next-level stuff. Cuz at
`22:13` this kind of next-level stuff. Cuz at
`22:13` this kind of next-level stuff. Cuz at the end of the day, this is going to get
`22:14` the end of the day, this is going to get
`22:14` the end of the day, this is going to get updated naturally. I don't have to spend
`22:16` updated naturally. I don't have to spend
`22:16` updated naturally. I don't have to spend any money, any effort, any work other
`22:18` any money, any effort, any work other
`22:18` any money, any effort, any work other than my monthly subscription to get an
`22:20` than my monthly subscription to get an
`22:20` than my monthly subscription to get an entire team of people building a better
`22:23` entire team of people building a better
`22:23` entire team of people building a better agent for me. It doesn't make sense for
`22:25` agent for me. It doesn't make sense for
`22:25` agent for me. It doesn't make sense for me to compete with a multi-billion
`22:27` me to compete with a multi-billion
`22:27` me to compete with a multi-billion dollar company unless I know for a fact
`22:30` dollar company unless I know for a fact
`22:30` dollar company unless I know for a fact I have one niche thing that I'm going to
`22:31` I have one niche thing that I'm going to
`22:31` I have one niche thing that I'm going to outcompete them in. And don't get me
`22:33` outcompete them in. And don't get me
`22:33` outcompete them in. And don't get me wrong. This isn't me. If you're building
`22:35` wrong. This isn't me. If you're building
`22:35` wrong. This isn't me. If you're building some sort of very specific little
`22:37` some sort of very specific little
`22:37` some sort of very specific little agentic structure that you think could
`22:38` agentic structure that you think could
`22:39` agentic structure that you think could compete with whatever Anthropic's doing,
`22:40` compete with whatever Anthropic's doing,
`22:40` compete with whatever Anthropic's doing, go for it. But at the end of the day,
`22:42` go for it. But at the end of the day,
`22:42` go for it. But at the end of the day, I'm not trying to compete with
`22:44` I'm not trying to compete with
`22:44` I'm not trying to compete with Anthropic. I'm trying to get business
`22:45` Anthropic. I'm trying to get business
`22:45` Anthropic. I'm trying to get business deliverables. I'm trying to help people
`22:48` deliverables. I'm trying to help people
`22:48` deliverables. I'm trying to help people work in their companies. I'm trying to
`22:49` work in their companies. I'm trying to
`22:49` work in their companies. I'm trying to use this technology to do something
`22:51` use this technology to do something
`22:51` use this technology to do something else, not just make a better version of
`22:53` else, not just make a better version of
`22:53` else, not just make a better version of the technology, right? Um
`22:56` the technology, right? Um
`22:56` the technology, right? Um and so for me, they're handling all of
`22:59` and so for me, they're handling all of
`22:59` and so for me, they're handling all of that absurdity so much easier, so much
`23:01` that absurdity so much easier, so much
`23:01` that absurdity so much easier, so much cheaper, right? If I was to do this with
`23:03` cheaper, right? If I was to do this with
`23:04` cheaper, right? If I was to do this with a Claude bot or something like this,
`23:05` a Claude bot or something like this,
`23:05` a Claude bot or something like this, this would cost me probably I'd already
`23:07` this would cost me probably I'd already
`23:07` this would cost me probably I'd already probably be at $100 in token usage based
`23:09` probably be at $100 in token usage based
`23:09` probably be at $100 in token usage based on the agents that are going in. Maybe
`23:11` on the agents that are going in. Maybe
`23:11` on the agents that are going in. Maybe not that much. It It'd be like $5 by
`23:13` not that much. It It'd be like $5 by
`23:13` not that much. It It'd be like $5 by now. But still, $5 in token usage and
`23:16` now. But still, $5 in token usage and
`23:16` now. But still, $5 in token usage and we're barely we've barely started. It's
`23:18` we're barely we've barely started. It's
`23:18` we're barely we've barely started. It's been a minute and a half. Other part is
`23:20` been a minute and a half. Other part is
`23:20` been a minute and a half. Other part is it's cleaner. I don't have to think
`23:21` it's cleaner. I don't have to think
`23:21` it's cleaner. I don't have to think about it. And people are like, oh, but
`23:23` about it. And people are like, oh, but
`23:23` about it. And people are like, oh, but what how you use it from your phone? You
`23:24` what how you use it from your phone? You
`23:24` what how you use it from your phone? You can use dispatch inside the Claude app,
`23:27` can use dispatch inside the Claude app,
`23:27` can use dispatch inside the Claude app, which I'll make a whole video on this.
`23:28` which I'll make a whole video on this.
`23:29` which I'll make a whole video on this. But you can literally just connect it to
`23:30` But you can literally just connect it to
`23:30` But you can literally just connect it to your phone right now. I can go log into
`23:32` your phone right now. I can go log into
`23:32` your phone right now. I can go log into my app, go say, "Hey, go do this thing."
`23:35` my app, go say, "Hey, go do this thing."
`23:35` my app, go say, "Hey, go do this thing." I can even have it control the browser
`23:36` I can even have it control the browser
`23:36` I can even have it control the browser and stuff as well. So,
`23:38` and stuff as well. So,
`23:38` and stuff as well. So, I
`23:39` I
`23:39` I I'm I called this in my Claude bot video
`23:41` I'm I called this in my Claude bot video
`23:41` I'm I called this in my Claude bot video that Anthropic would drop an update that
`23:44` that Anthropic would drop an update that
`23:44` that Anthropic would drop an update that made
`23:45` made
`23:45` made um Claude bot basically useless or not
`23:48` um Claude bot basically useless or not
`23:48` um Claude bot basically useless or not useless. It's still very useful and
`23:49` useless. It's still very useful and
`23:49` useless. It's still very useful and powerful. But like
`23:50` powerful. But like
`23:50` powerful. But like just so much more secure and simple by,
`23:53` just so much more secure and simple by,
`23:53` just so much more secure and simple by, you know, having it inside their app.
`23:55` you know, having it inside their app.
`23:55` you know, having it inside their app. Cuz all Claude bot isn't an AI. It's
`23:57` Cuz all Claude bot isn't an AI. It's
`23:57` Cuz all Claude bot isn't an AI. It's It's just a orchestration layer.
`23:59` It's just a orchestration layer.
`23:59` It's just a orchestration layer. Orchestration layers can be added to
`24:01` Orchestration layers can be added to
`24:01` Orchestration layers can be added to every other AI. And the companies that
`24:03` every other AI. And the companies that
`24:03` every other AI. And the companies that have the AI can do it better, faster,
`24:05` have the AI can do it better, faster,
`24:05` have the AI can do it better, faster, and cheaper than everyone else who's
`24:07` and cheaper than everyone else who's
`24:07` and cheaper than everyone else who's creating the wrapper in a third party.
`24:09` creating the wrapper in a third party.
`24:09` creating the wrapper in a third party. So, why not build systems and companies
`24:12` So, why not build systems and companies
`24:12` So, why not build systems and companies that assume that these apps, these
`24:14` that assume that these apps, these
`24:14` that assume that these apps, these processes are going to get better? Like
`24:17` processes are going to get better? Like
`24:17` processes are going to get better? Like that's what they were said 3 years ago.
`24:19` that's what they were said 3 years ago.
`24:19` that's what they were said 3 years ago. And that's what I've assumed. And that's
`24:20` And that's what I've assumed. And that's
`24:20` And that's what I've assumed. And that's what I've done for the last 3 years. And
`24:22` what I've done for the last 3 years. And
`24:22` what I've done for the last 3 years. And my processes just keep getting better. I
`24:24` my processes just keep getting better. I
`24:24` my processes just keep getting better. I don't have to feel like I'm running or
`24:26` don't have to feel like I'm running or
`24:26` don't have to feel like I'm running or chasing or trying to beat the competitor
`24:28` chasing or trying to beat the competitor
`24:28` chasing or trying to beat the competitor of AI. I'm just getting better every
`24:30` of AI. I'm just getting better every
`24:30` of AI. I'm just getting better every year with what I'm doing. So, yeah. I
`24:32` year with what I'm doing. So, yeah. I
`24:32` year with what I'm doing. So, yeah. I don't know. Short little rant there. But
`24:34` don't know. Short little rant there. But
`24:34` don't know. Short little rant there. But um
`24:36` um
`24:36` um Let's see. Let's go ahead and look at
`24:38` Let's see. Let's go ahead and look at
`24:38` Let's see. Let's go ahead and look at what's going on here. Um we don't get to
`24:40` what's going on here. Um we don't get to
`24:40` what's going on here. Um we don't get to see your thought process there. I need
`24:42` see your thought process there. I need
`24:42` see your thought process there. I need to So, there are ways to I didn't run it
`24:44` to So, there are ways to I didn't run it
`24:44` to So, there are ways to I didn't run it in this section to see what these agents
`24:46` in this section to see what these agents
`24:46` in this section to see what these agents are doing and actually um kind of dive
`24:48` are doing and actually um kind of dive
`24:48` are doing and actually um kind of dive in and look at their thinking even
`24:50` in and look at their thinking even
`24:50` in and look at their thinking even beyond just this basic kind of setup
`24:52` beyond just this basic kind of setup
`24:52` beyond just this basic kind of setup here.
`24:53` here.
`24:53` here. Um but let's see. Save YouTube comments.
`24:55` Um but let's see. Save YouTube comments.
`24:55` Um but let's see. Save YouTube comments. Nice. It set it to YouTube architecture
`24:57` Nice. It set it to YouTube architecture
`24:57` Nice. It set it to YouTube architecture video comments. Oh, I love that. Good
`24:59` video comments. Oh, I love that. Good
`24:59` video comments. Oh, I love that. Good time collected. Today is in fact the
`25:02` time collected. Today is in fact the
`25:02` time collected. Today is in fact the 20th. Good. Good. Good.
`25:05` 20th. Good. Good. Good.
`25:05` 20th. Good. Good. Good. Um
`25:06` Um
`25:06` Um And then there it is. Oh, and it's
`25:07` And then there it is. Oh, and it's
`25:07` And then there it is. Oh, and it's Marcy, dude.
`25:09` Marcy, dude.
`25:09` Marcy, dude. >> [laughter]
`25:09` >> [laughter]
`25:09` >> [laughter] >> You know how long it would have taken me
`25:12` >> You know how long it would have taken me
`25:12` >> You know how long it would have taken me to build an agent that does this well?
`25:15` to build an agent that does this well?
`25:15` to build an agent that does this well? Right? And yeah, maybe not that long.
`25:17` Right? And yeah, maybe not that long.
`25:17` Right? And yeah, maybe not that long. But it still would have been annoying to
`25:18` But it still would have been annoying to
`25:18` But it still would have been annoying to go to N8N and then have it scrape my
`25:21` go to N8N and then have it scrape my
`25:21` go to N8N and then have it scrape my YouTube comments and then have it pull
`25:23` YouTube comments and then have it pull
`25:23` YouTube comments and then have it pull that in and then have it turn it in I
`25:25` that in and then have it turn it in I
`25:25` that in and then have it turn it in I could just copy and paste it into a
`25:27` could just copy and paste it into a
`25:27` could just copy and paste it into a single really good solid agent like
`25:30` single really good solid agent like
`25:30` single really good solid agent like Claude or you know, Codex. And it's
`25:32` Claude or you know, Codex. And it's
`25:32` Claude or you know, Codex. And it's going to do all of this for me, right?
`25:33` going to do all of this for me, right?
`25:33` going to do all of this for me, right? Like there's people probably charging
`25:35` Like there's people probably charging
`25:35` Like there's people probably charging $60 for a month for a tool that does
`25:37` $60 for a month for a tool that does
`25:37` $60 for a month for a tool that does just this. Just what this agent did. Why
`25:40` just this. Just what this agent did. Why
`25:40` just this. Just what this agent did. Why waste your money on that when you can
`25:42` waste your money on that when you can
`25:42` waste your money on that when you can have all of the tools ever inside of one
`25:45` have all of the tools ever inside of one
`25:45` have all of the tools ever inside of one area and then build them as you need it.
`25:47` area and then build them as you need it.
`25:47` area and then build them as you need it. Again,
`25:48` Again,
`25:48` Again, people who are saying they need all
`25:50` people who are saying they need all
`25:50` people who are saying they need all these crazy agents, no. You You need
`25:53` these crazy agents, no. You You need
`25:53` these crazy agents, no. You You need them, sure. But you don't need to be
`25:55` them, sure. But you don't need to be
`25:55` them, sure. But you don't need to be building them. You don't need to be
`25:57` building them. You don't need to be
`25:57` building them. You don't need to be wasting your time. You need smart
`25:59` wasting your time. You need smart
`25:59` wasting your time. You need smart agents. Again, if you're working for
`26:01` agents. Again, if you're working for
`26:01` agents. Again, if you're working for Anthropic or if you're working for a
`26:03` Anthropic or if you're working for a
`26:03` Anthropic or if you're working for a startup that's really ahead, maybe it
`26:05` startup that's really ahead, maybe it
`26:05` startup that's really ahead, maybe it makes sense. Maybe. But for me, for
`26:07` makes sense. Maybe. But for me, for
`26:07` makes sense. Maybe. But for me, for people who are in business, for the vast
`26:09` people who are in business, for the vast
`26:09` people who are in business, for the vast majority of the populations who are
`26:11` majority of the populations who are
`26:11` majority of the populations who are going to get value out of this and
`26:13` going to get value out of this and
`26:13` going to get value out of this and actually run companies. Cuz remember,
`26:15` actually run companies. Cuz remember,
`26:15` actually run companies. Cuz remember, companies The only companies that exist
`26:17` companies The only companies that exist
`26:17` companies The only companies that exist aren't just AI companies. The people who
`26:19` aren't just AI companies. The people who
`26:19` aren't just AI companies. The people who are are doing well with refrigeration
`26:21` are are doing well with refrigeration
`26:21` are are doing well with refrigeration technology are not refrigeration
`26:23` technology are not refrigeration
`26:23` technology are not refrigeration companies. I mean, refrigeration
`26:25` companies. I mean, refrigeration
`26:25` companies. I mean, refrigeration companies do fairly well. They make
`26:26` companies do fairly well. They make
`26:26` companies do fairly well. They make multi-billion dollars. But like
`26:28` multi-billion dollars. But like
`26:28` multi-billion dollars. But like Coca-Cola. Coca-Cola exists because
`26:30` Coca-Cola. Coca-Cola exists because
`26:30` Coca-Cola. Coca-Cola exists because refrigeration exists, right?
`26:34` refrigeration exists, right?
`26:34` refrigeration exists, right? I mean, that's not why they exist. But
`26:35` I mean, that's not why they exist. But
`26:35` I mean, that's not why they exist. But it's They're The reason they're able to
`26:36` it's They're The reason they're able to
`26:36` it's They're The reason they're able to get their stuff out there and successful
`26:38` get their stuff out there and successful
`26:38` get their stuff out there and successful and give you a nice cold Coke is because
`26:39` and give you a nice cold Coke is because
`26:39` and give you a nice cold Coke is because of that technology. How does this
`26:41` of that technology. How does this
`26:41` of that technology. How does this technology help you do something that
`26:44` technology help you do something that
`26:44` technology help you do something that was either A, not done before or B, at a
`26:46` was either A, not done before or B, at a
`26:46` was either A, not done before or B, at a scale that was never possible before?
`26:49` scale that was never possible before?
`26:49` scale that was never possible before? Everyone's trying to chase the hype of
`26:51` Everyone's trying to chase the hype of
`26:51` Everyone's trying to chase the hype of ooh, here's the best AI agent that I
`26:53` ooh, here's the best AI agent that I
`26:53` ooh, here's the best AI agent that I made. And stop selling agents. Sell a
`26:55` made. And stop selling agents. Sell a
`26:55` made. And stop selling agents. Sell a company. Do a company that uses agents
`26:58` company. Do a company that uses agents
`26:58` company. Do a company that uses agents better than anyone else. And then guess
`26:59` better than anyone else. And then guess
`26:59` better than anyone else. And then guess what? If you build it around people who
`27:01` what? If you build it around people who
`27:01` what? If you build it around people who are already building better agents,
`27:02` are already building better agents,
`27:02` are already building better agents, you're just going to get better over
`27:03` you're just going to get better over
`27:03` you're just going to get better over time while they're wasting their time
`27:05` time while they're wasting their time
`27:05` time while they're wasting their time just focusing on something that's
`27:07` just focusing on something that's
`27:07` just focusing on something that's probably going to get replaced by the
`27:08` probably going to get replaced by the
`27:08` probably going to get replaced by the next Claude update anyway. Ag- Again,
`27:10` next Claude update anyway. Ag- Again,
`27:10` next Claude update anyway. Ag- Again, I'm not saying to be reliant on the big
`27:12` I'm not saying to be reliant on the big
`27:12` I'm not saying to be reliant on the big frontier companies. I'm saying
`27:14` frontier companies. I'm saying
`27:14` frontier companies. I'm saying understand that there's five or six
`27:15` understand that there's five or six
`27:15` understand that there's five or six companies, even open-source ones, that
`27:17` companies, even open-source ones, that
`27:17` companies, even open-source ones, that are going to do work that you don't need
`27:20` are going to do work that you don't need
`27:20` are going to do work that you don't need to do. And if you're in one of those
`27:21` to do. And if you're in one of those
`27:22` to do. And if you're in one of those companies, then it makes sense. Cool. Go
`27:24` companies, then it makes sense. Cool. Go
`27:24` companies, then it makes sense. Cool. Go for it. You want to be an agentic
`27:26` for it. You want to be an agentic
`27:26` for it. You want to be an agentic engineer using semantic frameworks or
`27:28` engineer using semantic frameworks or
`27:28` engineer using semantic frameworks or Crew AI or Python libraries that you're
`27:30` Crew AI or Python libraries that you're
`27:30` Crew AI or Python libraries that you're custom building, go for it. That's fun.
`27:32` custom building, go for it. That's fun.
`27:32` custom building, go for it. That's fun. I've done that. I love it. But I
`27:34` I've done that. I love it. But I
`27:34` I've done that. I love it. But I realized where my value was and where
`27:35` realized where my value was and where
`27:36` realized where my value was and where the value is going to be for the next
`27:37` the value is going to be for the next
`27:37` the value is going to be for the next decade. And it's not in that in my eyes.
`27:40` decade. And it's not in that in my eyes.
`27:40` decade. And it's not in that in my eyes. Again, for most people. If you're If
`27:44` Again, for most people. If you're If
`27:44` Again, for most people. If you're If you're one of those engineers, go do it.
`27:45` you're one of those engineers, go do it.
`27:45` you're one of those engineers, go do it. Cool. My on me. But it I guarantee you
`27:48` Cool. My on me. But it I guarantee you
`27:48` Cool. My on me. But it I guarantee you most people are not. And that's okay.
`27:50` most people are not. And that's okay.
`27:50` most people are not. And that's okay. You don't need to be one of those
`27:51` You don't need to be one of those
`27:51` You don't need to be one of those engineers. You need to be someone who
`27:53` engineers. You need to be someone who
`27:53` engineers. You need to be someone who does what you do well.
`27:55` does what you do well.
`27:55` does what you do well. Instructionalize your opinions, which is
`27:57` Instructionalize your opinions, which is
`27:57` Instructionalize your opinions, which is exactly what we're doing here.
`27:59` exactly what we're doing here.
`27:59` exactly what we're doing here. Um all right. Let's see what's going on.
`28:00` Um all right. Let's see what's going on.
`28:00` Um all right. Let's see what's going on. Gosh, I keep getting these rants, right?
`28:02` Gosh, I keep getting these rants, right?
`28:02` Gosh, I keep getting these rants, right? First batch of school intros saved.
`28:04` First batch of school intros saved.
`28:04` First batch of school intros saved. YouTube comments are saved. 150 plus
`28:06` YouTube comments are saved. 150 plus
`28:06` YouTube comments are saved. 150 plus people. I have it all in contents.
`28:07` people. I have it all in contents.
`28:07` people. I have it all in contents. Here's what I'm seeing. Um let's see.
`28:09` Here's what I'm seeing. Um let's see.
`28:09` Here's what I'm seeing. Um let's see. So, I've actually read through all of
`28:11` So, I've actually read through all of
`28:11` So, I've actually read through all of these. So, this will be a good test to
`28:12` these. So, this will be a good test to
`28:12` these. So, this will be a good test to see if it actually found things.
`28:14` see if it actually found things.
`28:14` see if it actually found things. Um data science full stack devs.
`28:15` Um data science full stack devs.
`28:15` Um data science full stack devs. Ricardo. Yep, Ricardo was cool cuz he he
`28:18` Ricardo. Yep, Ricardo was cool cuz he he
`28:18` Ricardo. Yep, Ricardo was cool cuz he he was coming at this from a software
`28:20` was coming at this from a software
`28:20` was coming at this from a software engineering angle and was really kind
`28:22` engineering angle and was really kind
`28:22` engineering angle and was really kind because he basically was like, "Hey, I
`28:23` because he basically was like, "Hey, I
`28:23` because he basically was like, "Hey, I really like the structure. It's
`28:24` really like the structure. It's
`28:24` really like the structure. It's non-hyped." Uh let's see. Fouad. Senior
`28:27` non-hyped." Uh let's see. Fouad. Senior
`28:27` non-hyped." Uh let's see. Fouad. Senior back end. Yep. Axel. The Bay Area. Yeah,
`28:29` back end. Yep. Axel. The Bay Area. Yeah,
`28:29` back end. Yep. Axel. The Bay Area. Yeah, I've started notice we're getting some
`28:31` I've started notice we're getting some
`28:31` I've started notice we're getting some Bay Area squeeze in here, which makes me
`28:32` Bay Area squeeze in here, which makes me
`28:32` Bay Area squeeze in here, which makes me feel good. Um
`28:34` feel good. Um
`28:34` feel good. Um Don Cameron. For those of you who don't
`28:36` Don Cameron. For those of you who don't
`28:36` Don Cameron. For those of you who don't know what swe means, software engineer.
`28:37` know what swe means, software engineer.
`28:37` know what swe means, software engineer. Just Yeah, simple as that. Um Don
`28:40` Just Yeah, simple as that. Um Don
`28:40` Just Yeah, simple as that. Um Don Cameron. 10 years in AI models. Found
`28:42` Cameron. 10 years in AI models. Found
`28:42` Cameron. 10 years in AI models. Found you because they were already building
`28:43` you because they were already building
`28:43` you because they were already building agents the hard way and your folder
`28:44` agents the hard way and your folder
`28:44` agents the hard way and your folder thesis made them stop and rethink. They
`28:46` thesis made them stop and rethink. They
`28:46` thesis made them stop and rethink. They want depth. They want your research
`28:47` want depth. They want your research
`28:47` want depth. They want your research paper. They want to share why it works,
`28:48` paper. They want to share why it works,
`28:48` paper. They want to share why it works, not just how. I think that's pretty
`28:50` not just how. I think that's pretty
`28:50` not just how. I think that's pretty agree, right? It's not that I'm saying
`28:52` agree, right? It's not that I'm saying
`28:52` agree, right? It's not that I'm saying agents suck or that I'm creating some
`28:54` agents suck or that I'm creating some
`28:54` agents suck or that I'm creating some super structured framework. It's that,
`28:55` super structured framework. It's that,
`28:56` super structured framework. It's that, "Hey, just structure your stuff by
`28:57` "Hey, just structure your stuff by
`28:57` "Hey, just structure your stuff by folders. Make it work. Then worry about
`28:59` folders. Make it work. Then worry about
`28:59` folders. Make it work. Then worry about the complexities after."
`29:01` the complexities after."
`29:01` the complexities after." Um technical adjacent. Yep, we do have a
`29:02` Um technical adjacent. Yep, we do have a
`29:03` Um technical adjacent. Yep, we do have a lot of technical adjacent people. Um
`29:05` lot of technical adjacent people. Um
`29:05` lot of technical adjacent people. Um Dean. Yep. Yep. Yep. Brett and
`29:07` Dean. Yep. Yep. Yep. Brett and
`29:07` Dean. Yep. Yep. Yep. Brett and Jacqueline. Absolutely product manager.
`29:08` Jacqueline. Absolutely product manager.
`29:08` Jacqueline. Absolutely product manager. I wonder if Do they have um
`29:11` I wonder if Do they have um
`29:11` I wonder if Do they have um Uh I didn't see the the other eyes.
`29:13` Uh I didn't see the the other eyes.
`29:13` Uh I didn't see the the other eyes. Wait. Oh, yeah. Jesse.
`29:15` Wait. Oh, yeah. Jesse.
`29:15` Wait. Oh, yeah. Jesse. I I'm doing my work with um
`29:17` I I'm doing my work with um
`29:17` I I'm doing my work with um KPMG and I want to try to do some work
`29:19` KPMG and I want to try to do some work
`29:19` KPMG and I want to try to do some work with Deloitte here soon. But we'll see
`29:21` with Deloitte here soon. But we'll see
`29:21` with Deloitte here soon. But we'll see how that goes. Uh Jacqueline. They need
`29:23` how that goes. Uh Jacqueline. They need
`29:23` how that goes. Uh Jacqueline. They need frameworks translated into their
`29:24` frameworks translated into their
`29:24` frameworks translated into their language. Uh repeatable systems. Yep,
`29:27` language. Uh repeatable systems. Yep,
`29:27` language. Uh repeatable systems. Yep, absolutely. Hopefully, they're getting a
`29:28` absolutely. Hopefully, they're getting a
`29:28` absolutely. Hopefully, they're getting a lot out of my work and stuff I've done
`29:30` lot out of my work and stuff I've done
`29:30` lot out of my work and stuff I've done there. Non-technical people who want to
`29:32` there. Non-technical people who want to
`29:32` there. Non-technical people who want to build. Appliance technicians, roof
`29:34` build. Appliance technicians, roof
`29:34` build. Appliance technicians, roof tilers. Yeah. Yeah, we gotten some cool
`29:36` tilers. Yeah. Yeah, we gotten some cool
`29:36` tilers. Yeah. Yeah, we gotten some cool people from Australia. I used to I I
`29:38` people from Australia. I used to I I
`29:38` people from Australia. I used to I I spent a lot of time up in Northern
`29:39` spent a lot of time up in Northern
`29:39` spent a lot of time up in Northern Territory in Darwin.
`29:41` Territory in Darwin.
`29:41` Territory in Darwin. Um and I spent a bunch of time in
`29:44` Um and I spent a bunch of time in
`29:44` Um and I spent a bunch of time in Katherine, a little bit south of Darwin
`29:45` Katherine, a little bit south of Darwin
`29:45` Katherine, a little bit south of Darwin as well when I was in the Marine Corps.
`29:47` as well when I was in the Marine Corps.
`29:47` as well when I was in the Marine Corps. Uh crazy place there, man. I almost got
`29:49` Uh crazy place there, man. I almost got
`29:49` Uh crazy place there, man. I almost got a felony for disrespecting the queen. Um
`29:52` a felony for disrespecting the queen. Um
`29:52` a felony for disrespecting the queen. Um that's a crazy story. I'll probably save
`29:54` that's a crazy story. I'll probably save
`29:54` that's a crazy story. I'll probably save that for another time.
`29:56` that for another time.
`29:56` that for another time. Um let's see. How do I structure this
`29:57` Um let's see. How do I structure this
`29:57` Um let's see. How do I structure this for my workflow? Yep. How do I make this
`30:00` for my workflow? Yep. How do I make this
`30:00` for my workflow? Yep. How do I make this persistent or stop re-explaining?
`30:02` persistent or stop re-explaining?
`30:02` persistent or stop re-explaining? Absolutely. What's the difference
`30:03` Absolutely. What's the difference
`30:03` Absolutely. What's the difference between skills, MCPs? I need to start
`30:05` between skills, MCPs? I need to start
`30:05` between skills, MCPs? I need to start answering this one more. I think this
`30:06` answering this one more. I think this
`30:07` answering this one more. I think this might be the first video that I do after
`30:09` might be the first video that I do after
`30:09` might be the first video that I do after this one is diving deeper into this cuz
`30:12` this one is diving deeper into this cuz
`30:12` this one is diving deeper into this cuz there's so many concepts out there. It's
`30:14` there's so many concepts out there. It's
`30:14` there's so many concepts out there. It's like they're all the same thing. Like
`30:16` like they're all the same thing. Like
`30:16` like they're all the same thing. Like you look at the back on all of them are
`30:18` you look at the back on all of them are
`30:18` you look at the back on all of them are just natural language with some really
`30:19` just natural language with some really
`30:19` just natural language with some really decent routing. Um and I'm simplifying
`30:22` decent routing. Um and I'm simplifying
`30:22` decent routing. Um and I'm simplifying it. There is important differences
`30:23` it. There is important differences
`30:23` it. There is important differences between it all, but there is a lot of
`30:25` between it all, but there is a lot of
`30:25` between it all, but there is a lot of confusion there and I need to hit that.
`30:27` confusion there and I need to hit that.
`30:27` confusion there and I need to hit that. I hit There is [laughter] a lot of
`30:28` I hit There is [laughter] a lot of
`30:28` I hit There is [laughter] a lot of confusion. Good job, Claude. Um cool.
`30:31` confusion. Good job, Claude. Um cool.
`30:31` confusion. Good job, Claude. Um cool. Obsidian integration. Yes. I got to show
`30:33` Obsidian integration. Yes. I got to show
`30:33` Obsidian integration. Yes. I got to show I'm I'm I I I use Obsidian to create
`30:35` I'm I'm I I I use Obsidian to create
`30:35` I'm I'm I I I use Obsidian to create mind maps sometimes.
`30:37` mind maps sometimes.
`30:37` mind maps sometimes. Um and so I really need to dive into
`30:39` Um and so I really need to dive into
`30:39` Um and so I really need to dive into that. Uh how do we use this teams? Yeah,
`30:41` that. Uh how do we use this teams? Yeah,
`30:41` that. Uh how do we use this teams? Yeah, going into get how you would share these
`30:43` going into get how you would share these
`30:43` going into get how you would share these files. I really think eventually it's
`30:45` files. I really think eventually it's
`30:45` files. I really think eventually it's going to be as simple as just logging
`30:47` going to be as simple as just logging
`30:47` going to be as simple as just logging into co-work and you all would have a
`30:49` into co-work and you all would have a
`30:49` into co-work and you all would have a shared context folder and they're going
`30:51` shared context folder and they're going
`30:51` shared context folder and they're going to handle a lot of that processing and
`30:52` to handle a lot of that processing and
`30:52` to handle a lot of that processing and work for you.
`30:54` work for you.
`30:54` work for you. Um but again,
`30:55` Um but again,
`30:55` Um but again, it's always nice to have your own
`30:56` it's always nice to have your own
`30:56` it's always nice to have your own control and build it.
`30:58` control and build it.
`30:58` control and build it. Uh your entire point is nonsense about
`31:00` Uh your entire point is nonsense about
`31:00` Uh your entire point is nonsense about tool calling. Yeah, that sounds right.
`31:02` tool calling. Yeah, that sounds right.
`31:02` tool calling. Yeah, that sounds right. 2023 era mega printing.
`31:04` 2023 era mega printing.
`31:04` 2023 era mega printing. >> [laughter]
`31:04` >> [laughter]
`31:04` >> [laughter] >> Oh, these it's
`31:06` >> Oh, these it's
`31:06` >> Oh, these it's the objections I have from the agent
`31:08` the objections I have from the agent
`31:08` the objections I have from the agent crowd or people is is hilarious cuz they
`31:10` crowd or people is is hilarious cuz they
`31:10` crowd or people is is hilarious cuz they they're totally missing what I've been
`31:12` they're totally missing what I've been
`31:12` they're totally missing what I've been doing in my point. Uh which is fine. You
`31:14` doing in my point. Uh which is fine. You
`31:15` doing in my point. Uh which is fine. You know, maybe I can explain it better, but
`31:16` know, maybe I can explain it better, but
`31:17` know, maybe I can explain it better, but uh I can always Yeah, I do think it is
`31:18` uh I can always Yeah, I do think it is
`31:18` uh I can always Yeah, I do think it is worth addressing. I fair fell in that.
`31:20` worth addressing. I fair fell in that.
`31:20` worth addressing. I fair fell in that. See, architecture. All right, let's see
`31:22` See, architecture. All right, let's see
`31:22` See, architecture. All right, let's see This is This is what I'm curious about.
`31:24` This is This is what I'm curious about.
`31:24` This is This is what I'm curious about. What What architecture did Claude choose
`31:27` What What architecture did Claude choose
`31:27` What What architecture did Claude choose for me or what pillars? All right, is
`31:29` for me or what pillars? All right, is
`31:29` for me or what pillars? All right, is this going to align with the pillars I
`31:31` this going to align with the pillars I
`31:31` this going to align with the pillars I had in my head? And again, you don't
`31:32` had in my head? And again, you don't
`31:32` had in my head? And again, you don't want to take this stuff at face value.
`31:33` want to take this stuff at face value.
`31:33` want to take this stuff at face value. Everyone say throw it in the Claude and
`31:35` Everyone say throw it in the Claude and
`31:35` Everyone say throw it in the Claude and it's going to make you rich. Stop that.
`31:37` it's going to make you rich. Stop that.
`31:37` it's going to make you rich. Stop that. If this aligns with your personal
`31:39` If this aligns with your personal
`31:39` If this aligns with your personal opinions, your conviction, then that's
`31:41` opinions, your conviction, then that's
`31:41` opinions, your conviction, then that's okay. If it doesn't,
`31:43` okay. If it doesn't,
`31:43` okay. If it doesn't, try tweaking it. That's okay. Sometimes
`31:45` try tweaking it. That's okay. Sometimes
`31:45` try tweaking it. That's okay. Sometimes it might know better, but the goal isn't
`31:47` it might know better, but the goal isn't
`31:47` it might know better, but the goal isn't for it to tell you how to do things.
`31:49` for it to tell you how to do things.
`31:49` for it to tell you how to do things. It's the goal is for it to organize how
`31:51` It's the goal is for it to organize how
`31:51` It's the goal is for it to organize how you're already doing it and make it
`31:52` you're already doing it and make it
`31:52` you're already doing it and make it better.
`31:53` better.
`31:53` better. Uh so let's see. Folder system itself.
`31:55` Uh so let's see. Folder system itself.
`31:55` Uh so let's see. Folder system itself. Three layer routing, Claude MD,
`31:56` Three layer routing, Claude MD,
`31:56` Three layer routing, Claude MD, workspace design, naming conventions,
`31:58` workspace design, naming conventions,
`31:58` workspace design, naming conventions, context as your core thesis can go 90%.
`32:00` context as your core thesis can go 90%.
`32:00` context as your core thesis can go 90%. Yep, yep, yep, yep. That's I mean,
`32:02` Yep, yep, yep, yep. That's I mean,
`32:02` Yep, yep, yep, yep. That's I mean, that's just fair, right? I I I want to
`32:04` that's just fair, right? I I I want to
`32:04` that's just fair, right? I I I want to be careful with how much it sticks on
`32:06` be careful with how much it sticks on
`32:06` be careful with how much it sticks on three layer routing cuz I do five layers
`32:09` three layer routing cuz I do five layers
`32:09` three layer routing cuz I do five layers in my research paper.
`32:11` in my research paper.
`32:11` in my research paper. Um
`32:12` Um
`32:12` Um the whole idea behind layer routing is
`32:14` the whole idea behind layer routing is
`32:14` the whole idea behind layer routing is nothing to do with crazy layers. It's
`32:15` nothing to do with crazy layers. It's
`32:16` nothing to do with crazy layers. It's the idea of how do I break down a task
`32:18` the idea of how do I break down a task
`32:18` the idea of how do I break down a task into its parts? Which parts are higher
`32:20` into its parts? Which parts are higher
`32:20` into its parts? Which parts are higher level in the abstraction, which parts
`32:22` level in the abstraction, which parts
`32:22` level in the abstraction, which parts are lower, right? If I'm building a
`32:24` are lower, right? If I'm building a
`32:24` are lower, right? If I'm building a watch, the process of putting the gears
`32:27` watch, the process of putting the gears
`32:27` watch, the process of putting the gears in are going to be more specific than
`32:29` in are going to be more specific than
`32:29` in are going to be more specific than what color I choose for the watch. And
`32:30` what color I choose for the watch. And
`32:30` what color I choose for the watch. And there's different layers in which it
`32:32` there's different layers in which it
`32:32` there's different layers in which it affects everything where each layer can
`32:35` affects everything where each layer can
`32:35` affects everything where each layer can be important to the other, but it's
`32:37` be important to the other, but it's
`32:37` be important to the other, but it's how detailed each layer gets. That's all
`32:39` how detailed each layer gets. That's all
`32:39` how detailed each layer gets. That's all it is. Sometimes you might need more
`32:41` it is. Sometimes you might need more
`32:41` it is. Sometimes you might need more than that, but I think three layers is
`32:42` than that, but I think three layers is
`32:42` than that, but I think three layers is usually good for most workflows.
`32:45` usually good for most workflows.
`32:45` usually good for most workflows. Um goes with 90% of people throughout
`32:48` Um goes with 90% of people throughout
`32:48` Um goes with 90% of people throughout the door. Absolutely. Foundation
`32:49` the door. Absolutely. Foundation
`32:49` the door. Absolutely. Foundation concepts that make the architecture make
`32:50` concepts that make the architecture make
`32:50` concepts that make the architecture make sense. Tokens, context. Yep, yep. Your
`32:53` sense. Tokens, context. Yep, yep. Your
`32:53` sense. Tokens, context. Yep, yep. Your hello world stuff lives here. Um
`32:55` hello world stuff lives here. Um
`32:55` hello world stuff lives here. Um foundations. Yeah, that's fair. Right,
`32:57` foundations. Yeah, that's fair. Right,
`32:57` foundations. Yeah, that's fair. Right, computing foundations. The It's not just
`32:59` computing foundations. The It's not just
`32:59` computing foundations. The It's not just about learning AI foundations, about
`33:01` about learning AI foundations, about
`33:01` about learning AI foundations, about learning why computing exists, right?
`33:03` learning why computing exists, right?
`33:03` learning why computing exists, right? We've been doing the same process for
`33:05` We've been doing the same process for
`33:05` We've been doing the same process for computing. If this, then that, up, down,
`33:07` computing. If this, then that, up, down,
`33:07` computing. If this, then that, up, down, Boolean logic for literally 200 plus
`33:09` Boolean logic for literally 200 plus
`33:09` Boolean logic for literally 200 plus years. Technically, 2,000 if you really
`33:11` years. Technically, 2,000 if you really
`33:11` years. Technically, 2,000 if you really dive into the the older ancient Greek
`33:13` dive into the the older ancient Greek
`33:13` dive into the the older ancient Greek things, but I have videos on that if you
`33:15` things, but I have videos on that if you
`33:15` things, but I have videos on that if you ever want to see them.
`33:17` ever want to see them.
`33:17` ever want to see them. Uh applied workflows. How do I do this
`33:18` Uh applied workflows. How do I do this
`33:18` Uh applied workflows. How do I do this thing for How do I do How do I do this
`33:22` thing for How do I do How do I do this
`33:22` thing for How do I do How do I do this for my thing? Yeah, that's exactly what
`33:25` for my thing? Yeah, that's exactly what
`33:25` for my thing? Yeah, that's exactly what this video seems to be right now. So
`33:26` this video seems to be right now. So
`33:26` this video seems to be right now. So content creation, school and first play
`33:28` content creation, school and first play
`33:28` content creation, school and first play play cool. Ecosystem skills, MCP
`33:30` play cool. Ecosystem skills, MCP
`33:30` play cool. Ecosystem skills, MCP servers, Claude, right? This converse
`33:32` servers, Claude, right? This converse
`33:32` servers, Claude, right? This converse confusion between skills and agent.
`33:33` confusion between skills and agent.
`33:33` confusion between skills and agent. Yeah, so this is this refers back to the
`33:36` Yeah, so this is this refers back to the
`33:36` Yeah, so this is this refers back to the previous point where people were kind of
`33:38` previous point where people were kind of
`33:38` previous point where people were kind of confused of
`33:41` confused of
`33:41` confused of Where is it? This, the difference here.
`33:43` Where is it? This, the difference here.
`33:43` Where is it? This, the difference here. So point four is into this pillar
`33:46` So point four is into this pillar
`33:46` So point four is into this pillar ecosystem. Um proposed folder structure.
`33:48` ecosystem. Um proposed folder structure.
`33:48` ecosystem. Um proposed folder structure. Okay, so here Here's important. Really
`33:50` Okay, so here Here's important. Really
`33:50` Okay, so here Here's important. Really pay attention to this. This isn't just
`33:52` pay attention to this. This isn't just
`33:52` pay attention to this. This isn't just for organizing for Claude. This is
`33:55` for organizing for Claude. This is
`33:55` for organizing for Claude. This is organizing for you. You want to look at
`33:57` organizing for you. You want to look at
`33:57` organizing for you. You want to look at this and be like, okay, if I'm writing
`34:00` this and be like, okay, if I'm writing
`34:00` this and be like, okay, if I'm writing and looking at my transcripts or reading
`34:01` and looking at my transcripts or reading
`34:01` and looking at my transcripts or reading it, is this is how I would want to look
`34:03` it, is this is how I would want to look
`34:03` it, is this is how I would want to look at it? If I wanted to come in here with
`34:05` at it? If I wanted to come in here with
`34:05` at it? If I wanted to come in here with Claude and start looking at the
`34:07` Claude and start looking at the
`34:07` Claude and start looking at the community transcripts and affecting my
`34:09` community transcripts and affecting my
`34:09` community transcripts and affecting my voice and brand, how would I do it? What
`34:11` voice and brand, how would I do it? What
`34:11` voice and brand, how would I do it? What order? Would I want subfolders? How many
`34:14` order? Would I want subfolders? How many
`34:14` order? Would I want subfolders? How many subfolders? Are you going to want to sit
`34:16` subfolders? Are you going to want to sit
`34:16` subfolders? Are you going to want to sit there and read through certain things,
`34:17` there and read through certain things,
`34:17` there and read through certain things, right? This is important. What is your
`34:19` right? This is important. What is your
`34:19` right? This is important. What is your thought process in how you would not
`34:21` thought process in how you would not
`34:21` thought process in how you would not just build this stuff, but evolve it?
`34:24` just build this stuff, but evolve it?
`34:24` just build this stuff, but evolve it? When would you want to come in and be a
`34:26` When would you want to come in and be a
`34:26` When would you want to come in and be a human and edit this markdown file or
`34:28` human and edit this markdown file or
`34:28` human and edit this markdown file or edit this one or add something? Maybe in
`34:30` edit this one or add something? Maybe in
`34:30` edit this one or add something? Maybe in the pillars, you want a place or a
`34:32` the pillars, you want a place or a
`34:32` the pillars, you want a place or a folder where you have testing pillars,
`34:34` folder where you have testing pillars,
`34:34` folder where you have testing pillars, new ideas, right? You want to think
`34:36` new ideas, right? You want to think
`34:36` new ideas, right? You want to think about all of that.
`34:37` about all of that.
`34:37` about all of that. Uh for right now, um I like all of that.
`34:40` Uh for right now, um I like all of that.
`34:40` Uh for right now, um I like all of that. Love the design. Also think the What did
`34:44` Love the design. Also think the What did
`34:44` Love the design. Also think the What did you What did you call these? Um
`34:47` you What did you call these? Um
`34:47` you What did you call these? Um what they ate
`34:49` what they ate
`34:49` what they ate things of what they want should be
`34:53` things of what they want should be
`34:53` things of what they want should be referenced
`34:55` referenced
`34:55` referenced or added in some way to
`34:59` or added in some way to
`34:59` or added in some way to a content pillar for each pillar
`35:03` a content pillar for each pillar
`35:03` a content pillar for each pillar individually
`35:05` individually
`35:05` individually as in four could go Uh wait, see.
`35:10` as in four could go Uh wait, see.
`35:10` as in four could go Uh wait, see. Statement four, difference Oh my gosh, I
`35:14` Statement four, difference Oh my gosh, I
`35:14` Statement four, difference Oh my gosh, I can't type anything. Difference between
`35:19` can't type anything. Difference between
`35:19` can't type anything. Difference between MCP, skills, etc. Should go in pillar
`35:24` MCP, skills, etc. Should go in pillar
`35:24` MCP, skills, etc. Should go in pillar four.
`35:26` four.
`35:26` four. Right? So this
`35:28` Right? So this
`35:28` Right? So this statement four, I think falls under
`35:30` statement four, I think falls under
`35:30` statement four, I think falls under ecosystem, right? What are you pulling
`35:32` ecosystem, right? What are you pulling
`35:32` ecosystem, right? What are you pulling in?
`35:33` in?
`35:33` in? Um
`35:34` Um
`35:34` Um pillar four and so on. So that's going
`35:36` pillar four and so on. So that's going
`35:37` pillar four and so on. So that's going to immediately just a little tweak along
`35:38` to immediately just a little tweak along
`35:38` to immediately just a little tweak along its thinking cuz I didn't see it mention
`35:40` its thinking cuz I didn't see it mention
`35:40` its thinking cuz I didn't see it mention that,
`35:41` that,
`35:41` that, right? That it was going to throw those
`35:43` right? That it was going to throw those
`35:43` right? That it was going to throw those in there, but I think these are more
`35:45` in there, but I think these are more
`35:45` in there, but I think these are more useful than like, "Hey, this is how you
`35:47` useful than like, "Hey, this is how you
`35:48` useful than like, "Hey, this is how you should write." It should say, "These are
`35:49` should write." It should say, "These are
`35:49` should write." It should say, "These are what people are saying and then we'll
`35:51` what people are saying and then we'll
`35:51` what people are saying and then we'll creatively write." Because you want to
`35:53` creatively write." Because you want to
`35:53` creatively write." Because you want to do two things. You want to direct Claude
`35:55` do two things. You want to direct Claude
`35:55` do two things. You want to direct Claude to be talking a specific way, but you
`35:58` to be talking a specific way, but you
`35:58` to be talking a specific way, but you also don't want to direct it too much
`36:00` also don't want to direct it too much
`36:00` also don't want to direct it too much because then all of your content will
`36:01` because then all of your content will
`36:01` because then all of your content will sound that way. Because that Yeah, that
`36:03` sound that way. Because that Yeah, that
`36:03` sound that way. Because that Yeah, that kind of just
`36:04` kind of just
`36:04` kind of just it locks you in too much and I've
`36:06` it locks you in too much and I've
`36:06` it locks you in too much and I've noticed that when you have it create
`36:07` noticed that when you have it create
`36:07` noticed that when you have it create scripts and things like this it it
`36:08` scripts and things like this it it
`36:08` scripts and things like this it it again, you're not trying to take the
`36:10` again, you're not trying to take the
`36:10` again, you're not trying to take the human out. You're trying to scale the
`36:12` human out. You're trying to scale the
`36:12` human out. You're trying to scale the human. You're trying to scale yourself.
`36:15` human. You're trying to scale yourself.
`36:15` human. You're trying to scale yourself. And a lot of people mistake automating
`36:17` And a lot of people mistake automating
`36:17` And a lot of people mistake automating with scaling. Automation does not mean
`36:21` with scaling. Automation does not mean
`36:21` with scaling. Automation does not mean you will scale. Just because you
`36:23` you will scale. Just because you
`36:23` you will scale. Just because you automate your DMs doesn't mean you're
`36:25` automate your DMs doesn't mean you're
`36:25` automate your DMs doesn't mean you're going to guarantee more people coming
`36:27` going to guarantee more people coming
`36:27` going to guarantee more people coming in. Especially in this day and age where
`36:29` in. Especially in this day and age where
`36:29` in. Especially in this day and age where people are just hating their bots. I
`36:32` people are just hating their bots. I
`36:32` people are just hating their bots. I went through and DM'd a thousand people
`36:35` went through and DM'd a thousand people
`36:35` went through and DM'd a thousand people myself. Now, behind the scenes you're
`36:36` myself. Now, behind the scenes you're
`36:37` myself. Now, behind the scenes you're like, "Oh, but that doesn't scale." It's
`36:38` like, "Oh, but that doesn't scale." It's
`36:38` like, "Oh, but that doesn't scale." It's the only things that scaled. I got a
`36:40` the only things that scaled. I got a
`36:40` the only things that scaled. I got a thousand people to join because they're
`36:41` thousand people to join because they're
`36:41` thousand people to join because they're like, "Oh my gosh, this is so great. I'm
`36:43` like, "Oh my gosh, this is so great. I'm
`36:43` like, "Oh my gosh, this is so great. I'm talking to a real person." Like it's the
`36:46` talking to a real person." Like it's the
`36:46` talking to a real person." Like it's the I've sent videos through Instagram,
`36:48` I've sent videos through Instagram,
`36:48` I've sent videos through Instagram, right? People are they're not thinking
`36:49` right? People are they're not thinking
`36:49` right? People are they're not thinking about the job to be done when they're
`36:51` about the job to be done when they're
`36:51` about the job to be done when they're creating these automations and processes
`36:53` creating these automations and processes
`36:53` creating these automations and processes and things like that.
`36:55` and things like that.
`36:55` and things like that. Um so let's see.
`36:56` Um so let's see.
`36:56` Um so let's see. Uh good call. Let me tap those recurring
`36:59` Uh good call. Let me tap those recurring
`36:59` Uh good call. Let me tap those recurring tasks in the pillars then build
`37:00` tasks in the pillars then build
`37:00` tasks in the pillars then build everything out. Cool. Here's how the
`37:02` everything out. Cool. Here's how the
`37:02` everything out. Cool. Here's how the eight audience the eight audience needs
`37:04` eight audience the eight audience needs
`37:04` eight audience the eight audience needs to land.
`37:06` to land.
`37:06` to land. That's It sounds weird, but I'll take
`37:08` That's It sounds weird, but I'll take
`37:08` That's It sounds weird, but I'll take it. Um pillar one architecture. How do I
`37:10` it. Um pillar one architecture. How do I
`37:10` it. Um pillar one architecture. How do I structure this for my How do I Yeah, see
`37:12` structure this for my How do I Yeah, see
`37:12` structure this for my How do I Yeah, see this is this is gold right here. This is
`37:14` this is this is gold right here. This is
`37:14` this is this is gold right here. This is what we're looking for, boys, right? We
`37:16` what we're looking for, boys, right? We
`37:16` what we're looking for, boys, right? We want we want to take these concepts and
`37:19` want we want to take these concepts and
`37:19` want we want to take these concepts and create a mental mapping of where it
`37:21` create a mental mapping of where it
`37:21` create a mental mapping of where it should be generating content, not how it
`37:24` should be generating content, not how it
`37:24` should be generating content, not how it should be generating content. Cuz now
`37:25` should be generating content. Cuz now
`37:25` should be generating content. Cuz now it's going to read this. When when we go
`37:27` it's going to read this. When when we go
`37:27` it's going to read this. When when we go later and I ask Claude, "Hey, think
`37:30` later and I ask Claude, "Hey, think
`37:30` later and I ask Claude, "Hey, think about the pillars. Look at what these
`37:31` about the pillars. Look at what these
`37:31` about the pillars. Look at what these pillars are. I want to create a series
`37:33` pillars are. I want to create a series
`37:33` pillars are. I want to create a series of videos one focusing on each pillar or
`37:36` of videos one focusing on each pillar or
`37:36` of videos one focusing on each pillar or area." Right? We could maybe combine
`37:38` area." Right? We could maybe combine
`37:38` area." Right? We could maybe combine pillar one and three for this video or
`37:40` pillar one and three for this video or
`37:40` pillar one and three for this video or something like that. We're always able
`37:42` something like that. We're always able
`37:42` something like that. We're always able to reference the concepts, not the
`37:44` to reference the concepts, not the
`37:44` to reference the concepts, not the structure, which is going to allow me to
`37:46` structure, which is going to allow me to
`37:46` structure, which is going to allow me to be creative in what I want to write
`37:48` be creative in what I want to write
`37:48` be creative in what I want to write about while simultaneously giving me the
`37:50` about while simultaneously giving me the
`37:50` about while simultaneously giving me the structure to plan future videos and
`37:52` structure to plan future videos and
`37:52` structure to plan future videos and things like that. Um
`37:54` things like that. Um
`37:54` things like that. Um so let's see. Went through, created Oop,
`37:57` so let's see. Went through, created Oop,
`37:57` so let's see. Went through, created Oop, yeah, let's let's go ahead and open it.
`37:59` yeah, let's let's go ahead and open it.
`37:59` yeah, let's let's go ahead and open it. Let's see. Pillar two foundations.
`38:00` Let's see. Pillar two foundations.
`38:00` Let's see. Pillar two foundations. Concepts And see again, this is why I
`38:02` Concepts And see again, this is why I
`38:02` Concepts And see again, this is why I like VS Code. And I'm absolutely going
`38:05` like VS Code. And I'm absolutely going
`38:05` like VS Code. And I'm absolutely going to work in this file later inside of VS
`38:07` to work in this file later inside of VS
`38:07` to work in this file later inside of VS Code. Um because well, that's just going
`38:09` Code. Um because well, that's just going
`38:09` Code. Um because well, that's just going to make life 10 times easier. So we come
`38:11` to make life 10 times easier. So we come
`38:11` to make life 10 times easier. So we come in, I can look into the pillars. And see
`38:13` in, I can look into the pillars. And see
`38:13` in, I can look into the pillars. And see now I can just zippity zop. Let me
`38:16` now I can just zippity zop. Let me
`38:16` now I can just zippity zop. Let me actually get rid of these instances and
`38:18` actually get rid of these instances and
`38:18` actually get rid of these instances and read through it. Also, Obsidian would be
`38:20` read through it. Also, Obsidian would be
`38:20` read through it. Also, Obsidian would be great to work inside of here. So maybe
`38:22` great to work inside of here. So maybe
`38:22` great to work inside of here. So maybe you have Obsidian and then Claude or you
`38:24` you have Obsidian and then Claude or you
`38:25` you have Obsidian and then Claude or you have this in there. I don't necessarily
`38:26` have this in there. I don't necessarily
`38:26` have this in there. I don't necessarily need I actually like my plugin that
`38:29` need I actually like my plugin that
`38:29` need I actually like my plugin that colors markdown better than sitting
`38:31` colors markdown better than sitting
`38:31` colors markdown better than sitting inside of Obsidian. Don't hate me for
`38:33` inside of Obsidian. Don't hate me for
`38:33` inside of Obsidian. Don't hate me for it. I know you can do that in there and
`38:34` it. I know you can do that in there and
`38:34` it. I know you can do that in there and there's a lot of ways to clean up
`38:35` there's a lot of ways to clean up
`38:35` there's a lot of ways to clean up Obsidian. I'm a very just practical,
`38:38` Obsidian. I'm a very just practical,
`38:38` Obsidian. I'm a very just practical, like to be at the base level person, but
`38:40` like to be at the base level person, but
`38:40` like to be at the base level person, but teach their own.
`38:41` teach their own.
`38:41` teach their own. Um folder itself is system. This is
`38:44` Um folder itself is system. This is
`38:44` Um folder itself is system. This is where 90% of the audience got through
`38:46` where 90% of the audience got through
`38:46` where 90% of the audience got through the door. Yeah, that's fair. I think
`38:48` the door. Yeah, that's fair. I think
`38:48` the door. Yeah, that's fair. I think that's actually So usually I'm careful
`38:49` that's actually So usually I'm careful
`38:49` that's actually So usually I'm careful with these types of statements because
`38:51` with these types of statements because
`38:51` with these types of statements because it'll regurgitate them a lot, but I
`38:53` it'll regurgitate them a lot, but I
`38:53` it'll regurgitate them a lot, but I think this seems like such a simple
`38:56` think this seems like such a simple
`38:56` think this seems like such a simple edit. It's not, I promise. Instead of
`38:58` edit. It's not, I promise. Instead of
`38:58` edit. It's not, I promise. Instead of saying this is the core thesis, say
`39:01` saying this is the core thesis, say
`39:01` saying this is the core thesis, say Jake wants you to understand, but not
`39:05` Jake wants you to understand, but not
`39:06` Jake wants you to understand, but not regurgitate
`39:07` regurgitate
`39:07` regurgitate that
`39:10` that
`39:10` that this is the core thesis that That simple
`39:12` this is the core thesis that That simple
`39:12` this is the core thesis that That simple thing is going to stop it from
`39:14` thing is going to stop it from
`39:14` thing is going to stop it from regurgitating, "Oh, this is the thesis
`39:15` regurgitating, "Oh, this is the thesis
`39:15` regurgitating, "Oh, this is the thesis that got 90% people." It's going to say
`39:17` that got 90% people." It's going to say
`39:17` that got 90% people." It's going to say that all the time if it said that. It
`39:18` that all the time if it said that. It
`39:18` that all the time if it said that. It still might a little bit here or there,
`39:20` still might a little bit here or there,
`39:20` still might a little bit here or there, but that's the aspect. And again, look
`39:22` but that's the aspect. And again, look
`39:22` but that's the aspect. And again, look how short this is. This isn't a lot.
`39:24` how short this is. This isn't a lot.
`39:24` how short this is. This isn't a lot. We're not adding a massive markdown
`39:26` We're not adding a massive markdown
`39:26` We're not adding a massive markdown file. You can do huge markdown files,
`39:28` file. You can do huge markdown files,
`39:28` file. You can do huge markdown files, right? So like we when we're looking at
`39:30` right? So like we when we're looking at
`39:30` right? So like we when we're looking at our transcripts, they're going to be
`39:31` our transcripts, they're going to be
`39:31` our transcripts, they're going to be large. Or when we're, you know, looking
`39:34` large. Or when we're, you know, looking
`39:34` large. Or when we're, you know, looking maybe Claude's going to get a little the
`39:35` maybe Claude's going to get a little the
`39:35` maybe Claude's going to get a little the main MD is going to get a little larger.
`39:37` main MD is going to get a little larger.
`39:37` main MD is going to get a little larger. Um this one actually, so case in point,
`39:41` Um this one actually, so case in point,
`39:41` Um this one actually, so case in point, this
`39:42` this
`39:42` this is nice because it gives an explanation
`39:44` is nice because it gives an explanation
`39:44` is nice because it gives an explanation of the voice, but then references my
`39:47` of the voice, but then references my
`39:47` of the voice, but then references my brand voice
`39:48` brand voice
`39:48` brand voice MD when when it's there.
`39:51` MD when when it's there.
`39:51` MD when when it's there. Um
`39:52` Um
`39:52` Um I'm actually going to have it create
`39:53` I'm actually going to have it create
`39:53` I'm actually going to have it create one. I'm going to give it one with my
`39:55` one. I'm going to give it one with my
`39:55` one. I'm going to give it one with my constraints. So, I have a folder right
`39:58` constraints. So, I have a folder right
`39:58` constraints. So, I have a folder right here. Let's go to my script lab. Again,
`40:00` here. Let's go to my script lab. Again,
`40:00` here. Let's go to my script lab. Again, this is my crazy absurd one. Right here,
`40:02` this is my crazy absurd one. Right here,
`40:02` this is my crazy absurd one. Right here, I have my voice. I think this one's too
`40:05` I have my voice. I think this one's too
`40:05` I have my voice. I think this one's too long, too wordy, it's not structured
`40:06` long, too wordy, it's not structured
`40:06` long, too wordy, it's not structured well. This is why I'm redoing a lot of
`40:08` well. This is why I'm redoing a lot of
`40:08` well. This is why I'm redoing a lot of this. But, one thing I do love about
`40:10` this. But, one thing I do love about
`40:10` this. But, one thing I do love about this is my hard constraints, right? No
`40:13` this is my hard constraints, right? No
`40:13` this is my hard constraints, right? No M-dashes. Antithesis pattern, stay away
`40:15` M-dashes. Antithesis pattern, stay away
`40:15` M-dashes. Antithesis pattern, stay away from it. Formal, informal. There's also
`40:17` from it. Formal, informal. There's also
`40:17` from it. Formal, informal. There's also a skill called
`40:18` a skill called
`40:19` a skill called humanizer. What is it? Humanizer skill.
`40:23` humanizer. What is it? Humanizer skill.
`40:23` humanizer. What is it? Humanizer skill. Uh boop boop boop boop boop. Yeah, the
`40:26` Uh boop boop boop boop boop. Yeah, the
`40:26` Uh boop boop boop boop boop. Yeah, the humanizer skill. It kind of does the
`40:27` humanizer skill. It kind of does the
`40:27` humanizer skill. It kind of does the same thing. So, if we read it, remove
`40:30` same thing. So, if we read it, remove
`40:30` same thing. So, if we read it, remove signs of AI writing from text, blah blah
`40:31` signs of AI writing from text, blah blah
`40:32` signs of AI writing from text, blah blah blah. Here's all the tools that it's
`40:33` blah. Here's all the tools that it's
`40:33` blah. Here's all the tools that it's allowed. Identify AI patterns, things
`40:36` allowed. Identify AI patterns, things
`40:36` allowed. Identify AI patterns, things like that. Now, the way it's identifying
`40:38` like that. Now, the way it's identifying
`40:38` like that. Now, the way it's identifying those patterns is
`40:40` those patterns is
`40:40` those patterns is essentially just what I'm doing here.
`40:42` essentially just what I'm doing here.
`40:42` essentially just what I'm doing here. Here's what these patterns are.
`40:45` Here's what these patterns are.
`40:45` Here's what these patterns are. Basically, it says here's the patterns
`40:46` Basically, it says here's the patterns
`40:46` Basically, it says here's the patterns that are detected. Significance. This
`40:48` that are detected. Significance. This
`40:48` that are detected. Significance. This one is quite nice. I kind of want to
`40:51` one is quite nice. I kind of want to
`40:51` one is quite nice. I kind of want to turn it into my own use though, because
`40:54` turn it into my own use though, because
`40:54` turn it into my own use though, because there's certain things in here that I
`40:55` there's certain things in here that I
`40:55` there's certain things in here that I actually do like to use. Like, I
`40:58` actually do like to use. Like, I
`40:58` actually do like to use. Like, I actually love quotes. I use quotes all
`40:59` actually love quotes. I use quotes all
`40:59` actually love quotes. I use quotes all the time in my natural writing.
`41:02` the time in my natural writing.
`41:02` the time in my natural writing. Um
`41:02` Um
`41:02` Um the artifacts, that's nice, but look,
`41:06` the artifacts, that's nice, but look,
`41:06` the artifacts, that's nice, but look, I'm sorry. If you're not paying
`41:07` I'm sorry. If you're not paying
`41:07` I'm sorry. If you're not paying attention and reading your scripts to
`41:09` attention and reading your scripts to
`41:09` attention and reading your scripts to see if they're still artifacts in it,
`41:11` see if they're still artifacts in it,
`41:11` see if they're still artifacts in it, that's a you problem. I I I think you
`41:13` that's a you problem. I I I think you
`41:13` that's a you problem. I I I think you should spend a little bit more time or
`41:14` should spend a little bit more time or
`41:14` should spend a little bit more time or if you can't tell Claude to not do that,
`41:17` if you can't tell Claude to not do that,
`41:17` if you can't tell Claude to not do that, there's a problem. So, I think that's a
`41:18` there's a problem. So, I think that's a
`41:18` there's a problem. So, I think that's a little extra and that's a lot of tokens
`41:20` little extra and that's a lot of tokens
`41:20` little extra and that's a lot of tokens that I don't necessarily need to use.
`41:22` that I don't necessarily need to use.
`41:22` that I don't necessarily need to use. So, I'd rather kind of work it in my own
`41:24` So, I'd rather kind of work it in my own
`41:25` So, I'd rather kind of work it in my own way. So, I'll I'm going to clean that up
`41:26` way. So, I'll I'm going to clean that up
`41:26` way. So, I'll I'm going to clean that up later and throw it in. But, for right
`41:28` later and throw it in. But, for right
`41:28` later and throw it in. But, for right now, great structure, we can add this
`41:30` now, great structure, we can add this
`41:30` now, great structure, we can add this later. I don't want to
`41:32` later. I don't want to
`41:32` later. I don't want to mess up my current chat and muddy it too
`41:35` mess up my current chat and muddy it too
`41:35` mess up my current chat and muddy it too much because of how much is in there.
`41:37` much because of how much is in there.
`41:37` much because of how much is in there. So,
`41:38` So,
`41:38` So, let's see. 10 files, five folders,
`41:40` let's see. 10 files, five folders,
`41:40` let's see. 10 files, five folders, Claude MD updated to phase two. We've
`41:42` Claude MD updated to phase two. We've
`41:42` Claude MD updated to phase two. We've got source materials with transcripts
`41:44` got source materials with transcripts
`41:44` got source materials with transcripts and files and we can add more into it
`41:45` and files and we can add more into it
`41:45` and files and we can add more into it later. We've got pillar files. We've got
`41:48` later. We've got pillar files. We've got
`41:48` later. We've got pillar files. We've got ideas, which there's nothing in there.
`41:51` ideas, which there's nothing in there.
`41:51` ideas, which there's nothing in there. We'll get that in there, which is great.
`41:52` We'll get that in there, which is great.
`41:52` We'll get that in there, which is great. And then my brand file. So, let's let's
`41:55` And then my brand file. So, let's let's
`41:55` And then my brand file. So, let's let's do one more thing cuz I think this video
`41:56` do one more thing cuz I think this video
`41:56` do one more thing cuz I think this video is starting to get a little long now.
`41:58` is starting to get a little long now.
`41:58` is starting to get a little long now. So, I want to I want to finish up. I'm
`42:00` So, I want to I want to finish up. I'm
`42:00` So, I want to I want to finish up. I'm actually going to do this right now. So,
`42:01` actually going to do this right now. So,
`42:01` actually going to do this right now. So, I'm going to go in and I'm going to take
`42:04` I'm going to go in and I'm going to take
`42:04` I'm going to go in and I'm going to take this
`42:06` this
`42:06` this and
`42:08` and
`42:08` and I'm going to go Honestly, I think you
`42:10` I'm going to go Honestly, I think you
`42:10` I'm going to go Honestly, I think you get my voice and you get the idea that
`42:12` get my voice and you get the idea that
`42:12` get my voice and you get the idea that the brand voice document shouldn't give
`42:15` the brand voice document shouldn't give
`42:15` the brand voice document shouldn't give too hard of constraints, but rather give
`42:17` too hard of constraints, but rather give
`42:17` too hard of constraints, but rather give directions
`42:19` directions
`42:19` directions or ideas that allow Claude to be aligned
`42:21` or ideas that allow Claude to be aligned
`42:21` or ideas that allow Claude to be aligned with me, not stuck in concrete.
`42:23` with me, not stuck in concrete.
`42:23` with me, not stuck in concrete. Um however, here is some important
`42:26` Um however, here is some important
`42:26` Um however, here is some important constraints that I have used and like.
`42:30` constraints that I have used and like.
`42:30` constraints that I have used and like. Please make me either Ah, here we go.
`42:32` Please make me either Ah, here we go.
`42:32` Please make me either Ah, here we go. Actually, this is what I'm going to
`42:32` Actually, this is what I'm going to
`42:32` Actually, this is what I'm going to change. So, Ooh, this is this is gold.
`42:34` change. So, Ooh, this is this is gold.
`42:34` change. So, Ooh, this is this is gold. If you are watching along this this far
`42:35` If you are watching along this this far
`42:35` If you are watching along this this far and are able to keep up with my
`42:36` and are able to keep up with my
`42:36` and are able to keep up with my insanity,
`42:37` insanity,
`42:37` insanity, what I noticed here, this is a giant
`42:39` what I noticed here, this is a giant
`42:39` what I noticed here, this is a giant Oops. This is a huge voice doc. It's too
`42:42` Oops. This is a huge voice doc. It's too
`42:42` Oops. This is a huge voice doc. It's too big. This can be broken down. This is
`42:44` big. This can be broken down. This is
`42:44` big. This can be broken down. This is too much context and tokens at the wrong
`42:46` too much context and tokens at the wrong
`42:46` too much context and tokens at the wrong time. I could give it to a separate
`42:47` time. I could give it to a separate
`42:48` time. I could give it to a separate instance of Claude after, especially all
`42:50` instance of Claude after, especially all
`42:50` instance of Claude after, especially all the constraints. And so, what I'm
`42:52` the constraints. And so, what I'm
`42:52` the constraints. And so, what I'm actually going to do
`42:53` actually going to do
`42:53` actually going to do is something little cheeky here.
`42:57` is something little cheeky here.
`42:57` is something little cheeky here. I think I think we should break down
`43:02` I think I think we should break down
`43:02` I think I think we should break down voice into two or three MDs instead of
`43:08` voice into two or three MDs instead of
`43:08` voice into two or three MDs instead of one. As in, we can have one that is for
`43:12` one. As in, we can have one that is for
`43:12` one. As in, we can have one that is for my
`43:13` my
`43:13` my tones,
`43:16` tones,
`43:16` tones, styles, ideas,
`43:18` styles, ideas,
`43:18` styles, ideas, one that is for
`43:22` one that is for
`43:22` one that is for long form versus short form editing of
`43:26` long form versus short form editing of
`43:26` long form versus short form editing of the ideas, and then one that is
`43:29` the ideas, and then one that is
`43:29` the ideas, and then one that is constraints, as in cleans up or is
`43:34` constraints, as in cleans up or is
`43:34` constraints, as in cleans up or is injected
`43:35` injected
`43:36` injected in at any time
`43:39` in at any time
`43:39` in at any time to speak on what not to do
`43:44` to speak on what not to do
`43:44` to speak on what not to do in your writing. As in, you as an AI
`43:49` in your writing. As in, you as an AI
`43:49` in your writing. As in, you as an AI happen to have patterns
`43:52` happen to have patterns
`43:52` happen to have patterns I don't like. So, we're going to here
`43:55` I don't like. So, we're going to here
`43:55` I don't like. So, we're going to here are those constraints.
`44:00` And we're going to say,
`44:00` And we're going to say, "What do you think?"
`44:02` "What do you think?"
`44:02` "What do you think?" Uh
`44:04` Uh
`44:04` Uh What do you think?
`44:06` What do you think?
`44:06` What do you think? It'll It'll figure it out.
`44:08` It'll It'll figure it out.
`44:08` It'll It'll figure it out. So, what we're doing here, the reason I
`44:10` So, what we're doing here, the reason I
`44:10` So, what we're doing here, the reason I wrote it in this way is I'm saying,
`44:12` wrote it in this way is I'm saying,
`44:12` wrote it in this way is I'm saying, "Hey, I actually like how you understand
`44:14` "Hey, I actually like how you understand
`44:14` "Hey, I actually like how you understand my voice. I'm happy with that. However,
`44:17` my voice. I'm happy with that. However,
`44:17` my voice. I'm happy with that. However, I don't want you to describe my voice. I
`44:19` I don't want you to describe my voice. I
`44:19` I don't want you to describe my voice. I want you to describe a markdown file
`44:22` want you to describe a markdown file
`44:22` want you to describe a markdown file that lets you refer to your future
`44:24` that lets you refer to your future
`44:24` that lets you refer to your future versions of you, right? That might not
`44:26` versions of you, right? That might not
`44:26` versions of you, right? That might not be this chat, understand why or where
`44:29` be this chat, understand why or where
`44:29` be this chat, understand why or where and be aligned with where my voice is,
`44:31` and be aligned with where my voice is,
`44:31` and be aligned with where my voice is, because telling it how to write creates
`44:34` because telling it how to write creates
`44:34` because telling it how to write creates more patterns that end up sounding AI
`44:36` more patterns that end up sounding AI
`44:36` more patterns that end up sounding AI anyway and across all your videos. So,
`44:38` anyway and across all your videos. So,
`44:38` anyway and across all your videos. So, you want to you want just enough chaos
`44:40` you want to you want just enough chaos
`44:40` you want to you want just enough chaos in there to be able to have enough. And
`44:42` in there to be able to have enough. And
`44:42` in there to be able to have enough. And then the idea of breaking it down into
`44:43` then the idea of breaking it down into
`44:43` then the idea of breaking it down into multiple markdown files. Here is the
`44:46` multiple markdown files. Here is the
`44:46` multiple markdown files. Here is the idea of like, "Okay, perhaps my one
`44:48` idea of like, "Okay, perhaps my one
`44:48` idea of like, "Okay, perhaps my one instance, right? I open Claude, I say,
`44:50` instance, right? I open Claude, I say,
`44:50` instance, right? I open Claude, I say, 'Read all three. We're going to write
`44:51` 'Read all three. We're going to write
`44:51` 'Read all three. We're going to write something.'" And then it knows about all
`44:53` something.'" And then it knows about all
`44:53` something.'" And then it knows about all of it. But, maybe I've already written
`44:54` of it. But, maybe I've already written
`44:54` of it. But, maybe I've already written something and I just need it to edit it
`44:56` something and I just need it to edit it
`44:56` something and I just need it to edit it and make sure there's some pillar talk
`44:58` and make sure there's some pillar talk
`44:58` and make sure there's some pillar talk in there.
`44:59` in there.
`44:59` in there. And I don't need to worry about the hard
`45:00` And I don't need to worry about the hard
`45:01` And I don't need to worry about the hard constraints as much. Or maybe I have two
`45:04` constraints as much. Or maybe I have two
`45:04` constraints as much. Or maybe I have two agents building kind of pillared script
`45:06` agents building kind of pillared script
`45:06` agents building kind of pillared script ideas and they make some really great
`45:08` ideas and they make some really great
`45:08` ideas and they make some really great sets and there's a lot of tokens and
`45:09` sets and there's a lot of tokens and
`45:10` sets and there's a lot of tokens and I've had it build like 15. And then I
`45:11` I've had it build like 15. And then I
`45:12` I've had it build like 15. And then I make another one that's not, you know,
`45:14` make another one that's not, you know,
`45:14` make another one that's not, you know, poisoned by the other chat and it's
`45:16` poisoned by the other chat and it's
`45:16` poisoned by the other chat and it's clean and small tokens and I have it go
`45:18` clean and small tokens and I have it go
`45:18` clean and small tokens and I have it go in and say, "Hey, can you go read these
`45:21` in and say, "Hey, can you go read these
`45:21` in and say, "Hey, can you go read these files that we made at scripts and just
`45:23` files that we made at scripts and just
`45:23` files that we made at scripts and just use my constraints and edit it." And
`45:25` use my constraints and edit it." And
`45:25` use my constraints and edit it." And let's see if it understood that. It
`45:26` let's see if it understood that. It
`45:26` let's see if it understood that. It should have from my prompt there, but
`45:28` should have from my prompt there, but
`45:28` should have from my prompt there, but let's find out. Constraint list is
`45:29` let's find out. Constraint list is
`45:29` let's find out. Constraint list is sharp. Yep, yep, yep. Breaks your voice.
`45:31` sharp. Yep, yep, yep. Breaks your voice.
`45:31` sharp. Yep, yep, yep. Breaks your voice. I like three. Voice and tone. How you
`45:33` I like three. Voice and tone. How you
`45:33` I like three. Voice and tone. How you sound, how you teach, how you move
`45:34` sound, how you teach, how you move
`45:34` sound, how you teach, how you move through writing. Directional, not rigid,
`45:36` through writing. Directional, not rigid,
`45:36` through writing. Directional, not rigid, hopefully. Gives me alignment without a
`45:38` hopefully. Gives me alignment without a
`45:38` hopefully. Gives me alignment without a straight jacket. Damn straight, Claude.
`45:39` straight jacket. Damn straight, Claude.
`45:39` straight jacket. Damn straight, Claude. You get me, brother.
`45:41` You get me, brother.
`45:41` You get me, brother. Former format patterns. Long form versus
`45:43` Former format patterns. Long form versus
`45:43` Former format patterns. Long form versus short form. How an IG script differs
`45:45` short form. How an IG script differs
`45:45` short form. How an IG script differs from a YouTube tutorial differs from an
`45:47` from a YouTube tutorial differs from an
`45:47` from a YouTube tutorial differs from an animation voiceover. Yes, yes, yes, yes,
`45:49` animation voiceover. Yes, yes, yes, yes,
`45:49` animation voiceover. Yes, yes, yes, yes, yes. Structural guidance for each
`45:51` yes. Structural guidance for each
`45:51` yes. Structural guidance for each format. And I don't think it needs to be
`45:53` format. And I don't think it needs to be
`45:53` format. And I don't think it needs to be that much. I think it only needs to be a
`45:54` that much. I think it only needs to be a
`45:54` that much. I think it only needs to be a paragraph per one. So, hopefully it
`45:55` paragraph per one. So, hopefully it
`45:55` paragraph per one. So, hopefully it doesn't go overboard there.
`45:57` doesn't go overboard there.
`45:57` doesn't go overboard there. Constraints. The never do this list. 12
`45:59` Constraints. The never do this list. 12
`45:59` Constraints. The never do this list. 12 rules plus anything else we add. This
`46:00` rules plus anything else we add. This
`46:00` rules plus anything else we add. This one gets read every time. That's also
`46:02` one gets read every time. That's also
`46:02` one gets read every time. That's also why it's nice, because updating
`46:04` why it's nice, because updating
`46:04` why it's nice, because updating constraints gives me a constraint file
`46:06` constraints gives me a constraint file
`46:06` constraints gives me a constraint file for all of my writings everywhere and I
`46:08` for all of my writings everywhere and I
`46:08` for all of my writings everywhere and I could use it outside of this folder as
`46:10` could use it outside of this folder as
`46:10` could use it outside of this folder as well. This is just going to be a good
`46:11` well. This is just going to be a good
`46:11` well. This is just going to be a good constraint file. Um I'd add a 13th about
`46:14` constraint file. Um I'd add a 13th about
`46:14` constraint file. Um I'd add a 13th about list-heavy structure. AI loves to
`46:16` list-heavy structure. AI loves to
`46:16` list-heavy structure. AI loves to organize everything in a number list and
`46:18` organize everything in a number list and
`46:18` organize everything in a number list and bullet points when a paragraph would
`46:19` bullet points when a paragraph would
`46:19` bullet points when a paragraph would sound more human.
`46:21` sound more human.
`46:21` sound more human. >> [snorts]
`46:22` >> [snorts]
`46:22` >> [snorts] >> Yeah, honestly, I I think that's fair.
`46:26` >> Yeah, honestly, I I think that's fair.
`46:26` >> Yeah, honestly, I I think that's fair. And I do never use that. I I was
`46:28` And I do never use that. I I was
`46:28` And I do never use that. I I was thinking about Sometimes in writing it's
`46:29` thinking about Sometimes in writing it's
`46:29` thinking about Sometimes in writing it's nice to organize that way, but I think
`46:31` nice to organize that way, but I think
`46:31` nice to organize that way, but I think that's fair.
`46:32` that's fair.
`46:33` that's fair. Um
`46:37` Yeah, are you are you just doing this
`46:37` Yeah, are you are you just doing this already?
`46:38` already?
`46:38` already? Did you just
`46:40` Did you just
`46:40` Did you just Did you say I'm going to do it? Okay.
`46:41` Did you say I'm going to do it? Okay.
`46:41` Did you say I'm going to do it? Okay. Yeah, do you want me to do it? Sure. I
`46:43` Yeah, do you want me to do it? Sure. I
`46:43` Yeah, do you want me to do it? Sure. I guess you've decided that you think you
`46:45` guess you've decided that you think you
`46:45` guess you've decided that you think you should do it. I'm okay that. Oh, I
`46:46` should do it. I'm okay that. Oh, I
`46:46` should do it. I'm okay that. Oh, I didn't mean to access your file. All
`46:48` didn't mean to access your file. All
`46:48` didn't mean to access your file. All right, whatever.
`46:49` right, whatever.
`46:49` right, whatever. Um either way, I'm going to go look at
`46:51` Um either way, I'm going to go look at
`46:51` Um either way, I'm going to go look at it in VS code. So, let's see. Brand
`46:53` it in VS code. So, let's see. Brand
`46:53` it in VS code. So, let's see. Brand voice, constraints, simple and clean.
`46:56` voice, constraints, simple and clean.
`46:56` voice, constraints, simple and clean. A little long, but that's okay. We'll
`46:58` A little long, but that's okay. We'll
`46:58` A little long, but that's okay. We'll take a look at it.
`46:59` take a look at it.
`46:59` take a look at it. Not too long. A little bit little bit
`47:01` Not too long. A little bit little bit
`47:02` Not too long. A little bit little bit shorter than the other one. This is I
`47:03` shorter than the other one. This is I
`47:03` shorter than the other one. This is I think this is a decent thing, right?
`47:04` think this is a decent thing, right?
`47:04` think this is a decent thing, right? About 46 lines to 100 lines. Once you
`47:07` About 46 lines to 100 lines. Once you
`47:07` About 46 lines to 100 lines. Once you get over 100 lines for a markdown file,
`47:09` get over 100 lines for a markdown file,
`47:09` get over 100 lines for a markdown file, you're probably you're probably missing
`47:12` you're probably you're probably missing
`47:12` you're probably you're probably missing breakdowns you could do. Like, you could
`47:13` breakdowns you could do. Like, you could
`47:13` breakdowns you could do. Like, you could realistically probably break something
`47:15` realistically probably break something
`47:15` realistically probably break something apart even more into thought process cuz
`47:18` apart even more into thought process cuz
`47:18` apart even more into thought process cuz your whole goal with these markdown
`47:20` your whole goal with these markdown
`47:20` your whole goal with these markdown files or these writings is to break
`47:22` files or these writings is to break
`47:22` files or these writings is to break apart thinking itself. When do you
`47:25` apart thinking itself. When do you
`47:25` apart thinking itself. When do you access to compartmentalize? In software,
`47:27` access to compartmentalize? In software,
`47:27` access to compartmentalize? In software, it's called separation of concerns. I'm
`47:30` it's called separation of concerns. I'm
`47:30` it's called separation of concerns. I'm just applying it to I guess
`47:32` just applying it to I guess
`47:32` just applying it to I guess neuroscience, not really neuroscience
`47:33` neuroscience, not really neuroscience
`47:33` neuroscience, not really neuroscience here. There's probably some sort of
`47:35` here. There's probably some sort of
`47:35` here. There's probably some sort of better way to look at this. I'll have to
`47:36` better way to look at this. I'll have to
`47:36` better way to look at this. I'll have to read some more neuroscience books to
`47:37` read some more neuroscience books to
`47:37` read some more neuroscience books to give you a better kind of
`47:39` give you a better kind of
`47:39` give you a better kind of um connection to psychology and whatnot,
`47:41` um connection to psychology and whatnot,
`47:41` um connection to psychology and whatnot, but not psychology. Psychology and
`47:43` but not psychology. Psychology and
`47:43` but not psychology. Psychology and neuroscience are slightly separate. I
`47:44` neuroscience are slightly separate. I
`47:44` neuroscience are slightly separate. I understand that. I'll get hate in the
`47:45` understand that. I'll get hate in the
`47:45` understand that. I'll get hate in the comments if I don't separate it. Maybe I
`47:47` comments if I don't separate it. Maybe I
`47:47` comments if I don't separate it. Maybe I won't. You guys are nice. I'm just going
`47:49` won't. You guys are nice. I'm just going
`47:49` won't. You guys are nice. I'm just going on a rant there. Anyway.
`47:50` on a rant there. Anyway.
`47:50` on a rant there. Anyway. Um
`47:52` Um
`47:52` Um Voice and tone, formats and patterns and
`47:54` Voice and tone, formats and patterns and
`47:54` Voice and tone, formats and patterns and constraints. Okay. Workspace is
`47:56` constraints. Okay. Workspace is
`47:56` constraints. Okay. Workspace is operational. We can drop in, we can move
`47:58` operational. We can drop in, we can move
`47:58` operational. We can drop in, we can move it there. Let's Let's do one final
`48:00` it there. Let's Let's do one final
`48:00` it there. Let's Let's do one final thing. So, ideas are empty, so we can go
`48:02` thing. So, ideas are empty, so we can go
`48:02` thing. So, ideas are empty, so we can go through. We're going to go and test this
`48:04` through. We're going to go and test this
`48:04` through. We're going to go and test this here soon. Let's test it on a fresh one,
`48:06` here soon. Let's test it on a fresh one,
`48:06` here soon. Let's test it on a fresh one, actually. So, let's look in.
`48:09` actually. So, let's look in.
`48:09` actually. So, let's look in. Hey, I want to make a new idea for a
`48:13` Hey, I want to make a new idea for a
`48:13` Hey, I want to make a new idea for a YouTube video. Let's see Let's see if it
`48:17` YouTube video. Let's see Let's see if it
`48:17` YouTube video. Let's see Let's see if it Let's see if it figures itself out here.
`48:19` Let's see if it figures itself out here.
`48:19` Let's see if it figures itself out here. Oops, I accidentally pasted that in, but
`48:21` Oops, I accidentally pasted that in, but
`48:21` Oops, I accidentally pasted that in, but that's okay. Um
`48:23` that's okay. Um
`48:23` that's okay. Um So, it went through, it read the
`48:25` So, it went through, it read the
`48:25` So, it went through, it read the context, it read the brand voice. It
`48:26` context, it read the brand voice. It
`48:26` context, it read the brand voice. It immediately knows where they all are
`48:28` immediately knows where they all are
`48:28` immediately knows where they all are because the Claude MD says so.
`48:32` because the Claude MD says so.
`48:32` because the Claude MD says so. Yes, Claude MD says so, good. So, we
`48:34` Yes, Claude MD says so, good. So, we
`48:34` Yes, Claude MD says so, good. So, we have our folder structure there. So, it
`48:36` have our folder structure there. So, it
`48:36` have our folder structure there. So, it shouldn't waste tokens doing blobs and
`48:38` shouldn't waste tokens doing blobs and
`48:38` shouldn't waste tokens doing blobs and trying to figure out whatever's there.
`48:39` trying to figure out whatever's there.
`48:39` trying to figure out whatever's there. It's just going straight to read them.
`48:41` It's just going straight to read them.
`48:41` It's just going straight to read them. It did have to do a glob here because it
`48:42` It did have to do a glob here because it
`48:42` It did have to do a glob here because it was checking to see if we had any ideas
`48:44` was checking to see if we had any ideas
`48:44` was checking to see if we had any ideas made already.
`48:46` made already.
`48:46` made already. I might actually edit that because I
`48:48` I might actually edit that because I
`48:48` I might actually edit that because I don't necessarily want it to read all of
`48:49` don't necessarily want it to read all of
`48:49` don't necessarily want it to read all of my ideas all the time, at least in that
`48:52` my ideas all the time, at least in that
`48:52` my ideas all the time, at least in that way, cuz I want to be careful how it's
`48:54` way, cuz I want to be careful how it's
`48:54` way, cuz I want to be careful how it's reading too much. Um it looks like it's
`48:57` reading too much. Um it looks like it's
`48:57` reading too much. Um it looks like it's reading a
`48:58` reading a
`48:58` reading a bunch. So, I would have I mean, that's
`48:59` bunch. So, I would have I mean, that's
`48:59` bunch. So, I would have I mean, that's just because I gave it really terrible
`49:01` just because I gave it really terrible
`49:01` just because I gave it really terrible description. I would say and
`49:03` description. I would say and
`49:03` description. I would say and compartmentalize, "Hey, I want to make
`49:05` compartmentalize, "Hey, I want to make
`49:05` compartmentalize, "Hey, I want to make an idea, use these pillars for real
`49:07` an idea, use these pillars for real
`49:07` an idea, use these pillars for real quick." But, ideas folder's empty, so it
`49:09` quick." But, ideas folder's empty, so it
`49:09` quick." But, ideas folder's empty, so it says, "What direction you learning?" Um
`49:11` says, "What direction you learning?" Um
`49:11` says, "What direction you learning?" Um format, tutorial. Let's do a tutorial
`49:16` format, tutorial. Let's do a tutorial
`49:16` format, tutorial. Let's do a tutorial style
`49:18` style
`49:18` style with a focus on ecosystem
`49:22` with a focus on ecosystem
`49:22` with a focus on ecosystem and foundations.
`49:25` and foundations.
`49:25` and foundations. Um I want to
`49:29` Um I want to
`49:29` Um I want to make it a little more technical, but
`49:34` make it a little more technical, but
`49:34` make it a little more technical, but have the non-technical
`49:36` have the non-technical
`49:36` have the non-technical group be able to understand it. So, you
`49:39` group be able to understand it. So, you
`49:40` group be able to understand it. So, you see right here, this is hundreds of
`49:42` see right here, this is hundreds of
`49:42` see right here, this is hundreds of functions that are being called right
`49:43` functions that are being called right
`49:43` functions that are being called right now essentially. This is telling it it's
`49:45` now essentially. This is telling it it's
`49:45` now essentially. This is telling it it's immediately reading my ecosystem one.
`49:47` immediately reading my ecosystem one.
`49:47` immediately reading my ecosystem one. It's understanding all of the concepts
`49:48` It's understanding all of the concepts
`49:48` It's understanding all of the concepts and foundation. It's looking at tutorial
`49:51` and foundation. It's looking at tutorial
`49:51` and foundation. It's looking at tutorial style, right? Our formats and patterns.
`49:54` style, right? Our formats and patterns.
`49:54` style, right? Our formats and patterns. It's understanding the audience, which
`49:56` It's understanding the audience, which
`49:56` It's understanding the audience, which is technical, right? So, if we come uh
`49:59` is technical, right? So, if we come uh
`49:59` is technical, right? So, if we come uh to
`50:01` to
`50:01` to Did we not make an audience one? Oh, oh
`50:02` Did we not make an audience one? Oh, oh
`50:02` Did we not make an audience one? Oh, oh yeah, okay. Uh what these people
`50:04` yeah, okay. Uh what these people
`50:04` yeah, okay. Uh what these people actually need, right? So, what this
`50:06` actually need, right? So, what this
`50:06` actually need, right? So, what this pillar covers, skills, things like that.
`50:08` pillar covers, skills, things like that.
`50:08` pillar covers, skills, things like that. It's all embedded into here and we
`50:10` It's all embedded into here and we
`50:10` It's all embedded into here and we actually have the comments. OH, IT ADDED
`50:12` actually have the comments. OH, IT ADDED
`50:12` actually have the comments. OH, IT ADDED the comments TO IT. I DIDN'T even think
`50:14` the comments TO IT. I DIDN'T even think
`50:14` the comments TO IT. I DIDN'T even think about that. That's actually brilliant.
`50:15` about that. That's actually brilliant.
`50:15` about that. That's actually brilliant. Having the comments of what people said
`50:18` Having the comments of what people said
`50:18` Having the comments of what people said from a question-wise.
`50:20` from a question-wise.
`50:20` from a question-wise. Oh, that's fire. I didn't even think
`50:22` Oh, that's fire. I didn't even think
`50:22` Oh, that's fire. I didn't even think about it. Good job. High five, Claude.
`50:23` about it. Good job. High five, Claude.
`50:23` about it. Good job. High five, Claude. High five. That was fire. Um and see now
`50:26` High five. That was fire. Um and see now
`50:26` High five. That was fire. Um and see now it's actually going to be able to go
`50:27` it's actually going to be able to go
`50:27` it's actually going to be able to go through that. And now I get to just work
`50:29` through that. And now I get to just work
`50:29` through that. And now I get to just work with my ideas, right? My writing, my
`50:31` with my ideas, right? My writing, my
`50:31` with my ideas, right? My writing, my concepts in a much faster way, much
`50:34` concepts in a much faster way, much
`50:34` concepts in a much faster way, much cleaner. And I could do the same thing
`50:35` cleaner. And I could do the same thing
`50:35` cleaner. And I could do the same thing inside the app. I could ask it that same
`50:37` inside the app. I could ask it that same
`50:37` inside the app. I could ask it that same exact thing and it's going to do the
`50:39` exact thing and it's going to do the
`50:39` exact thing and it's going to do the same process.
`50:41` same process.
`50:41` same process. Um
`50:42` Um
`50:42` Um but that is built and hopefully you'll
`50:45` but that is built and hopefully you'll
`50:45` but that is built and hopefully you'll all see a video of something built from
`50:48` all see a video of something built from
`50:48` all see a video of something built from this and it answers a lot of questions.
`50:50` this and it answers a lot of questions.
`50:50` this and it answers a lot of questions. But yeah, cool. That is That's the
`50:52` But yeah, cool. That is That's the
`50:52` But yeah, cool. That is That's the video. I think this is a long one. I
`50:53` video. I think this is a long one. I
`50:53` video. I think this is a long one. I don't even know how long I've been
`50:54` don't even know how long I've been
`50:54` don't even know how long I've been talking now, but hopefully someone's
`50:56` talking now, but hopefully someone's
`50:56` talking now, but hopefully someone's still here at the end. Either way,
`50:58` still here at the end. Either way,
`50:58` still here at the end. Either way, my name's Jake Van Cleef. If you're not
`51:00` my name's Jake Van Cleef. If you're not
`51:00` my name's Jake Van Cleef. If you're not part of my community, please go join it.
`51:02` part of my community, please go join it.
`51:02` part of my community, please go join it. It's free. We've got plenty of things
`51:04` It's free. We've got plenty of things
`51:04` It's free. We've got plenty of things there. If you have any questions,
`51:05` there. If you have any questions,
`51:05` there. If you have any questions, comments, concerns, drop them in the
`51:07` comments, concerns, drop them in the
`51:07` comments, concerns, drop them in the comments. That always helps me out. As
`51:09` comments. That always helps me out. As
`51:09` comments. That always helps me out. As you can see, I actually am using the
`51:11` you can see, I actually am using the
`51:11` you can see, I actually am using the crap out of them to make new stuff. So,
`51:14` crap out of them to make new stuff. So,
`51:14` crap out of them to make new stuff. So, if you want to affect and change what
`51:15` if you want to affect and change what
`51:15` if you want to affect and change what I'm making, comment below and let's see
`51:18` I'm making, comment below and let's see
`51:18` I'm making, comment below and let's see what we can do. Other than that, happy
`51:20` what we can do. Other than that, happy
`51:20` what we can do. Other than that, happy learning, everyone.