# How I Run Creative, Software, and Business Work as One System (Ai memory vs Human memory?)

**Channel:** Jake Van Clief  
**Uploaded:** 2026-04-28  
**Duration:** 14:04  
**URL:** [https://www.youtube.com/watch?v=hALln9wrrQo](https://www.youtube.com/watch?v=hALln9wrrQo)  

---

## Transcript

`00:02` So, most people treat creative work,
`00:02` So, most people treat creative work, technical work, and business work like
`00:04` technical work, and business work like
`00:04` technical work, and business work like they're different problems, but they're
`00:05` they're different problems, but they're
`00:05` they're different problems, but they're the same problem wearing different
`00:07` the same problem wearing different
`00:07` the same problem wearing different clothes. All right. Now, a year or two
`00:09` clothes. All right. Now, a year or two
`00:09` clothes. All right. Now, a year or two ago, sure, maybe we had different
`00:11` ago, sure, maybe we had different
`00:11` ago, sure, maybe we had different consultancies and whatnot. You had
`00:13` consultancies and whatnot. You had
`00:13` consultancies and whatnot. You had creative consultancies. You had business
`00:14` creative consultancies. You had business
`00:14` creative consultancies. You had business consultancies. You had technical
`00:16` consultancies. You had technical
`00:16` consultancies. You had technical consultancies. I've wrote a lot of
`00:18` consultancies. I've wrote a lot of
`00:18` consultancies. I've wrote a lot of articles on this, but they're kind of
`00:20` articles on this, but they're kind of
`00:20` articles on this, but they're kind of all one thing now because of AI. If you
`00:23` all one thing now because of AI. If you
`00:23` all one thing now because of AI. If you use AI properly internally. I'm going to
`00:25` use AI properly internally. I'm going to
`00:25` use AI properly internally. I'm going to show you what I mean by looking at one
`00:27` show you what I mean by looking at one
`00:27` show you what I mean by looking at one project through three different lenses.
`00:29` project through three different lenses.
`00:29` project through three different lenses. And by the end, hopefully, you're going
`00:31` And by the end, hopefully, you're going
`00:31` And by the end, hopefully, you're going to see why each lens was telling the
`00:32` to see why each lens was telling the
`00:32` to see why each lens was telling the same story. So, here's an artifact, a
`00:35` same story. So, here's an artifact, a
`00:35` same story. So, here's an artifact, a thing that I'm actually using right now
`00:36` thing that I'm actually using right now
`00:37` thing that I'm actually using right now to make money. People are paying me to
`00:39` to make money. People are paying me to
`00:39` to make money. People are paying me to do this. NLP Logics is a AI company,
`00:41` do this. NLP Logics is a AI company,
`00:41` do this. NLP Logics is a AI company, machine learning company. They were
`00:42` machine learning company. They were
`00:42` machine learning company. They were founded in 2011. So, they've been in
`00:45` founded in 2011. So, they've been in
`00:45` founded in 2011. So, they've been in machine learning and data science way
`00:47` machine learning and data science way
`00:47` machine learning and data science way longer than most of these people.
`00:48` longer than most of these people.
`00:48` longer than most of these people. Essentially, they wanted to work with me
`00:50` Essentially, they wanted to work with me
`00:50` Essentially, they wanted to work with me for a lot of the things I was doing, but
`00:51` for a lot of the things I was doing, but
`00:51` for a lot of the things I was doing, but specifically, they wanted four video
`00:53` specifically, they wanted four video
`00:53` specifically, they wanted four video animation series. They also wanted brand
`00:55` animation series. They also wanted brand
`00:55` animation series. They also wanted brand deck, some voiceover scripts, custom
`00:57` deck, some voiceover scripts, custom
`00:57` deck, some voiceover scripts, custom animation patterns, right? The whole
`00:59` animation patterns, right? The whole
`00:59` animation patterns, right? The whole pipeline for a new movement they're
`01:01` pipeline for a new movement they're
`01:01` pipeline for a new movement they're doing. And I promise you, they're going
`01:02` doing. And I promise you, they're going
`01:02` doing. And I promise you, they're going to be a much bigger company here soon
`01:03` to be a much bigger company here soon
`01:03` to be a much bigger company here soon working with me, but that's beside the
`01:05` working with me, but that's beside the
`01:05` working with me, but that's beside the point. Most agencies would take three
`01:07` point. Most agencies would take three
`01:07` point. Most agencies would take three weeks for one video. I delivered four
`01:10` weeks for one video. I delivered four
`01:10` weeks for one video. I delivered four fully animated, fully voiced videos in a
`01:12` fully animated, fully voiced videos in a
`01:13` fully animated, fully voiced videos in a day. The clients are real, the work is
`01:15` day. The clients are real, the work is
`01:15` day. The clients are real, the work is real, my process is real, and it's all
`01:18` real, my process is real, and it's all
`01:18` real, my process is real, and it's all in a single folder.
`01:20` in a single folder.
`01:20` in a single folder. I I'm not joking. I'm using one folder
`01:22` I I'm not joking. I'm using one folder
`01:22` I I'm not joking. I'm using one folder as my app for all of this. On the
`01:24` as my app for all of this. On the
`01:24` as my app for all of this. On the surface, it looks like three different
`01:25` surface, it looks like three different
`01:25` surface, it looks like three different jobs. I have creative work, I have
`01:27` jobs. I have creative work, I have
`01:27` jobs. I have creative work, I have software work, I also have business
`01:29` software work, I also have business
`01:29` software work, I also have business work. I'm thinking of strategy, what
`01:31` work. I'm thinking of strategy, what
`01:31` work. I'm thinking of strategy, what should be said in the videos, where is
`01:33` should be said in the videos, where is
`01:33` should be said in the videos, where is it going to bring them clients, where is
`01:34` it going to bring them clients, where is
`01:34` it going to bring them clients, where is the money? And most people would tell
`01:36` the money? And most people would tell
`01:36` the money? And most people would tell you that's three different skill sets,
`01:37` you that's three different skill sets,
`01:37` you that's three different skill sets, three different teams, three different
`01:39` three different teams, three different
`01:39` three different teams, three different bills, but my folder shows you something
`01:42` bills, but my folder shows you something
`01:42` bills, but my folder shows you something entirely different. Think about this.
`01:44` entirely different. Think about this.
`01:44` entirely different. Think about this. Someone goes to give Claude memory,
`01:46` Someone goes to give Claude memory,
`01:46` Someone goes to give Claude memory, right? They read Karpathy's gist last
`01:48` right? They read Karpathy's gist last
`01:49` right? They read Karpathy's gist last week, or they saw OpenBrain tutorial on
`01:51` week, or they saw OpenBrain tutorial on
`01:51` week, or they saw OpenBrain tutorial on YouTube. They sit down, they open their
`01:53` YouTube. They sit down, they open their
`01:53` YouTube. They sit down, they open their laptop, and they start setting up a
`01:55` laptop, and they start setting up a
`01:55` laptop, and they start setting up a vector database, PG vector, embedding
`01:57` vector database, PG vector, embedding
`01:57` vector database, PG vector, embedding model, MCP server, Obsidian second
`02:00` model, MCP server, Obsidian second
`02:00` model, MCP server, Obsidian second brain. By Tuesday, they've got it
`02:01` brain. By Tuesday, they've got it
`02:01` brain. By Tuesday, they've got it indexing three years of notes, and it's
`02:04` indexing three years of notes, and it's
`02:04` indexing three years of notes, and it's still pulling the wrong things. It's
`02:06` still pulling the wrong things. It's
`02:06` still pulling the wrong things. It's almost completely useless. Here's what I
`02:08` almost completely useless. Here's what I
`02:08` almost completely useless. Here's what I think is happening. There are three
`02:09` think is happening. There are three
`02:09` think is happening. There are three different bets right now on what memory
`02:11` different bets right now on what memory
`02:11` different bets right now on what memory means for a large language model, or for
`02:14` means for a large language model, or for
`02:14` means for a large language model, or for AI in general. And most people pick the
`02:16` AI in general. And most people pick the
`02:16` AI in general. And most people pick the loudest one without realizing the other
`02:18` loudest one without realizing the other
`02:18` loudest one without realizing the other two exist. The first bet is OpenBrain,
`02:21` two exist. The first bet is OpenBrain,
`02:21` two exist. The first bet is OpenBrain, vector embeddings. You take everything
`02:23` vector embeddings. You take everything
`02:23` vector embeddings. You take everything you've ever written, you turn it into
`02:24` you've ever written, you turn it into
`02:24` you've ever written, you turn it into numbers, you ask the model to find
`02:26` numbers, you ask the model to find
`02:26` numbers, you ask the model to find what's similar. It works. It's also slow
`02:28` what's similar. It works. It's also slow
`02:28` what's similar. It works. It's also slow to set up, expensive to maintain, and
`02:30` to set up, expensive to maintain, and
`02:30` to set up, expensive to maintain, and sometimes it can be very brittle until
`02:33` sometimes it can be very brittle until
`02:33` sometimes it can be very brittle until you don't notice that you're searching
`02:34` you don't notice that you're searching
`02:34` you don't notice that you're searching for something specific, and then the
`02:35` for something specific, and then the
`02:35` for something specific, and then the model returns nine vaguely related
`02:37` model returns nine vaguely related
`02:37` model returns nine vaguely related notes. This doesn't always happen, and
`02:39` notes. This doesn't always happen, and
`02:39` notes. This doesn't always happen, and you can set up amazing OpenBrain and
`02:41` you can set up amazing OpenBrain and
`02:41` you can set up amazing OpenBrain and Second Obsidian systems. This is not a
`02:43` Second Obsidian systems. This is not a
`02:43` Second Obsidian systems. This is not a hate or a criticism of that. I'm just
`02:46` hate or a criticism of that. I'm just
`02:46` hate or a criticism of that. I'm just describing the problems here. The second
`02:48` describing the problems here. The second
`02:48` describing the problems here. The second bet is that Karpathy's LLM wiki, which
`02:50` bet is that Karpathy's LLM wiki, which
`02:50` bet is that Karpathy's LLM wiki, which is really fun. I've used this and built
`02:52` is really fun. I've used this and built
`02:52` is really fun. I've used this and built some of my own myself, and he posted it
`02:54` some of my own myself, and he posted it
`02:54` some of my own myself, and he posted it earlier this month. Basically, it's
`02:56` earlier this month. Basically, it's
`02:56` earlier this month. Basically, it's similar to my ICM architecture, but the
`02:58` similar to my ICM architecture, but the
`02:58` similar to my ICM architecture, but the idea is that markdown files that an
`03:00` idea is that markdown files that an
`03:00` idea is that markdown files that an agent or AI can actively maintain.
`03:03` agent or AI can actively maintain.
`03:03` agent or AI can actively maintain. There's entity pages, audited regularly,
`03:05` There's entity pages, audited regularly,
`03:05` There's entity pages, audited regularly, contradictions are flagged, orphan pages
`03:07` contradictions are flagged, orphan pages
`03:07` contradictions are flagged, orphan pages are founded. It's smart, and it's the
`03:09` are founded. It's smart, and it's the
`03:09` are founded. It's smart, and it's the closest thing we have to Vannevar's Bush
`03:12` closest thing we have to Vannevar's Bush
`03:12` closest thing we have to Vannevar's Bush Memex from 1945.
`03:14` Memex from 1945.
`03:14` Memex from 1945. If you don't know what that is, go give
`03:15` If you don't know what that is, go give
`03:15` If you don't know what that is, go give that a Google. The trade is that you
`03:17` that a Google. The trade is that you
`03:17` that a Google. The trade is that you need a maintenance LLM running
`03:19` need a maintenance LLM running
`03:19` need a maintenance LLM running constantly, and the wiki only stays
`03:22` constantly, and the wiki only stays
`03:22` constantly, and the wiki only stays useful if the LLM keeps tending it. And
`03:24` useful if the LLM keeps tending it. And
`03:25` useful if the LLM keeps tending it. And we see other versions of this, like
`03:26` we see other versions of this, like
`03:26` we see other versions of this, like Hermes and different open source. And
`03:28` Hermes and different open source. And
`03:28` Hermes and different open source. And again, very useful, but there are those
`03:31` again, very useful, but there are those
`03:31` again, very useful, but there are those downsides there.
`03:32` downsides there.
`03:32` downsides there. Then, there's a third bet, and I don't
`03:35` Then, there's a third bet, and I don't
`03:35` Then, there's a third bet, and I don't think a lot of people are talking about
`03:36` think a lot of people are talking about
`03:36` think a lot of people are talking about it because it's not new enough to feel
`03:38` it because it's not new enough to feel
`03:39` it because it's not new enough to feel like a product, and it's kind of
`03:40` like a product, and it's kind of
`03:40` like a product, and it's kind of informal. It's the folder system you
`03:43` informal. It's the folder system you
`03:43` informal. It's the folder system you already have and your own brain.
`03:46` already have and your own brain.
`03:46` already have and your own brain. Think about it like this. If you're
`03:47` Think about it like this. If you're
`03:47` Think about it like this. If you're using Codex or Claude or Gemini CLI or a
`03:51` using Codex or Claude or Gemini CLI or a
`03:51` using Codex or Claude or Gemini CLI or a local model on your computer, when it
`03:53` local model on your computer, when it
`03:53` local model on your computer, when it opens a file at video production
`03:55` opens a file at video production
`03:55` opens a file at video production projects NLP Logics scene three,
`03:58` projects NLP Logics scene three,
`03:58` projects NLP Logics scene three, it's a path already told it everything.
`04:02` it's a path already told it everything.
`04:02` it's a path already told it everything. The video work, the client, pattern A
`04:04` The video work, the client, pattern A
`04:04` The video work, the client, pattern A pipeline, scene file conventions.
`04:06` pipeline, scene file conventions.
`04:06` pipeline, scene file conventions. AGENTS.md at the root cascades down.
`04:09` AGENTS.md at the root cascades down.
`04:09` AGENTS.md at the root cascades down. Context.md inside the project layers on
`04:11` Context.md inside the project layers on
`04:11` Context.md inside the project layers on top. By the time Claude reads the scene,
`04:14` top. By the time Claude reads the scene,
`04:14` top. By the time Claude reads the scene, it's already inherited the brand voice,
`04:16` it's already inherited the brand voice,
`04:16` it's already inherited the brand voice, the production rules, the specific shot
`04:18` the production rules, the specific shot
`04:18` the production rules, the specific shot list. And if you haven't understood my
`04:21` list. And if you haven't understood my
`04:21` list. And if you haven't understood my folder architecture, a folder
`04:22` folder architecture, a folder
`04:22` folder architecture, a folder architecture similar to this, you should
`04:24` architecture similar to this, you should
`04:24` architecture similar to this, you should go check out my other YouTube videos
`04:25` go check out my other YouTube videos
`04:25` go check out my other YouTube videos where I dive really deep on why and how
`04:28` where I dive really deep on why and how
`04:28` where I dive really deep on why and how I built these, and it's much more
`04:29` I built these, and it's much more
`04:29` I built these, and it's much more hands-on than what you're seeing in this
`04:30` hands-on than what you're seeing in this
`04:30` hands-on than what you're seeing in this animated video, but nothing got
`04:32` animated video, but nothing got
`04:32` animated video, but nothing got embedded, nothing's getting retrieved.
`04:35` embedded, nothing's getting retrieved.
`04:35` embedded, nothing's getting retrieved. The location of the information and the
`04:38` The location of the information and the
`04:38` The location of the information and the the actual routing of it did the work.
`04:41` the actual routing of it did the work.
`04:41` the actual routing of it did the work. This isn't new. Unix has done this since
`04:43` This isn't new. Unix has done this since
`04:43` This isn't new. Unix has done this since 1969. Environment variables scope down.
`04:46` 1969. Environment variables scope down.
`04:46` 1969. Environment variables scope down. CSS specificity scopes down. Your
`04:48` CSS specificity scopes down. Your
`04:48` CSS specificity scopes down. Your accountant filing cabinet does it, too.
`04:51` accountant filing cabinet does it, too.
`04:51` accountant filing cabinet does it, too. The folder name is a namespace. The
`04:53` The folder name is a namespace. The
`04:53` The folder name is a namespace. The namespace carries meaning. We just
`04:55` namespace carries meaning. We just
`04:55` namespace carries meaning. We just didn't have a model that could read it
`04:57` didn't have a model that could read it
`04:57` didn't have a model that could read it before.
`04:58` before.
`04:58` before. OpenBrain is content addressed.
`05:01` OpenBrain is content addressed.
`05:01` OpenBrain is content addressed. Karpathy's wiki is link addressed. The
`05:03` Karpathy's wiki is link addressed. The
`05:03` Karpathy's wiki is link addressed. The folder system is position addressed. And
`05:07` folder system is position addressed. And
`05:07` folder system is position addressed. And all three work, and theoretically, you
`05:09` all three work, and theoretically, you
`05:09` all three work, and theoretically, you could use all three together. For the
`05:11` could use all three together. For the
`05:11` could use all three together. For the kind of work you ship to a client by
`05:13` kind of work you ship to a client by
`05:13` kind of work you ship to a client by Friday, in my opinion, position is the
`05:15` Friday, in my opinion, position is the
`05:15` Friday, in my opinion, position is the cheapest, the fastest, and the only one
`05:17` cheapest, the fastest, and the only one
`05:17` cheapest, the fastest, and the only one that's still legible to a human, and a
`05:19` that's still legible to a human, and a
`05:20` that's still legible to a human, and a human is in the loop because it's more
`05:22` human is in the loop because it's more
`05:22` human is in the loop because it's more efficient. I remember where things are.
`05:25` efficient. I remember where things are.
`05:25` efficient. I remember where things are. I have context. My brain is a powerful
`05:28` I have context. My brain is a powerful
`05:28` I have context. My brain is a powerful context machine. We are creating systems
`05:31` context machine. We are creating systems
`05:31` context machine. We are creating systems that allow me to offload context to
`05:33` that allow me to offload context to
`05:33` that allow me to offload context to Claude or to a different AI using
`05:35` Claude or to a different AI using
`05:35` Claude or to a different AI using folders while simultaneously using my
`05:37` folders while simultaneously using my
`05:37` folders while simultaneously using my context to amplify that process. I'm
`05:40` context to amplify that process. I'm
`05:40` context to amplify that process. I'm still directing the AI. My prompts that
`05:43` still directing the AI. My prompts that
`05:43` still directing the AI. My prompts that are directing Claude or Codex through my
`05:46` are directing Claude or Codex through my
`05:46` are directing Claude or Codex through my file structure is an added layer of
`05:48` file structure is an added layer of
`05:48` file structure is an added layer of context that can be on the fly and is
`05:50` context that can be on the fly and is
`05:51` context that can be on the fly and is extremely efficient, extremely quick,
`05:53` extremely efficient, extremely quick,
`05:53` extremely efficient, extremely quick, and can be updated on the fly because
`05:55` and can be updated on the fly because
`05:55` and can be updated on the fly because that's how the human brain works. It's
`05:57` that's how the human brain works. It's
`05:57` that's how the human brain works. It's also infinitely abstracting. I really do
`06:00` also infinitely abstracting. I really do
`06:00` also infinitely abstracting. I really do believe that humans can have infinite
`06:03` believe that humans can have infinite
`06:03` believe that humans can have infinite levels of creative abstraction, but at
`06:05` levels of creative abstraction, but at
`06:05` levels of creative abstraction, but at the end of the day, the person who's
`06:07` the end of the day, the person who's
`06:07` the end of the day, the person who's setting up a PG vector on Tuesday, they
`06:09` setting up a PG vector on Tuesday, they
`06:09` setting up a PG vector on Tuesday, they had a folder system the whole time. They
`06:11` had a folder system the whole time. They
`06:11` had a folder system the whole time. They just thought memory had to look like a
`06:13` just thought memory had to look like a
`06:13` just thought memory had to look like a brain rather than get an outcome out of
`06:16` brain rather than get an outcome out of
`06:16` brain rather than get an outcome out of it. And this is really the important
`06:18` it. And this is really the important
`06:18` it. And this is really the important part. A lot of people are talking about
`06:20` part. A lot of people are talking about
`06:20` part. A lot of people are talking about features and processes, and that's fine.
`06:22` features and processes, and that's fine.
`06:22` features and processes, and that's fine. There's amazing ones out there, but
`06:23` There's amazing ones out there, but
`06:23` There's amazing ones out there, but think about outcomes. When a new client
`06:25` think about outcomes. When a new client
`06:25` think about outcomes. When a new client comes in, I don't start from scratch. I
`06:28` comes in, I don't start from scratch. I
`06:28` comes in, I don't start from scratch. I copy or use a folder system that already
`06:31` copy or use a folder system that already
`06:31` copy or use a folder system that already exists. The folder already has routing
`06:33` exists. The folder already has routing
`06:33` exists. The folder already has routing rules at the root, the context files in
`06:35` rules at the root, the context files in
`06:35` rules at the root, the context files in each subfolder, the brand templates, the
`06:36` each subfolder, the brand templates, the
`06:36` each subfolder, the brand templates, the voice stocks, the scene primitives, the
`06:38` voice stocks, the scene primitives, the
`06:38` voice stocks, the scene primitives, the export settings. The folder is my
`06:41` export settings. The folder is my
`06:41` export settings. The folder is my agency, and it's just locally on my
`06:43` agency, and it's just locally on my
`06:43` agency, and it's just locally on my computer. I just put the client inside
`06:45` computer. I just put the client inside
`06:45` computer. I just put the client inside of it as if for me. Now, of course, I
`06:48` of it as if for me. Now, of course, I
`06:48` of it as if for me. Now, of course, I could productionize this and make a
`06:50` could productionize this and make a
`06:50` could productionize this and make a product out of it, but why when I can
`06:52` product out of it, but why when I can
`06:52` product out of it, but why when I can sell the outcome regardless? I can hand
`06:54` sell the outcome regardless? I can hand
`06:55` sell the outcome regardless? I can hand this folder to other people who work
`06:56` this folder to other people who work
`06:56` this folder to other people who work with me, and they immediately have my
`06:58` with me, and they immediately have my
`06:58` with me, and they immediately have my context. There's no need to
`07:00` context. There's no need to
`07:00` context. There's no need to productionize it. However, I am building
`07:02` productionize it. However, I am building
`07:03` productionize it. However, I am building a production version of this involving
`07:04` a production version of this involving
`07:04` a production version of this involving Git and various Azure DevOps frameworks.
`07:07` Git and various Azure DevOps frameworks.
`07:07` Git and various Azure DevOps frameworks. I might even make it simpler with just
`07:08` I might even make it simpler with just
`07:08` I might even make it simpler with just GitHub to go proof of concept, but the
`07:10` GitHub to go proof of concept, but the
`07:10` GitHub to go proof of concept, but the idea is there. The NLP Logics project
`07:14` idea is there. The NLP Logics project
`07:14` idea is there. The NLP Logics project sits inside project/nlplogics.
`07:17` sits inside project/nlplogics.
`07:17` sits inside project/nlplogics. Four video series, each video gets its
`07:19` Four video series, each video gets its
`07:19` Four video series, each video gets its own pipeline. The brand voice cascades
`07:21` own pipeline. The brand voice cascades
`07:21` own pipeline. The brand voice cascades down from the project root. The
`07:23` down from the project root. The
`07:23` down from the project root. The animation patterns cascade in from the
`07:25` animation patterns cascade in from the
`07:25` animation patterns cascade in from the studio framework. I'm not designing the
`07:27` studio framework. I'm not designing the
`07:27` studio framework. I'm not designing the system every time. I'm filling it in.
`07:30` system every time. I'm filling it in.
`07:30` system every time. I'm filling it in. I'm adding an extra folder and building
`07:32` I'm adding an extra folder and building
`07:32` I'm adding an extra folder and building off of what has already been built. And
`07:35` off of what has already been built. And
`07:35` off of what has already been built. And then, something interesting happens.
`07:37` then, something interesting happens.
`07:37` then, something interesting happens. Other companies start asking how I do
`07:39` Other companies start asking how I do
`07:39` Other companies start asking how I do it. They want the system, not the
`07:40` it. They want the system, not the
`07:40` it. They want the system, not the videos. The folder structure is the IP.
`07:43` videos. The folder structure is the IP.
`07:43` videos. The folder structure is the IP. I can license I can implement it inside
`07:45` I can license I can implement it inside
`07:45` I can license I can implement it inside their org. I can become their advisor in
`07:48` their org. I can become their advisor in
`07:48` their org. I can become their advisor in their internal system instead of an
`07:49` their internal system instead of an
`07:49` their internal system instead of an external vendor. It's the same artifact,
`07:52` external vendor. It's the same artifact,
`07:52` external vendor. It's the same artifact, but a completely different business
`07:53` but a completely different business
`07:53` but a completely different business model. I'm selling a system, and the
`07:56` model. I'm selling a system, and the
`07:56` model. I'm selling a system, and the videos are just one of the outputs of
`08:00` videos are just one of the outputs of
`08:00` videos are just one of the outputs of that system. Here's what people get
`08:01` that system. Here's what people get
`08:01` that system. Here's what people get wrong about creative work and AI. They
`08:04` wrong about creative work and AI. They
`08:04` wrong about creative work and AI. They assume the model is replacing taste. The
`08:06` assume the model is replacing taste. The
`08:06` assume the model is replacing taste. The model is doing the opposite. It's
`08:08` model is doing the opposite. It's
`08:08` model is doing the opposite. It's handling everything around the taste, so
`08:10` handling everything around the taste, so
`08:10` handling everything around the taste, so you have more of it left over for the
`08:12` you have more of it left over for the
`08:12` you have more of it left over for the work. This is not to say that people
`08:14` work. This is not to say that people
`08:14` work. This is not to say that people aren't being lazy or using it the wrong
`08:16` aren't being lazy or using it the wrong
`08:16` aren't being lazy or using it the wrong way. There's a whole bunch of AI slop
`08:18` way. There's a whole bunch of AI slop
`08:18` way. There's a whole bunch of AI slop out there, but at the end of the day,
`08:20` out there, but at the end of the day,
`08:20` out there, but at the end of the day, there's a lot of things you can do with
`08:22` there's a lot of things you can do with
`08:22` there's a lot of things you can do with it. Case in point, I cloned my voice
`08:24` it. Case in point, I cloned my voice
`08:24` it. Case in point, I cloned my voice with ElevenLabs months ago, and I do use
`08:27` with ElevenLabs months ago, and I do use
`08:27` with ElevenLabs months ago, and I do use it for client work. Sometimes, people
`08:29` it for client work. Sometimes, people
`08:29` it for client work. Sometimes, people ask if that means I'm not really making
`08:31` ask if that means I'm not really making
`08:31` ask if that means I'm not really making the content, but I'm actually making way
`08:33` the content, but I'm actually making way
`08:33` the content, but I'm actually making way more of it. The voice clone reads the
`08:36` more of it. The voice clone reads the
`08:36` more of it. The voice clone reads the script. I write the script. I edit the
`08:37` script. I write the script. I edit the
`08:37` script. I write the script. I edit the script for hours. I think about the
`08:39` script for hours. I think about the
`08:39` script for hours. I think about the strategic needs for it. Why would it be
`08:41` strategic needs for it. Why would it be
`08:41` strategic needs for it. Why would it be there? I record the parts that need real
`08:43` there? I record the parts that need real
`08:43` there? I record the parts that need real performance. I composite the sections. I
`08:46` performance. I composite the sections. I
`08:46` performance. I composite the sections. I rebuild the animation when it doesn't
`08:47` rebuild the animation when it doesn't
`08:47` rebuild the animation when it doesn't land. And the system gives me back the
`08:51` land. And the system gives me back the
`08:51` land. And the system gives me back the hours I used to spend on the parts that
`08:53` hours I used to spend on the parts that
`08:53` hours I used to spend on the parts that don't matter or required 10, 15, 20
`08:56` don't matter or required 10, 15, 20
`08:56` don't matter or required 10, 15, 20 employees choosing colors that match the
`08:59` employees choosing colors that match the
`08:59` employees choosing colors that match the brand guide, re- recording sections to
`09:00` brand guide, re- recording sections to
`09:00` brand guide, re- recording sections to match the previous take, renaming files,
`09:02` match the previous take, renaming files,
`09:02` match the previous take, renaming files, resizing exports for different
`09:04` resizing exports for different
`09:04` resizing exports for different platforms. None of that is creative
`09:06` platforms. None of that is creative
`09:06` platforms. None of that is creative work. It's logistics with a creative
`09:08` work. It's logistics with a creative
`09:08` work. It's logistics with a creative project attached. The folder handles the
`09:12` project attached. The folder handles the
`09:12` project attached. The folder handles the logistics for me. The AI is amplifying
`09:15` logistics for me. The AI is amplifying
`09:15` logistics for me. The AI is amplifying the speed in which I can get those
`09:16` the speed in which I can get those
`09:16` the speed in which I can get those logistics done and the ease in which I
`09:19` logistics done and the ease in which I
`09:19` logistics done and the ease in which I can do it, but I'm still in control of
`09:22` can do it, but I'm still in control of
`09:22` can do it, but I'm still in control of the creative work. When I deliver a
`09:24` the creative work. When I deliver a
`09:24` the creative work. When I deliver a brand deck for a client, or I'm using
`09:26` brand deck for a client, or I'm using
`09:26` brand deck for a client, or I'm using the brand deck internally just to help
`09:27` the brand deck internally just to help
`09:28` the brand deck internally just to help with my automated process, the deck has
`09:30` with my automated process, the deck has
`09:30` with my automated process, the deck has voice samples, type pairings, color
`09:32` voice samples, type pairings, color
`09:32` voice samples, type pairings, color logic, animation patterns, the actual
`09:33` logic, animation patterns, the actual
`09:33` logic, animation patterns, the actual video pipeline they'll use. I've had
`09:35` video pipeline they'll use. I've had
`09:35` video pipeline they'll use. I've had clients tell me, and hey, I don't like
`09:37` clients tell me, and hey, I don't like
`09:37` clients tell me, and hey, I don't like this video. The logo looks bad. Here's
`09:38` this video. The logo looks bad. Here's
`09:39` this video. The logo looks bad. Here's our own logo. We want to change this.
`09:40` our own logo. We want to change this.
`09:40` our own logo. We want to change this. And I've changed it. The folder allows
`09:43` And I've changed it. The folder allows
`09:43` And I've changed it. The folder allows me to organize the generations from AI
`09:46` me to organize the generations from AI
`09:46` me to organize the generations from AI so that I can easily come in, edit, and
`09:48` so that I can easily come in, edit, and
`09:48` so that I can easily come in, edit, and swap it versus just a bunch of random AI
`09:50` swap it versus just a bunch of random AI
`09:50` swap it versus just a bunch of random AI slop that has no organization, no
`09:52` slop that has no organization, no
`09:52` slop that has no organization, no editability. It's a deck, it's It's
`09:55` editability. It's a deck, it's It's
`09:55` editability. It's a deck, it's It's workshop, it's a starter folder, all in
`09:57` workshop, it's a starter folder, all in
`09:57` workshop, it's a starter folder, all in one. The agencies before me would have
`10:00` one. The agencies before me would have
`10:00` one. The agencies before me would have called that three different
`10:01` called that three different
`10:01` called that three different deliverables. I just call it my one
`10:04` deliverables. I just call it my one
`10:04` deliverables. I just call it my one deliverable, the project, the retainer
`10:06` deliverable, the project, the retainer
`10:06` deliverable, the project, the retainer fee that they're paying. And I really
`10:08` fee that they're paying. And I really
`10:08` fee that they're paying. And I really want to double down on this concept, the
`10:10` want to double down on this concept, the
`10:10` want to double down on this concept, the creative lens, the software lens, and
`10:12` creative lens, the software lens, and
`10:12` creative lens, the software lens, and the business lens are all looking at the
`10:15` the business lens are all looking at the
`10:15` the business lens are all looking at the same thing. They used to be multiple
`10:17` same thing. They used to be multiple
`10:17` same thing. They used to be multiple agencies, one doing each thing, multiple
`10:20` agencies, one doing each thing, multiple
`10:20` agencies, one doing each thing, multiple consultancies, multiple areas for value.
`10:23` consultancies, multiple areas for value.
`10:23` consultancies, multiple areas for value. But because AI comes in and changes
`10:27` But because AI comes in and changes
`10:27` But because AI comes in and changes where the commodities of software, of
`10:29` where the commodities of software, of
`10:30` where the commodities of software, of audit reports, of deliverables is, we
`10:33` audit reports, of deliverables is, we
`10:33` audit reports, of deliverables is, we now have to operate at a new
`10:34` now have to operate at a new
`10:34` now have to operate at a new abstraction. The brand voice that's
`10:36` abstraction. The brand voice that's
`10:36` abstraction. The brand voice that's cascading down, that's a creative
`10:38` cascading down, that's a creative
`10:38` cascading down, that's a creative system. The path being the memory,
`10:41` system. The path being the memory,
`10:41` system. The path being the memory, that's the software system. The agency
`10:42` that's the software system. The agency
`10:43` that's the software system. The agency template I clone for new clients, that's
`10:45` template I clone for new clients, that's
`10:45` template I clone for new clients, that's the business system. And the runtime is
`10:47` the business system. And the runtime is
`10:47` the business system. And the runtime is whichever AI or base sets of AI agents,
`10:51` whichever AI or base sets of AI agents,
`10:51` whichever AI or base sets of AI agents, one agent, multiple agents, I am using.
`10:54` one agent, multiple agents, I am using.
`10:54` one agent, multiple agents, I am using. Right? And this is where you start to
`10:56` Right? And this is where you start to
`10:56` Right? And this is where you start to see the idea of where do you want to be
`10:57` see the idea of where do you want to be
`10:57` see the idea of where do you want to be locked in? Do you want local models? Do
`10:59` locked in? Do you want local models? Do
`10:59` locked in? Do you want local models? Do you want to just use Anthropic's Claude?
`11:01` you want to just use Anthropic's Claude?
`11:01` you want to just use Anthropic's Claude? Do you want to use Codex? Well, the
`11:03` Do you want to use Codex? Well, the
`11:03` Do you want to use Codex? Well, the freedom of having your own local folder
`11:06` freedom of having your own local folder
`11:06` freedom of having your own local folder system allows you to do all of that. And
`11:08` system allows you to do all of that. And
`11:08` system allows you to do all of that. And more importantly, package that and give
`11:10` more importantly, package that and give
`11:10` more importantly, package that and give it to your employees. So, as long as I'm
`11:13` it to your employees. So, as long as I'm
`11:13` it to your employees. So, as long as I'm getting more output, I can now hand this
`11:15` getting more output, I can now hand this
`11:16` getting more output, I can now hand this to a designer, to a front-end developer,
`11:17` to a designer, to a front-end developer,
`11:18` to a designer, to a front-end developer, to a back-end developer, and they're
`11:20` to a back-end developer, and they're
`11:20` to a back-end developer, and they're getting the abilities that used to only
`11:22` getting the abilities that used to only
`11:22` getting the abilities that used to only be one single employee. Each person is
`11:25` be one single employee. Each person is
`11:25` be one single employee. Each person is being amplified by this architecture,
`11:28` being amplified by this architecture,
`11:28` being amplified by this architecture, not replaced. My company can now compete
`11:30` not replaced. My company can now compete
`11:30` not replaced. My company can now compete with 100, 150 person companies. And it's
`11:34` with 100, 150 person companies. And it's
`11:34` with 100, 150 person companies. And it's all the same folder. The work I do for
`11:37` all the same folder. The work I do for
`11:37` all the same folder. The work I do for the channel on my YouTube page or my
`11:39` the channel on my YouTube page or my
`11:39` the channel on my YouTube page or my Instagram page uses the same
`11:41` Instagram page uses the same
`11:41` Instagram page uses the same architecture as the work I do for
`11:42` architecture as the work I do for
`11:43` architecture as the work I do for clients, which uses the same
`11:44` clients, which uses the same
`11:44` clients, which uses the same architecture as the system I license to
`11:46` architecture as the system I license to
`11:47` architecture as the system I license to other companies or work with other
`11:48` other companies or work with other
`11:48` other companies or work with other companies. One structure, three
`11:51` companies. One structure, three
`11:51` companies. One structure, three businesses sitting on top of it, three
`11:53` businesses sitting on top of it, three
`11:53` businesses sitting on top of it, three apparent skill sets that are actually
`11:55` apparent skill sets that are actually
`11:55` apparent skill sets that are actually just one skill repeat. And this is what
`11:58` just one skill repeat. And this is what
`11:58` just one skill repeat. And this is what systems thinking actually means in
`12:01` systems thinking actually means in
`12:01` systems thinking actually means in practice. Three problems with three
`12:03` practice. Three problems with three
`12:03` practice. Three problems with three vocabularies sitting on top of one
`12:06` vocabularies sitting on top of one
`12:06` vocabularies sitting on top of one shape. Once you see the shape, you stop
`12:08` shape. Once you see the shape, you stop
`12:08` shape. Once you see the shape, you stop solving each problem from scratch. You
`12:10` solving each problem from scratch. You
`12:10` solving each problem from scratch. You build the shape once, and the problems
`12:13` build the shape once, and the problems
`12:13` build the shape once, and the problems just solve themselves on top of it. This
`12:16` just solve themselves on top of it. This
`12:16` just solve themselves on top of it. This video that you are seeing now lives in
`12:20` video that you are seeing now lives in
`12:20` video that you are seeing now lives in the same folder structure I just spent 6
`12:22` the same folder structure I just spent 6
`12:22` the same folder structure I just spent 6 minutes describing. The script you're
`12:24` minutes describing. The script you're
`12:24` minutes describing. The script you're hearing is in my script.md file. The
`12:27` hearing is in my script.md file. The
`12:27` hearing is in my script.md file. The animation primitives I'm using are
`12:29` animation primitives I'm using are
`12:29` animation primitives I'm using are sitting in my 01 framework folder. The
`12:31` sitting in my 01 framework folder. The
`12:31` sitting in my 01 framework folder. The one thing, the whole thing, was made by
`12:34` one thing, the whole thing, was made by
`12:34` one thing, the whole thing, was made by working inside of this system, inside of
`12:36` working inside of this system, inside of
`12:36` working inside of this system, inside of this folder that this video is about.
`12:39` this folder that this video is about.
`12:39` this folder that this video is about. It's the same shape all the way down,
`12:41` It's the same shape all the way down,
`12:41` It's the same shape all the way down, and I am making money with it with
`12:43` and I am making money with it with
`12:43` and I am making money with it with clients. I am creating attention with it
`12:45` clients. I am creating attention with it
`12:45` clients. I am creating attention with it with my social media. I am learning
`12:47` with my social media. I am learning
`12:48` with my social media. I am learning software fundamentals for the next
`12:49` software fundamentals for the next
`12:49` software fundamentals for the next decade by failing and building with it.
`12:52` decade by failing and building with it.
`12:52` decade by failing and building with it. It wasn't perfect, it still isn't
`12:54` It wasn't perfect, it still isn't
`12:54` It wasn't perfect, it still isn't perfect. There's so many things I need
`12:55` perfect. There's so many things I need
`12:55` perfect. There's so many things I need to change and update and fix and work
`12:58` to change and update and fix and work
`12:58` to change and update and fix and work through. I'm just living at a new
`12:59` through. I'm just living at a new
`12:59` through. I'm just living at a new abstraction of software, of
`13:01` abstraction of software, of
`13:01` abstraction of software, of deliverables, of business, of creative
`13:04` deliverables, of business, of creative
`13:04` deliverables, of business, of creative think. The thing that changes when you
`13:06` think. The thing that changes when you
`13:06` think. The thing that changes when you build a system like this is what you
`13:08` build a system like this is what you
`13:08` build a system like this is what you spend your time on. The creative work,
`13:12` spend your time on. The creative work,
`13:12` spend your time on. The creative work, the business work, the technical work,
`13:14` the business work, the technical work,
`13:14` the business work, the technical work, all shift, maybe even expand in some
`13:18` all shift, maybe even expand in some
`13:18` all shift, maybe even expand in some ways and shrink in others. No matter
`13:21` ways and shrink in others. No matter
`13:21` ways and shrink in others. No matter what though, the business work
`13:22` what though, the business work
`13:22` what though, the business work compounds. I haven't gotten less busy
`13:25` compounds. I haven't gotten less busy
`13:25` compounds. I haven't gotten less busy because of these systems, I've just
`13:27` because of these systems, I've just
`13:27` because of these systems, I've just moved where I am busy. You stop staffing
`13:30` moved where I am busy. You stop staffing
`13:30` moved where I am busy. You stop staffing problems and start switching lenses.
`13:32` problems and start switching lenses.
`13:32` problems and start switching lenses. That's the whole thing. That's it. This
`13:35` That's the whole thing. That's it. This
`13:35` That's the whole thing. That's it. This is a methodology that you can bring into
`13:37` is a methodology that you can bring into
`13:37` is a methodology that you can bring into your process. If you want to learn more
`13:38` your process. If you want to learn more
`13:39` your process. If you want to learn more or dive deeper into these, I highly
`13:40` or dive deeper into these, I highly
`13:40` or dive deeper into these, I highly recommend either checking out my other
`13:42` recommend either checking out my other
`13:42` recommend either checking out my other YouTube videos, or I have it structured
`13:45` YouTube videos, or I have it structured
`13:45` YouTube videos, or I have it structured for the way of thinking in my actual
`13:47` for the way of thinking in my actual
`13:47` for the way of thinking in my actual school community. It's free to join, all
`13:49` school community. It's free to join, all
`13:49` school community. It's free to join, all the courses are completely free. Please
`13:51` the courses are completely free. Please
`13:51` the courses are completely free. Please go check it out. It can help you with
`13:53` go check it out. It can help you with
`13:53` go check it out. It can help you with this thinking. You'll start to see, as
`13:55` this thinking. You'll start to see, as
`13:55` this thinking. You'll start to see, as you watch all of them, they're all just
`13:57` you watch all of them, they're all just
`13:57` you watch all of them, they're all just parts of a greater whole.
`14:00` parts of a greater whole.
`14:00` parts of a greater whole. And as always,
`14:01` And as always,
`14:01` And as always, stay curious and happy learning.