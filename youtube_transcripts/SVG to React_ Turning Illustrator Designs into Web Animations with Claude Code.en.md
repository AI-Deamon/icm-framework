# SVG to React: Turning Illustrator Designs into Web Animations with Claude Code

**Channel:** Jake Van Clief  
**Uploaded:** 2026-03-09  
**Duration:** 11:59  
**URL:** [https://www.youtube.com/watch?v=izMBiWG3L24](https://www.youtube.com/watch?v=izMBiWG3L24)  

---

## Transcript

`00:02` Okay, so this video is going to be
`00:02` Okay, so this video is going to be relatively quick, but kind of
`00:03` relatively quick, but kind of
`00:03` relatively quick, but kind of interesting. My buddy needed a website
`00:05` interesting. My buddy needed a website
`00:05` interesting. My buddy needed a website for his storyboard he made. He also
`00:07` for his storyboard he made. He also
`00:07` for his storyboard he made. He also wanted custom animated characters that
`00:09` wanted custom animated characters that
`00:09` wanted custom animated characters that look like this built into the website.
`00:12` look like this built into the website.
`00:12` look like this built into the website. So, as you're scrolling, it looks all
`00:13` So, as you're scrolling, it looks all
`00:13` So, as you're scrolling, it looks all nice. He's telling his storyboard, but
`00:15` nice. He's telling his storyboard, but
`00:15` nice. He's telling his storyboard, but also scattered throughout it, you have
`00:18` also scattered throughout it, you have
`00:18` also scattered throughout it, you have these characters that you get to jump
`00:19` these characters that you get to jump
`00:19` these characters that you get to jump into and that they have these little
`00:21` into and that they have these little
`00:21` into and that they have these little animations, micro expressions. They're
`00:22` animations, micro expressions. They're
`00:22` animations, micro expressions. They're kind of hanging out with each other,
`00:24` kind of hanging out with each other,
`00:24` kind of hanging out with each other, right? They're able to almost react to
`00:26` right? They're able to almost react to
`00:26` right? They're able to almost react to each other in their own way. And then he
`00:28` each other in their own way. And then he
`00:28` each other in their own way. And then he also wanted to be able to have a full
`00:29` also wanted to be able to have a full
`00:29` also wanted to be able to have a full video embedded into the website where,
`00:32` video embedded into the website where,
`00:32` video embedded into the website where, you know, he can actually go through and
`00:33` you know, he can actually go through and
`00:33` you know, he can actually go through and show each of these characters in their
`00:35` show each of these characters in their
`00:35` show each of these characters in their own animated way. And there's of course,
`00:37` own animated way. And there's of course,
`00:37` own animated way. And there's of course, you know, quite a few here. He also had
`00:39` you know, quite a few here. He also had
`00:39` you know, quite a few here. He also had a professor who he was doing this for.
`00:41` a professor who he was doing this for.
`00:41` a professor who he was doing this for. This is for school work that wanted to
`00:43` This is for school work that wanted to
`00:43` This is for school work that wanted to know if there were ways in which AI
`00:45` know if there were ways in which AI
`00:45` know if there were ways in which AI could automate some of these processes.
`00:47` could automate some of these processes.
`00:47` could automate some of these processes. Cuz the professor's not against AI, but
`00:48` Cuz the professor's not against AI, but
`00:48` Cuz the professor's not against AI, but he also really loves design and he
`00:50` he also really loves design and he
`00:50` he also really loves design and he didn't really believe you could have AI
`00:52` didn't really believe you could have AI
`00:52` didn't really believe you could have AI generate these type of editable
`00:54` generate these type of editable
`00:54` generate these type of editable animations that were something that you
`00:57` animations that were something that you
`00:57` animations that were something that you could iterate on, that you could
`00:58` could iterate on, that you could
`00:58` could iterate on, that you could organize and really create something
`00:59` organize and really create something
`00:59` organize and really create something clean based off of original Adobe
`01:02` clean based off of original Adobe
`01:02` clean based off of original Adobe Illustrator files. I obviously took that
`01:04` Illustrator files. I obviously took that
`01:04` Illustrator files. I obviously took that as a challenge and here's essentially
`01:06` as a challenge and here's essentially
`01:06` as a challenge and here's essentially how I did it.
`01:08` how I did it.
`01:08` how I did it. Uh both I'm going to show you what I
`01:09` Uh both I'm going to show you what I
`01:09` Uh both I'm going to show you what I used and then dive into the actual
`01:11` used and then dive into the actual
`01:11` used and then dive into the actual process. Uh and why I did it. Now, this
`01:14` process. Uh and why I did it. Now, this
`01:14` process. Uh and why I did it. Now, this all started with my buddy's uh Adobe
`01:17` all started with my buddy's uh Adobe
`01:17` all started with my buddy's uh Adobe Illustrator actual storyboard that he
`01:19` Illustrator actual storyboard that he
`01:19` Illustrator actual storyboard that he was doing for class. And he actually
`01:21` was doing for class. And he actually
`01:21` was doing for class. And he actually went through and hand designed all of
`01:22` went through and hand designed all of
`01:22` went through and hand designed all of these, placed them, did all the, you
`01:24` these, placed them, did all the, you
`01:24` these, placed them, did all the, you know, specific designs for it. And uh he
`01:27` know, specific designs for it. And uh he
`01:27` know, specific designs for it. And uh he sent me the file and was like, "Hey, you
`01:29` sent me the file and was like, "Hey, you
`01:29` sent me the file and was like, "Hey, you know, can you animate these?" And I'm
`01:30` know, can you animate these?" And I'm
`01:30` know, can you animate these?" And I'm like, "Heck yeah, I would love to
`01:31` like, "Heck yeah, I would love to
`01:32` like, "Heck yeah, I would love to animate this." And then he came over to
`01:33` animate this." And then he came over to
`01:33` animate this." And then he came over to my house and we ended up working
`01:34` my house and we ended up working
`01:34` my house and we ended up working together on it. Uh we had to kind of
`01:36` together on it. Uh we had to kind of
`01:36` together on it. Uh we had to kind of relabel some of this stuff. So, we had
`01:38` relabel some of this stuff. So, we had
`01:38` relabel some of this stuff. So, we had to come in and actually label left iris,
`01:40` to come in and actually label left iris,
`01:40` to come in and actually label left iris, right iris, left eye, right eye, eyebrow
`01:44` right iris, left eye, right eye, eyebrow
`01:44` right iris, left eye, right eye, eyebrow left, eyebrow right because I knew that
`01:46` left, eyebrow right because I knew that
`01:46` left, eyebrow right because I knew that the AI was going to look for those
`01:49` the AI was going to look for those
`01:50` the AI was going to look for those specific indicators without knowing, you
`01:52` specific indicators without knowing, you
`01:52` specific indicators without knowing, you know, it's not necessarily going to see
`01:53` know, it's not necessarily going to see
`01:53` know, it's not necessarily going to see the image all the time. So, it's able to
`01:54` the image all the time. So, it's able to
`01:54` the image all the time. So, it's able to do that, right? It can take screenshots,
`01:56` do that, right? It can take screenshots,
`01:56` do that, right? It can take screenshots, but that's just a step I didn't want it
`01:58` but that's just a step I didn't want it
`01:58` but that's just a step I didn't want it to take. I wanted it to know right away
`02:00` to take. I wanted it to know right away
`02:00` to take. I wanted it to know right away which character was which, where the SVG
`02:02` which character was which, where the SVG
`02:02` which character was which, where the SVG was in that, you know, aspect. And then
`02:05` was in that, you know, aspect. And then
`02:05` was in that, you know, aspect. And then I also downloaded the entire storyboard
`02:07` I also downloaded the entire storyboard
`02:07` I also downloaded the entire storyboard as a
`02:08` as a
`02:08` as a SVG so that we could, you know, access
`02:11` SVG so that we could, you know, access
`02:11` SVG so that we could, you know, access it and whatnot, which you can actually
`02:13` it and whatnot, which you can actually
`02:13` it and whatnot, which you can actually see right here, right? So it's just a
`02:15` see right here, right? So it's just a
`02:15` see right here, right? So it's just a big, you know, essentially HTML file.
`02:17` big, you know, essentially HTML file.
`02:17` big, you know, essentially HTML file. This is an SVG file, but same same but
`02:19` This is an SVG file, but same same but
`02:19` This is an SVG file, but same same but different. So that it was easier for the
`02:22` different. So that it was easier for the
`02:22` different. So that it was easier for the AI to work with as well. So, first
`02:24` AI to work with as well. So, first
`02:24` AI to work with as well. So, first things first, I needed to go ahead and
`02:27` things first, I needed to go ahead and
`02:27` things first, I needed to go ahead and get two different GitHub libraries. The
`02:28` get two different GitHub libraries. The
`02:28` get two different GitHub libraries. The first one is UX or sorry, Remotion. Um
`02:34` first one is UX or sorry, Remotion. Um
`02:34` first one is UX or sorry, Remotion. Um skill
`02:36` skill
`02:36` skill which is basically Remotion is a library
`02:40` which is basically Remotion is a library
`02:40` which is basically Remotion is a library that allows you to create React
`02:44` that allows you to create React
`02:44` that allows you to create React animations programmatically, right? They
`02:46` animations programmatically, right? They
`02:46` animations programmatically, right? They came out with a kind of process that
`02:48` came out with a kind of process that
`02:48` came out with a kind of process that allows you to create animations in
`02:50` allows you to create animations in
`02:50` allows you to create animations in different ways. And if you've seen any
`02:52` different ways. And if you've seen any
`02:52` different ways. And if you've seen any of my other videos, you know I use this
`02:54` of my other videos, you know I use this
`02:54` of my other videos, you know I use this library a lot. It's very useful. You can
`02:56` library a lot. It's very useful. You can
`02:56` library a lot. It's very useful. You can create JavaScript and React things to
`02:58` create JavaScript and React things to
`02:58` create JavaScript and React things to create pretty amazing animations, right?
`03:01` create pretty amazing animations, right?
`03:01` create pretty amazing animations, right? That's how we actually created all of
`03:03` That's how we actually created all of
`03:03` That's how we actually created all of these, which I'll go into detail here
`03:04` these, which I'll go into detail here
`03:04` these, which I'll go into detail here shortly. And it can do a lot more than
`03:06` shortly. And it can do a lot more than
`03:06` shortly. And it can do a lot more than just those animations. It can do whole
`03:08` just those animations. It can do whole
`03:08` just those animations. It can do whole scenes and whatnot programmatically
`03:10` scenes and whatnot programmatically
`03:10` scenes and whatnot programmatically using front-end code, which AI is very
`03:13` using front-end code, which AI is very
`03:14` using front-end code, which AI is very good at writing, right? So, the way in
`03:16` good at writing, right? So, the way in
`03:16` good at writing, right? So, the way in which I actually decided to build on
`03:19` which I actually decided to build on
`03:19` which I actually decided to build on that and make it cleaner so that it
`03:20` that and make it cleaner so that it
`03:20` that and make it cleaner so that it would be a clean website and rather not
`03:22` would be a clean website and rather not
`03:22` would be a clean website and rather not look as crazy or vibe coded or at least
`03:24` look as crazy or vibe coded or at least
`03:24` look as crazy or vibe coded or at least just really amplify, you know, his
`03:27` just really amplify, you know, his
`03:27` just really amplify, you know, his storyboard that he has here. This looks
`03:29` storyboard that he has here. This looks
`03:29` storyboard that he has here. This looks a little bit vibe coded, so I'm going to
`03:30` a little bit vibe coded, so I'm going to
`03:30` a little bit vibe coded, so I'm going to change some of the fonts and stuff. Um
`03:33` change some of the fonts and stuff. Um
`03:33` change some of the fonts and stuff. Um is
`03:34` is
`03:34` is something called the UI UX Pro skill.
`03:39` something called the UI UX Pro skill.
`03:39` something called the UI UX Pro skill. This is an amazing library skill that I
`03:42` This is an amazing library skill that I
`03:42` This is an amazing library skill that I think everyone needs to get for their
`03:44` think everyone needs to get for their
`03:44` think everyone needs to get for their Claude. Basically, it's a skill that
`03:46` Claude. Basically, it's a skill that
`03:46` Claude. Basically, it's a skill that helps it design what the front end
`03:48` helps it design what the front end
`03:48` helps it design what the front end should look like, what a website should
`03:49` should look like, what a website should
`03:49` should look like, what a website should look like. It goes through a whole
`03:50` look like. It goes through a whole
`03:50` look like. It goes through a whole process. They have it all built in here
`03:52` process. They have it all built in here
`03:52` process. They have it all built in here and it's quite nice. I use it a lot. Um
`03:55` and it's quite nice. I use it a lot. Um
`03:55` and it's quite nice. I use it a lot. Um it's just it's just easier to catch it
`03:57` it's just it's just easier to catch it
`03:57` it's just it's just easier to catch it up and use the skill rather than have to
`03:59` up and use the skill rather than have to
`03:59` up and use the skill rather than have to prompt Claude so much to get it even
`04:01` prompt Claude so much to get it even
`04:01` prompt Claude so much to get it even close to what I want. Uh this really
`04:03` close to what I want. Uh this really
`04:03` close to what I want. Uh this really gets it closer there. So these two uh
`04:05` gets it closer there. So these two uh
`04:05` gets it closer there. So these two uh GitHub repos are requirement. Obviously,
`04:08` GitHub repos are requirement. Obviously,
`04:08` GitHub repos are requirement. Obviously, um you're going to want Claude. You can
`04:10` um you're going to want Claude. You can
`04:10` um you're going to want Claude. You can use Claude code inside of the app.
`04:13` use Claude code inside of the app.
`04:13` use Claude code inside of the app. Personally for me, I really love Claude
`04:16` Personally for me, I really love Claude
`04:16` Personally for me, I really love Claude inside of VS Code. Um right? Just being
`04:19` inside of VS Code. Um right? Just being
`04:19` inside of VS Code. Um right? Just being able to sit here, I can actually go
`04:21` able to sit here, I can actually go
`04:21` able to sit here, I can actually go through and let's say um we're looking
`04:24` through and let's say um we're looking
`04:24` through and let's say um we're looking at these, right? These characters. I can
`04:26` at these, right? These characters. I can
`04:26` at these, right? These characters. I can click and see them right there. These
`04:28` click and see them right there. These
`04:28` click and see them right there. These are the SVGs we downloaded from his
`04:31` are the SVGs we downloaded from his
`04:31` are the SVGs we downloaded from his Adobe Illustrator, which I'll show in a
`04:32` Adobe Illustrator, which I'll show in a
`04:32` Adobe Illustrator, which I'll show in a second. And it just becomes easier to
`04:34` second. And it just becomes easier to
`04:34` second. And it just becomes easier to actually talk with and work with the AI
`04:37` actually talk with and work with the AI
`04:37` actually talk with and work with the AI on building things and actually asking
`04:40` on building things and actually asking
`04:40` on building things and actually asking like, "Okay, like here I'm specifically
`04:42` like, "Okay, like here I'm specifically
`04:42` like, "Okay, like here I'm specifically saying use the UI/UX skill to redesign."
`04:45` saying use the UI/UX skill to redesign."
`04:45` saying use the UI/UX skill to redesign." I had made a version of the website
`04:47` I had made a version of the website
`04:47` I had made a version of the website myself before. Um you can have Claude do
`04:50` myself before. Um you can have Claude do
`04:50` myself before. Um you can have Claude do it and I wanted the UI/UX to kind of
`04:52` it and I wanted the UI/UX to kind of
`04:52` it and I wanted the UI/UX to kind of redesign what it was. But again, just
`04:55` redesign what it was. But again, just
`04:55` redesign what it was. But again, just you know, you don't have to use this
`04:57` you know, you don't have to use this
`04:57` you know, you don't have to use this IDE, you don't have to use VS Code.
`04:59` IDE, you don't have to use VS Code.
`04:59` IDE, you don't have to use VS Code. Again, I have installation instructions
`05:01` Again, I have installation instructions
`05:01` Again, I have installation instructions on how to use Claude code with this. Go
`05:03` on how to use Claude code with this. Go
`05:03` on how to use Claude code with this. Go check that video out, but I just wanted
`05:04` check that video out, but I just wanted
`05:05` check that video out, but I just wanted to share that. From there, I just had a
`05:06` to share that. From there, I just had a
`05:06` to share that. From there, I just had a whole bunch of conversations with it,
`05:08` whole bunch of conversations with it,
`05:08` whole bunch of conversations with it, right? I I kept saying, "Hey, let's take
`05:11` right? I I kept saying, "Hey, let's take
`05:11` right? I I kept saying, "Hey, let's take these individual SVGs and create some
`05:13` these individual SVGs and create some
`05:13` these individual SVGs and create some animations out of it." We kind of worked
`05:15` animations out of it." We kind of worked
`05:15` animations out of it." We kind of worked back and forth. We made a couple little
`05:18` back and forth. We made a couple little
`05:18` back and forth. We made a couple little designs here or there. Just a really
`05:19` designs here or there. Just a really
`05:20` designs here or there. Just a really long conversation. And then, you know,
`05:22` long conversation. And then, you know,
`05:22` long conversation. And then, you know, what I did like, what I didn't like, and
`05:23` what I did like, what I didn't like, and
`05:24` what I did like, what I didn't like, and it's just editing it. And what it's
`05:25` it's just editing it. And what it's
`05:25` it's just editing it. And what it's doing is it's taking these SVGs, right?
`05:28` doing is it's taking these SVGs, right?
`05:28` doing is it's taking these SVGs, right? And it's actually um adding them because
`05:31` And it's actually um adding them because
`05:31` And it's actually um adding them because of the Remotion skill and how Remotion
`05:33` of the Remotion skill and how Remotion
`05:33` of the Remotion skill and how Remotion works into these kind of components and
`05:36` works into these kind of components and
`05:36` works into these kind of components and these processes, right? So like the
`05:38` these processes, right? So like the
`05:38` these processes, right? So like the scene for the emotion showcase that you
`05:41` scene for the emotion showcase that you
`05:41` scene for the emotion showcase that you see here,
`05:43` see here,
`05:43` see here, here.
`05:44` here.
`05:44` here. Hm. Wait, no. Here. Here.
`05:47` Hm. Wait, no. Here. Here.
`05:47` Hm. Wait, no. Here. Here. This scene is all React and front-end
`05:50` This scene is all React and front-end
`05:50` This scene is all React and front-end code, right? That's That's how it's
`05:52` code, right? That's That's how it's
`05:52` code, right? That's That's how it's being built, and it's all right here.
`05:54` being built, and it's all right here.
`05:54` being built, and it's all right here. But, this scene is also referencing
`05:57` But, this scene is also referencing
`05:57` But, this scene is also referencing other emotions and data, right? So, like
`05:59` other emotions and data, right? So, like
`05:59` other emotions and data, right? So, like when it references a certain character,
`06:01` when it references a certain character,
`06:01` when it references a certain character, or it's pulling in a character, it's
`06:03` or it's pulling in a character, it's
`06:03` or it's pulling in a character, it's actually pulling them from components,
`06:05` actually pulling them from components,
`06:05` actually pulling them from components, right? Like our characters. So, like
`06:07` right? Like our characters. So, like
`06:07` right? Like our characters. So, like what is the head and the colors of each
`06:09` what is the head and the colors of each
`06:09` what is the head and the colors of each one? What are the emotion profiles? When
`06:11` one? What are the emotion profiles? When
`06:11` one? What are the emotion profiles? When are they blinking? What is the iris
`06:13` are they blinking? What is the iris
`06:13` are they blinking? What is the iris speed for each one, right? Here's the
`06:15` speed for each one, right? Here's the
`06:15` speed for each one, right? Here's the joy. Here's the smooth. So, all of this
`06:18` joy. Here's the smooth. So, all of this
`06:18` joy. Here's the smooth. So, all of this stays editable, and this is the most
`06:19` stays editable, and this is the most
`06:19` stays editable, and this is the most important part. This entire folder
`06:22` important part. This entire folder
`06:22` important part. This entire folder structure now becomes my app for editing
`06:26` structure now becomes my app for editing
`06:26` structure now becomes my app for editing videos. I was able to pull out of Adobe
`06:29` videos. I was able to pull out of Adobe
`06:29` videos. I was able to pull out of Adobe Illustrator or any of these apps and
`06:31` Illustrator or any of these apps and
`06:31` Illustrator or any of these apps and start editing videos or websites or
`06:34` start editing videos or websites or
`06:34` start editing videos or websites or anything like that.
`06:36` anything like that.
`06:36` anything like that. Gosh, I have so many tabs. I'm so sorry,
`06:38` Gosh, I have so many tabs. I'm so sorry,
`06:38` Gosh, I have so many tabs. I'm so sorry, everyone. This is all over the place.
`06:45` I'm able to edit these things using
`06:45` I'm able to edit these things using natural language, right? I'm able to sit
`06:48` natural language, right? I'm able to sit
`06:48` natural language, right? I'm able to sit there and actually come in and say,
`06:50` there and actually come in and say,
`06:50` there and actually come in and say, "Hey,
`06:52` "Hey,
`06:52` "Hey, and I have two monitors, so usually I
`06:53` and I have two monitors, so usually I
`06:53` and I have two monitors, so usually I don't have to dip between these. Uh I
`06:55` don't have to dip between these. Uh I
`06:55` don't have to dip between these. Uh I don't like the eyes on sadness. I don't
`06:58` don't like the eyes on sadness. I don't
`06:58` don't like the eyes on sadness. I don't like the eyes on sadness.
`07:01` like the eyes on sadness.
`07:01` like the eyes on sadness. What can I do?" And when I send that,
`07:04` What can I do?" And when I send that,
`07:04` What can I do?" And when I send that, it's not just reading the whole motion
`07:06` it's not just reading the whole motion
`07:06` it's not just reading the whole motion base. I have a project MD, which is
`07:09` base. I have a project MD, which is
`07:09` base. I have a project MD, which is being read as Claude MD. I actually need
`07:10` being read as Claude MD. I actually need
`07:11` being read as Claude MD. I actually need to change that to say Claude MD, so it
`07:12` to change that to say Claude MD, so it
`07:12` to change that to say Claude MD, so it knows that I always read it. But, um
`07:14` knows that I always read it. But, um
`07:14` knows that I always read it. But, um that shows where to go for certain
`07:16` that shows where to go for certain
`07:16` that shows where to go for certain folders, where to get the original at uh
`07:19` folders, where to get the original at uh
`07:19` folders, where to get the original at uh SVG. So, instead of it having to sit
`07:21` SVG. So, instead of it having to sit
`07:21` SVG. So, instead of it having to sit here and read through the whole code
`07:22` here and read through the whole code
`07:22` here and read through the whole code base, read through everything, it
`07:24` base, read through everything, it
`07:24` base, read through everything, it already has built-in context of where it
`07:26` already has built-in context of where it
`07:26` already has built-in context of where it needs to go. I'm able to edit these
`07:29` needs to go. I'm able to edit these
`07:29` needs to go. I'm able to edit these things using natural language, right?
`07:32` things using natural language, right?
`07:32` things using natural language, right? I'm able to sit there and actually come
`07:34` I'm able to sit there and actually come
`07:34` I'm able to sit there and actually come in and say, "Hey,
`07:37` in and say, "Hey,
`07:37` in and say, "Hey, and I have two monitors, so usually I
`07:38` and I have two monitors, so usually I
`07:38` and I have two monitors, so usually I don't have to dip between these. Uh I
`07:40` don't have to dip between these. Uh I
`07:40` don't have to dip between these. Uh I don't like the eyes on sadness. I don't
`07:43` don't like the eyes on sadness. I don't
`07:43` don't like the eyes on sadness. I don't like the eyes on sadness.
`07:47` like the eyes on sadness.
`07:47` like the eyes on sadness. What can I do? And when I send that,
`07:50` What can I do? And when I send that,
`07:50` What can I do? And when I send that, it's not just reading the whole motion
`07:51` it's not just reading the whole motion
`07:51` it's not just reading the whole motion base. I have a project MD, which is
`07:54` base. I have a project MD, which is
`07:54` base. I have a project MD, which is being read as Claude MD. I actually need
`07:56` being read as Claude MD. I actually need
`07:56` being read as Claude MD. I actually need to change that to say Claude MD, so it
`07:58` to change that to say Claude MD, so it
`07:58` to change that to say Claude MD, so it knows always read it, but
`07:59` knows always read it, but
`07:59` knows always read it, but um that shows where to go for certain
`08:01` um that shows where to go for certain
`08:01` um that shows where to go for certain folders, where to get the original
`08:04` folders, where to get the original
`08:04` folders, where to get the original SVG. So, instead of it having to sit
`08:06` SVG. So, instead of it having to sit
`08:06` SVG. So, instead of it having to sit here and read through the whole code
`08:07` here and read through the whole code
`08:07` here and read through the whole code base, read through everything, it
`08:09` base, read through everything, it
`08:09` base, read through everything, it already has built-in context of where it
`08:12` already has built-in context of where it
`08:12` already has built-in context of where it needs to go. So, then I can just sit
`08:14` needs to go. So, then I can just sit
`08:14` needs to go. So, then I can just sit there and edit fine details. With other,
`08:17` there and edit fine details. With other,
`08:17` there and edit fine details. With other, you know, AI image systems and things
`08:19` you know, AI image systems and things
`08:19` you know, AI image systems and things like that, you have to constantly redo
`08:22` like that, you have to constantly redo
`08:22` like that, you have to constantly redo the entire video or sections of it or
`08:24` the entire video or sections of it or
`08:24` the entire video or sections of it or destroys half of what you're trying to
`08:25` destroys half of what you're trying to
`08:25` destroys half of what you're trying to do. Versus here, when I say I want to
`08:28` do. Versus here, when I say I want to
`08:28` do. Versus here, when I say I want to edit the eye, it's literally only going
`08:30` edit the eye, it's literally only going
`08:30` edit the eye, it's literally only going to go in, oops, wrong one. It's only
`08:33` to go in, oops, wrong one. It's only
`08:33` to go in, oops, wrong one. It's only going to go in, it's going to find the
`08:36` going to go in, it's going to find the
`08:36` going to go in, it's going to find the specific component for that character,
`08:39` specific component for that character,
`08:39` specific component for that character, it's going to look, oh, you mean the
`08:40` it's going to look, oh, you mean the
`08:40` it's going to look, oh, you mean the iris speed for Joy or for Fear, maybe
`08:43` iris speed for Joy or for Fear, maybe
`08:43` iris speed for Joy or for Fear, maybe the pattern that's looking, and that's
`08:45` the pattern that's looking, and that's
`08:45` the pattern that's looking, and that's what it's going to assign, it's going to
`08:46` what it's going to assign, it's going to
`08:46` what it's going to assign, it's going to assign the speeds, to assign the
`08:48` assign the speeds, to assign the
`08:48` assign the speeds, to assign the movements of it, right? Is there certain
`08:50` movements of it, right? Is there certain
`08:50` movements of it, right? Is there certain things? It's creating the variables that
`08:52` things? It's creating the variables that
`08:53` things? It's creating the variables that I need to be able to edit all these,
`08:55` I need to be able to edit all these,
`08:55` I need to be able to edit all these, right? All of these, all of this code, I
`08:58` right? All of these, all of this code, I
`08:58` right? All of these, all of this code, I can come in and edit if I'd like. Or
`09:00` can come in and edit if I'd like. Or
`09:00` can come in and edit if I'd like. Or because the AI is really good at it, it
`09:02` because the AI is really good at it, it
`09:02` because the AI is really good at it, it can edit small parts of it without
`09:03` can edit small parts of it without
`09:03` can edit small parts of it without messing up the rest of the code base,
`09:06` messing up the rest of the code base,
`09:06` messing up the rest of the code base, allowing for really refined edits of
`09:09` allowing for really refined edits of
`09:09` allowing for really refined edits of each character in scenes or in places.
`09:12` each character in scenes or in places.
`09:12` each character in scenes or in places. And then when I'm done editing it in the
`09:13` And then when I'm done editing it in the
`09:13` And then when I'm done editing it in the scene, I can take that character of this
`09:16` scene, I can take that character of this
`09:16` scene, I can take that character of this scene, one that I really like, and say,
`09:19` scene, one that I really like, and say,
`09:19` scene, one that I really like, and say, "Hey, can we throw that into my React
`09:22` "Hey, can we throw that into my React
`09:22` "Hey, can we throw that into my React front-end website?" Which is exactly
`09:24` front-end website?" Which is exactly
`09:25` front-end website?" Which is exactly what we did here, right? So, we had the
`09:27` what we did here, right? So, we had the
`09:27` what we did here, right? So, we had the website, it had the storyboards with the
`09:29` website, it had the storyboards with the
`09:29` website, it had the storyboards with the normal SVG, it took that character and
`09:31` normal SVG, it took that character and
`09:31` normal SVG, it took that character and added them in. Now, I understand some of
`09:34` added them in. Now, I understand some of
`09:34` added them in. Now, I understand some of this may still be very confusing, and
`09:36` this may still be very confusing, and
`09:36` this may still be very confusing, and there's a lot to dive into. I'm making
`09:38` there's a lot to dive into. I'm making
`09:38` there's a lot to dive into. I'm making much more structured courses on SVGs, on
`09:41` much more structured courses on SVGs, on
`09:41` much more structured courses on SVGs, on React, on front end, on software
`09:43` React, on front end, on software
`09:43` React, on front end, on software fundamentals because people are starting
`09:45` fundamentals because people are starting
`09:45` fundamentals because people are starting to discover now people with software
`09:47` to discover now people with software
`09:47` to discover now people with software fundamentals and understanding AI
`09:49` fundamentals and understanding AI
`09:49` fundamentals and understanding AI fundamentals and design fundamentals
`09:52` fundamentals and design fundamentals
`09:52` fundamentals and design fundamentals become like powerhouses in today's
`09:54` become like powerhouses in today's
`09:54` become like powerhouses in today's world. I mean, I was able to design this
`09:56` world. I mean, I was able to design this
`09:56` world. I mean, I was able to design this in 3 hours, right? This would have taken
`09:58` in 3 hours, right? This would have taken
`09:58` in 3 hours, right? This would have taken me weeks before if I was doing it
`10:00` me weeks before if I was doing it
`10:00` me weeks before if I was doing it manually, but the information, the
`10:02` manually, but the information, the
`10:02` manually, but the information, the understanding of design, the small
`10:04` understanding of design, the small
`10:04` understanding of design, the small nuances still existed, still were
`10:06` nuances still existed, still were
`10:06` nuances still existed, still were required. The the base of Adobe
`10:09` required. The the base of Adobe
`10:09` required. The the base of Adobe Illustrator was still there. This was
`10:11` Illustrator was still there. This was
`10:11` Illustrator was still there. This was the start of all of this, right? And
`10:13` the start of all of this, right? And
`10:14` the start of all of this, right? And this is the whole idea behind like my
`10:15` this is the whole idea behind like my
`10:15` this is the whole idea behind like my idea of pushing we don't need agents, we
`10:18` idea of pushing we don't need agents, we
`10:18` idea of pushing we don't need agents, we need file structures. This file
`10:20` need file structures. This file
`10:20` need file structures. This file structure allows me to do all this
`10:22` structure allows me to do all this
`10:23` structure allows me to do all this editing. Everything you see in front of
`10:24` editing. Everything you see in front of
`10:25` editing. Everything you see in front of you without a single app. No app is
`10:28` you without a single app. No app is
`10:28` you without a single app. No app is needed other than Claude or an agent or
`10:31` needed other than Claude or an agent or
`10:31` needed other than Claude or an agent or an AI, which I guess counts as an app,
`10:33` an AI, which I guess counts as an app,
`10:33` an AI, which I guess counts as an app, but you see what I'm saying? Like I
`10:34` but you see what I'm saying? Like I
`10:34` but you see what I'm saying? Like I don't need Adobe Illustrator and all
`10:35` don't need Adobe Illustrator and all
`10:35` don't need Adobe Illustrator and all these I could do all of this work inside
`10:38` these I could do all of this work inside
`10:38` these I could do all of this work inside of a folder, right? Appless work is now
`10:41` of a folder, right? Appless work is now
`10:41` of a folder, right? Appless work is now possible. We're entering a different
`10:43` possible. We're entering a different
`10:43` possible. We're entering a different abstraction to get the work done in a
`10:45` abstraction to get the work done in a
`10:45` abstraction to get the work done in a different way using
`10:48` different way using
`10:48` different way using natural language. Just natural language.
`10:51` natural language. Just natural language.
`10:51` natural language. Just natural language. Instead of me having to click through
`10:52` Instead of me having to click through
`10:52` Instead of me having to click through all these UI and stuff, I can say, "Ah,
`10:54` all these UI and stuff, I can say, "Ah,
`10:54` all these UI and stuff, I can say, "Ah, I don't really like that. Do this
`10:55` I don't really like that. Do this
`10:55` I don't really like that. Do this instead."
`10:56` instead."
`10:56` instead." Crazy question, are we able to convert
`10:58` Crazy question, are we able to convert
`10:58` Crazy question, are we able to convert that into our own HTML slider? Hey,
`11:00` that into our own HTML slider? Hey,
`11:00` that into our own HTML slider? Hey, that's fine. I want more slides than
`11:01` that's fine. I want more slides than
`11:01` that's fine. I want more slides than anything else, right? I'm able to work
`11:03` anything else, right? I'm able to work
`11:03` anything else, right? I'm able to work with the program and capture my
`11:05` with the program and capture my
`11:05` with the program and capture my intention through natural language while
`11:07` intention through natural language while
`11:07` intention through natural language while still not sacrificing the minute
`11:11` still not sacrificing the minute
`11:11` still not sacrificing the minute editability. Now, there's certain things
`11:12` editability. Now, there's certain things
`11:13` editability. Now, there's certain things that you might want to still edit inside
`11:15` that you might want to still edit inside
`11:15` that you might want to still edit inside of Adobe Illustrator or something like
`11:17` of Adobe Illustrator or something like
`11:17` of Adobe Illustrator or something like that, right? That might be able to do
`11:18` that, right? That might be able to do
`11:18` that, right? That might be able to do faster, but there's also other things
`11:20` faster, but there's also other things
`11:20` faster, but there's also other things you don't want to edit, especially once
`11:22` you don't want to edit, especially once
`11:22` you don't want to edit, especially once the scenes are put together. So, you're
`11:23` the scenes are put together. So, you're
`11:23` the scenes are put together. So, you're just balancing where the human is.
`11:26` just balancing where the human is.
`11:26` just balancing where the human is. Well, that's it. I just wanted to make a
`11:27` Well, that's it. I just wanted to make a
`11:27` Well, that's it. I just wanted to make a quick video on this today, everyone. Um
`11:29` quick video on this today, everyone. Um
`11:29` quick video on this today, everyone. Um other than that, if you have any
`11:30` other than that, if you have any
`11:30` other than that, if you have any questions, comments, concerns,
`11:32` questions, comments, concerns,
`11:32` questions, comments, concerns, obviously, you know, you're probably
`11:33` obviously, you know, you're probably
`11:33` obviously, you know, you're probably watching this from my community right
`11:35` watching this from my community right
`11:35` watching this from my community right now. If you're not in my community and
`11:37` now. If you're not in my community and
`11:37` now. If you're not in my community and this is a YouTube video, go ahead and
`11:39` this is a YouTube video, go ahead and
`11:39` this is a YouTube video, go ahead and click the link in the description to go
`11:40` click the link in the description to go
`11:40` click the link in the description to go come check out all my tutorials and
`11:42` come check out all my tutorials and
`11:42` come check out all my tutorials and become part of a community that's
`11:44` become part of a community that's
`11:44` become part of a community that's building real stuff that's going to last
`11:46` building real stuff that's going to last
`11:46` building real stuff that's going to last 10 years, not just some random AI guru
`11:49` 10 years, not just some random AI guru
`11:49` 10 years, not just some random AI guru crap.
`11:50` crap.
`11:50` crap. Uh I'm sorry to say crap, but you know
`11:52` Uh I'm sorry to say crap, but you know
`11:52` Uh I'm sorry to say crap, but you know what I mean in that world. Anyway, much
`11:54` what I mean in that world. Anyway, much
`11:54` what I mean in that world. Anyway, much love everyone. I'll see you around.
`11:56` love everyone. I'll see you around.
`11:56` love everyone. I'll see you around. Until next time, happy learning.